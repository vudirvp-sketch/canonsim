# worklog — canonsim

> Cap: 10 entries, one in / one out. 3–5 lines per entry: what changed, why,
> files touched. No diffs, no command output, no reasoning traces. Long-term
> rationale belongs in `docs/DECISIONS.md`. Pre-trim history lives in git.
---
why: the owner's control-plane call — the SSI package governs WITHOUT becoming a runtime dependency or a second source of truth (D-024); the shapes are the executable: docguard's own counts-over-forms law extended, never a new parallel tool (D-198 honored)
files: docs/ssi/** (34 reference files + SSI_OVERLAY.md(new)), AGENTS.md (§2.9), scripts/docguard.py, tests/test_docguard.py, docs/AGENT_NAVIGATION.md, docs/{TASKS.md,DECISIONS.md}, STATUS.md, worklog.md — 40+ paths (the reference copy one unit, the 3–5 soft limit honestly over, the iter-239 doc-admission precedent); 2344+16 + ruff + docguard clean (zero functional change, zero core change, the LOG untouched); iter-232/231 evicted per the cap
---
why: the freeze pins the initiative's blast radius at zero public-contract drift BEFORE any SSI machinery lands; KI#99 registered outside the initiative per the owner's instruction (may close earlier than the phases)
files: docs/TASKS.md (the ssi rows + the ledger line), docs/DECISIONS.md (D-221 into the compound row), STATUS.md (KI#99 + the KI#98 cleanup), worklog.md — 4 paths; 2344+9 + ruff + docguard clean (doc-only, zero code, the LOG untouched); iter-230 evicted per the cap
iter-252 · 2026-09-26 · ssi8-gc-pass (Phase 7, the same go-ahead): the GC pass FIRED — the N020 sweep over every artifact class (19 scripts, fixtures, playscripts, packs, docs incl. every subdir — every file consumer-referenced; zero tracked-ignored; zero orphans; the honest negative: no dead committed artifact beyond the marker family) + the three deletion cards (D-230): the redundant .gitkeep family in NON-empty dirs DELETED via git rm (7 × 0 bytes; sim/systems/.gitkeep retained — the reserved skeleton's only content), sim/ RETAINED (zero imports since birth BUT authority holds: NAV §1's reserved row + D-037 + INV-3's scope — consumer absence alone is never deletion evidence), the KI#99 doubled workbench/workbench/runtime/ tree the owner-side cleanup card (the iter-246 fix landed, the tree regenerates, the repo carries zero change); the overlay: N020 VERIFIED + block I VERIFIED + phase 7 CLOSED
why: N020's own law — deletion needs consumer_absence AND authority_absence AND the historical check AND recovery, never apparent inactivity; the sweep's negative result is itself the finding (the cap/collapse/KI-cleanup discipline works), and sim/'s retention demonstrates the gate refusing a tempting deletion the authority still claims
files: core/.gitkeep, brief/.gitkeep, cli/.gitkeep, render/.gitkeep, tests/.gitkeep, tests/playscripts/.gitkeep, content/tavern_pack/.gitkeep (DELETED, git rm), docs/DECISIONS.md (D-230 + the ID column), docs/ssi/SSI_OVERLAY.md (N020 + block I + phase 7 + §5's fired note), docs/TASKS.md (ssi-8 DONE + the ledger), STATUS.md, worklog.md — 12 paths; 2384+9 + ruff + docguard + topology --check clean (zero code change, the LOG untouched)
---
iter-251 · 2026-09-26 · ssi7-semantic-diff-layer (Phase 6, the owner's «ssi7 и/или ssi8 ==> можешь начать» go-ahead): the semantic diff layer over T1 LANDED — scripts/semantic_diff.py the instrument (pure stdlib, ZERO core imports — the independent-re-derivation oracle; header semantics schema_version/seed/pack vs the environmental python/commit ignored by design and named; the anchors id/t/type/actor/target/cause + deep field paths + the prefix relation + both-or-neither run-side RNG fingerprints) + tests/test_semantic_diff.py the 22-test claim packet (independence arms GREEN, mutation teeth RED, the fresh-run↔golden companion) + TEST_PLAN §1.4 + the T1 row pointer + the overlay (block L VERIFIED, phase 6 CLOSED) + NAV (scripts row + §3 routing); D-229 (R3) the PCC record
why: the phase's pre-registered design — T1's env-pin (§1.1, the documented decision) leaves the cross-environment half of replay verification unanswered; the layer ADDS the oracle (ids/types/causes/actors/targets + RNG fingerprints, interpreter/line-ending independent), never a T1 «bug fix»; the admission per D-198 rides the topology.py instrument+pin precedent (the named consumer: §1.1's documented limitation, the FAQ's standing pitfall)
files: scripts/semantic_diff.py(new), tests/test_semantic_diff.py(new), docs/TEST_PLAN.md (§1.4 + the T1 row), docs/ssi/SSI_OVERLAY.md (block L + phase 6), docs/DECISIONS.md (D-229), docs/AGENT_NAVIGATION.md (scripts row + routing), docs/TASKS.md (ssi-7 DONE + the ledger), STATUS.md, worklog.md — 9 paths (the instrument + the pin + the state docs, AGENTS §2.3 honestly over); 2384+9 + ruff + docguard + topology --check clean (zero core change — no core/ file imported or edited, the LOG untouched)
---
iter-250 · 2026-09-26 · ssi6-canonical-read-seam (Phase 5, the owner's «продолжай работу» go-ahead over the confirmed Phase 3 closure): the canonical read seam LANDED — workbench/canonical_read.py the ONE workbench core-import module (a pure re-export shell over the 10-name read surface — log: read_log/validate_header/EventRecord/LogError, fold: fold/initial_projection/present_in_order, pack: load_pack/PackError, rng: stable_hash; __all__ the pinned surface) + the three consumers (scene_build, observatory_read, scene_ir) re-pointed onto it at zero behavior change + the law executable twice over: test_architecture (h) the core-import ban outside the seam + the seam's watchlist row hard-pinned by topology --check (the drift pin RED on the missing row → GREEN in-iteration); ssi-5 explicitly NOT opened (the N018 evidence gate unmet — that phase needs the owner's fresh co-change evidence or the explicit skip, never momentum); D-228 (R3) the PCC record
why: the phase's pre-registered design (ssi-2/D-222: INV-4's sanctioned-module pattern) over the map's named consumer set (D-224 §5) — the workbench app layer's core-read edge narrowed to exactly one auditable module; the pure re-export form (the ssi-4 facade precedent) holds zero behavior change while the two executables pin the edge from both sides (the consumers by the import ban, the seam's own surface by the watchlist reads cell — it grows only with a same-iteration map update)
files: workbench/canonical_read.py(new), workbench/{scene_build.py,observatory_read.py,scene_ir.py}, tests/test_architecture.py, scripts/topology.py (the WATCHLIST sync), docs/{SSI_TOPOLOGY.md (the map refresh + the watchlist row), WORKBENCH_APP_LAW.md (§24 the one-seam edge), ssi/SSI_OVERLAY.md (phase 5 CLOSED), DECISIONS.md (D-228), TASKS.md (ssi-6 DONE + the ledger, iter-239 evicted)}, STATUS.md (KI#99 deleted per the 2-iteration law), worklog.md (iter-240 evicted) — 13 paths (the 3–5 soft limit honestly over: the seam + the three consumers + the two executables' syncs + the state docs, AGENTS §2.3); 2362+9 + ruff + docguard + topology --check clean (zero behavior change, zero core change — no core/ file edited, the LOG untouched)
---
iter-249 · 2026-09-26 · ssi4-step3-phase-close (Phase 3 step 3, the FINAL): the PERSISTENCE/OPERATIONS half out of the facade — store.py (SCHEMA + InferenceStore), migration.py, operations.py (the gateway family — the ops deriving at their true owner), compiled.py extracted verbatim; the facade = the PURE re-export shell (176 lines); PHASE 3 CLOSED: 2511 → 10 owner modules + the facade, zero behavior change across the three steps, the public surface/ops/claim packet byte-stable throughout; the drift-pin test re-pinned to the emission-free facade (an op re-added there = RED); D-227 (R3) the PCC record
why: the phase's acceptance bar — the audit-confirmed hotspot decomposed by the LAW's own §2 semantic owners at zero public-surface drift, each step its own R2/R3 iteration with its PCC record (the ssi phase ladder's own law); the ops' derivation now landing at operations.py is the map's honest end-state, not a cosmetic move
files: workbench/application/inference/{__init__.py(the pure facade),store.py(new),migration.py(new),operations.py(new),compiled.py(new)}, tests/test_topology.py (the re-pinned breach), docs/{SSI_TOPOLOGY.md (the final rows), ssi/SSI_OVERLAY.md (phase 3 CLOSED), DECISIONS.md (D-227), TASKS.md}, STATUS.md, worklog.md (iter-239 evicted) — 8 paths; 2361+9 + ruff + docguard + topology --check clean (zero behavior change, zero core change, the LOG untouched)
---
iter-248 · 2026-09-26 · ssi4-step2-resolution-half (Phase 3 step 2): the RESOLUTION half out of the facade — profile.py (the InferenceProfile document + the validation family, §19.1's ACCEPTED laws) + resolver.py (STATES + resolve + _chain_item_state, the law's §6/§8 effective-state resolver, a pure function of the profile) extracted verbatim; the facade 1157→700 lines (the store/migration/operations/compiled halves + the re-exports; __all__ completed with ChainItem/RELATIONS/effective_temperature); the map rows refreshed in the same iteration — the drift pin went RED on the facade reads + the two missing inventory rows (the designed loudness), then green; D-226 the (R3) PCC record
why: the same method as step 1 — each half lands at its semantic owner in its own R2/R3 iteration, the public import surface byte-stable (tests/test_inference.py still untouched and green), the facade converging on the pure re-export shell; the resolver's purity (no I/O, no store, no gateway) is now structural, not just conventional
files: workbench/application/inference/{__init__.py(shrunk),profile.py(new),resolver.py(new)}, docs/SSI_TOPOLOGY.md (the map rows + §0/§5 refresh notes), docs/DECISIONS.md (D-226), docs/TASKS.md, STATUS.md, worklog.md (iter-238 evicted) — 7 paths; 2361+9 + ruff + docguard + topology --check clean (zero behavior change, zero core change, the LOG untouched)
---
iter-247 · 2026-09-26 · ssi4-step1-inference-package (Phase 3 step 1, the owner's «ssi4 вроде открыть можно» go-ahead): workbench/application/inference.py (2511 lines) became the inference/ PACKAGE — the semantic-control DATA half extracted verbatim (library.py the control model + the 85-control data + the indexes; chain.py the §11 membership/order semantics; presets.py; relations.py) + the facade __init__.py (the behavior halves pending their steps + the re-exports, __all__ completed with the de-facto public names); the public import surface workbench.application.inference UNCHANGED — tests/test_inference.py untouched and green over the moved code, the gateway ops inference.read/update byte-stable; D-225 the (R3) PCC record in the compound DECISIONS row
why: the ssi-3 audit CONFIRMED inference.py the active hotspot (born 2511 in two iterations, single in-repo dep) — the split follows the LAW's own §2 semantic owners (never an external template) and the packlint decomposition method (the proven strangler: D-175, pack.py 3990→290 at zero functional change); the facade-first shape keeps every consumer import stable while the halves move out one R2/R3 step at a time
files: workbench/application/inference.py → workbench/application/inference/{__init__,library,chain,presets,relations}.py, pyproject.toml (the packages list), scripts/topology.py (the WATCHLIST successor = the facade), tests/test_topology.py (the drift-pin breach re-anchored to the facade row prefix), docs/{SSI_TOPOLOGY.md (the map refresh), LLAMA_CPP_INFERENCE_CONTROL_LAW.md, WORKBENCH_APP_LAW.md, AGENT_NAVIGATION.md, ssi/SSI_OVERLAY.md, DECISIONS.md (D-225), TASKS.md}, STATUS.md, worklog.md (iter-237 evicted) — 15 paths; 2361+9 + ruff + docguard + topology --check clean (zero behavior change, zero core change, the LOG untouched)
---
iter-246 · 2026-09-26 · ki99-ui-state-path (the owner's «ki99 закрыть» half of the ssi-4/ki99 call) — KI#99 CLOSED: the shell's ui_state path doubling fixed at its root — globalize_path("res://") returns the project root WITH the trailing slash, the raw three-call base-dir walk counted the slash as a level and landed one short, the appended literal doubled workbench/ (KI#98's class); the fix normalizes the slash FIRST then walks exactly the real project-root depth, correct for both engine forms; the contract pin derives the depth from project.godot's committed location (a moved root goes red, never a silent wrong path)
why: the owner's observed workbench/workbench/runtime/ tree was the base-dir walk's slash-counting bug, not a git-hygiene problem — the one-more-get_base_dir candidate would stay slash-dependent (wrong under the no-slash engine form); the depth-derived pin keeps the .gd honest without a second source (D-024)
files: workbench/presentation/redot/scripts/shell.gd (the _ui_state_path fix), tests/test_shell_contract.py (the new path pin), STATUS.md (KI#99 CLOSED + the header/Next step), worklog.md (iter-236 evicted), docs/TASKS.md (the ledger line, iter-235 evicted) — 5 paths; 2361+9 + ruff + docguard + topology --check clean (zero core change, the LOG untouched)
---
iter-245 · 2026-09-26 · ssi3-close-verify — the owner's Phase 2 verification call («вторую фазу вроде завершили»): the INDEPENDENT re-verification of D-224 in a fresh session — EVERY recorded figure reproduced exactly at the recorded BASE_COMMIT 08300a0 (the inclusive last-150 window: loop↔pack co=11, touches 13/14, lift 9.07, J 0.69, all co-changes iter-90..168; pack 3990→290, loop 1024@window-start; the state-doc pairs 148/144; the top-8 pair table identical; 82 modules; the god-objects' last touches iter-168/107/145/169; the executable battery 2360+9 + ruff + docguard + topology --check green) + two R0 precisions landed: DECISIONS.md's compound-row ID column completed with D-224 (the body carried the record, the ID list stopped at D-223 — citations now resolve per the file's own law) and the dormancy wording made god-object-precise (core/'s post-iter-169 touches = exactly the three gloss/lint commits iter-191/199/200 — economy.py constants + the packlint rename guard; the directory-level «~75 iterations» claim was loose, the REFUTED verdict unchanged)
why: the owner's protocol — the previous session's numbers are re-verified with independent tooling, never trusted; the map is the ssi-4/5 gates' evidence base, so its citation resolution and its dormancy claim must be exact; SSI_TOPOLOGY §0 now records the reproduction + the window-anchoring trap (the figures bind to BASE_COMMIT and the git log -150 window is INCLUSIVE of iter-90 — a HEAD anchor or an exclusive range shifts every count by one, the trap this re-check itself hit first)
files: docs/DECISIONS.md (the ID column + the D-224 precision), docs/SSI_TOPOLOGY.md (§0 the re-verification record, §4 the verdict precision), docs/TASKS.md (the ssi-3 row + the ledger line, iter-234 evicted), STATUS.md (the header + Next step), worklog.md (iter-235 evicted) — 5 paths; 2360+9 + ruff + docguard + topology --check clean (doc-only, zero functional change, zero core change, the LOG untouched; the D-022 exception — the owner's fresh verification request)
---
iter-244 · 2026-09-26 · ssi3-topology-audit — Phase 2 CLOSED (D-224, the owner's go-ahead): docs/SSI_TOPOLOGY.md the machine-readable map (the 82-module owner/reads/writes/emits table over core/+workbench/ Python — the mechanical columns ast/git-derived, the owner column authored from NAV §1 + the specs) + scripts/topology.py the NEW audit instrument (admitted per D-198: the named consumers ssi-4/ssi-5 + the N018 trajectory audit; --audit the full JSON at any HEAD, --check the drift pin — inventory/owners/watchlist reads+emits) + tests/test_topology.py (the real-repo drift pin + 6 crafted breaches) + the co-change/trajectory audit over the last 150 commits (iter-90..HEAD): line counts CONFIRMED exactly; loop.py↔pack.py the #1 code pair numerically (11×, lift 9.07, J 0.69) but HISTORICAL — all co-changes iter-90..168 (the add-a-system era), both dormant since, pack decomposed 3990→290 via packlint; ssi-5's core scope SHRINKS (live coupling refuted, core dormant ~75 iterations), ssi-4's target CONFIRMED the active hotspot (inference.py born 2511 in 2 iterations, single dep); the overlay updates (block B VERIFIED, N018 VERIFIED — the trajectory audit now the executable, phase 2 CLOSED)
why: N018's own law — architecture health from co-change/trajectory evidence, never a line-count snapshot; the external analysis's numbers were accurate but its implied risk profile was stale, and the audit's verdict re-scopes the strangler phases accordingly (the map is the evidence base the ssi-4/5 splits consume)
files: docs/SSI_TOPOLOGY.md(new), scripts/topology.py(new), tests/test_topology.py(new), docs/ssi/SSI_OVERLAY.md, docs/AGENT_NAVIGATION.md, docs/{TASKS.md,DECISIONS.md(D-224)}, STATUS.md, worklog.md — 8 paths; 2360+9 + ruff + docguard + topology --check clean (zero functional change, zero core change, the LOG untouched)
---
iter-243 · 2026-09-26 · ssi3-executable-rules — Phase 1's close (D-223, the owner's chosen eight, never all twenty): SSI-N001 no-hidden-effect + SSI-N002 no-new-primitive-without-gap landed in tests/test_architecture.py as the stdlib-ast family's widening — N001 the wall-clock/entropy import ban (time/datetime/secrets/uuid across core/sim/render/brief/cli; INV-2's static surface widened from the bare random monopoly — the entropy authority stays core/rng.py, workbench/ outside the determinism envelope per TEST_PLAN §8.4) + N002 the admission-closure check (every PACKAGE_DIR carries its NAV §1 owner row — the sim/ row landed with it, the completion fix, not a waiver) + the five docguard families (N006/N007/N010/N017/N018) from iter-242 confirmed live at HEAD + N018/N020's FULL executable forms OPEN by design (N018's trajectory audit fires with ssi-3/Phase 2, N020's gc-gate with ssi-8/Phase 7 — the overlay's rule table carries the states, never a silent skip) + the other twelve rules reference-only on the owner's call (AGENTS §2.8)
why: the control plane's claims must be EXECUTABLE and honest where they are not — the eight rules ride the two existing instruments (test_architecture + docguard, never a new tool, D-198), and every OPEN state is a named phase's future instrument, never a quiet skip
files: tests/test_architecture.py (+2 checks), docs/AGENT_NAVIGATION.md (the sim/ row), docs/{TASKS.md,DECISIONS.md(D-223)}, STATUS.md (the header + Next step: ssi-3/Phase 2 the next owner-gated row), worklog.md — 6 paths; 2353+9 + ruff + docguard clean (zero functional change, zero core change, the LOG untouched)
---
