# worklog — canonsim

> Cap: 10 entries, one in / one out. 3–5 lines per entry: what changed, why,
> files touched. No diffs, no command output, no reasoning traces. Long-term
> rationale belongs in `docs/DECISIONS.md`. Pre-trim history lives in git.
---
iter-244 · 2026-09-26 · ssi3-topology-audit — Phase 2 CLOSED (D-224, the owner's go-ahead): docs/SSI_TOPOLOGY.md the machine-readable map (the 82-module owner/reads/writes/emits table over core/+workbench/ Python — the mechanical columns ast/git-derived, the owner column authored from NAV §1 + the specs) + scripts/topology.py the NEW audit instrument (admitted per D-198: the named consumers ssi-4/ssi-5 + the N018 trajectory audit; --audit the full JSON at any HEAD, --check the drift pin — inventory/owners/watchlist reads+emits) + tests/test_topology.py (the real-repo drift pin + 6 crafted breaches) + the co-change/trajectory audit over the last 150 commits (iter-90..HEAD): line counts CONFIRMED exactly; loop.py↔pack.py the #1 code pair numerically (11×, lift 9.07, J 0.69) but HISTORICAL — all co-changes iter-90..168 (the add-a-system era), both dormant since, pack decomposed 3990→290 via packlint; ssi-5's core scope SHRINKS (live coupling refuted, core dormant ~75 iterations), ssi-4's target CONFIRMED the active hotspot (inference.py born 2511 in 2 iterations, single dep); the overlay updates (block B VERIFIED, N018 VERIFIED — the trajectory audit now the executable, phase 2 CLOSED)
why: N018's own law — architecture health from co-change/trajectory evidence, never a line-count snapshot; the external analysis's numbers were accurate but its implied risk profile was stale, and the audit's verdict re-scopes the strangler phases accordingly (the map is the evidence base the ssi-4/5 splits consume)
files: docs/SSI_TOPOLOGY.md(new), scripts/topology.py(new), tests/test_topology.py(new), docs/ssi/SSI_OVERLAY.md, docs/AGENT_NAVIGATION.md, docs/{TASKS.md,DECISIONS.md(D-224)}, STATUS.md, worklog.md — 8 paths; 2360+9 + ruff + docguard + topology --check clean (zero functional change, zero core change, the LOG untouched)
---
iter-243 · 2026-09-26 · ssi3-executable-rules — Phase 1's close (D-223, the owner's chosen eight, never all twenty): SSI-N001 no-hidden-effect + SSI-N002 no-new-primitive-without-gap landed in tests/test_architecture.py as the stdlib-ast family's widening — N001 the wall-clock/entropy import ban (time/datetime/secrets/uuid across core/sim/render/brief/cli; INV-2's static surface widened from the bare random monopoly — the entropy authority stays core/rng.py, workbench/ outside the determinism envelope per TEST_PLAN §8.4) + N002 the admission-closure check (every PACKAGE_DIR carries its NAV §1 owner row — the sim/ row landed with it, the completion fix, not a waiver) + the five docguard families (N006/N007/N010/N017/N018) from iter-242 confirmed live at HEAD + N018/N020's FULL executable forms OPEN by design (N018's trajectory audit fires with ssi-3/Phase 2, N020's gc-gate with ssi-8/Phase 7 — the overlay's rule table carries the states, never a silent skip) + the other twelve rules reference-only on the owner's call (AGENTS §2.8)
why: the control plane's claims must be EXECUTABLE and honest where they are not — the eight rules ride the two existing instruments (test_architecture + docguard, never a new tool, D-198), and every OPEN state is a named phase's future instrument, never a quiet skip
files: tests/test_architecture.py (+2 checks), docs/AGENT_NAVIGATION.md (the sim/ row), docs/{TASKS.md,DECISIONS.md(D-223)}, STATUS.md (the header + Next step: ssi-3/Phase 2 the next owner-gated row), worklog.md — 6 paths; 2353+9 + ruff + docguard clean (zero functional change, zero core change, the LOG untouched)
---
iter-242 · 2026-09-26 · ssi2-ssi-foundation — Phase 1 (the owner's same call): D-222 — the READ-ONLY reference copy landed docs/ssi/ (the 34-file external package verbatim, md5-pinned; the placement via D-198's admission ladder: docs/ the existing mechanism — the D-214/D-218/D-219 pack-admission precedents + the worldbuild//ref//blueprint/ subdir precedents; a top-level ssi/ REJECTED, the runtime dependency REJECTED standing) + docs/ssi/SSI_OVERLAY.md the ONE authored file (the A–L block matrix onto the repo's existing mechanisms as a table — AGENTS §4/§2, the STATUS KI lifecycle, DECISIONS, test_architecture + the stoplist test, TEST_PLAN's stack; the executable negative-rules subset; the PCC record form; the deletion-card form; the phase ladder; the two over-cap doctrine files docguard-allowlisted on the phases.md research-archive precedent) + the risk ladder R0–R5 as AGENTS.md §2.9 (a refinement of the 3–5-file soft limit: R0–R2 zero added bureaucracy, R3+ self-declares the class tag and carries the compact PCC record in the DECISIONS row — never a separate file per change) + docguard's SSI shape families (the block matrix complete, the closed state vocabulary, the explicit waive/skip laws, the declared eight-rule subset with instruments-on-disk, the phase ladder owner-gated with N018's evidence bar on phases 2/4, the R3+ PCC fields, the AGENTS ladder pin) + the test_docguard extension (the fixture + 7 crafted-breach tests) + the NAV §1/§2/§3 + STATUS FAQ routing
why: the owner's control-plane call — the SSI package governs WITHOUT becoming a runtime dependency or a second source of truth (D-024); the shapes are the executable: docguard's own counts-over-forms law extended, never a new parallel tool (D-198 honored)
files: docs/ssi/** (34 reference files + SSI_OVERLAY.md(new)), AGENTS.md (§2.9), scripts/docguard.py, tests/test_docguard.py, docs/AGENT_NAVIGATION.md, docs/{TASKS.md,DECISIONS.md}, STATUS.md, worklog.md — 40+ paths (the reference copy one unit, the 3–5 soft limit honestly over, the iter-239 doc-admission precedent); 2344+16 + ruff + docguard clean (zero functional change, zero core change, the LOG untouched); iter-232/231 evicted per the cap
---
iter-241 · 2026-09-26 · ssi1-architecture-freeze — the owner's 2026-09-26 SSIEC-v3 control-plane call (the external governance package as a CONTROL PLANE over the repo, never a runtime dependency, no big-bang rewrite): Phase 0 CLOSED — D-221 the architecture freeze (the eight public-contract surfaces — event schema, log format, fold, scheduler order, RNG, pack, checkpoint, the gateway's public ops — confirmed at their standing owners INV-1..5/EVENT_SCHEMA/PACK_SPEC, never re-stated; the standing no-touch floor core/log.py, core/rng.py, core/schema.py, the fold mechanics; frozen for the initiative's duration) + the ssi track registered in TASKS.md (ssi-1..ssi-8 rows, phases 2..7 owner-gated, D-198 task-intake) + KI#99 opened (the shell's ui_state path DOUBLES workbench/ — globalize_path("res://") carries the trailing slash, the first get_base_dir() consumes only it, the appended "/workbench/runtime/…" doubles; KI#98's class; source-verified against Godot 4.4/Redot 26.2 ustring.cpp + project_settings.cpp) + KI#98 deleted per the 2-iteration cleanup law
why: the freeze pins the initiative's blast radius at zero public-contract drift BEFORE any SSI machinery lands; KI#99 registered outside the initiative per the owner's instruction (may close earlier than the phases)
files: docs/TASKS.md (the ssi rows + the ledger line), docs/DECISIONS.md (D-221 into the compound row), STATUS.md (KI#99 + the KI#98 cleanup), worklog.md — 4 paths; 2344+9 + ruff + docguard clean (doc-only, zero code, the LOG untouched); iter-230 evicted per the cap
---
iter-240 · 2026-09-26 · inf2-full-chip-library — the owner's 2026-09-26 «в прошлой итерации ты начал => но результат вообще неудовлетворительный! Очень коряво, криво и косо + далеко не все сэмплеры и настройки есть. Дорабатывай и доделывай по-человечески» call: the inf-1 slice completed honestly — the FULL control library (85 controls over the 15 reviewed categories: the COMPLETE sampling surface — Top-K/P, Min-P, Typical-P, Top-N-Sigma, the penalties family, the DRY family, the XTC family, the Mirostat family, Dynatemp, Adaptive-P, Ignore EOS, Logit Bias, Seed — plus model/device/memory/loading/moe/cpu/chat-reasoning/structured/server/observability/speculative/rope/special/lora; every disabled/AUTO form the runtime's own literal) + the typed relation DATA (the mirostat effective_noop from the --help's own words «Top K, Nucleus and Locally Typical samplers are ignored», the temperature-0 deterministic noop, the family requires — DRY/Mirostat/dynatemp/adaptive/XTC/speculative/custom-template) + the 9-member chain (inf-1 5-member profiles upgrade at load, the operator's order preserved) + the four §30 presets as transparent diff-previewed documents + the workspace pinning (the profile document's OWN workspace section — one file, two named sections, never a second store) + the DATA-DRIVEN Redot workspace (inference.gd rebuilt: the editors build from the read document's own value_type/forms/limits metadata, the rows build ON the read — inf-1's compose-before-read bug, the live flow rendering EMPTY category groups, repaired; the search/pinned-strip/collapsible-categories/advanced-rung/preset-row regions; the carried-editor-values law — the unsaved edits survive a refresh) + the platform compile seam (SEMANTIC_FLAG_TABLE + build_semantic_command — field-keyed values in, flag syntax out, unknown fields LOUD, the legacy build_server_command byte-stable as the deployment path; the 138-token duplicate-ownership guard) + D-220
why: the owner's report named BOTH defects' root cause — a hand-encoded 13-control slice composing before its data: the missing families and the empty live surface were the same bug (the UI re-encoding the vocabulary client-side + the rows built at compose time); the smallest architecture that fixes both is the data-driven full library over the one read document + the typed relations + the one-file workspace section
files: workbench/application/inference.py (rewritten: the full library, the relations, the presets, the store's values-mapping profile, the chain upgrade, the workspace section), workbench/platform/llama_process.py (SEMANTIC_FLAG_TABLE + build_semantic_command + semantic_flag_tokens), scripts/workbench_app.py (compile_semantic + the semantic spawn/preview + the full-token guard), workbench/presentation/redot/scripts/{inference.gd(rewritten),strings.gd}, tests/{test_inference.py(rewritten, 34 tests),test_workbench_app.py,test_managed_backend.py,test_shell_proof.py,_managed_fake_server.py (the argparse short-flag-cluster fix — the token-scan parse)}, docs/{LLAMA_CPP_INFERENCE_CONTROL_LAW.md,FRONTEND_UIUX_LAW.md,DECISIONS.md,TASKS.md}, STATUS.md, worklog.md — 14 paths (the 3–5 soft limit honestly over, the inf-1 precedent); 2344+9 (CI) / 2352+1 (REDOT_EXE — the inference capture double-run byte-identical, VLM-verified: the pinned chips «Temperature: 0.7 · EFFECTIVE», the collapsible categories with counts, the 9-member chain with the INEFFECTIVE/INACTIVE states and reasons) + ruff + docguard clean (zero core change, the LOG untouched); iter-233 evicted per the cap
iter-239 · 2026-09-25 · inf1-inference-control-slice — the owner's 2026-09-25 chip-workspace hand-off (the external llama_cpp_chip_workspace_spec_2026-09-25.md + the reviewed «флаги llama.cpp.txt» snapshot, both research inputs never vendored): the llama.cpp semantic inference-control layer as a NATIVE workbench capability — docs/LLAMA_CPP_INFERENCE_CONTROL_LAW.md (D-219, the binding owner: Settings ≠ Inference Control, the three-layer split raw-capability/semantic-control/UI-projection, the chip-as-semantic-runtime-control identity) + workbench/application/inference.py the semantic core (the 13-control library over 5 categories — Context, GPU Layers auto/all/explicit, Flash Attention, Fit, KV K/V, Temperature, Top-K, Top-P, Min-P, Penalties, Seed, Chat Template, the ordered chain; the profile store inference.json; the deterministic resolver with the evidence-pinned relations — temperature-0 deterministic preserves the sampler configuration, the runtime's own disabled forms INACTIVE, chain-membership INEFFECTIVE; the compiled launch surface; the one-way migrate_launch_semantics schema/1→settings/2+inference/1) + build_server_command's additive params (samplers ';', seed, fit, kv — the legacy command byte-stable) + the extra_args duplicate-ownership guard (both flag forms matched, the compile-time conflict) + the gateway family (inference.read/update) + chat's BASE re-point + the run document's REQUESTED/EFFECTIVE pair + inference.gd the Inference surface + the Chat projection row + the Settings deployment slim + the --inference-document proof injection
why: the flat launch fields had no meaning layer and Settings was drifting into the flag browser the chip specification forbids; the vertical slice proves the model across control kinds (value/mode/toggle/chain) — the deferred ladder (capability discovery, the library/pinning workspace, presets/scenarios/recipes/hardware, the DRY/XTC/Mirostat/MoE/server groups) rides its own owner-gated rows, never one advanced-flags bucket
files: docs/LLAMA_CPP_INFERENCE_CONTROL_LAW.md(new), workbench/application/inference.py(new), workbench/application/settings.py, workbench/application/operations/{composition.py,backend.py}, workbench/platform/llama_process.py, scripts/workbench_app.py, workbench/presentation/redot/scripts/{inference.gd(new),shell.gd,strings.gd}, scripts/visual_proof.py, tests/{test_inference.py(new),test_settings.py,test_workbench_app.py,test_managed_backend.py,test_operations.py,test_shell_contract.py}, docs/{WORKBENCH_APP_LAW.md,FRONTEND_UIUX_LAW.md,VISUAL_SYSTEM_UI.md,DECISIONS.md,TASKS.md,AGENT_NAVIGATION.md}, STATUS.md, worklog.md — 26 paths (the 3–5 soft limit honestly over: the semantic core + the surface + the claim packet + the state docs, AGENTS §2.3, the obs-2 precedent); 2335+8 (CI) / 2343+1 (REDOT_EXE — the eight visual packets incl. the inf-1 inference capture, VLM-verified composition: the nav's WORK group with Inference selected, the profile strip, the ordered chain with Top-K INEFFECTIVE-yet-preserved, the state badges) + ruff + docguard clean (zero core change, the LOG untouched; the live-binary check: the compiled forms pinned from the reviewed snapshot, the sandbox's older llama.cpp build refusing the newer forms loudly — the honest capability-authority demonstration)
---
iter-238 · 2026-09-25 · ki98-launcher-url-route — the owner's 2026-09-25 report («в какой-то из итераций с Workbench.bat всё работало, но потом перестало... проводник — не могу открыть и выбрать языковые модели, и они не обнаруживаются» over the GGUFs sitting in workbench/runtime/models): KI#98 pinned dead — the iter-227 bind-URL forward shipped the gateway's full ENDPOINT (transport.url, the /op route INCLUDED, the wb-4 shape) as the Redot child's --gateway-url, while gateway_client.gd owns the route itself (base + "/op") — every shell request landed on /op/op → HTTP 404 → app.status dead → session.create never dispatched → the Models manager's Add-local/Add-folder buttons never enabled («проводник не открывается»), model.list never answered («модели не обнаруживаются»), the managed llama-server never spawned; the false pin (the bind-line test's banner WITHOUT the route) kept CI green while the real banner always carried it — iter-228's transport-13 chain and iter-232's scan latch treated symptoms of a dead circuit whose root cause was the URL
why: the launcher's parse treated the banner remainder as host:port (rstrip("/") cannot strip a route); the one-route owner is the CLIENT (gateway_client.gd appends /op — the committed project setting, the env var, and the default all use the ROOT form), so the forwarded value must be scheme://host:port
files: scripts/workbench_launch.py (the path-stripping parse + the KI#98 docstring law), tests/test_workbench_launch.py (the false pin re-pinned over the REAL banner shape + the NEW end-to-end pin: the REAL gateway process's banner → the parse → root+"/op" POSTs 200 — banner/route/parse drift all go RED), STATUS.md (KI#98 + the FAQ URL clause + the KI#97 cleanup), docs/TASKS.md (the ledger line), worklog.md — 5 paths; the full circuit verified live against the real gateway + the fake managed llama-server: session.create → model.list (the dropped GGUF discovered) → model.load (the managed spawn COMPLETED) → chat.send (the reply COMPLETED) → model.unload; 2309+8 + ruff + docguard clean (the REDOT_EXE run green against redot-26.2-stable: 2316+1, the seven visual packets)
---
iter-237 · 2026-09-25 · corpus-rehoming — the owner's «переработай документацию нормально и мне не приходилось её загружать постоянно» call over the re-delivered 8-file v5.2 corpus: the loss/ownership audit (the LAW distillation ~80% faithful with 10 material losses; the app spec ~60% implemented, its un-landed contracts dangling on 26 unresolvable §-references; the Observatory analytical layer + world-presentation contracts unowned) then the re-homing — docs/WORKBENCH_APP_LAW.md + docs/OBSERVATORY_LAW.md + docs/WORLD_PRESENTATION_LAW.md (NEW owners, §-numbering preserved 1:1 so every existing `app §N`/Observatory `§N` citation resolves) + the repairs: FRONTEND_UIUX_LAW +§11.1/§14.1/§20/§21.1/§21.2/§22.1/§22.2 (dense-data/forms/search, task modelling, chat ergonomics, the AI-interaction contract, usability metrics, the acceptance benchmark + G1–G12) + VISUAL_SYSTEM_UI +§1.1/§2.1/§2.2/§12 (Gestalt, dimension tokens, typography, two-layer identity) + REDOT_ENGINE_INDEX §20.1 (the version-admission gate) + CONTRACTS §5 the §-map + the drifted LAW §50/§51/§39/§42 citations re-pointed
why: the corpus was admitted piecemeal (one doc per owner call) and the three biggest domains never got repo owners — the §-references resolved to nothing a fresh agent could read, and the missing contracts (typography, tokens, chat ergonomics, dense-data) are exactly what made the first UI iterations drift (the theme re-pinned three times, the follow law absent at birth)
files: docs/{WORKBENCH_APP_LAW.md(new), OBSERVATORY_LAW.md(new), WORLD_PRESENTATION_LAW.md(new), FRONTEND_UIUX_LAW.md, VISUAL_SYSTEM_UI.md, REDOT_ENGINE_INDEX.md, AGENT_NAVIGATION.md, CONTRACTS.md, TASKS.md, DECISIONS.md}, STATUS.md, worklog.md, scripts/docguard.py (the §6.1 allowlist entry) — 13 paths; 2306+2 + ruff + docguard clean (doc-only, the LOG untouched)
---
iter-236 · 2026-09-25 · obs2-live-run-read-seam — the live run over the READ-side seam (FRONTEND_UIUX_LAW §25's P1 continuation + the selection model's first consumer §6): workbench/observatory_read.py the seam's ONE home (the bounded read model over core.log.read_log — the wb-1 scene_build read-side edge; the operations package stays CanonSim-free per its envelope): observatory.runs (the discovery scan, honest per-run degradation, content truths only) + observatory.read (the after-cursor window over EVENT IDS — default 50/cap 200 the ONE ceiling, authority CANONICAL rendered never guessed, duplicate-id/stale-cursor/corrupt refused loud) + operations/observatory.py the ops layer + the composition's injected runs-root+schema wiring (half-wired refused) + observatory.gd v0.2 the live feed (picker/Refresh/Earlier-Later; the selection the EVENT ID on button metadata restoring by ID; the inspector scrolling with cause + state changes as DATA — only the READ rung confirms under a selection; the DISTINCT empties probing/NO RUNS/NO EVENTS/refused; the breadcrumb run → event) + the shell's two-signal hosting + --obs-document the proof-only injection (a REAL op document, the same feed path; the meta's observatory_run)
why: the LAW's own ladder — obs-1 validated the grammar honestly-empty; obs-2 feeds it from the canonical source without a second transport or authority (INVARIANT 1/2); the Redot 26.2 lessons found by the VLM/runtime verification are pinned (JSON.parse_string the static form; JSON numbers parse as FLOATS — _int_text/_normalize_numbers; the GridContainer value column needs EXPAND_FILL; JSON null is a PRESENT key)
files: workbench/{observatory_read.py(new),application/operations/observatory.py(new),application/operations/composition.py}, scripts/workbench_app.py, workbench/presentation/redot/scripts/{observatory.gd,shell.gd,strings.gd}, scripts/visual_proof.py, tests/{test_observatory_read.py(new),test_shell_contract.py,test_shell_proof.py,test_operations.py}, STATUS.md, docs/{TASKS.md,DECISIONS.md}, worklog.md — 13 paths; 2308+8 (CI) / 2315+1 (REDOT_EXE — the LOADED capture double-run byte-identical, distinct from the empty slice, VLM-verified + the headless inspector dump green) + ruff + docguard clean; iter-225 evicted per the cap
iter-235 · 2026-09-25 · obs1-observatory-slice — the P1 vertical UX slice (FRONTEND_UIUX_LAW §25/§50/§51 — the interaction grammar BEFORE full analytical backend coverage, the standing resolution's own law: no jump from documentation to an 1800-line Observatory): the nav rail adopts the §4.1 IA — WORK (Chat · Observatory; Simulation/Inference planned) / RESOURCES (Models; Prompts/History) / SYSTEM (Settings; Runs/Diagnostics — the LAW's map, Runs added, Inference kept, nothing dropped) + scripts/observatory.gd the responsibility-split seed (LAW §18: the surface composes ITSELF over the injected theme + the shell's own _tr Callable — one boundary, one token source): the semantic breadcrumb, the context identity strip (pack/run/seed/tick/profile/revision, the honest no-session values), the World Question contract (QUESTION/CLASS/TARGET/SCOPE — the DRAFT lifecycle, nothing asked nothing claimed), ONE primary read-only view (the event table's TICK/EVENT/KIND/AUTHORITY columns — the authority axis in the representation; the DISTINCT NO DATA semantics, never no-match/no-evidence), the inspector region (HSplit, nothing selected — honestly), the evidence ladder (READ/BRANCH/STATE/DIVERGENCE/PERSISTENCE, every rung's unknown as TEXT) — zero fabricated content, zero dispatch, zero new transport + the runner drift fix (visual_proof.py's --surface choices=[chat,settings] lagged the shell through wb-8/9/obs-1 — the vocabulary is the shell's, the runner never gates it)
why: the law's queue (obs-1 next after ux-1) + the VLM-verified composition (all regions render, no overlap, no clipping) — the grammar validated BEFORE the backend, the Observatory no longer absent even as a stub
files: workbench/presentation/redot/scripts/{observatory.gd(new),shell.gd,strings.gd}, scripts/visual_proof.py, tests/{test_shell_contract.py,test_shell_proof.py}, STATUS.md, docs/{TASKS.md,DECISIONS.md,AGENT_NAVIGATION.md}, worklog.md — 10 paths; 2290+7 (CI) / 2296+1 (REDOT_EXE, the observatory capture green) + ruff + docguard clean; iter-224 evicted per the cap
