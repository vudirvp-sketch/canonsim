# worklog — canonsim

> Cap: 10 entries, one in / one out. 3–5 lines per entry: what changed, why,
> files touched. No diffs, no command output, no reasoning traces. Long-term
> rationale belongs in `docs/DECISIONS.md`. Pre-trim history lives in git.
---

iter-231 · 2026-09-25 · ci-unred-nav-variation — the owner's 2026-09-25 engine report (the nonexistent add_theme_type_variation call killing _ready at _build_nav_rail + the unused row warning) + the CI-red report (runs 82–84 red since iter-227, «Run python -m pytest -q» failing): KI#93 — the launcher/gateway pipe chain owned nothing of its buffering (the gateway child's bind line block-buffered in the pipe unless the host exported PYTHONUNBUFFERED — the sandbox does, CI/owner machines do not; the 90s boot probe honestly failed «the gateway failed to serve (exit code 0)»; local green had masked CI red for three iterations) + KI#94 — iter-230's NavButton wiring over a Control method Redot 26.2 does not have (the API is the theme_type_variation PROPERTY)
why: the verification gap itself sheltered the bug — every local run rode the sandbox's global PYTHONUNBUFFERED=1; the engine's own error message was the API truth (the Godot 4 docs cross-reference: 6 property mentions, 0 method mentions)
files: scripts/workbench_launch.py (the gateway child rides -u + the supervisor line-buffers its own stdout), workbench/presentation/redot/scripts/shell.gd (the property form ×2 + the dead row local), tests/test_workbench_launch.py (the env-stripping spawn), tests/test_shell_contract.py (the property pin + the negative method pin), STATUS.md, docs/TASKS.md, worklog.md — 7 paths; 2285+6 + ruff + docguard clean, VERIFIED TWICE (the standard env AND the stripped PYTHONUNBUFFERED env — the CI/owner condition); iter-221 evicted per the cap

---

iter-230 · 2026-09-25 · wb-12-token-audit-chat-follow — the owner's «тема и UI все так же убоги, тема ужасная» + «в чате при получении сообщений от языковой модели => не происходит плавной прокрутки вниз» calls (the v5.2 plans pack the direction hand-off): theme@0.3 (the neutral ramp re-pinned over the Catppuccin Mocha VALUE reference — §6's mechanisms-not-looks: the layered luminance separation is the mechanism; ONE accent #89b4fa; accent_deep retired; the NavButton variation + styles/chip_busy) + the chat follow law (the tween over the scrollbar's float value, the layout-settle await, the near-bottom gate, follow on every role, the bounded late-layout re-settle) + the GENERATING chip (the §5 matrix state's visible carrier: pulsing dot AND label)
why: the owner's named blockers — the hated warm-orange/brown incoherent theme (blue-navy surfaces + brown pressed states + orange accent) and the dead-feeling chat (no follow on replies, the assistant-only integer jump that raced the autowrap layout); the v5.2 pack named the visual direction, the admitted VISUAL_SYSTEM_UI law owns the form
files: workbench/presentation/redot/themes/workbench_theme.tres, workbench/presentation/redot/scripts/shell.gd, tests/test_shell_contract.py, docs/STATUS.md, docs/TASKS.md, docs/AGENT_NAVIGATION.md, docs/CONTRACTS.md, docs/DECISIONS.md, README.md, worklog.md — 10 paths (the 3–5 soft limit honestly over: the theme + the shell + the claim packet + the state docs, AGENTS §2.3); 2285+6 + ruff + docguard clean (zero core change, zero pack change, the LOG untouched)

---

iter-229 · 2026-09-26 · visual-system-admission — the owner's 2026-09-26 «вот и отлично, зафиксируй где нужно и обращайся при дальнейшей работе! нынешний ui - ужасен! поэтому всеми тремя руками и ногами - за!» call over the external 24-section Visual System and UI Engineering instruction document: docs/VISUAL_SYSTEM_UI.md ADMITTED (D-211, the D-207 pattern) — the surface-driven grammar + the token taxonomy + the neutral/one-accent law + the not-color-only invariant + the per-surface state matrix + the transplantation protocol (mechanisms, not looks) + the reference pack (§2.8; ThemeGen a tool; SillyTavern license-sensitive) + the §8 per-row UI report + the anti-patterns + the wb-12+ queue; the standing resolutions: functional first, the doc is the law for each row never a parallel program, the app §18 evidence law wins over quiet chrome
why: the current UI is horrible (the owner's own words) and the visual work needs ONE law owner inside the repo — the external document is ephemeral (D-024/D-200), the routing was homeless; every visual row now routes through the admitted law before any palette lands
files: docs/VISUAL_SYSTEM_UI.md, docs/AGENT_NAVIGATION.md, docs/TASKS.md, docs/DECISIONS.md, STATUS.md, worklog.md — 6 paths (doc-only); 2284+6 + ruff + docguard clean

---

iter-228 · 2026-09-26 · wb-11-load-unload-runs — the owner's 2026-09-26 report over the wb-10 handback («молча висят + транспорт результ 13» + «модель выбрать не могу, там пусто, моделей не видно, кнопка выбрать модель не работает (проводник не открывается)» + the evidence «llama.cpp и модели запускаются штатно если отдельно запускать»): the transport-13 freeze chain pinned dead — backend.py's model.load/model.unload as RUNS (the chat.send pattern: the fast dispatch walks the ladder + admits, the minutes-class spawn/stop rides the worker thread OFF the coarse dispatch lock; the WIRE SHAPE CHANGE — the execution document returns, the terminal truth rides run.get, the FAILED diagnostics carry the observed cause, §21; the single-slot + in-flight guards; the rows' own §12 deadlines 330/30) + llama_process.py's _PipeDrain (one daemon reader per captured pipe into a bounded 64KB tail ring — the Windows pipe-buffer wedge + the os.set_blocking '(empty)' bug dead, stderr_tail cross-platform) + shell.gd's run-poll circuits (model-load-get/model-unload-get on the shared tick; the honest picker/load guards — never a silent return, §18; the failed scan re-arm so the empty list never sticks)
why: the synchronous minutes-class load under the gateway's one coarse lock starved EVERY concurrent request past the client's 10s budget (transport_result_13 = HTTPRequest RESULT_TIMEOUT) — the empty Models list, the dead session-gated buttons, the silent UI; the owner's evidence isolated the bug to the workbench's integration, not llama.cpp
files: workbench/application/operations/backend.py, workbench/platform/llama_process.py, workbench/presentation/redot/scripts/shell.gd, tests/test_backend_row.py, tests/test_shell_contract.py, docs/CONTRACTS.md, docs/TASKS.md, docs/DECISIONS.md, STATUS.md, worklog.md — 10 paths (the 3–5 soft limit honestly over: the run rework spans the handler + the platform + the frontend + the claim packet + the state docs, AGENTS §2.3); 2284+6 + ruff + docguard clean (zero core change, zero pack change, the LOG untouched, INV-4's three-surface form unchanged)

---

iter-225 · 2026-09-25 · redot-engine-index — the owner's external Redot/Godot agent reference index ADMITTED into the repo (the owner's «отразить в репозитории» call — the tmpfiles artifact ephemeral, the engine routing homeless): docs/REDOT_ENGINE_INDEX.md (the version firewall + the routing map; admission deltas only — §2.2/§2.3 reconciled to the landed wb surface + the v5.2 docs' external-by-law status, §10's skills pinned agent-side, the external changelog cut) + the routing surfaces (NAV §1/§2/§3 + STATUS FAQ + the wb-9+ pointers in TASKS/CONTRACTS §5 + README) + docguard's allowlist entry (§6.1's substance record) + D-207 into the standing-rows family row (30 held)
why: the Redot surface (the whole frontend + the network seams, wb-9+ next) needs in-time routing — the agent must hit the version firewall BEFORE any Godot-ism lands, and the index rots by design (the REFERENCES quarterly pass its refresh)
files: docs/REDOT_ENGINE_INDEX.md, scripts/docguard.py, docs/AGENT_NAVIGATION.md, docs/TASKS.md, docs/CONTRACTS.md, docs/DECISIONS.md, README.md, STATUS.md, worklog.md — 9 paths (doc-only, the owner's explicit call, AGENTS §2.3's scope note); 2223+6 + ruff + docguard clean; iter-215 evicted per the cap
iter-224 · 2026-09-25 · gdscript-fix-delivery-protocol — the wb-8 handback closed (the owner's 2026-09-25 report — five shell.gd parse refusals at 806/861/868/999/1042, adjacent string literals across lines, the Python-ism GDScript refuses + the «тебе ничего комитить и пушить не надо» directive): the five notes ONE literal per line + tests/test_shell_contract.py's adjacent-literal ban over every committed .gd + AGENTS §9/§12 the delivery protocol's standing form (D-206 — the agent NEVER runs git commit/push against the owner's repository: no access exists; the sandbox git bookkeeping only; every file-touching iteration delivers the delta archive BOTH channels — the chat attachment + the tmpfiles.org direct link with md5 and byte size — and closes with the owner-side Git Bash block: cd /c/Users/fallo/OneDrive/Desktop/repo/canonsim, git status --short, git add <explicit paths>, git status --short, commit, push, re-runnable)
why: the owner's own hands are the repository's only write path — an agent-side commit/push is a protocol violation by construction; and the parse refusal was the owner's live blocker (the report must carry the apply commands, never assumptions)
files: workbench/presentation/redot/scripts/shell.gd, tests/test_shell_contract.py, AGENTS.md, docs/DECISIONS.md, docs/TASKS.md, STATUS.md, worklog.md — 7 paths (the 3–5 soft limit honestly over: the fix + the pin + the protocol law + the state docs, AGENTS §2.3); 2223+6 + ruff + docguard clean (zero core change, the LOG untouched); iter-214 evicted per the cap
iter-223 · 2026-09-24 · wb-8-managed-models — the managed-models row LANDED (the owner's «выбирать модель я должно из интерфейса» + «llama.cpp тоже запускаться при загрузке модели» + «настройки подтягиваться и самые нужные флаги» calls over the v5.2 brief — frontend §46 Phase A's Models row + app §11.1's MANAGED half): workbench/platform/llama_process.py (the process mechanics, §2's platform/process row: build_server_command the typed honest default flag set — -m/--host/--port/-ngl/-c/-fa on/-a/--jinja/--no-webui + extras verbatim, re-verified 2026-09-25; LlamaServerProcess spawn/exit-observation/the §11.1 bounded graceful stop with the OBSERVED exit code; the readiness wait over an INJECTED probe — zero network imports) + scripts/workbench_app.py's --managed form (_ManagedBackend the port-face wrapper at the composition root — healthy delegates unchanged, load-on-down spawns llama-server WITH the caller's model, the spawned unload stops the process, Ctrl+C ours only, the honest 'unavailable' cause — §12.1's sibling, rests SELECTED, re-load legal) + backend.py's model.states READ + the Redot Models surface (shell.gd: discovery + lifecycle states + Load/Unload with the minutes-class per-call timeout over gateway_client.gd's timeout_s envelope, the honest notes, FAILED shown never hidden; the owner-reported warnings fixed — _tag/_body) + tests/test_managed_backend.py (17) + tests/_managed_fake_server.py + the shell-contract wb-8 pins
why: the row the owner's calls opened — the model flow becomes a UI flow; the process stays behind the gateway (G8) and behind the port (INV-4 — the wrapper decides WHEN a process must exist, the HTTP stays cli/engine.py's)
files: workbench/platform/llama_process.py, workbench/platform/__init__.py, scripts/workbench_app.py, workbench/application/operations/backend.py, workbench/presentation/redot/scripts/shell.gd, workbench/presentation/redot/scripts/gateway_client.gd, tests/test_managed_backend.py, tests/_managed_fake_server.py, tests/test_shell_contract.py, tests/test_backend_row.py, tests/test_workbench_app.py, pyproject.toml, docs/CONTRACTS.md, docs/TASKS.md, docs/DECISIONS.md, docs/AGENT_NAVIGATION.md, README.md, STATUS.md, worklog.md — 19 paths (the 3–5 soft limit honestly over: the platform mechanics + the launcher policy + the frontend surface + the claim packet + the state docs, AGENTS §2.3, the scope noted here); 2222+6 + ruff + docguard clean (zero core change, zero pack change, the LOG untouched, INV-4's two-surface form unchanged); iter-213 evicted per the cap
iter-222 · 2026-09-24 · wb-7-live-chat — the live chat circuit LANDED (the owner's «продолжай работу по wb 7» + «могу ли я подключить llama.cpp к реготу» call over the v5.2 brief — frontend §46 Phase A's Chat row + app §22's launcher): scripts/workbench_app.py the composition root + loopback serve (Gateway + compose_workbench_operations(backend=LlamaServerClient) + LoopbackHttpTransport — the ONE launcher where the two sanctioned surfaces meet; --no-backend the honest admission form; the startup health probe evidence-not-gate; the missing-models-dir loud AppError) + the Redot half — presentation/redot/scripts/gateway_client.gd (the typed POST /op client: the sequential queue, operation_answered/transport_failed, NO endpoint of its own — the shell configures it) + shell.gd's live circuit (app.status → session.create → chat.send → the run.get poll to the truthful terminal COMPLETED/FAILED/CANCELED/FAILED_TO_CANCEL/UNKNOWN; Stop = run.cancel; the honest NOT CONNECTED/unreachable/refusal notes; the bounded §15 message list; the proof mode stays the static deterministic form — no network in the capture) + project.godot's canonism_workbench/gateway/url (the committed default URL, the arg>env>setting resolution order) + tests/test_workbench_app.py (8 tests: the wiring law, the honest no-backend form, the loud arguments, the live loopback roundtrip, THE END-TO-END chat over HTTP against the live stub llama-server, the dead-endpoint FAILED close) + test_shell_contract.py's three wb-7 pins (the G8 executable half: no endpoint/llama-API text in the client)
why: the row the owner's call opened — the Chat surface's real machinery (the wb-2 placeholder's honest successor); the frontend dials the GATEWAY only (frontend §47 G8), llama.cpp stays behind the engine adapter (INV-4's two-surface form unchanged — the port injected at the composition root, never imported)
files: scripts/workbench_app.py, workbench/presentation/redot/scripts/gateway_client.gd, workbench/presentation/redot/scripts/shell.gd, workbench/presentation/redot/project.godot, tests/test_workbench_app.py, tests/test_shell_contract.py, .gitignore, docs/CONTRACTS.md, docs/TASKS.md, docs/DECISIONS.md, docs/AGENT_NAVIGATION.md, README.md, STATUS.md, worklog.md — 14 paths (the 3–5 soft limit honestly over: the launcher + the frontend half + the claim packet + the state docs, AGENTS §2.3, the scope noted here); 2202+6 + ruff + docguard clean (zero core change, zero pack change, the LOG untouched); iter-212 evicted per the cap

---

iter-226 · 2026-09-26 · wb-9-model-flow — the owner's model-flow call («определись где будет папка с llama.cpp и моделями» + «открыл воркбенч, зашел и загрузил модель» + «подтянуть модель откуда угодно» + «настройки запуска llama.cpp... сэмплеры всякие»): the model flow end to end — workbench/platform/model_fetch.py (D-208: INV-4's THIRD sanctioned surface, the outbound model-assets fetch — the URL vocabulary, the .part stream + the atomic rename, the injected §12 checkpoint) + the model.fetch work kind (the admission gates pre-run, the registry's live progress surface, the kind's own 3600s deadline) + workbench/application/settings.py (the typed store + backend.settings ops + the atomic persistence) + llama_process.py's sampler flags + workbench_app.py's MANAGED default + the exe auto-discovery + scripts/workbench_launch.py the ONE-COMMAND launcher + shell.gd's real Settings surface + the Models manager; the runtime layout workbench/runtime/{models,llama.cpp,settings.json} gitignored
why: the owner's live blocker — the Redot shell showed "gateway unreachable" because the gateway + the managed form + the exe were three manual steps; the row collapses them into one command and makes the Settings surface real (never "бутафорское")
files: workbench/platform/{model_fetch.py,__init__.py}, workbench/application/settings.py, workbench/application/operations/{execution.py,models.py,composition.py,backend.py}, workbench/platform/llama_process.py, scripts/{workbench_app.py,workbench_launch.py}, workbench/presentation/redot/scripts/shell.gd, tests/{test_model_fetch.py,test_settings.py,test_workbench_app.py,test_managed_backend.py,test_shell_contract.py,test_architecture.py,test_operations.py,test_gateway.py}, AGENTS.md, docs/{DECISIONS.md,TASKS.md,CONTRACTS.md,AGENT_NAVIGATION.md}, README.md, STATUS.md, worklog.md — 26 paths (the 3–5 soft limit honestly over: the platform surface + the settings family + the launcher + the frontend + the claim packets + the law edits, AGENTS §2.3); 2258+6 + ruff + docguard clean (zero core change, zero pack change, the LOG untouched); iter-216 evicted per the cap

---

iter-227 · 2026-09-26 · wb-10-owner-experience — the owner's fix list (the launcher's Popen `buffering` TypeError + «редот у меня такой …\Redot_v26.2-stable_windows_win64, где найти redot.exe и как его подключить?» + «просто открывающийся проводник и выбор уже скаченных локальных моделей» + «пользователь не должен вводить команды чтобы запустить или скачать что-либо!» + «интерфейс вверх убожества»): workbench_launch.py the folder-aware Redot resolution (release folder → the engine exe; CLI/env strict-verbatim, the persisted launcher.json + the Desktop-shaped auto-scan + the native tk folder picker as recovery; the observed bind URL forwarded to the Redot child; the bare "--" separator stripped — the latent wb-9 gateway-arg bug) + the model.import work kind (local copy with .part + atomic rename, live progress + cooperative cancel, the admission gates pre-run, NO network — INV-4 untouched) + discover()'s models_root + shell.gd's native-picker Models manager (Add local models…/Add folder… over the OS dialogs, Open models folder via OS.shell_open, the URL fetch demoted to the collapsed advanced row) + the theme@0.2 visual pass + the Workbench.bat/Workbench Setup.bat zero-command entries
why: the owner's reported blockers — the launcher crashed at spawn, the Redot path was a folder not an exe, and the model manager demanded URLs instead of the local files the owner already has; the fixes make the whole flow double-click-and-pick
files: scripts/workbench_launch.py, workbench/application/operations/models.py, workbench/application/operations/composition.py, workbench/presentation/redot/scripts/shell.gd, workbench/presentation/redot/themes/workbench_theme.tres, workbench/presentation/redot/project.godot, Workbench.bat, Workbench Setup.bat, tests/test_model_import.py, tests/test_workbench_launch.py, tests/test_shell_contract.py, tests/test_workbench_app.py, tests/test_operations.py, docs/{TASKS.md,CONTRACTS.md,DECISIONS.md,AGENT_NAVIGATION.md}, README.md, STATUS.md, worklog.md — 18 paths (the 3–5 soft limit honestly over: the launcher rework + the import kind + the frontend surface + the theme + the entries + the claim packets + the state docs, AGENTS §2.3); the tests + counts in STATUS; iter-217 evicted per the cap
