# worklog — canonsim

> Cap: 10 entries, one in / one out. 3–5 lines per entry: what changed, why,
> files touched. No diffs, no command output, no reasoning traces. Long-term
> rationale belongs in `docs/DECISIONS.md`. Pre-trim history lives in git.

---
iter-263 · 2026-09-27 · carrier-surface (D-239 — the owner's question: which authored institutions in Sarrow Vale should be canonical carriers of world change, which intentionally remain narrative surfaces, and can a minimal world-track experiment distinguish the cases without a new runtime primitive): the THREE-MOVE PROBE landed as world-track law (the reader audit → the ablation arm → the injection arm, WORLD_TESTS §7's third member + WORLD_AUTHORING §7's doctrine row; every arm a scratch pack copy through the real load_pack — the lint part of the instrument; determinism twin-verified) + the measured matrix over the composition witness (A1 the market's material leg ablated → the whole year diverges: no council, no vigil, the PC never caught — a LIVE carrier; A2 the economic leg → one account prop, the render-read state carrier; I1/I2 the council/vigil knowledge legs → +6/+23 records, the watch-briefing transfer consumer measured, the novelty-gradient honest boundary; I3a/I3b the market-mourns prop trigger → the move release massive (talks 287→2, the council pile-up 1→208) and the ramble release minimal-but-material (+5 records, rumors 22→25, one materialized scene prop); C1/C2 the calendar line's consumer routes ALL CLOSED, measured twice)
why: the owner's question changes the J-1/J-2 approach — the verdict is PER-LEG never per-institution (the market carries all four bands; the calendar line is an intentional surface, its consumer routes closed by construction and measured); the honest J-1 rows are the READER legs (the mourns hook, the trade door, the knowledge legs — each on existing primitives, each with its measured price), so the authored unit landed at ANCHOR_REGION §6.5 (the fifth meso unit, the §6.1 class) with the embodiment owner-gated; J-2's remainder confirmed with the refined cascade price; four expressibility boundaries named (the calendar grammar, the flow grammar, the ignite resolver's no-hooks surface, the absent action state block) — none needing a new primitive for the answer
files: docs/{iterations/iter-263-carrier-surface-report.md (new, the owner's RU deliverable), worldbuild/WORLD_TESTS.md (§7 the probe + §9 the record), worldbuild/WORLD_AUTHORING.md (§7 the doctrine row), worldbuild/ANCHOR_REGION.md (§6.5 the market as a carrier assembly), blueprint/phases.md (§6 the iter-263 record), TASKS.md (the ledger, iter-252 evicted), DECISIONS.md (D-239 into the compound row)}, STATUS.md, worklog.md (iter-253 evicted) — 8 paths; 2445+9 + ruff + docguard + topology --check clean (R0 — doc-only, the D-022 exception: the owner's fresh question; zero core, zero pack, the LOG untouched, the scratch instruments outside the repo)

---
iter-262 · 2026-09-26 · composition-diagnosis (D-238 — the owner's iter-262 brief: reproduce the iter-261 J-evidence, discriminate the competing explanations, land only the warranted change): the twin-fold CAUSAL-INERTNESS test (52 calendar turns + 5 institutional events removed from the realized composition log → the final projection byte-identical; all references cause-chain neighbors) + the static consumer enumeration (market_opens/fair_opens: tale lines only; `destroyed` read by the fire layer alone; the one materialized fire loss = the ignition affordance) + the scratch falsifiers (a J-1 suppression gate is provably cosmetic; J-3's proposed urgency structurally dead — max road fear 10, the decay-catchup-before-rolls law) + the J-2 REFUTATION finding the LIVE committed dead-data bug: pressure_pack's convene_the_bench/plead_for_coal carried authored knowledge blocks the `wait` resolver silently dropped (KI#103, the KI#15 family, the iter-53 hooks precedent's twin)
why: the previous report classified J-2 as pack-data-only — refuted both ways (no pack surface writes location flags from actions; the knowledge leg was resolver-blocked); the diagnosis demanded the fix it found (a committed pack's authored intent going live unchanged), J-1 re-frames to the market-as-meso-carrier world-track row (never a calendar gate), J-3 closes as a non-gap, and the province knowledge remainder down-grades to pure pack data (the scratch falsifier measured the live rumor cascade 22→25 — the owner's content call)
files: core/resolvers.py (the `_wait` knowledge minting + docstring — KI#103's fix; empty blocks stay empty, the other four fixtures byte-identical), tests/{test_pressure_pack.py (+1 the plea-is-audible witness), fixtures/pressure_smoke_seed42.jsonl (regenerated — exactly the two the_watch_pleads lines gain their authored 6 records each)}, docs/{iterations/iter-262-diagnosis-report.md (new, the owner's RU deliverable), blueprint/phases.md (§6 the record), TASKS.md (the ledger, iter-253 evicted), DECISIONS.md (D-238 into the compound row), TEST_PLAN.md (§9 the causal-inertness prism row), AGENT_NAVIGATION.md (§1 the iterations row)}, STATUS.md, worklog.md (iter-252 evicted) — 11 paths; 2445+9 + ruff + docguard + topology --check clean (R2 — one resolver's minting surface, no new primitive, the calendar/LOD/scheduler/B2/B3/A2 untouched)
---
iter-261 · 2026-09-26 · p1-composition-timing (D-237 — the owner's iter-261 brief over the parked intake-40 P1 set: P1-1 opened by the explicit «prove composition» call, P1-10 = temp-1's half (b) test arm): the Province INTEGRATED witness — tests/playscripts/province_composition.json (province_pack@0.1, seed 2, existing verbs: lamp → keep arson → failed pay-tin steal → market arson → the 519000-tick year) + tests/test_p1_composition.py (16 RELATIONAL oracles: the crime family end-to-end incl. the rotation briefing spreading the whole suspicion stack to the relief + the arrest/caught; the fire→guild_councils; both griefs→the vigil incl. the YEAR-LATER re-fire off the decay-0 residue; the calendar year + the macro's 4 account flows; the condensation + the 287-talk band; the final-projection persistence; the B3 discipline 383/383; the OCC miss director_0001 2520→3449; the byte-identical twin)
why: the brief's §3 provenance fix — province_calendar is a 3-step temporal STRESS shape (all 265 talks one urgency entry), never integrated-world evidence; the composition question needed ONE run walking the interacting chains over the EXISTING substrate (no new content — §17's law), the oracles RELATIONS per §12 (no giant golden log), the honest gaps classified (the garrison/murmur arms at their own committed witnesses — witness-window limits, never missing mechanics); the H1/H2 season pair measured: the material outcomes invariant, the deltas door-only (rejected 20→0, checks 1→11, two final-projection props) — B1 NO promotion evidence, the J-gaps all pack-level
files: tests/{test_p1_composition.py (new), test_mechanics.py (+8), playscripts/province_composition.json (new)}, scripts/mechanics.py (census --log/--script the runtime A..H loss census + the timing subcommand the two-times table — the first named assignment_tick consumer), docs/{iterations/iter-261-composition-report.md (new, the owner's RU deliverable, the language exception owner-directed), blueprint/phases.md (§6 the record), TASKS.md (the ledger, iter-251 evicted), DECISIONS.md (D-237), TEST_PLAN.md (§9's third standing instance), AGENT_NAVIGATION.md (§1/§3), worldbuild/WORLD_TESTS.md (§9)}, STATUS.md, worklog.md (iter-251 evicted) — 13 paths; 2444+9 + ruff + docguard + topology --check clean (R2 — periphery instrument arms + a witness script + tests + docs; zero core change, the LOG untouched, B2/B3/A2 untouched)

---
iter-260 · 2026-09-26 · temp1-landing (D-236 (R4) + the schema 0.3 bump — the owner's 2026-09-26 contract pick over the iter-257 fork card: A2 + B2/B3, B1 deferred): A2 the crossing-family slicing contract PINNED as tests/test_temp1_contract.py (the declared tavern families count-invariant across [wait 720] vs [720 x wait 1] at the four seeds over the recorded surface; the minimal pair projection+fingerprint invariance, wait-only delta; the door families FREE with the seed-125 coerce flip + the seed-1001 cascade + the one fingerprint divergence PINNED — a silent "healing" of them is the failure, never the fix); B3 the semantic-origin provenance through the EXISTING primitive (IntentData.assignment_tick stamped at the ONE enqueue door `_enqueue_autonomous`, riding `_provenance` to accepted AND rejected autonomous resolutions; player intents never carry it), B2's runtime UNTOUCHED (the province_calendar witness: all 265 talks still in the last five ticks, the origins [1800, 516600], the deferral latency <= 518269 — `event.t` stays THE canonical timestamp); B1 DEFERRED behind the card's five reopening conditions
why: the card's own law — the contract decision is the owner's gate, and the pick landed; B3 names both times without re-timing anything (provenance, never a scheduler law), and the pinned door divergences make any future scheduler-semantics drift loud; the schema bump rides the cause_hook additive precedent (0.2 -> 0.3, EVENT_SCHEMA §7/§8) with all five fixtures regenerated per TEST_PLAN §3 — every canonical field byte-identical to BASE, the pre/post comparison recorded in D-236's PCC
files: core/{intent.py (IntentData.assignment_tick), loop.py (the enqueue-door stamp + `_provenance` + the two docstrings)}, schemas/event.schema.json (0.3 $id + the documented property), docs/EVENT_SCHEMA.md (§1/§7/§8), tests/{test_temp1_contract.py (new, 20 tests), test_smoke.py, test_semantic_diff.py, test_observatory_read.py (the three version-pin syncs), fixtures/*.jsonl (x5 regenerated)}, docs/{blueprint/phases.md (§6's decision record), DECISIONS.md (D-236 into the compound row), TASKS.md (temp-1 DONE + the ledger, iter-250 evicted), TEST_PLAN.md (§9's second standing instance)}, STATUS.md, worklog.md (iter-250 evicted) — 12 paths; 2421+9 + ruff + docguard + topology --check clean (R4 — one additive provenance field over the existing primitive, zero runtime-semantics change, the canonical outputs byte-verified)

---

iter-259 · 2026-09-26 · div1-probe (D-235 + KI#101/102 CLOSED — the owner's «и прочими» call, the third opened row): scripts/divergence_probe.py the future-divergence minimal-pair probe — one same-seed BASE/PERTURBED pair over the balance-harness arms (--directors-off the T8 default; --pacing-off/--systems-minus through balance_harness's own materializers, imported never duplicated), the semantic_diff oracle's §1.4 verdict + the bounded records (first-divergence, causal-path ≤8 links, persistence + family deltas + the projection delta); exit 0/1/2, loud inputs
why: TEST_PLAN §9's first-prism row needed the instrument that answers WHERE two futures diverge and whether it persists — the harness answers only the distribution question, the probe answers the pair question; the probe's first live pair exposed KI#101 (the oracle's _deep_diff zip strict crashing on unequal-length lists before the length line — fixed + pinned) and KI#102 (the architecture test's glob vs its committed-files law, red wherever scratch/ exists — the exclusion set now names the ignored dir)
files: scripts/{divergence_probe.py(new), semantic_diff.py (the KI#101 fix)}, tests/{test_divergence_probe.py(new), test_semantic_diff.py (the KI#101 pin), test_architecture.py (the KI#102 closure)}, docs/{TEST_PLAN.md (§9's first-prism row), AGENT_NAVIGATION.md (§1/§3), DECISIONS.md (D-235 into the standing-row compound), TASKS.md (div-1 DONE + the ledger, iter-249 evicted)}, STATUS.md, worklog.md (iter-249 evicted) — 10 paths; 2401+9 + ruff + docguard + topology --check clean (R2 — two periphery instruments + two bug fixes, zero core change, the LOG untouched)

---
iter-258 · 2026-09-26 · cov1-census (D-234 — the owner's «цензус cov-1» call, the second opened row): `mechanics.py census` the action-to-consequence forward walk — pack action → parser verb (the t=0 grammar snapshot over brief.parser's own derivation) → realized (the committed corpus's authored steps, pack-scoped by the script's pack field + the autonomous urgency/hook references) → canonical events → per-event downstream consumers (the matrix's own reverse derivation, agreed by a pin test); the compact view = the coverage line + the FLAGGED rows
why: the admission lint answers only CAN-consume, never DOES-realize — intake-37 measured the gap (value mutations pass lint AND goldens on unexercised paths); the census's UNREALIZED set is that measured surface (tavern 6 / province 19 / pressure 2 at landing), the next pack gate's coverage line and the mutation-escape falsifier's input; a REGENERABLE derived report, never a second truth (byte-identical re-derivation pinned)
files: scripts/mechanics.py (the census subcommand + the CLI wiring), tests/test_mechanics.py (7 census tests), docs/{TEST_PLAN.md (§9's strong-follow-up cell), AGENT_NAVIGATION.md (§5's vocabulary), DECISIONS.md (D-234 into the standing-row compound), TASKS.md (cov-1 DONE + the ledger, iter-248 evicted)}, STATUS.md, worklog.md (iter-248 evicted) — 8 paths; 2392+9 + ruff + docguard + topology --check clean (R2 — a new periphery instrument, zero core change, the LOG untouched)

---
iter-257 · 2026-09-26 · temp1-contract-research (the owner's «работай над temp-1 …» call, the first opened row — research-first, zero engine code per the row's own law): the temporal-contracts fork card landed in phases.md §6 — both intake-40 signals verified at HEAD with scratch runners (D-197, never staged): the wait-slicing A/B ×4 seeds (the crossing families invariant 8/8, the door families diverge seed-dependently, the minimal pair projection-equal), the province_calendar clustering CONFIRMED TOTAL (all 265 talks in the last 5 ticks)
why: the row's law — the contract decision is the owner's gate, never a silent reconciliation; the card names the shared root mechanism (`_run_beat`'s entry_tick enqueue law — the door-committed families record at the landing tick, never the beat tick — plus the per-event ambient hook minting), presents the A1/A2/A3 and B1/B2/B3 forks with the §2.7 synthesis attempted and honestly discarded, and names the falsifiers per pick; the scratch runners stay out of the repo
files: docs/blueprint/phases.md (§6's temp-1 card), docs/TASKS.md (the temp-1 row + the ledger, iter-247 evicted), STATUS.md, worklog.md (iter-247 evicted) — 4 paths; 2385+9 + ruff + docguard + topology --check clean (doc-only, the D-022 exception — the owner's fresh call)

---
iter-256 · 2026-09-26 · log1-reader-version-gate (KI#100 CLOSED — D-233's fix row, the second half of the owner's «продолжи работы с документами, я согласен с вердиктами» call): read_log now derives the expected schema_version from the passed schema's $id and refuses a mismatched/foreign header loudly — the module-level _extract_schema_version (the writer's own path promoted: both doors derive through ONE function), the writer delegating to it, its append-mode check kept as the canon-write door's defense-in-depth; the canonical read seam + the whole read surface (chronicle/observatory/mechanics/checkpoint/the harnesses — every read_log caller) inherit the gate for free
why: the intake-40 routing's one code defect — the writer/reader asymmetry (append always refused a stale header; read never did) violated the repo's own migration law (a schema bump between runs is a migration, never an append/read); the fix lands at the reader boundary so EVERY consumer gets the gate, not just the resume door; all committed fixtures carry the current version (T1's guard pins it), so the gate is additive — zero existing pins broken, 2384→2385
files: core/log.py (the shared derivation + the read_log version gate + the docstrings), tests/test_core.py (the stale-header pin: the refusal + the clean-read arm), tests/test_render.py (the hand-crafted 0.1-header synced to the current version — the seed-read test's subject was never stale-version tolerance), STATUS.md (KI#100 CLOSED + the header/Next step), docs/TASKS.md (log-1 DONE + the ledger line, iter-246 evicted), worklog.md (iter-246 evicted) — 6 paths; 2385+9 + ruff + docguard + topology --check clean (R2 — one core read-boundary behavior change; the LOG untouched)

---
iter-255 · 2026-09-26 · doc10-intake40-routing (D-233, the owner's «продолжи работы с документами, я согласен с вердиктами» call over the tmpfiles.org upload): the 2818-line testing-and-verification ultimate corpus ROUTED — the intake-40 block in phases.md §6: CONFIRMED-owned core (every § owned by TEST_PLAN §§1/2/8/9 + WORLD_TESTS + the 11 playscripts — zero rows, zero doc edits, the D-024 law) + the ONE code defect (P0-1 the reader's stale-header acceptance → KI#100 + the fix row log-1) + the M4 semantics pick (P0-2 → D-233: the implemented test-pinned type-based form stays, TEST_PLAN §2 worded exactly, M4b the reserved name for any future experienced-repetition consumer) + THREE parked rows (temp-1 the temporal contracts — two gated halves, research-first; cov-1 the census — a minimal extension of mech-2's derived index; div-1 the future-divergence probe — over ssi-7's semantic_diff oracle); the document FORM refused (a second TEST_PLAN by content, self-forbidden by its own §4, AGENTS §2.8 — the convenience-copy law); the SC-* queue + the P1/P2 sets + the periodic recert refused or parked (D-175); zero worldbuild routing, no REFERENCES entry
why: the corpus is a second TEST_PLAN by content and the repo's own caps forbid that form; the durable atoms route to their owning docs (KI/STATUS, the fix row/TASKS, the M4 sync/TEST_PLAN §2, the record/phases.md §6), everything else already owned; the temporal signals are contract questions, not bugs — §11's authority law parks them behind the owner's contract decision, never a silent reconciliation
files: docs/blueprint/phases.md (§6 the intake-40 block), docs/DECISIONS.md (D-233 into the intake compound row), docs/TEST_PLAN.md (§2 the M4 wording sync), docs/TASKS.md (the four rows log-1/temp-1/cov-1/div-1 + the ledger line, iter-245 evicted; the §6.1 cruft pass — the seven ssi DONE rows collapsed to their D-row pointers, 640→599, the header's own done-detail law), STATUS.md (the header + KI#100 opened + Next step), worklog.md (iter-245 evicted) — 6 paths; 2384+9 + ruff + docguard + topology --check clean (doc-only, the D-022 exception — the owner's fresh call)

---
iter-254 · 2026-09-26 · doc9-agents-fork-synthesis (D-232, the optional half of the owner's «согласен с твоими вердиктами => приступай» call): the design-fork law's missing half LANDED — AGENTS.md §2.7 extended with the synthesis step (intake-39's one fresh atom, the doctrine's §19): enumerate the candidates' advantage/disadvantage axes as independent axes → decompose every disadvantage intrinsic-to-mechanism vs artifact-of-formulation → construct the candidate satisfying the union of advantage constraints and none of the disadvantage-avoidance constraints (a redesign dissolving the trade-off, never a weighted compromise) → name/verify emergence → the honest comparison (kept only if it matches-or-beats the best parent on every material axis while eliminating at least one material disadvantage, else discarded for selection; a re-labeled parent or averaged parameters is not a synthesis); the selection half (D-198) untouched
why: the fork law owned only the selection half — L15's combined-design part already asks the question («is there a combination that preserves the advantages while neutralizing the disadvantages»), and §2.7 now owns the procedure that answers it; the honest-comparison clause keeps selection the standing fallback, so the law gains a step without losing its discipline; zero rows (D-175) — a law refinement, never a queue row
files: AGENTS.md (§2.7), docs/DECISIONS.md (D-232 into the intake compound row), docs/TASKS.md (the ledger line, iter-242 evicted), STATUS.md (the header + the rolling DONE block), worklog.md (iter-243 evicted) — 5 paths; 2384+9 + ruff + docguard + topology --check clean (doc-only, the D-022 exception — the owner's fresh call)
