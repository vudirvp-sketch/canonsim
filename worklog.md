# worklog — canonsim

> Cap: 10 entries, one in / one out. 3–5 lines per entry: what changed, why,
> files touched. No diffs, no command output, no reasoning traces. Long-term
> rationale belongs in `docs/DECISIONS.md`. Pre-trim history lives in git.
---
iter-236 · 2026-09-25 · obs2-live-run-read-seam — the live run over the READ-side seam (FRONTEND_UIUX_LAW §25's P1 continuation + the selection model's first consumer §6): workbench/observatory_read.py the seam's ONE home (the bounded read model over core.log.read_log — the wb-1 scene_build read-side edge; the operations package stays CanonSim-free per its envelope): observatory.runs (the discovery scan, honest per-run degradation, content truths only) + observatory.read (the after-cursor window over EVENT IDS — default 50/cap 200 the ONE ceiling, authority CANONICAL rendered never guessed, duplicate-id/stale-cursor/corrupt refused loud) + operations/observatory.py the ops layer + the composition's injected runs-root+schema wiring (half-wired refused) + observatory.gd v0.2 the live feed (picker/Refresh/Earlier-Later; the selection the EVENT ID on button metadata restoring by ID; the inspector scrolling with cause + state changes as DATA — only the READ rung confirms under a selection; the DISTINCT empties probing/NO RUNS/NO EVENTS/refused; the breadcrumb run → event) + the shell's two-signal hosting + --obs-document the proof-only injection (a REAL op document, the same feed path; the meta's observatory_run)
why: the LAW's own ladder — obs-1 validated the grammar honestly-empty; obs-2 feeds it from the canonical source without a second transport or authority (INVARIANT 1/2); the Redot 26.2 lessons found by the VLM/runtime verification are pinned (JSON.parse_string the static form; JSON numbers parse as FLOATS — _int_text/_normalize_numbers; the GridContainer value column needs EXPAND_FILL; JSON null is a PRESENT key)
files: workbench/{observatory_read.py(new),application/operations/observatory.py(new),application/operations/composition.py}, scripts/workbench_app.py, workbench/presentation/redot/scripts/{observatory.gd,shell.gd,strings.gd}, scripts/visual_proof.py, tests/{test_observatory_read.py(new),test_shell_contract.py,test_shell_proof.py,test_operations.py}, STATUS.md, docs/{TASKS.md,DECISIONS.md}, worklog.md — 13 paths; 2308+8 (CI) / 2315+1 (REDOT_EXE — the LOADED capture double-run byte-identical, distinct from the empty slice, VLM-verified + the headless inspector dump green) + ruff + docguard clean; iter-225 evicted per the cap
iter-235 · 2026-09-25 · obs1-observatory-slice — the P1 vertical UX slice (FRONTEND_UIUX_LAW §25/§50/§51 — the interaction grammar BEFORE full analytical backend coverage, the standing resolution's own law: no jump from documentation to an 1800-line Observatory): the nav rail adopts the §4.1 IA — WORK (Chat · Observatory; Simulation/Inference planned) / RESOURCES (Models; Prompts/History) / SYSTEM (Settings; Runs/Diagnostics — the LAW's map, Runs added, Inference kept, nothing dropped) + scripts/observatory.gd the responsibility-split seed (LAW §18: the surface composes ITSELF over the injected theme + the shell's own _tr Callable — one boundary, one token source): the semantic breadcrumb, the context identity strip (pack/run/seed/tick/profile/revision, the honest no-session values), the World Question contract (QUESTION/CLASS/TARGET/SCOPE — the DRAFT lifecycle, nothing asked nothing claimed), ONE primary read-only view (the event table's TICK/EVENT/KIND/AUTHORITY columns — the authority axis in the representation; the DISTINCT NO DATA semantics, never no-match/no-evidence), the inspector region (HSplit, nothing selected — honestly), the evidence ladder (READ/BRANCH/STATE/DIVERGENCE/PERSISTENCE, every rung's unknown as TEXT) — zero fabricated content, zero dispatch, zero new transport + the runner drift fix (visual_proof.py's --surface choices=[chat,settings] lagged the shell through wb-8/9/obs-1 — the vocabulary is the shell's, the runner never gates it)
why: the law's queue (obs-1 next after ux-1) + the VLM-verified composition (all regions render, no overlap, no clipping) — the grammar validated BEFORE the backend, the Observatory no longer absent even as a stub
files: workbench/presentation/redot/scripts/{observatory.gd(new),shell.gd,strings.gd}, scripts/visual_proof.py, tests/{test_shell_contract.py,test_shell_proof.py}, STATUS.md, docs/{TASKS.md,DECISIONS.md,AGENT_NAVIGATION.md}, worklog.md — 10 paths; 2290+7 (CI) / 2296+1 (REDOT_EXE, the observatory capture green) + ruff + docguard clean; iter-224 evicted per the cap
---
iter-234 · 2026-09-25 · ux1-frontend-minimums — the P0 minimums row (FRONTEND_UIUX_LAW §25's queue head, the owner's «начать работать в этом направлении» direction call): the _tr() localization boundary (scripts/strings.gd — the en/ru catalogs, ~190 keys × 2; the migration moved 181 keys over 197 shell sites; locale resolution --lang > CANONSIM_LANG > OS, proof mode resolves explicit-only so a --lang ru capture stays byte-stable; Cyrillic rendered RUNTIME_VERIFIED via the ru screenshot) + the reduced-motion setting (the chat follow's direct jump + the busy pulse parked behind the flag; the Interface section's toggle, persisted UI-local in workbench/runtime/ui_state.json, proof never touches it) + the viewport/min-size policy (project.godot: min 1152x700, stretch canvas_items/expand) + the focus/keyboard baseline (Esc stops a live generation, Ctrl+. anywhere, task-aware focus entry: composer/refresh/first field) + KI#97 — the REDOT_EXE-gated proof pins had drifted silent through wb-8/9 (canon_shell@0.1, surfaces without models, 1280x720): repaired against the live shell, the seam harness pins its own window (1280x720, scaling disabled — a harness never inherits the product's resize contract); the Settings self-description rows re-honest (theme@0.4, the language row, the keyboard row) + the parse lesson pinned (Object.tr's native signature makes `static func tr(` a PARSE ERROR — the catalog's resolver is `lookup`)
why: the law's own order — the boundary BEFORE surface count grows (obs-1's strings ride _tr from birth); the owner's sandbox Redot install call made the gated packets runnable again and the stale pins visible (a skip is not a pass)
files: workbench/presentation/redot/scripts/{shell.gd,strings.gd(new),seam_proof.gd}, workbench/presentation/redot/project.godot, tests/{test_shell_contract.py,test_shell_proof.py}, STATUS.md, docs/{TASKS.md,DECISIONS.md,AGENT_NAVIGATION.md}, worklog.md — 11 paths; 2289+6 (CI) and 2294+1 (REDOT_EXE, the five gated packets green) + ruff + docguard clean; iter-223 evicted per the cap
iter-232 · 2026-09-25 · ki95-ki96-oled-repin — the owner's 2026-09-25 report (the CI-red cancellation test + «проводник опять сломался видимо, я не могу папки открыть и модели не показывает языковые» + «цвет лучше взять темный под oled мониторы, но не синий такой убогий»): KI#95 — the wb-9 cancellation test's single checkpoint() call RACED the main thread's run.cancel dispatch (a fast CI worker passed through the "unreachable" AssertionError closed the run FAILED — the honest truth for an unrequested abort, the TEST buggy; the poll form, bounded 20s, three-form verified: sandbox, forced fast worker, delayed cancel) + KI#96 — the Models surface's scan latch (_models_requested) froze the discovery list after one successful scan (hand-dropped GGUFs never appeared without a manual Refresh; the latch retired — every surface entry re-scans, the negative pin owns the ban) + theme@0.4 (D-213 — the OLED re-pin the owner called: the truly neutral near-black ramp over #050505 + the ONE teal accent #4cc9a6, values-only, 21 WCAG contrast pairs measured, scripts/contrast_check.py)
why: the owner's two shipped defects (the CI-red test racing the cancel dispatch; the Models list frozen behind a one-shot success latch) + the OLED palette call — every fix pinned by its own test, the honesty laws intact
files: tests/test_model_fetch.py, workbench/presentation/redot/scripts/shell.gd, workbench/presentation/redot/themes/workbench_theme.tres, tests/test_shell_contract.py, docs/{STATUS.md,TASKS.md,DECISIONS.md}, worklog.md — 8 paths; 2285+6 + ruff + docguard clean (zero core change, the LOG untouched)


iter-231 · 2026-09-25 · ci-unred-nav-variation — the owner's 2026-09-25 engine report (the nonexistent add_theme_type_variation call killing _ready at _build_nav_rail + the unused row warning) + the CI-red report (runs 82–84 red since iter-227, «Run python -m pytest -q» failing): KI#93 — the launcher/gateway pipe chain owned nothing of its buffering (the gateway child's bind line block-buffered in the pipe unless the host exported PYTHONUNBUFFERED — the sandbox does, CI/owner machines do not; the 90s boot probe honestly failed «the gateway failed to serve (exit code 0)»; local green had masked CI red for three iterations) + KI#94 — iter-230's NavButton wiring over a Control method Redot 26.2 does not have (the API is the theme_type_variation PROPERTY)
why: the verification gap itself sheltered the bug — every local run rode the sandbox's global PYTHONUNBUFFERED=1; the engine's own error message was the API truth (the Godot 4 docs cross-reference: 6 property mentions, 0 method mentions)
files: scripts/workbench_launch.py (the gateway child rides -u + the supervisor line-buffers its own stdout), workbench/presentation/redot/scripts/shell.gd (the property form ×2 + the dead row local), tests/test_workbench_launch.py (the env-stripping spawn), tests/test_shell_contract.py (the property pin + the negative method pin), STATUS.md, docs/TASKS.md, worklog.md — 7 paths; 2285+6 + ruff + docguard clean, VERIFIED TWICE (the standard env AND the stripped PYTHONUNBUFFERED env — the CI/owner condition); iter-221 evicted per the cap

---

iter-230 · 2026-09-25 · wb-12-token-audit-chat-follow — the owner's «тема и UI все так же убоги, тема ужасная» + «в чате при получении сообщений от языковой модели => не происходит плавной прокрутки вниз» calls (the v5.2 plans pack the direction hand-off): theme@0.3 (the neutral ramp re-pinned over the Catppuccin Mocha VALUE reference — §6's mechanisms-not-looks: the layered luminance separation is the mechanism; ONE accent #89b4fa; accent_deep retired; the NavButton variation + styles/chip_busy) + the chat follow law (the tween over the scrollbar's float value, the layout-settle await, the near-bottom gate, follow on every role, the bounded late-layout re-settle) + the GENERATING chip (the §5 matrix state's visible carrier: pulsing dot AND label)
why: the owner's named blockers — the hated warm-orange/brown incoherent theme (blue-navy surfaces + brown pressed states + orange accent) and the dead-feeling chat (no follow on replies, the assistant-only integer jump that raced the autowrap layout); the v5.2 pack named the visual direction, the admitted VISUAL_SYSTEM_UI law owns the form
files: workbench/presentation/redot/themes/workbench_theme.tres, workbench/presentation/redot/scripts/shell.gd, tests/test_shell_contract.py, docs/STATUS.md, docs/TASKS.md, docs/AGENT_NAVIGATION.md, docs/CONTRACTS.md, docs/DECISIONS.md, README.md, worklog.md — 10 paths (the 3–5 soft limit honestly over: the theme + the shell + the claim packet + the state docs, AGENTS §2.3); 2285+6 + ruff + docguard clean (zero core change, zero pack change, the LOG untouched)

---

iter-229 · 2026-09-26 · visual-system-admission — the owner's 2026-09-26 «вот и отлично, зафиксируй где нужно и обращайся при дальнейшей работе! нынешний ui - ужасен! поэтому всеми тремя руками и ногами - за!» call over the external 24-section Visual System and UI Engineering instruction document: docs/VISUAL_SYSTEM_UI.md ADMITTED (D-211, the D-207 pattern) — the surface-driven grammar + the token taxonomy + the neutral/one-accent law + the not-color-only invariant + the per-surface state matrix + the transplantation protocol (mechanisms, not looks) + the reference pack (§2.8; ThemeGen a tool; SillyTavern license-sensitive) + the §8 per-row UI report + the anti-patterns + the wb-12+ queue; the standing resolutions: functional first, the doc is the law for each row never a parallel program, the app §18 evidence law wins over quiet chrome
why: the current UI is horrible (the owner's own words) and the visual work needs ONE law owner inside the repo — the external document is ephemeral (D-024/D-200), the routing was homeless; every visual row now routes through the admitted law before any palette lands
files: docs/VISUAL_SYSTEM_UI.md, docs/AGENT_NAVIGATION.md, docs/TASKS.md, docs/DECISIONS.md, STATUS.md, worklog.md — 6 paths (doc-only); 2284+6 + ruff + docguard clean

---

iter-228 · 2026-09-26 · wb-11-load-unload-runs — the owner's 2026-09-26 report over the wb-10 handback («молча висят + транспорт результ 13» + «модель выбрать не могу, там пусто, моделей не видно, кнопка выбрать модель не работает (проводник не открывается)» + the evidence «llama.cpp и модели запускаются штатно если отдельно запускать»): the transport-13 freeze chain pinned dead — backend.py's model.load/model.unload as RUNS (the chat.send pattern: the fast dispatch walks the ladder + admits, the minutes-class spawn/stop rides the worker thread OFF the coarse dispatch lock; the WIRE SHAPE CHANGE — the execution document returns, the terminal truth rides run.get, the FAILED diagnostics carry the observed cause, §21; the single-slot + in-flight guards; the rows' own §12 deadlines 330/30) + llama_process.py's _PipeDrain (one daemon reader per captured pipe into a bounded 64KB tail ring — the Windows pipe-buffer wedge + the os.set_blocking '(empty)' bug dead, stderr_tail cross-platform) + shell.gd's run-poll circuits (model-load-get/model-unload-get on the shared tick; the honest picker/load guards — never a silent return, §18; the failed scan re-arm so the empty list never sticks)
why: the synchronous minutes-class load under the gateway's one coarse lock starved EVERY concurrent request past the client's 10s budget (transport_result_13 = HTTPRequest RESULT_TIMEOUT) — the empty Models list, the dead session-gated buttons, the silent UI; the owner's evidence isolated the bug to the workbench's integration, not llama.cpp
files: workbench/application/operations/backend.py, workbench/platform/llama_process.py, workbench/presentation/redot/scripts/shell.gd, tests/test_backend_row.py, tests/test_shell_contract.py, docs/CONTRACTS.md, docs/TASKS.md, docs/DECISIONS.md, STATUS.md, worklog.md — 10 paths (the 3–5 soft limit honestly over: the run rework spans the handler + the platform + the frontend + the claim packet + the state docs, AGENTS §2.3); 2284+6 + ruff + docguard clean (zero core change, zero pack change, the LOG untouched, INV-4's three-surface form unchanged)

---
iter-233 · 2026-09-25 · frontend-uiux-law-admission — the owner's tmpfiles hand-off of the external Ultimate Frontend/UI/UX/Visual Architecture pack + the «начать работать в этом направлении» direction call: docs/FRONTEND_UIUX_LAW.md ADMITTED (D-214) as the frontend interaction-architecture law owner — the doctrine + the workspace/selection/focus/compare/timeline/graph/evidence-ladder grammars + the epistemic grammar + the query lifecycle + empty semantics + drill-down + the Evidence Capsule + T1–T8 + the accessibility/reduced-motion/responsive/localization contracts + the component law + Scene IR integration + bounded rendering + the verification matrix + the P0–P4 order + the 20 invariants (627 lines, three cruft rounds 632→627, the enum/invariant lists the substance — docguard allowlist per §6.1); VISUAL_SYSTEM_UI §11 the companion-requirements routing (the drift the pack diagnosed: accessibility/keyboard/reduced-motion/responsive/localization materially missing from the short law — the lossy-compression defect) + §10 re-pointed (ux-1/obs-1 before indefinite peripheral polish); TASKS the ux-1 + obs-1..N rows registered
why: the pack's own §52/§58 — the highest-value correction is NOT "make the UI prettier" but the missing interaction foundation; the admission is P0 item 1 (repair working-law/specification integrity) + items 3–7 at the normative level (the shared context/selection/focus/epistemic/task contracts now have a repo-side law owner), and the queue re-point unblocks the Observatory slice (P1) the pack names the project's defining absent surface
files: docs/FRONTEND_UIUX_LAW.md, docs/VISUAL_SYSTEM_UI.md, docs/TASKS.md, docs/AGENT_NAVIGATION.md, scripts/docguard.py, docs/DECISIONS.md, STATUS.md, worklog.md — 8 paths (doc-only; an admission's routing surface, the iter-225/229 precedent); 2285+6 + ruff + docguard clean (zero code change, the LOG untouched); iter-227 evicted per the cap
