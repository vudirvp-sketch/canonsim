# Runtime artifacts — never committed (AGENTS.md §7, INV-5)
logs/
output/

# Research-intervention scratch (intake-37/D-197): probe runners, mutated
# pack copies, spike drafts — session-transient, never staged, never
# archived; the durable residue routes to phases.md §6 / DECISIONS / TASKS
scratch/

# DF Legends XML exports — runtime data for track B, never committed
# (df_design.md determinism quarantine: no golden DF fixtures; iter-8e)
dfworlds/

# JSONL outside tests/fixtures/ is runtime log data (INV-5)
*.jsonl
!tests/fixtures/*.jsonl

# Python bytecode, caches, packaging
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
build/
dist/

# Redot project cache (CONTRACTS §5 D1: the engine binary is an external
# toolchain; generated cache never committed)
workbench/presentation/redot/.godot/

# Environments & secrets
.venv/
.env
