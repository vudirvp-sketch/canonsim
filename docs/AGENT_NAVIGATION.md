# AGENT_NAVIGATION.md — Where Things Are

> Current-state map: describes what IS, not what happened. Update §1 in the
> same iteration any structure changes. No history, no narrative.

## 1. Where things are

| Path | Purpose | Status |
|---|---|---|
| `README.md` | entry point, repo map | stable |
| `AGENTS.md` | operating law for agents | stable |
| `STATUS.md` | iteration state, KIs, FAQ | every iteration |
| `worklog.md` | capped short-term memory | every iteration |
| `docs/VISION.md` | frozen why + honest limits | frozen |
| `docs/MVP_SCOPE.md` | phase-0 tech spec (TavernSim v0) | active |
| `docs/EVENT_SCHEMA.md` | event contract | active |
| `docs/INTENT_SCHEMA.md` | intent contract (the proposal side of the door; trigger-gated, written iter-2) | active |
| `docs/ROADMAP.md` | phases, gates, tracks, donors | active |
| `docs/TASKS.md` | backlog + iteration statuses | active |
| `docs/TEST_PLAN.md` | phase-0 verification stack: T0–T8 + M1–M5 + gate protocol + UAP crosswalk + the schema-bump migration procedure (the iter-6 trigger-fired spec) | active (iter-6) |
| `docs/TECH_NOTES.md` | rotting notes: models, hardware, DF spike | live |
| `docs/DECISIONS.md` | ADR-lite, stable decisions | append-only |
| `docs/SPECS_BACKLOG.md` | just-in-time spec queue | active |
| `docs/CORE_DESIGN_RESEARCH.md` | depth-first core design: reference synthesis, gaps, proposals | absorbed (D-029; P2c accepted — D-033); retired at the phase-0 gate review (`doc-1`) |
| `docs/BLUEPRINT.md` | reference distillation: resolution ledger (12 mechanisms) + cross-cutting laws + build index; entry to `docs/blueprint/` | active (iter-0u); maintained at deep-dive time |
| `docs/blueprint/` | per-part applied synthesis: `phase0.md` (iter-1..6 combined donor designs), `phases.md` (phases 1–6 architecture + cross-cutting) | active; read before the matching iteration/phase |
| `docs/REFERENCES.md` | external source catalog: donors, licenses, phase gating | active (rots — quarterly review) |
| `docs/REFERENCES_DEEP.md` | index + format template + iteration plan for per-ref deep dives; the deep-dive content lives in `docs/ref/` (D-026) | active (rots — quarterly review alongside `doc-2`) |
| `docs/ref/` | per-reference deep dives (one file per source: `neighborly.md`, `mesa.md`, `df_legends_xml.md`, `df_worldgen.md`, `cdda_data_json.md`, `paradox_scripting.md`, `rimworld.md`, `l4d_director.md`, `alien_isolation.md`, `wesnoth_wml.md`, `endless_sky_dsl.md`, `ink.md`, `tracery.md`, `brogue.md`, `dcss.md`, `keeperrl.md`, `generative_agents.md`, `ai_town.md`, `letta.md`, `azgaar_fmg.md`, `natural_earth.md`, `geonames.md`, `libtcod.md`, `rot_js.md`, `red_blob_games.md`, `entt.md`, `bevy.md`, `eventstore.md`, `sqlite_fts5.md`, `duckdb.md`, `sqlite_vec.md`, `agent_memory_atlas.md`, …); mechanics, take/adapt/inspire/strengths/weaknesses/verdict | active (rots — quarterly review alongside `doc-2`) |
| `schemas/event.schema.json` | machine-readable event contract | synced with `docs/EVENT_SCHEMA.md` |
| `core/` | engine: `schema` (mini-validator, D-032), `rng` (RngBank), `ids`, `clock`, `queue`, `log` (the only canon-write path; iter-37: `IMPORTANCE_ORDER` — the single owner of the importance ordering, imported by the chronicle gate and the DIR-2 metric), `fold` (projection seed + apply; pair map + crime-status conventions; st-1: `present_entities`/`present_in_order` — the structural presence reads, single owner of the presence set), `pack` (loader + lint incl. the iter-11 texture-block lint and the st-1 present_at + present_entities clauses), `intent` (front-door machinery: preconditions, checks, OCC, knowledge resolution incl. `destination_location`; iter-11: the texture path — shape-gated reference as data, `requires_for`, the `texture` noun + `texture_noun` test; st-1: the per-present-target `knows` expansion — `present_at` sites, the `{present}` slot; tune-1: `pack_importance` takes the event type — the story-critical hook, D-059; pack-2/iter-29: the `spot_available` door check — the closed set's 15th test, layer-param lint-checked), `resolvers` (the action-resolver registry — grows only with a new MECHANIC; tune-1/iter-27: `recuperate` — pack-declared actor `status_effects`, the rest action's fatigue counter-play; `movement_changes` — the single owner of the carried-item position contract, used by move/flee and the rotation, KI#46; iter-11: `_pickup` texture branch — a take-success IS the promotion, D-054), `transitions` (the generic layer engine — the layer vocabulary is pack data: iter-20/D-057 `spot_state`/`halt_flag`/`follow_ups`; fire is the first declared layer), `knowledge` (iter-3: derived per-knower index, salience, transfer decay, telling reaction, expectation checks), `crime` (iter-3: suspicion reactions + status flip + arrest; iter-4: arrest resolution; iter-16/KI#46: `rotation_plan` rides `movement_changes` — carried items travel with the rotating participant), `scheduler` (the system-pass DAG), `predicates` (iter-40/drama-1: the JSON predicate grammar over the projection — the three v0.1 leaf kinds + compound `all`/`any`/`not` + the implicit-AND list root + the generalized `prop` leaf; the single owner of the closed leaf/compound/comparator vocabularies, imported by the director and the pack lint; contract `DIRECTOR_SPEC.md` §3), `onaction` (iter-42/drama-3: the on_action dispatch — the pack reaction table keyed by committed event type; the `witnesses` scope + the per-entity quantified gate + the alarm-shaped state change; the one-hop law; contract `DIRECTOR_SPEC.md` §3c), `leverage` (iter-44/social-1: the secrets reaction + the fact-cluster read — a novel knower of a pack-declared secret token (`rules.json::secrets`, lint incl. the mintable-token vocabulary) mints a `leverage_gained` fact event (no knowledge/hooks/state, expiry a read-side fold via `live_leverage`); system row `MVP_SCOPE.md` §5 #9), `echo` (iter-46/social-2: the psychological echo — a PURE read-side fold over the knowledge view (P3e: per-NPC valence decaying linearly with ticks since learned, fidelity-scaled, clamped; a missing pair IS zero; `rules.json::echo` linted incl. the mintable-token law); writes NOTHING — no events, no state, no templates, no metrics; consumed by the intent door's `echo_at_least` gate (INTENT_SCHEMA §3 — the P2b behavior gate; the window law rides `WINDOWED_TESTS`), never by the director (L6 — never an entropy input); DORMANT in the committed pack (content-5 owns the driver)), `traits` (iter-55/leg-1: the trait crystallization fold — P3f, the LEGEND_SPEC sketch's trait half; a knower holding `threshold` DISTINCT tokens of a `rules.json::traits` family crystallizes the belief, the source event ids riding as provenance (the expansion law); the echo's sibling read model: writes nothing, feeds no entropy (L6), no decay term, `at_tick` gates contribution; the `_traits` lint (closed vocabularies, mintable tokens, one-sided membership); LIVE consumer since leg-2 (iter-56): the brief's recalled_facts reads the fold — belief lines lead, the family records render nothing raw, `expand_trait` the demand side (BRIEF_SPEC §3.5)), `reflection` (iter-57/leg-3: the memory-compaction MINT — reflection-on-recurrence, a WRITE unlike the echo/traits read models: a knower whose held records of a `rules.json::reflection` family reach the block's threshold (REPEATS — recurrence, breadth is the traits' axis) mints ONE reflection event per (knower, insight) per run through the canon door (originals never dropped, INV-1/INV-5), `outcome.provenance` the `list[event_id]` demand handle, the never-re-reflect law (`view.holds`), `stale_reflections` the read-side stale flag (empty on whole logs; the scavenge screening retr-1 must consult) + `expand_reflection` the demand side; no RNG, no hooks/state changes (L6 — knowledge-side canon); the `_reflection` lint; ARMED (leg-3b, iter-58: the live insight pair — the measured recurrence day1_full seeds 123/128, zero corpus regen; contract `docs/LEGEND_SPEC.md` (written just-in-time at the row, D-022)), `retrieval` (iter-59/retr-1, STORE-1: the read-side retrieval ladder — an in-memory REBUILDABLE SQLite index over (pack, events): static lore rows + every knowledge record (the known_by fold), FTS5 keyword matching (the unicode61 word view — underscored dry tokens are word-retrievable) + the sqlite-vec probe with the pure-Python cosine scan as the rung's semantic definition + the keyword floor when FTS5 is absent (L12: a rung failure degrades, never breaks); the deterministic re-ranker alpha*recency + beta*authority + gamma*bm25 + delta*cosine, coefficients pack data (`rules.json::retrieval`, lint `_retrieval`), ties by construction order (rowid); the `knower` query parameter IS the known_by boundary — facts never vector-searched, never knower-free; the two LEGEND_SPEC sec4/sec5 contract points live (the stale exclusion at build, the source-outranks demotion); DORMANT in the runtime — the mediator's keyword query is the consumer (BRIEF_SPEC sec9)), `director` (iter-4: consequence buffer + triggers + narrative entropy + on/off policy; iter-5: `policy_from_rules`; iter-36/DIR-1: the pacing clock — pack-gated RAMP/PEAK/REST/STAGNATION over entropy, clock-gated stagnation releases, explicit triggers ungated; iter-38/DIR-3: the climax layer — `climax_floor` the third threshold (pack data) + the one-beat PEAK_CLIMAX boss state + the climax release path + the `permit_climax` policy question; iter-39/DIR-4: the multi-channel quiet split — `director.channels` pack data (per-channel floors + the closed `CHANNEL_INPUTS` bindings), `SeededHook.channel`, the per-hook quiet gate (`permit_quiet`), the channel entropies pure map; iter-40/drama-1: triggers delegate to the predicate grammar, the `weight_multiplier` (effective weights in entropy + channels, `beat_tick` threaded), `first_time_only` the burn set; iter-47/arc-1: the release-chain layer — `director.arcs` pack chains (`ArcConfig` + `arcs_from_rules`, the order law's per-run cursor + the gap law + the entropy mirror, DIRECTOR_SPEC §3d); contract `DIRECTOR_SPEC.md` §3/§3a/§5), `urgencies` (iter-4: P2b goal ticker — NPC probability rolls through the intent door), `states` (iter-4: fatigue/intoxication/fear decay passes at clock beats; iter-4a: `reset_on_rotation` resets on the watch_change event + per-axis last-change decay baseline), `loop` (tick driver + playscript runner + the reaction cascade + clock-crossing rotations + clock-crossing beats in tick order; iter-4a: the playscript feed is player-driven only; iter-5: public session doors `open`/`run_steps`/`close`), `metrics` (iter-6: M1–M5 + emergent-chain count as pure functions of `(events, projection)` — Mesa `DataCollector` inverted; system classification is pack data at `rules.json::metrics.system_of_type`; iter-37/DIR-2: `eventless_beat_stretches` — the phase-3 exit criterion's instrument, tale-gate scene events over the `urgencies.beat_ticks` window axis, gate-read per `Grammar.tale_gate`) | iter-1..4 landed, iter-4a audited, iter-5 session-factored, iter-6 gate-closed, iter-11 texture door |
| `sim/systems/` | reserved for periphery systems — anything the canon door must dispatch is kernel mechanics and lives in `core/` (the import-boundary law, D-031/D-037: core never imports sim) | reserved (empty skeleton since iter-0d) |
| `content/tavern_pack/` | the setting as data | v0.1 + iter-3 (all 12 actions executable; expectations + telling + suspicion mapping + watch rotation + pair-relations seeds + movement sighting templates) + iter-4 (director.hooks config, urgencies.entries, arrest.resolution_* fields, status_values +`caught`) + iter-5 (templates.json completed into the tracery grammar: symbols, alternatives, conditionals, tale_gate; display names carry articles) + iter-6 (`rules.json::metrics.system_of_type` — event-type → systems touched map, pack data for M1 cross-system share) + iter-11 (`take` texture block — texture-capable, real `unique_slots: ["hearth"]`; iter-11a: conditional take templates rendering the promoted slot noun) + iter-15/st-1 (`move` gains the actor-held per-present-target expansion template — the arrival snapshot, INTENT_SCHEMA §7; `brief.present_entities` block budgets + caps + the card-marker table; 2 pair-relation seeds — drunk→guard fear, maid→barkeep trust) + iter-20/D-057 (`layout` on all 5 locations + `brief.present_entities.scene_line_fields`; the fire layer's `follow_ups`/`spot_state`/`halt_flag` vocabulary restructured into layer data) + tune-1/iter-27 (the 13th action `rest` + `status_effects`; `importance.story_critical_events` + the `story_critical_event` score term; `tale_gate` low→medium; `metrics.system_of_type` gains `rest`) + tune-2/iter-28 (`brief.present_entities.card_markers` — the prop-path marker table: wary on suspicion ≥ 25, suspect/caught on the player's card) + pack-2/iter-29 (the arson `spot_available` requires row — arson on ashes is a door rejection) + iter-36/DIR-1 (`director.pacing` — the peak/rest clock block: peak_floor 25, min_peak 1, min_rest 1, lint-checked against the stagnation floor) + iter-38/DIR-3 (`pacing.climax_floor` 75 = 3× the peak floor — the third threshold layer, declared-but-dormant: no hook carries the climax flag yet, the owner's content call) + iter-39/DIR-4 (`director.channels` — threat 3 / social 5 / ambient 2 + input bindings; both hooks channel-tagged, the ambient channel declared-but-dormant) + iter-42/drama-3 (`on_action` — the reaction table: the dormant `document_check` → `crowd_wary` entry, the iter-38 climax-flag pattern; `templates.json` gains `crowd_wary`) + iter-44/social-1 (`secrets` — the fact-cluster registry: the ONE live theft token over pc_01, blackmail, 720 ticks; `templates.json` gains `leverage_gained`; story-critical + system_of_type rows) + iter-45/social-1b (the 15th action `coerce` — the leverage spend over the `coerce` resolver: the `leverage_over` door, the `balance` pair-shift block, DORMANT driver per the iter-38/42 pattern; `secrets.spend_event`; `templates.json` gains `coerce`; story-critical + system_of_type rows) + iter-46/social-2 (`echo` — the residue valence table: scale [0,100], fidelity_weight 100/50/25 over the chain, the four walkthrough tokens (the theft sighting dread 25 / wariness 35 fades 720; the fire-starting sighting dread 60 / wariness 30 fades 1440; the inferred absence wariness 30; the bar noise wariness 15 fades 360), papers_unsatisfactory undeclared — the iter-44 institutional call; DORMANT: no consumer carries echo_at_least, the 10-seed A/B 10/10 byte-identical, zero corpus regen); + iter-55/leg-1 (`traits` — the belief table: threshold 3, the theft family [sighting, noise, inferred absence] crystallizing `paranoid_about_thieves`, DORMANT); + iter-58/leg-3b (`reflection` — ARMED: the live insight pair `sneak_at_work_here` over the repeated sighting + `trouble_by_the_bar` over the repeated noise, threshold 2, the measured recurrence day1_full seeds 123/128; `templates.json` gains `conclusion_drawn`, story-critical, zero corpus regen); loaded + linted by `core/pack.py` |
| `render/` | the deterministic renderer: `tracery.py` (CHRON-1 engine — grammar + ShufflePool + modifiers + save/restore + conditionals, cosmetic stream only) · `chronicle.py` (chronicle / scene card / entity views / replay report — pure functions of the log; iter-11a: the `texture_slot` derived context slot — a texture-path take renders the promoted slot noun, no canon target) | landed iter-5 |
| `brief/` | the phase-1 mediator circuit (LLM-free engine side): `assembler.py` (BRIEF-1 — the eight-block pipeline: directives / scene delta / scene texture / present entities (st-1, iter-15: the entity cards — present set + observable surface + pair tokens + promoted props; iter-20/D-057: the pack-declared `scene_line_fields` render canon-from-birth; tune-2/iter-28: the prop-path `card_markers` table — threshold rows + value rows, the `markers=` segment) / recalled facts (iter-56/leg-2: the derived-trait read — the PC's crystallized beliefs lead as belief lines, the family records render nothing raw) / scheduled lore / voice exemplars / active options; two-level token budgets, whole-block eviction, `[truncated:N]` markers; pure functions of (log, ledger), zero RNG — `docs/BRIEF_SPEC.md` owns the contract) · `validator.py` (VALID-1 — the proposal→check→commit gate: the closed proposal document, honest verdicts `supported/contradicted/insufficient_data`, ExpectedVersion OCC, fact-transaction pass-through, the ≤2-regens protocol, golden-set plumbing; pure functions of (proposal, log, pack) — `docs/VALIDATION_SPEC.md` owns the contract) · `ledger.py` (SCENE-1, iter-10 — the session scene ledger: entry shape + discrete lifecycle, scenes as PC-location intervals (a pure log fold), the structural-pinning + validation gateway (scope/canon/laundering/unique-slot/duplicate), beat-pass contradiction retirement, scene-close bulk retirement, texture-OCC withdrawals, the promotion marker, `check_delta_shape` (the boundary-side delta gate, iter-12); session render state under the D-049 determinism quarantine — mechanism `docs/blueprint/phases.md` §1, window contract `docs/BRIEF_SPEC.md` §3.3) · `mediator.py` (iter-12, D-055 — the narrator boundary's pure document layer: the narrator call document (brief + `narrator_protocol`), the closed response document `{prose, texture_delta?, proposal?}` with deep shape gates at the boundary, `feedable_intents` (noun resolution / actor / duplicate filters → WITHDRAWN notes), `promotions_in` (the committed-promotion scan) — formats `docs/BRIEF_SPEC.md` §7.1 + `docs/VALIDATION_SPEC.md` §7.1) · `parser.py` (iter-31, phase 2, D-062 — the mode-C boundary's pure document layer: `grammar_snapshot` (pack verbs with pack-derived field constraints ∪ addressable nouns — canon entities + live texture entries; ghost interactivity structurally impossible), `parse_call` (utterance + grammar + protocol, byte-deterministic), `parse_reply_from_mapping` (the closed {intent \| question \| no_intent} gate — loud ParseError, never a feed) — contract owner `docs/PARSER_SPEC.md`) | landed iter-8; +validator iter-9; +ledger iter-10; +boundary docs iter-12; +parser door iter-31 |
| `cli/` | the play interface: `main.py` (batch `play`/`chronicle`/`state`/`replay` + the interactive session: `look`, `wait N`, `play`, `narrate [<reply.json> | dry]` — the agent-in-the-loop narrator door, `directors on|off`, `seed`) · `mediator.py` (iter-12, D-055 — the mediator session loop, periphery orchestration D-046: the D-049 beat cycle over `Simulator` + `SceneLedger`, the file doors `output/mediator/call_<N>.md` ↔ `reply_<N>.json`, the L12 ladder narrator→template→dry, the regen budget; iter-13/KI#44: the accepted-beat `BEAT` verdict-summary lines; `docs/VALIDATION_SPEC.md` §7.1) · `parser.py` (iter-31, phase 2, D-062 — the mode-C session door `ParserDoor`: emit_call/apply_reply over `output/parser/parse_<N>.md` ↔ `parse_reply_<N>.json`, the pin law (`SceneLedger.pin` — the reference IS the pin), the feed through the front door + promotion wiring, the `say`/`say apply` commands; contract owner `docs/PARSER_SPEC.md`) · `__main__.py` (`python -m cli` entry) | landed iter-5; +narrate iter-12; +BEAT summaries iter-13; +say door iter-31 |
| `scripts/` | operator tooling (CLI-class, D-046): `balance_harness.py` (iter-6: the 1000-sim distribution harness for KI#4 close; harness contract owner `docs/TEST_PLAN.md` §6; iter-37/DIR-2: the pacing A/B `--pacing on|off` — the clock-off arm = the pack minus `director.pacing`, a linted variant under `output/pack_nopacing/`, requires `--directors on` — plus the eventless-stretch block; outputs `output/balance_<N>_seed<S>_<on|off>.txt`, a `_nopacing` suffix marks the clock-off arm — gitignored runtime artifact, reproducible from the seed range) · `df_survey.py` (iter-8e: the DF Legends XML empirical survey — sanitize+stream parsing core for bg-1; iter-8f: truncation survival via `RecoveringReader`; iter-8g: `--audit` mode — coverage census per section/record-tag + every unique child-tag set per record tag, UNDOCUMENTED markers for tags outside the coverage matrix, bg-1/KI#36; measured numbers owner `docs/TECH_NOTES.md` §3.1; coverage matrix `docs/ref/df_legends_xml.md`; reports to `output/df_survey_*.txt`) · `df_import.py` (bg-1: the SQLite sink — loads a world export into `output/df_world_<stem>.sqlite3`; typed cores + EAV + `event_participant` + generic JSON `records` over the unchanged survey core; truncation policy = flagged partial default / `--strict` abort; bg-2: **the plus pass, sink v2** — the companion's `historical_events` land in a separate `event_plus_fields` EAV keyed by the same ids, everything else in the companion counted-not-stored (D-051's recorded deferral fired, D-063); recipe owner `docs/TECH_NOTES.md` §3.2; regression `tests/test_df_import.py`; DB + exports are gitignored runtime artifacts — `dfworlds/` stays outside the repo) · `df_taxonomy.py` (bg-2: the event-taxonomy survey — 15 event-type plans + the birth gap + war/beast-attack collection plans over a sink DB, fixed quantile-spread selection, per-entry participants/place/reconstructed-cause/witness/consequence; reports to `output/df_taxonomy_<stem>.txt`; content owner `docs/TAXONOMY.md`; regression `tests/test_df_taxonomy.py`) · `df_briefer.py` (bg-3: the briefer spike over the sink DB — the POV mini-briefer (`cases`/`call`: the assignment frame + subject card + in-window records via the participant prefix scan, plus fields merged main-precedence, the 60-record cap with the loud marker), the reverse-validation gate (`apply`: closed verdicts `supported/contradicted/beyond_records/unknown_*`, prose never parsed, the anchor shape gate), the ≤2-regen ladder with the dry floor, the retrieval stress (`stress`: quantile figure spread + double-build byte-compare; reports to `output/df_briefer/stress_<world>_<stem>.txt`, call/reply files under `output/df_briefer/` — gitignored runtime); recipe + numbers owner `docs/TECH_NOTES.md` §3.3; regression `tests/test_df_briefer.py`) · `profile_harness.py` (iter-30/perf-1: the long-tick timing profile — story phase + grid-aligned waits to the tick target, clean + cProfile double-run with a same-process byte-compare probe, read-side timings over the finished log; measured numbers owner `docs/TECH_NOTES.md` §8; reports to `output/perf_<ticks>_seed<S>_<on|off>.txt` — gitignored runtime artifact) | landed iter-6; +iter-8e/8f/8g; +bg-1; +bg-2; +bg-3; +iter-30 |
| `tests/` | test suite | smoke + architecture fitness, T0, minimal T1/T2, core units, loop e2e (iter-1); intent units, the 12 actions e2e, scheduler DAG, transition units, INV-3 stoplist (iter-2; +`brief/` iter-10a); T3 blind-NPC + knowledge/transfer/expectation suites, crime/rotation/OCC suites, iter-3 pack lint (iter-3); director + urgencies + states suites, arrest resolution updated, iter-4 pack lint (iter-4); the pacing-clock suite — the state machine (spike→PEAK, min-duration hysteresis, rest→quiet band, world-respike), REST suppression + post-rest resume, the explicit-trigger-mid-rest law, the no-double-advance guard, pack-gating (no pacing = v0.1), the pacing lint refusals (iter-36); tracery engine + chronicle + CLI suites incl. T1-chronicle and the directors A/B wiring (iter-5); the metrics module + the iter-6 T1 fixture-regeneration guard + the T8 single-factor A/B suite (iter-6); the brief assembler suite — byte-identity, beat-window mirror, budgets, eviction, brief pack lint (iter-8); the validator suite — the closed-document boundary, the verdict truth table, stale/rebased/first-break OCC, intent grammar gate, the regen protocol, the committed golden set (iter-9); the bg-1 parsing-core regression suite — sanitize/recover/census/audit + the UNDOCUMENTED marker + the measured in-flight-record truncation behavior on a tiny synthetic DF-like XML (iter-8g, extended bg-1); the bg-1 SQLite-sink suite — typed cores/EAV, participant index, membership/parent links, generic records, truncation policy (both modes), content determinism, the bg-3 consumer query (bg-1); the scene-ledger suite — scene derivation/presence, the gateway refusal vocabulary, the one-way lifecycle, contradiction retirement, withdrawals, the golden delta fixture replay (iter-10); the texture-promotion door suite — shape gates/`requires_for`/the texture noun tests, e2e canon birth / failed attempt / soft rejection, texture-path OCC attribution, the texture-block pack lint (iter-11; hardening iter-11a); the mediator suite — the call/response document contracts, the refusal→regen→dry ladder, promotion e2e + withdrawal, call-bytes determinism, the CLI `narrate` wiring (iter-12); the phase-1 regression set — the narrator-beat corpus replay + the accepted-beat `BEAT` summary lines (iter-13; grown every validation-beats session since — the per-session probe families live in the fixture's `source` field, the coverage tallies in `STATUS.md`); the presence suite — the per-present-target expansion e2e + the entity-card block (cards/pair tokens/caps/promoted props/eviction rank) + the present_at lint clauses (iter-15); the tune-1 suite — rest e2e (fatigue reduction + the zero-delta quiet beat), the story-critical importance hook + the tale-split regression pin, the new lint refusals (story-critical typo, status_effects on a wrong resolver/axis/zero delta) (iter-27); the tune-2 suite — the card-marker rows (wary at the threshold, the below-threshold boundary, the value-row vocabulary, the blind-delta pin) + the card_markers lint refusals (iter-28); the pack-2 suite — the destroyed-yard arson rejection (no post-rejection fire events) + the fresh-location happy path + the layer-param lint (iter-29); the parser suite — the snapshot's derived verb/noun grammar (incl. the position-tracked near enum), the call document's bytes, the closed reply gate's refusal vocabulary, `SceneLedger.pin`'s law, the door e2e (feed / question / no_intent / attempt-is-a-fact / pin-and-promote / failed-attempt-keeps-pin / the shared-ledger scene-close retirement) + the `say` CLI wiring (iter-31); the parse corpus replay — the say-door regression set from the ten live iter-32/33 sessions (fixtures/parse_replies.json; the corpus machinery's narrator-cycle note override + per-cycle state assert) (iter-32; +batch-2 iter-33); the bg-2 suites — the sink plus pass (companion events → `event_plus_fields`, main-count invariance, v1/absent companion, plus truncation) + the taxonomy survey (plan rendering incl. plus detail + presence flags, the witness `hfid1`/`hfid2` EAV path, birth gap, war/beast collection rows, determinism, v1 degradation, the spread) (bg-2); the bg-3 suite — the POV brief (assignment/card/records, plus merge, the loud truncation marker), the closure blind-spot pin (reputation hfid1/hfid2 invisible), the verdict families (supported/contradicted/beyond_records/unknown_*), the shape-gate parametrized refusals, the regen ladder (refusal→notes→accept) + the exhaustion dry floor, the case listing, the stress smoke (bg-3); the climax layer suite — the boss-beat exit, the layered-threshold/peak-minimum/placement gates, the quiet-path exclusion, explicit-beats-climax, the no-double-boss guard, the flagless-layer + two-layer byte-identity pins, the two lint refusals (iter-38); the multi-channel suite — the channel-entropies input binding, the quiet-channel-while-another-burns law (+ the v0.1 comparison arm), the escalation self-block on the pack's own numbers, the global clock gate, explicit-triggers-ungated, the channelless/dormant-tag fallbacks, the shared budget + cross-channel tiebreak, director-off, climax-ignores-channels, the four lint refusals (iter-39); the predicate suite — the drama-1 grammar: the leaf truth tables (incl. the pack-trigger parity pin + the prop leaf's honest-missing/bool-guard laws), the compounds + implicit-AND list root, the loud backstop, the closed vocabularies (tests/test_predicates.py) + the drama-1 director additions — the multiplier wiring + the pack's escalation, the truncation order, the burn law + the repeatable-tag differential, the compound trigger, the six lint refusals (iter-40); the option-layer suite — the v0.1 payload path (the id-sequence pin), the pack wiring + the live glance release, the weight/tie/escalation-flip/zero-out/deferred-release laws, the whole-key payload override, entropy untouched, the closed-boss no-mark law, the burn through the pick, the seven lint refusals (iter-41); the on_action suite — the witnesses scope (dedup, event order), the clamped delta + no-op drop + the numeric-home law, the quantified gate (honest-missing/bool-guard), the alarm-shaped event + the one-hop termination, the append composition + the lazy-group discipline through the real door, the crowded-room live path via linted pack variants, the byte-identity arm, the twelve lint refusals (tests/test_onaction.py) (iter-42); the document_check suite — the ladder e2e (verdict → arrest → caught irreversible), the talked-down branch, the deferred release + the climax-path release (the seed pins: 19/93/2/125), the pack declarations, the door rejections, the chronicle branch lines (tests/test_doccheck.py) (iter-43); the leverage suite — the fact-cluster shape + the novelty/holder/dedup/cascade laws, the told-secret briefing path, the expiry boundary fold, the chronicle line, both A/B arms (no-births byte-identity + births-as-only-divergence), the ten lint refusals (tests/test_leverage.py) (iter-44); the coerce suite — the fold's spent law (the spend kills at its tick, expiry beside it), the door's first fold-reading test, the balance resolver (neutral-materialization/clamp/quiet beat), the urgency gate's silence, the e2e spend on a crafted driver (one secret buys one play, the window-close OCC rejection, the chronicle line), the lint refusals incl. the spender-must-gate contract (tests/test_coerce.py) (iter-45); the echo suite — the fold laws (linear fade, the boundary death, fidelity scaling, renewal, clamp, zero-absence, order), the door unit + the attribution exclusion, the urgency gate's silence, the e2e live-fire on crafted copies (the driver fires once then fades silent; the action-gated window-close rejection), the committed residue pin, the byte-identity arm, the eleven lint refusals (tests/test_echo.py) (iter-46); the arc suite — the chain laws (the order law reordering the quiet path + holding explicit triggers, the gap spacing, the explicit mid-gap bypass, future-members-count via the PEAK-hold pin, passed-instances-excluded via the quiet-probe differential, one-play-per-arc-beat, the climax path's chain+gap composition, director-off, the empty-block inert law, the committed-pack dormancy pin) + the eight lint refusals (tests/test_director.py) (iter-47); the panic suite — the live echo dispatch (the compounding spike+contagion, the cause actor's own-shout fear, the one-hop termination, the decay baseline, the chronicle line after the shout, the empty-room law, the stripped-entry byte-identity, the declarations + the corpus-evidence guard) (tests/test_panic.py) (iter-48); the arc-driver suite — the live-fire march on the committed pack (the relief's check then the barkeep's sweep, the gap arithmetic beat 1→3, the canon order after the check, the PEAK_CLIMAX closing mark, the snapshot mint), the arcs-stripped A/B arm (the inversion: the sweep's event landing BEFORE the check's — the gap law load-bearing), the defused-relief order-law stall, the fingerprint identity + the quiet-seed byte-identity (the stream containment), the declarations (tests/test_arc_driver.py) (iter-52; the iter-47 arc suite's committed-pack dormancy pin flipped to the live-declaration pin in the same iteration); the ambient-driver suite — the quiet march on a crafted idle stage (the wait-seeded tag, the beat-720 release through the ambient channel's own floor, the room-heard mint with the rotated guard honestly absent, the burn, the STAGNATION end state), the chronicle line in log order, the directors-off arm, the day1 all-PEAK containment, the seeding-stripped mechanism isolation, the fingerprint identity incl. the heard record's watch-change transfer, the declarations (tests/test_ambient.py) (iter-53); the derived-trait read suite — the belief-line laws (the replacement, the exact line shape, the cross tick, the below-threshold gate, the hearsay dedup, the cap, the declaration order, the no-block gate, the zero-regen witness) + the expansion family (tests/test_brief.py + test_traits.py); the reflection suite — the mint laws (the threshold crossing live-fire, the provenance list, the inferred/exact record, the below-threshold floor, the never-re-reflect gate, the within-event dedup, the knower gate, the insight-as-ordinary-knowledge law, determinism + RNG-free, L6 structural) + the stale family (empty on whole logs, firing after scavenge) + the expansion family + the lint family (tests/test_reflection.py) (iter-57); the retrieval suite — the rung family (FTS5 lore matching, the OR recall, the knower boundary, the keyword floor, the cosine scan RAG shape, the probe degradation, the vector validation), the re-ranker signals + ties, the stale scavenged law, the source-precedence live-fire (the higher-scoring reflection demoted below its own sources), the lint family, the 10-seed zero-price A/B (tests/test_retrieval.py) (iter-59) |
| `tests/fixtures/` | committed golden fixtures for T0/T1 (log lines, byte-identical run outputs) | `plumbing_smoke_seed42.jsonl` (iter-1; env-pinned — header records the Python version; kept byte-identical iter-4..14; **regenerated iter-15/st-1** — the arrival-snapshot records ride the 4 move events, +10 knowledge records, event stream/state_changes byte-identical — the iter-3 precedent: a pack-behavior growth commits the regen in the same iteration) · `validation_golden.json` (iter-9 — the validator's golden set over the smoke fixture log: pinned verdict cases for all three verdicts; `docs/VALIDATION_SPEC.md` §9) · `texture_deltas.json` (iter-10, grown iter-11/11a — the scene-ledger golden delta fixture: 17 narrator-delta cases replayed through the gateway against smoke-log prefixes, pinning all 8 refusal reasons (laundering/unique_slot incl. the post-promotion one-object claim) + no-ops + the full ledger state; `docs/BRIEF_SPEC.md` §3.3) · `narrator_beats.json` (iter-13, grown every validation-beats session since — the phase-1 regression set: full narrator response documents distilled from the live agent-in-the-loop `narrate` sessions (one seed per session; per-session provenance and findings live in the fixture's own `source` field — never restated here), replayed through the REAL mediator cycle (Simulator + Mediator + SceneLedger): the refusal vocabulary in full, both door outcomes, the rebased/stale anchor paths, and the per-session probe families; the `'anchor'` placeholder resolves to the advertised anchor; the corpus's size and coverage tallies live in `STATUS.md`; `docs/VALIDATION_SPEC.md` §7.1; **re-distilled iter-43** — the 4 seed-93 cases re-pinned to the document_check world, the regen the content landing implied, the iter-15 regen precedent; **re-distilled iter-44** — the 9 seed-93/seed-15 cases re-pinned to the leverage world, the same precedent) · `parse_replies.json` (iter-32, parse-1; grown iter-33, batch 2 — the phase-2 parse-reply regression set: full say-cycle documents (utterance + probes + reply) distilled from the ten live iter-32/33 sessions (seeds 125/42/4/23/8/41/111/65/30/32; provenance + the PARSER_SPEC §6 tallies live in the fixture's `source` field — never restated), replayed through the REAL mode-C stack (Simulator + Mediator + ParserDoor over one shared ledger): every off-grammar probe family caught loudly, both door outcomes, the pin law's live paths (incl. the two RETIRED-terminal deaths — scene close + narrator withdrawal), the door-batch fire cascade + wait-720 decay batch, the disambiguation ladder, the question/no-intent surfaces, the per-cycle state pins; `docs/PARSER_SPEC.md` §6/§7) |
| `tests/playscripts/` | seed + ordered-intent fixtures | `plumbing_smoke.json` (iter-1), `day1_theft_and_arson.json` (iter-2 — the §3 walkthrough: steal, take, drop-lamp, fire chain), `day1_full.json` (iter-6 — the T8 gate playscript, seed 125: take → 2 steals → wait 720 → move backyard → drop_break → wait 720) |
| `docs/DIRECTOR_SPEC.md` | director runtime contract (the trigger fired at iter-4 start — `SPECS_BACKLOG.md` sketch row) | written iter-4; +§5 pacing clock iter-36; +§5 climax layer iter-38; +§5 channels iter-39; +§3/§3a predicate grammar + weight multiplier iter-40; +§3b option layer iter-41; +§3c on_action iter-42; +§3d arc layer iter-47; §3d live driver iter-52; §5 ambient channel live iter-53 |
| `docs/BRIEF_SPEC.md` | brief assembly contract (the trigger fired at phase-1 start — `SPECS_BACKLOG.md` sketch row): the eight-block pipeline, budgets, eviction, render format §7 + the narrator call document §7.1, just-in-time deferrals | written iter-8; §9 flip iter-10; +§7.1 iter-12; +§3.4 presence iter-15; +§3.5 derived-trait clause iter-56 |
| `docs/VALIDATION_SPEC.md` | validation contract (the trigger fired at phase-1 start — `SPECS_BACKLOG.md` sketch row): the closed proposal document, verdict semantics, ExpectedVersion OCC, the fact transaction, the regen protocol, scene-ledger protocol clauses, golden-set plumbing, the response document + session wiring §7.1 | written iter-9; §8/§10 scene-ledger clauses added iter-10; +§7.1 iter-12 |
| `docs/PARSER_SPEC.md` | the mode-C parser boundary contract (the trigger fired at phase-2 start — `SPECS_BACKLOG.md` sketch row): the external-parser file contract, the grammar snapshot (pack verbs ∪ addressable nouns — ghost interactivity structurally impossible), the closed reply document {intent \| question \| no_intent}, the pin law + the feed wiring, the ≥90% valid-intents measurement, the just-in-time deferrals (runtime engine, buttons, multi-intent) | written iter-31 |
| `docs/TAXONOMY.md` | the bg-2 DF event taxonomy: the 16-target type map with E/E+/R/GAP ontology verdicts, the 120-entry corpus (participants/place/cause/witness/consequence per entry), the measured findings (the witness gap, the `hfid1`/`hfid2` index blind spot, plus-only fields) + the bg-3 consumer caveats | written bg-2 (D-063) |
| `docs/LEGEND_SPEC.md` | the reflection & compaction contract (the LEGEND_SPEC sketch's compression half, written just-in-time at the leg-3 row — the D-022 law; the trait half is D-084's, linked): the compaction law (reflection-on-recurrence, never `summarize_messages_in_place`), the reflection event shape, the provenance law, the stale law, the pack contract + lint laws, the dormancy + arming row | written iter-57 (D-086) |

## 2. Reading gradient (what to read before working)

| Scale | Read, in order |
|---|---|
| Trivial — typo, single value, doc fix | `STATUS.md` |
| Normal — bugfix, small feature, one system | this file → `STATUS.md` → `worklog.md` |
| Deep — new system, schema/queue/director touch | + `AGENTS.md` → `docs/BLUEPRINT.md` (ledger rows for the component) → `docs/MVP_SCOPE.md` → `docs/EVENT_SCHEMA.md` |
| Huge — phase gate, architecture, restructure | + `docs/ROADMAP.md` → `docs/VISION.md` → `docs/DECISIONS.md` |
| LLM-track (`bg-*`) | + `docs/TECH_NOTES.md` → `docs/SPECS_BACKLOG.md` |

## 3. Information ownership (anti-drift map)

| Information | Single owner | Everyone else |
|---|---|---|
| Phase-0 scope: entities, systems, actions, tests | `docs/MVP_SCOPE.md` | link, never restate |
| Event fields, enums, header, versioning | `docs/EVENT_SCHEMA.md` ↔ `schemas/event.schema.json` (2-place sync, test-enforced) | examples must match |
| Phases, gates, kill-criteria, donors | `docs/ROADMAP.md` | link |
| What to do next | `docs/TASKS.md` | link |
| Why it all exists, honest limits | `docs/VISION.md` | link |
| Stable design decisions | `docs/DECISIONS.md` | link |
| Core-design research & depth proposals | `docs/CORE_DESIGN_RESEARCH.md` | link; accepted items move to DECISIONS/TASKS, line flips to `absorbed` |
| Cross-reference resolutions & donor combinations per build component | `docs/BLUEPRINT.md` (ledger + laws) + `docs/blueprint/<part>.md` (application) | link, never restate; cite ledger row IDs (e.g. "per RNG-1") |
| Volatile: model stack, hardware, DF pitfalls | `docs/TECH_NOTES.md` | link, expect rot |
| External sources: donors, licenses, phase gating | `docs/REFERENCES.md` (catalog) ↔ `docs/ROADMAP.md` §4 (active shortlist) ↔ per-source deep dives (`docs/REFERENCES_DEEP.md` index + `docs/ref/<source>.md` files, D-026) | link, never restate |
| Agent law, invariants | `AGENTS.md` | `STATUS.md` carries one-liners only |
| Where things are | this file | `README.md` carries a short map |

Duplication rule: if a fact must appear twice (e.g. invariant one-liners), the
copy is a link or a one-line summary — never a second full statement.

## 4. Update rules

- Structure changed (new dir/file/schema) → update §1 in the same iteration.
- New pitfall discovered → `STATUS.md` FAQ (≤20) if durable, `docs/TECH_NOTES.md`
  if it rots.
- This file never carries history or narrative.

## 5. Log hygiene

Runtime logs live in `logs/` (gitignored). Never open one whole: use
`tail -n 50`, `wc -l`, or a `python -c` filter by type/actor/tick. Rendered
chronicles go to `output/` (gitignored).
