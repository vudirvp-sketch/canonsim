# AGENT_NAVIGATION.md — Where Things Are

> Current-state map: describes what IS, not what happened. Update §1 in the
> same iteration any structure changes. No history, no narrative.

## 1. Where things are

| Path | Purpose | Status |
|---|---|---|
| `README.md` | entry point, repo map | stable |
| `AGENTS.md` | operating law for agents | stable |
| `STATUS.md` | iteration state, KIs, FAQ | every iteration |
| `worklog.md` | capped short-term memory | every iteration |
| `docs/VISION.md` | frozen why + honest limits | frozen |
| `docs/MVP_SCOPE.md` | phase-0 tech spec (TavernSim v0) | active |
| `docs/EVENT_SCHEMA.md` | event contract | active |
| `docs/INTENT_SCHEMA.md` | intent contract (the proposal side of the door; trigger-gated, written iter-2) | active |
| `docs/ROADMAP.md` | phases, gates, tracks, donors | active |
| `docs/TASKS.md` | backlog + iteration statuses | active |
| `docs/TEST_PLAN.md` | phase-0 verification stack: T0–T8 + M1–M5 + gate protocol + UAP crosswalk + the schema-bump migration procedure (the iter-6 trigger-fired spec) | active (iter-6) |
| `docs/TECH_NOTES.md` | rotting notes: models, hardware, DF spike | live |
| `docs/DECISIONS.md` | ADR-lite, stable decisions | append-only |
| `docs/SPECS_BACKLOG.md` | just-in-time spec queue | active |
| `docs/CORE_DESIGN_RESEARCH.md` | depth-first core design: reference synthesis, gaps, proposals | absorbed (D-029; P2c accepted — D-033); retired at the phase-0 gate review (`doc-1`) |
| `docs/BLUEPRINT.md` | reference distillation: resolution ledger (12 mechanisms) + cross-cutting laws + build index; entry to `docs/blueprint/` | active (iter-0u); maintained at deep-dive time |
| `docs/blueprint/` | per-part applied synthesis: `phase0.md` (iter-1..6 combined donor designs), `phases.md` (phases 1–6 architecture + cross-cutting) | active; read before the matching iteration/phase |
| `docs/REFERENCES.md` | external source catalog: donors, licenses, phase gating | active (rots — quarterly review) |
| `docs/REFERENCES_DEEP.md` | index + format template + iteration plan for per-ref deep dives; the deep-dive content lives in `docs/ref/` (D-026) | active (rots — quarterly review alongside `doc-2`) |
| `docs/ref/` | per-reference deep dives (one file per source: `neighborly.md`, `mesa.md`, `df_legends_xml.md`, `df_worldgen.md`, `cdda_data_json.md`, `paradox_scripting.md`, `rimworld.md`, `l4d_director.md`, `alien_isolation.md`, `wesnoth_wml.md`, `endless_sky_dsl.md`, `ink.md`, `tracery.md`, `brogue.md`, `dcss.md`, `keeperrl.md`, `generative_agents.md`, `ai_town.md`, `letta.md`, `azgaar_fmg.md`, `natural_earth.md`, `geonames.md`, `libtcod.md`, `rot_js.md`, `red_blob_games.md`, `entt.md`, `bevy.md`, `eventstore.md`, `sqlite_fts5.md`, `duckdb.md`, `sqlite_vec.md`, `agent_memory_atlas.md`, …); mechanics, take/adapt/inspire/strengths/weaknesses/verdict | active (rots — quarterly review alongside `doc-2`) |
| `schemas/event.schema.json` | machine-readable event contract | synced with `docs/EVENT_SCHEMA.md` |
| `core/` | engine: `schema` (mini-validator, D-032), `rng` (RngBank), `ids`, `clock`, `queue`, `log` (the only canon-write path), `fold` (projection seed + apply; pair map + crime-status conventions), `pack` (loader + lint incl. the iter-11 texture-block lint), `intent` (front-door machinery: preconditions, checks, OCC, knowledge resolution incl. `destination_location`; iter-11: the texture path — shape-gated reference as data, `requires_for`, the `texture` noun + `texture_noun` test), `resolvers` (the 12-action registry; iter-11: `_pickup` texture branch — a take-success IS the promotion, D-054), `transitions` (the generic layer engine — fire as pack data), `knowledge` (iter-3: derived per-knower index, salience, transfer decay, telling reaction, expectation checks), `crime` (iter-3: suspicion reactions + status flip + arrest; iter-4: arrest resolution), `scheduler` (the system-pass DAG), `director` (iter-4: consequence buffer + triggers + narrative entropy + on/off policy; iter-5: `policy_from_rules`), `urgencies` (iter-4: P2b goal ticker — NPC probability rolls through the intent door), `states` (iter-4: fatigue/intoxication/fear decay passes at clock beats; iter-4a: `reset_on_rotation` resets on the watch_change event + per-axis last-change decay baseline), `loop` (tick driver + playscript runner + the reaction cascade + clock-crossing rotations + clock-crossing beats in tick order; iter-4a: the playscript feed is player-driven only; iter-5: public session doors `open`/`run_steps`/`close`), `metrics` (iter-6: M1–M5 + emergent-chain count as pure functions of `(events, projection)` — Mesa `DataCollector` inverted; system classification is pack data at `rules.json::metrics.system_of_type`) | iter-1..4 landed, iter-4a audited, iter-5 session-factored, iter-6 gate-closed, iter-11 texture door |
| `sim/systems/` | reserved for periphery systems — anything the canon door must dispatch is kernel mechanics and lives in `core/` (the import-boundary law, D-031/D-037: core never imports sim) | reserved (empty skeleton since iter-0d) |
| `content/tavern_pack/` | the setting as data | v0.1 + iter-3 (all 12 actions executable; expectations + telling + suspicion mapping + watch rotation + pair-relations seeds + movement sighting templates) + iter-4 (director.hooks config, urgencies.entries, arrest.resolution_* fields, status_values +`caught`) + iter-5 (templates.json completed into the tracery grammar: symbols, alternatives, conditionals, tale_gate; display names carry articles) + iter-6 (`rules.json::metrics.system_of_type` — event-type → systems touched map, pack data for M1 cross-system share) + iter-11 (`take` texture block — texture-capable, real `unique_slots: ["hearth"]`; iter-11a: conditional take templates rendering the promoted slot noun); loaded + linted by `core/pack.py` |
| `render/` | the deterministic renderer: `tracery.py` (CHRON-1 engine — grammar + ShufflePool + modifiers + save/restore + conditionals, cosmetic stream only) · `chronicle.py` (chronicle / scene card / entity views / replay report — pure functions of the log; iter-11a: the `texture_slot` derived context slot — a texture-path take renders the promoted slot noun, no canon target) | landed iter-5 |
| `brief/` | the phase-1 mediator circuit (LLM-free engine side): `assembler.py` (BRIEF-1 — the seven-block pipeline: directives / scene delta / scene texture / recalled facts / scheduled lore / voice exemplars / active options; two-level token budgets, whole-block eviction, `[truncated:N]` markers; pure functions of (log, ledger), zero RNG — `docs/BRIEF_SPEC.md` owns the contract) · `validator.py` (VALID-1 — the proposal→check→commit gate: the closed proposal document, honest verdicts `supported/contradicted/insufficient_data`, ExpectedVersion OCC, fact-transaction pass-through, the ≤2-regens protocol, golden-set plumbing; pure functions of (proposal, log, pack) — `docs/VALIDATION_SPEC.md` owns the contract) · `ledger.py` (SCENE-1, iter-10 — the session scene ledger: entry shape + discrete lifecycle, scenes as PC-location intervals (a pure log fold), the structural-pinning + validation gateway (scope/canon/laundering/unique-slot/duplicate), beat-pass contradiction retirement, scene-close bulk retirement, texture-OCC withdrawals, the promotion marker, `check_delta_shape` (the boundary-side delta gate, iter-12); session render state under the D-049 determinism quarantine — mechanism `docs/blueprint/phases.md` §1, window contract `docs/BRIEF_SPEC.md` §3.3) · `mediator.py` (iter-12, D-055 — the narrator boundary's pure document layer: the narrator call document (brief + `narrator_protocol`), the closed response document `{prose, texture_delta?, proposal?}` with deep shape gates at the boundary, `feedable_intents` (noun resolution / actor / duplicate filters → WITHDRAWN notes), `promotions_in` (the committed-promotion scan) — formats `docs/BRIEF_SPEC.md` §7.1 + `docs/VALIDATION_SPEC.md` §7.1) | landed iter-8; +validator iter-9; +ledger iter-10; +boundary docs iter-12 |
| `cli/` | the play interface: `main.py` (batch `play`/`chronicle`/`state`/`replay` + the interactive session: `look`, `wait N`, `play`, `narrate [<reply.json> | dry]` — the agent-in-the-loop narrator door, `directors on|off`, `seed`) · `mediator.py` (iter-12, D-055 — the mediator session loop, periphery orchestration D-046: the D-049 beat cycle over `Simulator` + `SceneLedger`, the file doors `output/mediator/call_<N>.md` ↔ `reply_<N>.json`, the L12 ladder narrator→template→dry, the regen budget; iter-13/KI#44: the accepted-beat `BEAT` verdict-summary lines; `docs/VALIDATION_SPEC.md` §7.1) · `__main__.py` (`python -m cli` entry) | landed iter-5; +narrate iter-12; +BEAT summaries iter-13 |
| `scripts/` | operator tooling (CLI-class, D-046): `balance_harness.py` (iter-6: the 1000-sim distribution harness for KI#4 close; harness contract owner `docs/TEST_PLAN.md` §6; outputs `output/balance_<N>_seed<S>_<on|off>.txt` — gitignored runtime artifact, reproducible from the seed range) · `df_survey.py` (iter-8e: the DF Legends XML empirical survey — sanitize+stream parsing core for bg-1; iter-8f: truncation survival via `RecoveringReader`; iter-8g: `--audit` mode — coverage census per section/record-tag + every unique child-tag set per record tag, UNDOCUMENTED markers for tags outside the coverage matrix, bg-1/KI#36; measured numbers owner `docs/TECH_NOTES.md` §3.1; coverage matrix `docs/ref/df_legends_xml.md`; reports to `output/df_survey_*.txt`) · `df_import.py` (bg-1: the SQLite sink — loads a world export into `output/df_world_<stem>.sqlite3`; typed cores + EAV + `event_participant` + generic JSON `records` over the unchanged survey core; truncation policy = flagged partial default / `--strict` abort; recipe owner `docs/TECH_NOTES.md` §3.2, rationale D-051; regression `tests/test_df_import.py`; DB + exports are gitignored runtime artifacts — `dfworlds/` stays outside the repo) | landed iter-6; +iter-8e/8f/8g; +bg-1 |
| `tests/` | test suite | smoke + architecture fitness, T0, minimal T1/T2, core units, loop e2e (iter-1); intent units, the 12 actions e2e, scheduler DAG, transition units, INV-3 stoplist (iter-2; +`brief/` iter-10a); T3 blind-NPC + knowledge/transfer/expectation suites, crime/rotation/OCC suites, iter-3 pack lint (iter-3); director + urgencies + states suites, arrest resolution updated, iter-4 pack lint (iter-4); tracery engine + chronicle + CLI suites incl. T1-chronicle and the directors A/B wiring (iter-5); the metrics module + the iter-6 T1 fixture-regeneration guard + the T8 single-factor A/B suite (iter-6); the brief assembler suite — byte-identity, beat-window mirror, budgets, eviction, brief pack lint (iter-8); the validator suite — the closed-document boundary, the verdict truth table, stale/rebased/first-break OCC, intent grammar gate, the regen protocol, the committed golden set (iter-9); the bg-1 parsing-core regression suite — sanitize/recover/census/audit + the UNDOCUMENTED marker + the measured in-flight-record truncation behavior on a tiny synthetic DF-like XML (iter-8g, extended bg-1); the bg-1 SQLite-sink suite — typed cores/EAV, participant index, membership/parent links, generic records, truncation policy (both modes), content determinism, the bg-3 consumer query (bg-1); the scene-ledger suite — scene derivation/presence, the gateway refusal vocabulary, the one-way lifecycle, contradiction retirement, withdrawals, the golden delta fixture replay (iter-10); the texture-promotion door suite — shape gates/`requires_for`/the texture noun tests, e2e canon birth / failed attempt / soft rejection, texture-path OCC attribution, the texture-block pack lint (iter-11; hardening iter-11a); the mediator suite — the call/response document contracts, the refusal→regen→dry ladder, promotion e2e + withdrawal, call-bytes determinism, the CLI `narrate` wiring (iter-12); the phase-1 regression set — the narrator-beat corpus replay + the accepted-beat `BEAT` summary lines (iter-13; corpus grown to 25 cases incl. the full refusal vocabulary, exhaustion→dry, door-rejected intents, rebased intents — iter-14) |
| `tests/fixtures/` | committed golden fixtures for T0/T1 (log lines, byte-identical run outputs) | `plumbing_smoke_seed42.jsonl` (iter-1; env-pinned — header records the Python version; iter-4/5/6 kept byte-identical — the 58-tick scenario crosses no beat) · `validation_golden.json` (iter-9 — the validator's golden set over the smoke fixture log: pinned verdict cases for all three verdicts; `docs/VALIDATION_SPEC.md` §9) · `texture_deltas.json` (iter-10, grown iter-11/11a — the scene-ledger golden delta fixture: 17 narrator-delta cases replayed through the gateway against smoke-log prefixes, pinning all 8 refusal reasons (laundering/unique_slot incl. the post-promotion one-object claim) + no-ops + the full ledger state; `docs/BRIEF_SPEC.md` §3.3) · `narrator_beats.json` (iter-13, grown iter-14 — the phase-1 regression set: 25 full narrator response documents distilled from two live agent-in-the-loop sessions (seeds 125 + 8), replayed through the REAL mediator cycle (every validator refusal reason incl. `event_type_mismatch`/`cannot_know`, the gateway's `stale_ref` at the beat level, the regen-exhaustion ladder → dry, and both door outcomes — committed success + committed `intent_rejected`; the `'anchor'` placeholder resolves to the advertised anchor); `docs/VALIDATION_SPEC.md` §7.1) |
| `tests/playscripts/` | seed + ordered-intent fixtures | `plumbing_smoke.json` (iter-1), `day1_theft_and_arson.json` (iter-2 — the §3 walkthrough: steal, take, drop-lamp, fire chain), `day1_full.json` (iter-6 — the T8 gate playscript, seed 125: take → 2 steals → wait 720 → move backyard → drop_break → wait 720) |
| `docs/DIRECTOR_SPEC.md` | director runtime contract (the trigger fired at iter-4 start — `SPECS_BACKLOG.md` sketch row) | written iter-4 |
| `docs/BRIEF_SPEC.md` | brief assembly contract (the trigger fired at phase-1 start — `SPECS_BACKLOG.md` sketch row): the seven-block pipeline, budgets, eviction, render format §7 + the narrator call document §7.1, just-in-time deferrals | written iter-8; §9 flip iter-10; +§7.1 iter-12 |
| `docs/VALIDATION_SPEC.md` | validation contract (the trigger fired at phase-1 start — `SPECS_BACKLOG.md` sketch row): the closed proposal document, verdict semantics, ExpectedVersion OCC, the fact transaction, the regen protocol, scene-ledger protocol clauses, golden-set plumbing, the response document + session wiring §7.1 | written iter-9; §8/§10 scene-ledger clauses added iter-10; +§7.1 iter-12 |

## 2. Reading gradient (what to read before working)

| Scale | Read, in order |
|---|---|
| Trivial — typo, single value, doc fix | `STATUS.md` |
| Normal — bugfix, small feature, one system | this file → `STATUS.md` → `worklog.md` |
| Deep — new system, schema/queue/director touch | + `AGENTS.md` → `docs/BLUEPRINT.md` (ledger rows for the component) → `docs/MVP_SCOPE.md` → `docs/EVENT_SCHEMA.md` |
| Huge — phase gate, architecture, restructure | + `docs/ROADMAP.md` → `docs/VISION.md` → `docs/DECISIONS.md` |
| LLM-track (`bg-*`) | + `docs/TECH_NOTES.md` → `docs/SPECS_BACKLOG.md` |

## 3. Information ownership (anti-drift map)

| Information | Single owner | Everyone else |
|---|---|---|
| Phase-0 scope: entities, systems, actions, tests | `docs/MVP_SCOPE.md` | link, never restate |
| Event fields, enums, header, versioning | `docs/EVENT_SCHEMA.md` ↔ `schemas/event.schema.json` (2-place sync, test-enforced) | examples must match |
| Phases, gates, kill-criteria, donors | `docs/ROADMAP.md` | link |
| What to do next | `docs/TASKS.md` | link |
| Why it all exists, honest limits | `docs/VISION.md` | link |
| Stable design decisions | `docs/DECISIONS.md` | link |
| Core-design research & depth proposals | `docs/CORE_DESIGN_RESEARCH.md` | link; accepted items move to DECISIONS/TASKS, line flips to `absorbed` |
| Cross-reference resolutions & donor combinations per build component | `docs/BLUEPRINT.md` (ledger + laws) + `docs/blueprint/<part>.md` (application) | link, never restate; cite ledger row IDs (e.g. "per RNG-1") |
| Volatile: model stack, hardware, DF pitfalls | `docs/TECH_NOTES.md` | link, expect rot |
| External sources: donors, licenses, phase gating | `docs/REFERENCES.md` (catalog) ↔ `docs/ROADMAP.md` §4 (active shortlist) ↔ per-source deep dives (`docs/REFERENCES_DEEP.md` index + `docs/ref/<source>.md` files, D-026) | link, never restate |
| Agent law, invariants | `AGENTS.md` | `STATUS.md` carries one-liners only |
| Where things are | this file | `README.md` carries a short map |

Duplication rule: if a fact must appear twice (e.g. invariant one-liners), the
copy is a link or a one-line summary — never a second full statement.

## 4. Update rules

- Structure changed (new dir/file/schema) → update §1 in the same iteration.
- New pitfall discovered → `STATUS.md` FAQ (≤20) if durable, `docs/TECH_NOTES.md`
  if it rots.
- This file never carries history or narrative.

## 5. Log hygiene

Runtime logs live in `logs/` (gitignored). Never open one whole: use
`tail -n 50`, `wc -l`, or a `python -c` filter by type/actor/tick. Rendered
chronicles go to `output/` (gitignored).
