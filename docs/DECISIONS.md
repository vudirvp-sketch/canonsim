# DECISIONS.md — ADR-lite

> Append-only **within a phase**; each entry: decision → why → consequence.
> Supersede with a new entry, never delete mid-phase. Cap: 30 entries,
> enforced by collapsing at phase gates (D-034): family merges write compound
> IDs (`D-018/D-022/D-029`) so external citations still resolve; compressed rows
> keep the decision and link the single owner of the detail (D-024 anti-drift
> — spec-restatement inside a decision row is duplication, not substance).
> Pre-collapse history lives in git. Collapsed 46→30 at the phase-0→1 gate
> (iter-7 intake); 41→30 at the phase-1 gate (iter-26 — the collapse ran
> inside the gate-review iteration per the owner's quality-first directive);
> 35→30 at the phase-2 gate (iter-35, the same precedent); 48→30 at the
> phase-3→4 gate (iter-54 — the phase-3 landing rows folded into the
> pacing/grammar/social/arc/content families, the verdicts into the
> gate-verdict family; the landing detail lives in TASKS + TEST_PLAN §6 +
> git, the single owners).

| ID | Date | Decision | Why | Consequence |
|---|---|---|---|---|

| D-001/D-009 | 2026-08-25 | Repo language law: docs, code, identifiers, commits in English; owner chat in Russian; prototype pack display names in English | token economy, owner request; consistency with logs/tests | all agents write English; final-product localization is pack data, zero code |
| D-002/D-010/D-032 | 2026-08-25 | Data contracts: JSON / JSONL + JSON Schema, no XML tooling (XML only as DF Legends *input*); EVENT_SCHEMA.md ↔ `schemas/event.schema.json` 2-place sync, test-enforced (the doc example is a test fixture); schema validation = the stdlib mini-validator `core/schema.py` for the used subset (`type`/`properties`/`required`/`enum`/`pattern`/`additionalProperties`/`items`/`minimum`/`$defs`/`$ref`) — any other keyword fails loudly | Python-stdlib native — the log is JSONL; `jsonschema` is a runtime dependency (D-012 forbids); hand-rolled structural checks duplicate the contract (the exact drift D-010 prevents) | `schemas/event.schema.json` is the machine contract; the log writer is the enforcement point — no invalid line can enter a log; T0 re-validates the doc examples; pack-file JSON-Schemas stay a phase-6 rung |
| D-003/D-023 | 2026-08-25 | Event-sourcing shape: JSONL log = truth, SQLite = derived index (INV-1); runtime state = the incremental projection `state_new = state_old.apply(event)`; `fold(log)` is for T2 replay and rebuild only — never the startup hot path | the ambiguity risks O(N²) at startup if fold is misused as the runtime state path (KI#5) | replay = fold(log); the DB can be dropped and rebuilt at any time; fold stays the truth-test (T2) and the rebuild path |
| D-004/D-028 | 2026-08-25 | Determinism law: no wall-clock anywhere, including the log header (header carries seed, python, schema_version, commit, pack — no timestamps); INV-2 = single point of randomness control — one master seed, named streams deterministically derived via the RngBank authority (`stable_hash(s) = int.from_bytes(hashlib.sha256(s.encode()).digest()[:8], "big")`, environment-independent); cosmetic draws can never desync canon replay | byte-identical determinism; the old "one Random instance" wording contradicted RNG-1 in three load-bearing places and was stricter than its own multi-stream donors; a built-in `hash()` derivation would silently depend on `PYTHONHASHSEED` | AGENTS.md §4 is the single reading owner; the iter-0v audit sub-clauses live as blueprint/spec sub-clauses (list in git worklog iter-0v) |
| D-005/D-038/D-039/D-040/D-041 | 2026-08-25 | Director & autonomous mechanics (iter-4/4a): the director is a consequence planner, never an improviser — complications are seeded at event time in a per-run buffer, never invented later; narrative entropy = observable state only (hook weights + global suspicion + visible threats); explicit triggers (time/place/threshold) fire causally; releases ride the intent door as `director_<N>` (the director never writes canon); director-off keeps seeding, suppresses releases (T8 baseline); crossings fire in TICK order. Autonomous: P2b goal ticker — per-NPC urgency specs roll d100 on the substantive stream; a precondition failure stays SILENT (the world's noise floor absorbs autonomous attempts); arrest resolution cause-chained to the attempt, `caught` irreversible (T4). Audit laws (iter-4a): the playscript feed is player-driven only (an autonomous ending NEVER advances the script); the crime-status flip guards the pack's ordered `status_values` progression; `reset_on_rotation` resets BOTH participants; dead pack keys deleted | causality by construction (RimWorld storyteller = the named anti-pattern); D-021 — the world must act without the PC; small-formula dynamics, not a planner; capture/escape is a binary outcome with no chase sub-system (phase-3+); KI#17–20 found by the owner-requested audit (124 probe runs clean) | director-off A/B mandatory (T8); beat order decay → urgencies → director; autonomous intents enqueue at `entry.tick`, never `beat_tick`, decay commits at `beat_tick`; full detail: `docs/DIRECTOR_SPEC.md` (the runtime contract owner) + `docs/INTENT_SCHEMA.md` + git worklog iter-4/4a |
| D-006/D-007/D-008 | 2026-08-25 | v0 epistemics shape: no group reputation (guard-to-guard transfer models it); rumor = knowledge transfer with fidelity decay; lies = crafted knowledge records | "reputation with the watch" = transfer events at watch change; distortion comes from source incompleteness — no extra system; foundation for phase-4 `believes/lies` | one mechanic covers gossip and hearsay distortion; no truth field on records in v0 |
| D-011/D-012/D-013/D-014/D-046 | 2026-08-28 | **Repo meta & the executable surface**: no CHANGELOG.md (git + STATUS own history); the repo name a placeholder; the concept docs live outside the repo; stdlib-only runtime (D-012, AGENTS §5/§8 — any dependency addition stops for owner confirmation); D-046 `scripts/` is operator tooling INSIDE the executable invariants (PACKAGE_DIRS += scripts + the coverage-closure test — a new top-level code dir joins in the same iteration it lands; the print exemption is CLI-class tools only; the INV-4 import check runs against every package dir since iter-6a) | a top-level code dir outside `PACKAGE_DIRS` silently escapes INV-4's check; dev tooling stays capped at pytest + ruff | the stoplist scope (INV-3) stays engine-side (core/ + sim/ + brief/) with the periphery interpretation test-owned |
| D-015 | 2026-08-25 | License filter lifted; usefulness & phase is the filter | no monetization planned; open bases adapted freely incl. functional re-invention | donors gated by phase (ROADMAP §4); phase 0 uses zero external code |
| D-016/D-017/D-024/D-026/D-027 | 2026-08-25 | Reference architecture (four places, four jobs): `docs/REFERENCES.md` = the catalog (owner survey rev v2 merged; licenses verified + tagged with check date, unverified quarantined in §11, quarterly review); `docs/ref/<source>.md` = one deep-dive file per source, `docs/REFERENCES_DEEP.md` = index + format template (single-file dives projected 2500–3500 lines, 4–6× the cap); `docs/BLUEPRINT.md` + `docs/blueprint/` = the distillation — the 12-row resolution ledger + cross-cutting laws + build index; the blueprint owns cross-reference resolutions and combinations (mechanics stay in refs, contracts in the specs; single-owner rule preserved — link, never restate, the D-024 anti-drift discipline) | rich but unverified survey (incl. unverifiable, misattributed repos); after 19 reference iterations nothing combined the verdicts — cross-ref tensions (MTTH vs determinism, mutable ECS vs INV-1, GA retrieval vs known_by) each needed one resolved mechanism, recorded once | admission rule: verify via GitHub exact-name search (web SEO misses small repos); donors are inspiration/patterns, never 1:1 copies (`REFERENCES.md` §0.7); a deep dive updates only its ledger rows; a resolution-changing ref needs a worklog note + a superseding D-entry; each per-ref file naturally ≤300 lines (the split-when-bloated law) |
| D-018/D-022/D-029 | 2026-08-27 | Concept absorption & digestion complete: manifestos and owner texts land **surgically into existing docs** (the D-018 pattern — never a new canonical file); one research pass granted then closed (Q4 = no); digestion declared complete at iter-0w — skeleton (phases 0–6, three layers, INV-1..5) confirmed, blueprint = the single build guide | a fifth canonical layer would rot, duplicate owners, and violate the caps; foundation hardening before code; 33 references replaced vague intentions with named donor-backed mechanisms without moving a phase gate | BRIEF/VALIDATION sketches gained sensory-emitter + beat-boundary delta + structural injection-neutralization clauses; P3e `psychological_echo`; sequencing is iteration-counted (calendar dropped); the doc-loop exception law: no doc pass without a fresh owner request |
| D-019/D-020/D-021/D-033 | 2026-08-28 | CORE_DESIGN_RESEARCH proposals accepted and built: M3 (causal chain length) / M4 (novelty) / M5 (non-PC share) into the metric baseline (P1b–P1d); sparse NPC↔NPC pair relations (P2a); minimal goal ticker (P2b); salience-based talk topics (P2c — knowledge *used*, not just stored) | depth-equation factors must be measurable, not asserted; teller↔listener trust needed a data home; the world must act without the PC for T8 to mean anything; the last owner-pending proposal flipped to accepted per its lifecycle rule | no event-schema break (relations are state); full LLM planning — never (Generative Agents anti-precedent); M5 non-trivially non-zero by construction; metric thresholds set from the measured baseline, same protocol as M1/M2 |
| D-025/D-034 | 2026-08-26 | Cap laws: `docs/*.md` ≤600 lines with the substance-over-cruft filter (AGENTS.md §6.1) replacing the rigid 400-line wall; DECISIONS.md ≤30 entries, enforced by collapsing superseded/absorbed entries into consolidated compound-ID rows at phase gates (`ROADMAP.md` §5) — never by mid-iteration cuts | iter-0i trimmed substance (enum lists, field names, pseudo-code) to fit a hard cap — the owner flagged: "hard cap = crutches, not quality"; the gate-collapse keeps the cap's pressure and the append-only invariant intact with a natural archiving rhythm | filler/restatements/linker-chains = cut always; named systems, field lists, enum values, per-source verdicts = never cut to fit; a cap breach triggers a cruft pass first, over-cap substance stays with a worklog rationale; the file may sit over cap between gates; within a phase: supersede, never delete |
| D-030/D-031 | 2026-08-27 | **The owner-directed principle rounds absorbed surgically** (the D-018 pattern — never a new canonical file): D-030 darkness is architecture — tone = data asymmetry inside the existing 8 systems (asymmetric deltas, an observable price marker on every socially meaningful behavior, irreversibility, rumor lethality per D-006); the core stays axis-blind (a future pack adds axes with zero code change); the grim line admissible only as the post-gate `pack-1` candidate under the PACK_SPEC trigger; D-031 the quality round — L13 the abstraction cost gate (Rule-of-Three, the 4-branch registry threshold) + L14 the elegance checklist (BLUEPRINT §2), type discipline + fail-fast + the registry + negative tests (phase0 §1/§2/§6), the architecture fitness test, the stack frozen stdlib-only through phase 2, mypy parked as `qa-1` | the owner fears rot into piled-up complexity; content-first proposals exposed the scope-creep failure mode (a spec before its trigger, ad-hoc mechanics) | AGENTS §4 canon-write-privilege line + §9 the quality DoD bullet; dev tooling capped at pytest + ruff |
| D-035/D-036/D-050 | 2026-08-29 | **The write-path discipline** (engine internals): D-035/D-036 the commit gate + the spread-pass discipline — every event enters through `Simulator._commit` (state deltas validated against the projection BEFORE the write, INV-1's privilege line D-031), the per-layer spread singleton (one live pass per layer, shared causes, KI#16); D-050 the derived indexes live beside their single mutation funnel (iter-8h): `KnowledgeView`'s `who → token → source-ids` (add is the only writer; `before_source` semantics preserved) and the Simulator's `(entity, prop) → latest-committer-tick` (`_commit` the only writer; the decay baseline reads the index — KI#19); plus the same-funnel scan eliminations (the beat-window break, the top-1 salience, the forward-once OCC fold, the once-per-call entropy) | each index has exactly one writer — the invariant is checkable by reading one function; the golden fixtures byte-identical through the pass | the perf numbers: profiled at iter-30 (TECH_NOTES §8 — event-linear, schema validation the hot spot); tests: the fold/replay suites |
| D-037 | 2026-08-28 | iter-3 epistemics architecture: (1) knowledge state = a derived per-knower index (`core/knowledge.py` `KnowledgeView`) rebuilt by folding `knowledge` records — never projection props; (2) system reactions are event-driven, dispatched from the commit door (`_commit` → `_react`: crime first, telling second) — no call site can forget them, the cascade terminates; (3) watch rotations fire on clock crossing, never pre-seeded — a run still ends when its script's queue drains; (4) the systems live in `core/`, **not `sim/systems/`** — anything the canon door must dispatch is kernel mechanics (the import-boundary law) | (1) keeps INV-1 pure (memory = fold of the log, T2 applies); (2) removes the "forgot to react" bug class at zero scheduling cost; (3) pre-seeding would extend every run to tick 1080; (4) core never imports sim — law wins over the stale plan note | suspicion reacts to **novel tokens only**; the arrest threshold has a single owner (`crime_watch.arrest.requires_suspicion`); expectation violations chain to the axis-specific mover; `crime_status` lives on the player entity (namespace-collision avoidance); the telling shares the teller's most salient **novel** fact with one-step fidelity decay + an acceptance roll; official briefings transfer unrolled |
| D-042/D-043/D-044/D-047 | 2026-08-28 | The read side is pure functions of the log — render AND brief: every render entry point builds its `Engine` on a fresh `RngBank` from the log HEADER seed (same log → same bytes in any process/`PYTHONHASHSEED`; a growing log keeps its rendered prefix; tracery ~350 stdlib lines, cosmetic stream only, ShufflePool no-immediate-repeat; the importance gate is pack data `tale_gate.min_importance`); M1–M5 + the emergent-chain count are pure functions of `(events, projection)` in `core/metrics.py` (Mesa `DataCollector` inverted — the simulator never knows a metric exists); the assembler is **zero RNG** (dry structured tokens, L2 — `docs/BRIEF_SPEC.md` owns the skeleton: two-level budgets, whole-block eviction in the fixed order, directives never dropped, `[truncated:N]` markers, ranking caps ≠ budget drops, all budgets at `rules.json::brief`); `scripts/balance_harness.py` runs the 1000-sim distribution (contract `docs/TEST_PLAN.md` §6) | render determinism must be env-independent and prefix-stable; a runtime-coupled metric creates feedback risk + hot-path cost; the same purity law extended to the phase-1 mediator input (D-047's clause: deterministic token proxy keeps byte-identity stdlib-only); separating ranking caps from budget drops keeps the anti-silent-drop law about assembly budgets, not retrieval windows | the 1000-seed baseline is the M-threshold reference (M5 p50=0.77, chains p50=20, M3_mean p50=13.81, M1 p50=0.24, M4 p50=0.18; full table reproducible, output gitignored); T1 covers the chronicle via the log fixture; the session = one opened Simulator, byte-identical to the batch run; brief bytes pin-tested (golden fixture); the real tokenizer must not change committed formats without a spec bump (BRIEF_SPEC §8) |
| D-045/D-058/D-064/D-083 | 2026-09-05 | **The phase-0/1/2/3 gate verdicts — PASS** (iter-6/26/35/54, the evidence checkpoints per ROADMAP §5; the phase table owns closed/open — these rows are the verdict records): D-045 phase 0 — all six MVP_SCOPE §16 exit criteria met, no kill-criteria hit, T6/T1/T8 (26 chains OFF)/T7/T3/T5 green, the ontology proved combinatorial depth, day1_full/seed 125 became the canonical fixture, phase-1+ metrics compare against the recorded baseline; D-058 phase 1 — 0 canon violations over 109 live beats (the exit criterion), the corpus at 105 distilled cases replayed through the REAL mediator cycle, the full §5 re-run (M1=0.417/M2=0.500 ON seed 125, T8 OFF 26 chains, T7 reads as a story), DECISIONS collapsed 41→30; D-064 phase 2 — 35/35 boundary validity over 51 combined utterances (0 honest misfires, 10/10 off-grammar probes caught), the §5 re-run IDENTICAL to iter-26's numbers (the parser never touched a canon path), the corpus 105 narrator + 10 parse cases green, DECISIONS collapsed 35→30 | the gate is the evidence checkpoint, not a feature deadline; each verdict's evidence detail: TEST_PLAN §4/§6 + the worklog gate entries | phase 4 opened at iter-55 on the owner's call (the iter-36 precedent); each verdict row stands for its phase, folding into this family at the next gate — D-083 executed the fold it predicted |
| D-048/D-049/D-053/D-056/D-057 | 2026-08-31 | **The scene/brief read-side stack** (the phase-1 structural landings, owner-directed): D-048/D-049/D-053 the scene ledger — canon vs texture (the design iter-8a, hardening iter-8b, the LLM-free half iter-10: the scene lifecycle, the pin/retire vocabulary, the laundering refusals, the D-049 determinism quarantine — the ledger is session render state, never replayable, T1/T2 never touch it); D-056 st-1 presence & entity cards (iter-15: the arrival snapshot, the present set, the card surface — BRIEF_SPEC §3.4); D-057 the pack-declared `layout` (iter-20: canon-from-birth on the scene line, no initial_projection seeding — KI#48's correction: canon_slot reads BOTH prop sources) | the read side is pure functions of (log, ledger) with zero assembler RNG (L2); the projections are folds, the cards are the narrator's surface | contracts: BRIEF_SPEC §3.3/§3.4 + blueprint §1; tests: the ledger/presence/brief suites (iter-10/15/20) |
| D-051/D-063 | 2026-09-01 | **The track-B DF pipeline closed** (bg-1 + bg-2, the owner-supplied world exports): D-051 the SQLite sink — HANDLED records load as typed cores + EAV + `event_participant` + generic JSON, truncation flagged PARTIAL by default (`--strict` aborts), the DB a rebuildable content-deterministic index of the export bytes (INV-1's shape on foreign canon; no wall-clock in `meta`); D-063 the taxonomy + the sink v2 plus pass — `docs/TAXONOMY.md` (120 entries over the 16 target types, measured findings incl. the hfid1/hfid2 participant-index blind spot), the companion's `historical_events` in `event_plus_fields` (same ids, fields only) | the epistemic closure IS the participant index (blind spot inherited — DF history is canon-dense, epistemology-empty: the legends structure donates, knowledge propagation stays ours to design, phases.md §4) | recipes + numbers: TECH_NOTES §3.1-§3.3; tools: scripts/df_{survey,import,taxonomy,briefer}.py; tests: the bg suites (bg-1/bg-2) |
| D-052 | 2026-08-29 | **The validator contract** (iter-9, `docs/VALIDATION_SPEC.md` — the trigger-fired spec owns the field-level detail; `brief/validator.py` is the LLM-free half): the mediator's gate is a pure function of (proposal, log, pack) — one CLOSED structured proposal document (claims + intents + `expected_event_seq`; no prose field exists, so injection is neutralized structurally, D-018 — loud `ProposalError` on any shape drift, emitter bugs crash); honest verdicts `supported/contradicted/insufficient_data` evaluated against CURRENT canon under closed-world semantics (invented entity/event = contradicted; unmodeled prop = insufficient, never fabricated — UAP); ExpectedVersion OCC = the intent-door semantics reused (INTENT_SCHEMA §4: stale+broken → refused with first-break attribution; stale+intact → rebased; intents pass through with anchors untouched — the door stays the single commit path, D-037); the ≤2-regens protocol owns the call-budget reconciliation (2-call steady state = narrator + extraction OR regen; 3-call worst case = narrator + 2 regens — bounded, counted, exhaustion → dry mode L12) | every law reuses existing machinery (intent OCC, validate_shape, KnowledgeView, fold — D-018/D-035/D-037 discipline); the C-parser (phase 2) and the narrator boundary wire the same shapes; the scene-ledger gateway (iter-10) reuses the refusal shape + regen budget | the golden set (`tests/fixtures/validation_golden.json`) pins verdict semantics as committed data — computed, never LLM-judged; `invented`/`unverifiable`/`regen_count` are the bg-3 harness metrics; DECISIONS transiently 37/30 (collapse at the phase-1→2 gate, D-034) |
| D-054/D-055 | 2026-08-29 | **The narrator boundary — both halves** (iter-11/12): (a) the texture promotion door, the LLM-free half (`core/intent.py` + `resolvers.py::_pickup` texture branch; contract `docs/INTENT_SCHEMA.md` §2/§3/§7; mechanism blueprint §1): a texture-capable action's pack `texture` block (`requires` + `knowledge`, lint-checked, `{texture_slot}` not `{target}`) REPLACES the canon preconditions for intents carrying the mediator-resolved reference `{entry, scope, slot, value}` (`requires_for` — one split at proposal, completion, and OCC attribution; the reference is DATA on the Intent, shape-gated loud; core NEVER sees the ledger, INV-3/D-031); a take-success on the reference IS the promotion — one `StateChange(scope_target, slot, None → value)`, the object's canon birth (the gateway's canon-slot check outranks any texture on the slot from that moment); a failure promotes nothing (live+pinned); ghost interactivity structurally impossible; `unique_slots: ["hearth"]` (cross-scope re-establishment = the laundering-family refusal). (b) agent-in-the-loop, external at dev-time (owner verdict in chat): the call is a FILE contract — the mediator emits `output/mediator/call_<N>.md` (the brief + `narrator_protocol`, BRIEF_SPEC §7.1) and the operator writes `reply_<N>.json`, ONE closed `{prose, texture_delta?, proposal?}` (VALIDATION_SPEC §7.1) with deep shape gates AT the boundary; a refused document never feeds intents — the beat regens whole; the mediator's noun resolution (`feedable_intents`) drops non-player actors / non-live entries / per-document duplicates as WITHDRAWN notes, never events; beat order D-049's; L12 from day one: narrator → template → dry | promotion is the ONLY texture→canon path (zombie texture cannot arise legally; inter-scope movement = take = committed event), reusing the door's whole spine (shape gate, preconditions, checks, OCC, knowledge) instead of a parallel write path (D-037); zero LLM/network/runtime-dependency code — INV-4 unchanged as written, nothing entered the codebase (no §8 stop needed); integration (llama.cpp inside SoW, the dumb-terminal wiring) stayed behind the phase-1 gate exactly as ROADMAP §6 demands; the SoW audit parked as `bg-6` | `brief/mediator.py` (pure document layer) + `cli/mediator.py` (session orchestration, periphery D-046) + the `SceneLedger` wiring; the phase-1 corpus (105 cases) replays the refusal vocabulary, both door outcomes, and the rebased/stale anchor paths through the real cycle; local inference (TECH_NOTES §1) remains the owner-gated runtime-engine candidate (`engine-1`) |
| D-059/D-060/D-061 | 2026-08-31 | **The phase-1 polish menu — three landings on the owner's finish-phase-1 directive**: (a) tune-1 (iter-27): the story-critical importance hook — pack-listed event types (`rules.json::importance.story_critical_events`, lint-closed) score +2 `story_critical_event` (`pack_importance` takes the event type, all emit sites); the tale gate follows low→medium — the RULE owns the signal/noise split per D-045(b), the gate is not the primary knob; + the `recuperate` resolver + `status_effects` pack block (`rest`, the 13th action, 60 ticks, fatigue −30 — the KI#4 counter-play, pack data end to end); day1_full OFF renders 47 events → 14 tale lines, zero decay/wariness noise (pinned); the 1000-sim baseline re-run IDENTICAL. (b) tune-2 (iter-28): `brief.present_entities.card_markers` — the prop-path marker table (threshold rows + value rows, closed marker surface `status.<axis>`/`relations.<axis>`/`crime_status`, lint-checked; the segment is `markers=`); the pack ships `wary` at `relations.suspicion` ≥ 25 (aligned with the `status_suspect_at` flip) + `suspect`/`caught` on the player's card — the narrator's read surface for standing state (the scene_delta half is lawful blindness, blind-NPC D-037: the CARD is the narrator's surface, the delta window the player's). (c) pack-2 (iter-29): the `spot_available` door check — the closed precondition set's 15th test (the target location holds an unburning spot of the declared layer, the exact condition the ignite resolver keys on; the `layer` param lint-checked) — arson on a destroyed or fully-burning location is an `intent_rejected` no-op with `failed_test target.spot_available`, the door-outcome vocabulary's fourth axis | the T7 phase-1 gate re-read flagged the noise floor (27 of 47 day1_full events were decay/wariness repetition — a bare gate raise would have cut story events the raw rule scores low); iter-17's live finding: the suspicion axis and the `crime_status` flip were claimable-but-invisible through the brief; the iter-2a audit note + iter-24's live probe: arson on ashes logged a no-ignition success that pretended the world changed | T1 fixture untouched by (b)/(c); tests 610→629 across the three; the seed-41 corpus probe flipped with (c) (renamed, the old pin lives in git); field-level detail: INTENT_SCHEMA §3 (the requires row), BRIEF_SPEC §3.4/§6 (the marker table law) |
| D-062 | 2026-09-01 | **The phase-2 parser boundary — mode C is EXTERNAL at dev-time over a file contract, mirroring D-055 on the player's free text** (iter-31, the owner's start-phase-2 call — phase 1 closed at D-058, the polish menu done, the remainder owner-gated with no declared candidate): the repo-side half is `brief/parser.py` (pure — the grammar snapshot: the pack's verbs with pack-DERIVED field constraints (ticks positive-int when drawn-`N`, method = `rules.checks.methods` keys, near = the action's ignition-layer spots on the actor's folded position, texture = a live-entry reference) ∪ the addressable nouns (canon entities + live ledger entries — ghost interactivity structurally impossible, blueprint §2); `parse_call` (utterance + grammar + protocol, byte-deterministic); `parse_reply_from_mapping` — the closed {intent \| question \| no_intent} document gated AT the boundary: off-grammar output is a loud ParseError, never a feed — the world never moves on a malformed parse) + `cli/parser.py` `ParserDoor` (the session door: emit (retire window + scene sync + document) → apply (gate against the CURRENT snapshot → pin → feed ONE step through the front door → promotions wired exactly like the narrator path); the gate does NOT duplicate door-owned checks (target-required, the one-path law, preconditions) — one owner per law) + the `say`/`say apply` session commands sharing the session's ONE ledger (D-049). **The pin law lands wired:** `SceneLedger.pin` — blueprint §1(a)'s first consumer (an Intent referencing a texture entry pins it; the reference IS the pin — a door-rejected attempt still pins, a failed attempt keeps it live+pinned, un-pinning does not exist). The trigger-fired `docs/PARSER_SPEC.md` owns the contracts + the ≥90% measurement (parse validity ≠ world legality — attempts are facts; door outcomes never count as parse failures) | the D-055 precedent (zero LLM/network/runtime-dependency code — INV-4 unchanged as written; the runtime engine (llama.cpp + GBNF) stays the owner-gated `engine-1` decision, and grammar-constrained decoding will make off-grammar output structurally impossible at the source — the dev-time gate is its stand-in); field constraints derive from pack data only (INV-3: a second pack requires zero engine changes, the snapshot derivation included); disambiguation is surfaced as questions (uncertainty is asked, never guessed — the exit criterion counts boundary-accepted intents per utterance) | `say <text>` / `say apply <reply>` join the session (MVP_SCOPE §12's phase-0 list untouched — phase-2 commands are spec-owned, the narrate precedent); output/parser/* + output/mediator/* are gitignored runtime artifacts; off-grammar replies leave the cycle open (fix the file, re-apply — dev-time semantics; the runtime re-ask ladder is PARSER_SPEC §7's deferral); tests 629→654 (+25: snapshot derivation incl. the position-tracked near enum, call-document bytes, the gate's refusal vocabulary, pin's law, door e2e — feed/question/no_intent/attempt-is-a-fact/pin-and-promote (seed-probed both ways)/failed-attempt-keeps-pin/the shared-ledger scene-close retirement — + the CLI wiring); DECISIONS transiently 34/30 (collapse due at the phase-2→3 gate per D-034) |
| D-065/D-066/D-067/D-068 | 2026-09-01 | **The director's pacing stack landed (DIR-1..DIR-4, iter-36..39, the owner's "continue per the plan" calls; phase 3 opened at iter-36 — phases open on the owner's call, the iter-31 precedent)**: D-065 the pacing clock — the per-run deterministic `PacingClock` RAMP/PEAK/REST/STAGNATION over narrative entropy, advanced once per beat, `director.pacing` pack data + lint, PEAK/REST suppress the quiet path, explicit triggers ungated (D-005: causality is not pacing), a pack without the block byte-identical (INV-3); D-066 the eventless-stretch instrument — `core.metrics.eventless_beat_stretches` (the phase-3 exit criterion's measurement: the maximal runs of beat windows with no scene event, the tale gate the same reader the chronicle uses) + the harness `--pacing on|off` A/B; measured 1000 seeds: max stretch 1, every day1_full run ends in PEAK (the all-PEAK law — a measurable quiet delta needs a quiet-walk script, answered at the gate by D-082's stage); D-067 the climax layer — the third entropy layer `climax_floor` (75 = 3× the peak floor, the L4D2 three-intensity rule), the climax release path (boss beats END peaks, the one-beat PEAK_CLIMAX state, REST after unconditionally, a closed boss never burns — the option gate is consulted), climax hooks never ride the stagnation path; D-068 the multi-channel quiet split — `director.channels` (threat/social/ambient, the L4D three-director family), per-hook channel opt-in, the channel's own floor against its own entropy (a quiet social channel can inject while threat burns), clock/budget/climax/explicit-triggers stay global, channelless hooks keep the v0.1 global floor | each layer byte-identical on the committed runs at its landing (the all-PEAK law — the unit suites pin the machinery directly); the whole stack is dormant vocabulary until a live consumer declares it, the content rows own that | contracts: DIRECTOR_SPEC §5 the runtime owner, TEST_PLAN §6 the measured records, TASKS dir-1..4 the landing rows; tests: the pacing/climax/channel suites (iter-36/38/39, tests/test_director.py) |
| D-069/D-070/D-071 | 2026-09-01 | **The Paradox event grammar landed (drama-1/2/3, iter-40..42 — the phase-3 backlog's grammar column, the owner's "continue work per the plan" calls)**: D-069 the predicate + weight layer — trigger specs as JSON predicates (`core/predicates.py`: the three v0.1 leaves + compound all/any/not + the prop leaf, loud backstop), the `weight_multiplier` object (the entropy sensor reads the effective weight per beat), `first_time_only` (the burn law), MTTH the named anti-pattern (TIME-1); D-070 the option layer — per-option availability gates + ai_chance-style weighting (a pure deterministic pick: heaviest wins, ties by declaration order, zero never picked, all-closed defers), payload overrides whole-key, the immediate/option/after lifecycle maps onto seed/choose/apply (no literal effect blocks — the door's commit + reaction dispatch is the after); D-071 the on_action dispatch — a pack table keyed by committed event type, appended AFTER the hardcoded reactions (the donor's append-not-overwrite), the witnesses scope + the per-entity quantified gate + the alarm-shaped state change, one event per entry, the one-hop lint terminates the cascade; the ctx `any`/`random` scope helpers recorded-not-built (the first-consumer law) | the grammar is data: a second pack exercises it with zero engine edits (INV-3's substance); the pack's live declarations were dormant until the content rows | contracts: DIRECTOR_SPEC §3/§3a-§3c, TASKS drama-1..3 the landing rows, TEST_PLAN §6 the measured records; tests: tests/test_predicates.py + the drama suites (iter-40/41/42) |

| D-072/D-075/D-077/D-080/D-082 | 2026-09-05 | **The content column landed — every layer gains a live consumer (content-1/2/5/3 + the echo engine, iter-43/46/48/51/53; the owner's content calls resolved per DIRECTOR_SPEC §11)**: D-072 the document_check action (the 14th, over the inspect resolver: the verdict token mapped to the standing arrest machinery, the climax flag live on the watcher pair — the boss path consults the option gate, a closed boss never burns); D-075 the psychological echo — `core/echo.py`, the residue as a pure read-side fold over the knowledge view (per-NPC valence, linear decay, fidelity-scaled, clamped; writes nothing, feeds no entropy — the L6 fence), consumed by the intent door's `echo_at_least` gate; D-077 the alarm panic echo (content-2: the through-the-walls law as one on_action entry — witnesses +10 fear, the contagion quarter of the direct spike, story-critical with its own chronicle line, zero engine edits); D-080 the echo driver (content-5: the guard's jittery-watcher beat — `echo_at_least dread >= 15`, p=100 the compulsion semantics; the wariness arm measured and refused — 34 cases + the never-fading rotation-renewed residue); D-082 the ambient driver (content-3: the drunkard's ramble, the L4D Music analog — weight 0, the ambient channel's own quiet gate its only road, seeded on the wait action's hooks, the resolver-sparse minting gap closed, one murmur per run) | the landing pattern is pack-data-over-standing-doors (zero engine edits except the hooks-minting fix); each footprint measured first (TEST_PLAN §6 owns every A/B); the corpus deliberate pins follow the iter-51 pattern | contracts: TASKS content-1/2/3/5 + social-2, TEST_PLAN §6, DIRECTOR_SPEC §5/§11; tests: test_doccheck.py, test_echo.py, test_panic.py, test_ambient.py |
| D-073/D-074/D-078 | 2026-09-03 | **The social stack landed — secrets, leverage, the spend, the driver (social-1/1b + content-4, iter-44/45/49, the P3a CK3 add_hook precedent: a hook IS an event — target, type, expiry, cause)**: D-073 the fact clusters — `core/leverage.py` + `rules.json::secrets`: a novel knower of a pack-declared secret token mints a `leverage_gained` fact event (no knowledge/hooks/state — the cascade terminates; expiry a read-side fold, `live_leverage` the read owner; told secrets confer leverage — the briefing mints the relief's cluster); D-074 the coerce door — the 15th action over the coerce resolver: `leverage_over` the door's first fold-reading precondition (the facts re-read at the caller's own tick, the unconditional tick-window OCC re-check), the spend a NEW event naming the cluster (never a mutation — the fold kills it; one secret buys one play), the balance as pack data (subject-directed pair-axis shifts); D-078 the driver — the drunkard's urgency entry re-armed by the REPLACEMENT law (the slot and weight stay, so the per-beat draw count holds and the corpus's designed ladders hold; an ADDED entry was measured to flip 3 ladders — the coupling resolved by D-079's stream split, after which the add-safety is 10/10) | the corpus re-distills: 9 cases (iter-44) → the fixed-point runner (iter-49: 2 id re-pins + the tail re-pin, zero ladder flips — the regen laws live in the STATUS FAQ) | contracts: MVP_SCOPE §5/§6, TASKS social-1/1b + content-4, TEST_PLAN §6; tests: tests/test_leverage.py + test_coerce.py (iter-44/45/49) |
| D-076/D-081 | 2026-09-03 | **The release-chain layer landed + its driver (arc-1 + content-6, iter-47/52 — the DF event_collections / Paradox event-chain precedent, the release-side twin of the grammar)**: D-076 the engine — `director.arcs` pack chains: the ORDER law (a member tag is a candidate only while it is its arc's CURRENT member, all release paths, explicit triggers included — pack-declared causality, not pacing), the GAP law (`min_gap_beats` spacing on the quiet/climax paths only, never the explicit one), the entropy mirror (passed members' instances stop counting — the burn law's twin), the one-sided membership lint; D-081 the driver — the aftermath chain `[possible_document_check_relief, barkeep_wary_sweep]` gap 2: the GAP law LOAD-BEARING (the unchained sweep would land its event BEFORE the check's own — the same-tick intent ordering, a causality lie; the march holds it to the day's last event), the successor trigger-less + climax-flagged + weight 0 (the entropy footprint exactly zero) + first_time_only, seeded on the steal failure appended last; measured: day1 9/10 byte-identical + seed 125's one appended event, the corpus 105/105 pin-green ZERO re-distill (the first content landing with none — the seeding event's hooks field is the birth record, the FAQ law 9); the NPC-move successor arm refused (the tune-3/st-6 owner-gated fork) | the D-065 nopacing record superseded in part at both landings (the closing beat rides the climax path, which the clock-off pack removes) | contracts: DIRECTOR_SPEC §3d + §11, TASKS arc-1/content-6, TEST_PLAN §6; tests: tests/test_director.py (iter-47) + test_arc_driver.py |
| D-079 | 2026-09-03 | **engine-2 landed — the urgency-roll stream split, per-entry streams** (iter-50-engine2-stream-split, the owner's "quality over speed" fork call resolving the engine-2 owner-gate: spend the iteration now, buy the permanent add-safety — the content-5 fork "replacement law vs engine-2 first" taken the quality way): (1) the LAW (INV-2, the add-safety clause): every pack urgency entry rolls d100 on its OWN content-addressed stream `urgency:<npc>:<kind>` (`core/rng.py::urgency_stream_name` — the single owner of the naming grammar; lazily registered on first use, so the closed-set tripwire survives verbatim for every non-family name; the (npc, kind) pair is pack-linted unique, making the name injective) — an added or removed entry shifts NEITHER a later check draw (the substantive stream is untouched) NOR another entry's rolls (each entry's stream is its own). The nesting law reworked: an urgency-family stream may shadow the assured `substantive` run scope (`assure` guard), the ONE legal nesting — cosmetic-inside-substantive stays the loud RNG-1 desync tripwire, and substantive-inside-urgency raises (a bug, not a feature); (2) the DESIGN MEASUREMENT (the record of the refused alternative): the engine-2 row recorded "a third named stream" (singular); the single shared `urgency` stream was landed as a candidate and REFUSED by its own A/B — add-safety 4/10 day1_full byte-identical (the iter-49 refused scenario: an added p=40 entry, silent by gate): the entries COUPLE by draw position (the added entry's per-beat draw shifts every later beat's roll — the maid's urgency stops firing), i.e. the single stream delivers check-add-safety but leaves the per-landing corpus-regen burden in place, which is exactly what the row exists to remove; the per-entry design re-measured 10/10 byte-identical. A firing added entry is NOT a violation: it lands new canon (its own events + legitimate downstream), the content-landing protocol's territory; (3) the one-time migration flip (the row's recorded price): the flip A/B 0/10 byte-identical — every seed diverges at the beat-720 region (the checks shift by the removed draws; fingerprints 15→6 / 17→10 / 20→14 / 38→11 / 35→13; seed 125's doccheck ladder flips), paid in-iteration through the fixed-point corpus runner (identity-proved first on the unshifted stream, 105/105 zero-change; the iter-50 FAQ laws 7-8: the write-back into the fixture, and the pristine-restart rule — a measured-and-refused candidate never lands in the fixtures); (4) the pins: 4 bank-family laws (nesting, the reverse-nesting refusal, lazy registration + per-seed derivation, the name grammar) + the e2e add-safety byte-identity (the steal's check AFTER the beats — six rolls precede its dice, so a surviving draw position proves the decoupling) + the duplicate-pair lint refusal; the corpus: 2 narrator cases re-distilled (the flee-pursuit check flipped; `caught_at_the_door_of_his_own_distraction` migrates to the refusal family — its caught-fleeing knowledge claims die with the vanished flee_caught event, the deliberate `token_absent` needle; the caught path stays pinned by `caught_fleeing_the_burning_tavern`), 1 parse pin (s7's wait-720 batch 15→14), 2 unit seed re-probes (the coerce gate 7→4, the KI#17 order 2→3) | the per-(site) hash alternative (a d100 derived from stable_hash(seed:npc:beat) without the bank) was rejected flat: the RngBank is the only door to entropy (L5) — a per-site hash bypasses the draw counters, the audit guard, and the fingerprint, an INV-2 violation dressed as a simplification; the constructor-registration alternative (the Simulator passing the pack-derived stream set at bank construction) was weighed and rejected on ripple cost (loop.py + every unit test's bank construction) against the lazy family registration, which keeps the tripwire for all non-family names while the family names are built by one function and pack-linted — the typo surface does not exist; the replacement law is RETIRED as a requirement (a re-arm stays an available technique when the table's semantics prefer it — content-5/6 land by ADDING entries now, the regen covering only the landing's own events, the iter-48 scale) | tests 952→959, ruff clean; TASKS (engine-2 done; content-5/6 rows relieved), TEST_PLAN §6 (the two A/B arms recorded), BLUEPRINT RNG-1 + phase0 §1 (the family law), DIRECTOR_SPEC §7, README, STATUS (header + FAQ laws 7-8 + Next step), worklog synced; DECISIONS transiently 45/30 kept per §6.1 (collapse due at the phase-3→4 gate per D-034) |

| D-084/D-085/D-086/D-087/D-088/D-089 | 2026-09-05 | **Phase 4 (Knowledge & scene) opened + leg-1 trait crystallization (D-084, iter-55 — the owner's "start phase 4" call, the iter-36 opener precedent) + leg-2 the brief's derived-trait read (D-085, iter-56 — leg-1's first consumer) + leg-3 reflection & memory compaction (D-086, iter-57 — the LEGEND_SPEC compression half, written just-in-time at the row, the D-022 law) + leg-3b the tavern reflection set (D-087, iter-58 — the arming, the owner's session call: "the ideal solution" analysis first, the content-column precedent iter-43) + retr-1 the retrieval ladder (D-088, iter-59 — the owner's "continue per plans" call, the §2-design verdict standing from iter-58) + scene-1 the scene manager + mode B (D-089, iter-60 — the owner's "continue per plans" call, TASKS' top row)**: (1) the opener — ROADMAP §2's phase-4 row flipped to open, the backlog drafted from phases.md §4 (leg-1..leg-4, retr-1, scene-1, blind-1), the exit criterion reads T3's zero-leak law extended to the phase-4 surfaces; (2) the fold — `core/traits.py`: a knower holding `threshold` DISTINCT tokens of a pack-declared family (`rules.json::traits`, the closed key set, lint `core/pack.py::_traits`) crystallizes the belief, the contributing records' source event ids riding as PROVENANCE — the expansion law (the source is always queryable, the belief a derived view, never a replacement; INV-1 preserved); the echo's sibling read model — writes nothing, feeds no entropy (the L6 fence), no decay term, `at_tick` gates contribution, breadth not repetition; the lint refuses dead vocabulary (threshold >= 2, family >= threshold, unmintable tokens, one-sided cross-family membership, the belief-token collision); (3) the read (D-085) — `brief/assembler.py::_recalled_fact_lines` reads the fold as DATA at the assembly tick: the PC's crystallized beliefs LEAD the recalled_facts block as belief lines `- belief <token> (t <cross>, sources: <ids>)` (cross = the threshold crossing, the latest source event's tick; the ids the expansion law's demand handle), the family records render nothing raw (the derived view replaces them; a below-threshold family stays raw — no belief, no replacement), belief lines count against `max_items` (the top-k law); `core/traits.py::expand_trait` is the demand side — every family record the knower holds, acquisition order (4) the compaction (D-086) — `core/reflection.py`: reflection-on-recurrence — a knower whose held records of a pack-declared family (`rules.json::reflection`, closed key set, lint `core/pack.py::_reflection`) reach the block's threshold (REPEATS: recurrence, where traits count breadth — the echo makes repetition felt, the reflection makes it KNOWN) mints ONE reflection EVENT per (knower, insight) per run through the canon door (compaction emits higher-level entries that are themselves log entries; originals never dropped, INV-1/INV-5 — letta's `summarize_messages_in_place` the named anti-pattern), `outcome.provenance` the `list[event_id]` demand handle (the source always queryable, the reflection a derived view, never a replacement; `expand_reflection` the demand side), the never-re-reflect law (`view.holds` on the insight — records are never dropped, one mint is final), `stale_reflections` the read-side stale flag (empty on any whole log by INV-1; the derived-store scavenge screening retr-1 must consult), no RNG in the mint (an inference, not a stochastic act), no hooks/state changes (L6 — knowledge-side canon, the director untouched by construction), the knower gate kind npc, `docs/LEGEND_SPEC.md` the contract owner; (5) the arming (D-087) — the committed pack's LIVE block: the measured recurrence (day1_full seeds 123/128, the retried theft) mints the insight pair `sneak_at_work_here` over `figure_reaching_for_purse` (the watcher's conclusion — the targeted guard holds the sighting twice) and `trouble_by_the_bar` over `noise_by_the_bar` (the room's — the barkeep, the drunkard, the serving maid each hold the noise twice), threshold 2, one insight per axis of the theft's evidence; the event `conclusion_drawn` renders "{actor} had noticed it before, and named it: {knows}." and rides `importance.story_critical_events` (a named conclusion is a tale beat — the knowledge-flow precedent: knowledge_transfer/rumor_told story-critical; the director untouched — importance feeds the tale gate and the metrics, never entropy); (6) the ladder (D-088) — `core/retrieval.py`: the read-side retrieval service over (pack, events), an in-memory REBUILDABLE SQLite index (INV-1's derived-store shape — drop, replay, re-index; never a canon write), the corpus = static lore rows (pack declaration order) + every knowledge record (event order — the known_by fold), FTS5 BM25 the zero-dep rung (the unicode61 word view — dry underscored tokens are word-retrievable, token equality, no stemming; OR-joined quoted terms — recall first, the ranker sorts), the sqlite-vec extension PROBED (the pip loader then the plain name; absence = normal operation, D-012 — optional environmental capability, never a dependency) with the vec0 kNN table when vectors ship, the pure-Python cosine scan as the rung's SEMANTIC DEFINITION (rung equivalence: the same top-k, ties by rowid; a probe or DDL failure degrades to the scan, L12 — never a break), the keyword containment floor when the SQLite build lacks FTS5 (no bm25 signal — the honest degradation); the re-ranker score = alpha*recency + beta*authority + gamma*bm25 + delta*cosine over the union (FTS5 matches OR cosine top-k — never an empty result from a rung failure; with vectors the nearest always ride, the RAG shape; without them a no-match query returns the honest empty), coefficients pack data (`rules.json::retrieval`, closed key set, lint `_retrieval`: non-negative with at least one positive — an all-zero block is dead data; knn_k >= 1; vectors keyed by existing lore ids, one dimension, finite values only), per-kind signal sources (recency fact-side `1/(1+tick-at)`, lore timeless 0; authority = authored lore 1.0 / the fact's fidelity chain rank; bm25 `goodness/(1+goodness)`; cosine raw, top-k only — rung equivalence), ties by construction order (rowid); the `knower` query parameter IS the known_by boundary — `knower=None` retrieves lore only, facts reachable solely through a knower's own memory, and vectors live on lore rows only (the hard boundary structural, not conventional); the two LEGEND_SPEC contract points LIVE: the stale law (the build consults `stale_reflections` — a stale reflection's minted record excluded at insert) and the source-outranks law (a retrieved reflection orders below ALL its retrieved provenance sources — the deterministic formalization of the contradiction case: a ranker cannot judge semantic contradiction, it enforces the precedence that makes it harmless); DORMANT in the runtime (the mediator's keyword query is the consumer — BRIEF_SPEC §9's deferral; the committed block declarative-only, the leg-1 precedent: the block ships with the engine row, zero corpus price by construction); (7) the scene manager + mode B (D-089) — `brief/scene.py::speaking_queue`: the chorus is a queue, not a convention — the present, `brief.actors`-declared NPCs at the current scene's location, pack declaration order (INV-2), capped by `brief.chorus.max_actor_calls` (the per-beat actor-call budget; the beyond-cap NPCs the L12 template rung — the chronicle already renders their beats, never a blocked beat), the player never queued (mode A owns its call) and ambients never (the knower gate: a group holds records but does not speak); the knower parameter `assemble_brief(knower=...)` — mode A (None, the player) the committed corpus bytes byte-identical by construction, mode B the SAME pipeline with three parameterized halves (scene_delta reads the knower's own perception — the blind-NPC law parameterized; recalled_facts its own records and beliefs — the per-knower traits fold, the leak surface closed by construction: a held-by-another token can never render; directives + voice_exemplars the `brief.actors` entry — the actor's role and voice, never the narrator's) and four SHARED blocks (scene_texture — one ledger per scene, the chorus reads the same texture block; present_entities — observables, L6; scheduled_lore; active_options — the door's grammar, not the knower's); the knower gate: an id that is neither the player nor a declared pack NPC is a loud ValueError; `narrator_call(knower=...)` the actor call document — the protocol's `actor: <id>` first line (mode A's bytes unchanged, the committed corpus shape) | the alternatives: a belief EVENT refused — it would double-count the knowledge channel and break INV-1's single-writer shape; counting records instead of distinct tokens refused — repetition is the echo's renewal, the breadth reading keeps small families honest; ranking belief lines by the three-signal score refused — a belief has no single recency (the fold carries event ids, not a record tick), declaration order is the deterministic answer, and beliefs lead the block by construction; the provenance ids ride the line INLINE — without them nobody could ever demand the expansion the reflection-mint alternatives: a knowledge-record-only shape refused — `provenance: list[event_id]` does not fit the record's single `source`, and patching the §3 contract is a schema bump for what the outcome object already carries (EVENT_SCHEMA §11, the DF `victim_hfid` shape); an armed v0.1 block refused at THIS row — a live block appends events, the corpus price rides the arming row (leg-3b) instead of the engine row (the arc-1/iter-47 precedent); the arming's own refused: ONE combined insight over both families — a mixed-evidence conclusion muddles the axis split (the figure is the watcher's business, the noise the room's); a non-story-critical landing — a named conclusion is the tale's highest-level beat, cutting it would hide the phase's product behind the gate; arming guard_02's told-dupe — not the pack's call: the never-re-reflect gate over the BRIEFED insight blocks it by law, the honest hearsay knower; a recurrence threshold of 1 refused at lint — a first occurrence is an event, not a pattern; the retr-1 refusals: AND-joined match terms — recall-first wins (an AND reading of a two-keyword query returns nothing on this corpus; bm25 sums what each row matched, the ranker owns precision); FACTS in the vec rung — the hard boundary refuses it twice (vectors map lore ids only, and facts enter solely through the knower's fold); a per-entry lore authority weight — no consumer reads it yet (L13, scope discipline: authored lore is uniformly 1.0, the pack may differentiate when the query side exists); an index persistence/caching layer — rebuild-per-build is the INV-1-purest shape at v0.1 scale (the persistence arrives with the scale that needs it); an LLM-judged contradiction detector for the source-outranks law — the deterministic precedence formalization instead (the ranker enforces the safe ordering, semantics stay the narrator's); dedup by `knows` token inside the ladder — the consumer's law (the brief dedups; the ladder serves evidence, duplicate rows are separate addresses); the scene-1 refusals: a per-knower texture window refused — one ledger per scene, the chorus reads the SAME texture block (D-049's own law; parameterizing it would fork render state); parameterized present_entities refused — the cards are observables (L6), the room's structural answer is the same for every present party, the leak surface lives in knowledge and perception, never in the cards; a present-but-undeclared NPC in the queue refused — the pack's declaration is the eligibility gate (the DORMANT family: silence is pack data, not an error); an `actor:` line in every call document refused — mode A's bytes are the committed corpus shape, the asymmetry documented (the player is the narrator's subject by construction); the session wiring + the actor reply door + the mediator's keyword query refused at THIS row — scene-2's (the underdeliver law: one task, one iteration); | measured live: the canonical seed-125 run — the guard pair crystallizes `paranoid_about_thieves` (the eyewitness 2 source events, the hearsay knower 1), the room's other witnesses hold two tokens and honestly stay uncrystallized; the PC holds no family token on ANY committed corpus — leg-2's lens changes ZERO brief bytes: the 10-seed day1_full A/B (seeds 120..129) 10/10 byte-identical (brief + log), zero corpus regen, the golden fixture's no-belief line pin; the testimony-crystallization fidelity floor stays an owner-gated recorded question (v0.1 counts any held token however learned) leg-3 measured: the recurrence EXISTS on the committed corpus — day1_full seeds 123/128 (the PC retries the theft; the targeted guard holds the sighting twice, the room the noise twice); leg-3b measured: the corpus price ZERO — the narrator corpus (105 cases) and the parse corpus (10 cases) replay through the real mediator/parser cycles with ZERO reflection mints, the T1 golden byte-identical (plumbing mints no knowledge), 8/10 day1 seeds byte-identical, exactly 123/128 mint (4 events each: the guard + the room trio at the second pickpocket, cause-chained, provenance [ev_0002, ev_0015]); the told-conclusion law LIVE — the watch-change briefing tells the relief guard the minted insight (told/partial, one step down) and view.holds blocks his own re-derivation (he holds the sighting twice from the one transfer, mints nothing): the craft-pack live-fire mints exactly one event (provenance [ev_0002, ev_0015], cause-chained, the cascade terminating), the stale fold empty on whole logs and firing after scavenge; the 10-seed day1_full A/B (120..129) 10/10 byte-identical through the landing — zero corpus regen (DORMANT, the pack's own declaration the gate); retr-1 measured: the corpus price ZERO — the 10-seed day1_full A/B (committed pack vs a block-less copy, seeds 120..129) 10/10 byte-identical (the block is declarative-only, nothing in the runtime queries it); the live-fire over the armed pack's own seed-123 log — the guard's reflection `sneak_at_work_here` (inferred/exact, score 1.77) retrieved beside its two provenance sightings (saw/partial, score 1.15) by the query "purse sneak": the score alone would put the conclusion FIRST, the law orders it LAST — the law over the score, the live pin; the stale scavenged arm (the event universe minus ev_0002) serves no sneak record and keeps the survivor; the probe reality pinned (vec_loaded False on the reference environment, the scan answering; a lying probe degrades to the scan, never breaks); the keyword floor and the OR recall pinned; determinism double-build identical, PYTHONHASHSEED-independent; scene-1 measured: the queue live on day1 seed 123 — (guard_01, barkeep) mid-tavern, (guard_02, barkeep) after the watch change (PRESENCE swaps the guard, never a convention about who talked), empty at the backyard (no declared actor remains); the leak surface ZERO — every declared actor's brief leak-free at every beat window (6 windows over 5 actors: every scene_delta line maps to a perceived event, every recalled_facts line to a held record or belief), the guard-only sighting token absent from the maid's brief; the mode-B belief line live on seed 125 (the guard's `paranoid_about_thieves` leads HIS brief with his own provenance, the relief's hearsay mint his own, the maid's no-belief fold honest); the corpus price zero — mode A over the committed pack (now carrying chorus+actors) byte-identical to the block-less copy, 1055→1085 green with ZERO fixture regen | contracts: phases.md §4 (P3f), BRIEF_SPEC §3.5 (the derived-trait clause, the same-commit law §8) + TASKS leg-1/leg-2 + STATUS (the read-side-folds FAQ); tests: tests/test_traits.py (18+4 — the read-model laws, the mechanics, the expansion family, the live canonical pins, the lint family) + tests/test_brief.py (+10 — the replacement law, the line shape, the cross tick, the hearsay dedup, the cap law, the declaration order, the no-block gate, the zero-regen witness), 991→1004→1026→1030 green, ruff clean; + LEGEND_SPEC.md (the leg-3 row's just-in-time write, SPECS_BACKLOG flipped; §7 the arming record at iter-58) + TASKS leg-3/leg-3b (done, iter-58); tests: + tests/test_reflection.py (22 + 4 committed-arming pins at iter-58 — the mint laws, the provenance law, the never-re-reflect gate, the within-event dedup, the knower gate, determinism, L6, the stale family, the expansion family, the lint family + the four conclusions, the told-conclusion block, the tale line, the 8/10 byte-identity witness); + the retrieval contracts: phases.md §4 (the retrieval paragraph — the ladder's architecture owner) + LEGEND_SPEC §4/§5 (the two contract points, marked live) + BRIEF_SPEC §9 (the deferred row flipped) + TASKS retr-1 done + tests: + tests/test_retrieval.py (25 — the DORMANT gate + the zero-price A/B, the rung family: FTS5 lore matching + the OR recall + the knower boundary + the honest empty + the keyword floor + the cosine scan RAG shape + the probe degradation + the vector validation, the re-ranker signals + the pack-coefficient flip + the tie-break, the stale whole-log/scavenged pair, the source-precedence live-fire + the alone-ranked-by-score arm, the lint family, the close guard), 1030→1055 green, ruff clean; KI#71 (the sqlite-vec license drift) opened+closed; + the scene-1 contracts: BRIEF_SPEC §3.9 + §6 (the chorus + actors pack contract, the same-commit law §8) + §7.1 (the actor line) + §9 (two rows flipped) + TASKS scene-1 done + the scene-2 row (the wiring, the actor reply door, the keyword query) + tests: + tests/test_scene.py (30 — the queue law family: presence/pack/kind gates, the cap, the pure fold; the mode-B laws: the byte-identical default, the actor's own role/voice, the shared blocks, the knower gate, determinism, the corpus-price A/B; the leak suite core over every declared actor at every beat window; the actor call document pair; the lint family), 1055→1085 green, ruff clean |
