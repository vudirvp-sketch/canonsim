# DECISIONS.md — ADR-lite

> Append-only **within a phase**; each entry: decision → why → consequence.
> Supersede with a new entry, never delete mid-phase. Cap: 30 entries,
> enforced by collapsing at phase gates (D-034): family merges write compound
> IDs (`D-018/D-022/D-029`) so external citations still resolve; compressed rows
> keep the decision and link the single owner of the detail (D-024 anti-drift
> — spec-restatement inside a decision row is duplication, not substance).
> Pre-collapse history lives in git. Collapsed 46→30 at the phase-0→1 gate
> (iter-7 intake); 41→30 at the phase-1 gate (iter-26 — the collapse ran
> inside the gate-review iteration per the owner's quality-first directive);
> 35→30 at the phase-2 gate (iter-35, the same precedent).

| ID | Date | Decision | Why | Consequence |
|---|---|---|---|---|

| D-001/D-009 | 2026-08-25 | Repo language law: docs, code, identifiers, commits in English; owner chat in Russian; prototype pack display names in English | token economy, owner request; consistency with logs/tests | all agents write English; final-product localization is pack data, zero code |
| D-002/D-010/D-032 | 2026-08-25 | Data contracts: JSON / JSONL + JSON Schema, no XML tooling (XML only as DF Legends *input*); EVENT_SCHEMA.md ↔ `schemas/event.schema.json` 2-place sync, test-enforced (the doc example is a test fixture); schema validation = the stdlib mini-validator `core/schema.py` for the used subset (`type`/`properties`/`required`/`enum`/`pattern`/`additionalProperties`/`items`/`minimum`/`$defs`/`$ref`) — any other keyword fails loudly | Python-stdlib native — the log is JSONL; `jsonschema` is a runtime dependency (D-012 forbids); hand-rolled structural checks duplicate the contract (the exact drift D-010 prevents) | `schemas/event.schema.json` is the machine contract; the log writer is the enforcement point — no invalid line can enter a log; T0 re-validates the doc examples; pack-file JSON-Schemas stay a phase-6 rung |
| D-003/D-023 | 2026-08-25 | Event-sourcing shape: JSONL log = truth, SQLite = derived index (INV-1); runtime state = the incremental projection `state_new = state_old.apply(event)`; `fold(log)` is for T2 replay and rebuild only — never the startup hot path | the ambiguity risks O(N²) at startup if fold is misused as the runtime state path (KI#5) | replay = fold(log); the DB can be dropped and rebuilt at any time; fold stays the truth-test (T2) and the rebuild path |
| D-004/D-028 | 2026-08-25 | Determinism law: no wall-clock anywhere, including the log header (header carries seed, python, schema_version, commit, pack — no timestamps); INV-2 = single point of randomness control — one master seed, named streams deterministically derived via the RngBank authority (`stable_hash(s) = int.from_bytes(hashlib.sha256(s.encode()).digest()[:8], "big")`, environment-independent); cosmetic draws can never desync canon replay | byte-identical determinism; the old "one Random instance" wording contradicted RNG-1 in three load-bearing places and was stricter than its own multi-stream donors; a built-in `hash()` derivation would silently depend on `PYTHONHASHSEED` | AGENTS.md §4 is the single reading owner; the iter-0v audit sub-clauses live as blueprint/spec sub-clauses (list in git worklog iter-0v) |
| D-005/D-038/D-039/D-040/D-041 | 2026-08-25 | Director & autonomous mechanics (iter-4/4a): the director is a consequence planner, never an improviser — complications are seeded at event time in a per-run buffer, never invented later; narrative entropy = observable state only (hook weights + global suspicion + visible threats); explicit triggers (time/place/threshold) fire causally; releases ride the intent door as `director_<N>` (the director never writes canon); director-off keeps seeding, suppresses releases (T8 baseline); crossings fire in TICK order. Autonomous: P2b goal ticker — per-NPC urgency specs roll d100 on the substantive stream; a precondition failure stays SILENT (the world's noise floor absorbs autonomous attempts); arrest resolution cause-chained to the attempt, `caught` irreversible (T4). Audit laws (iter-4a): the playscript feed is player-driven only (an autonomous ending NEVER advances the script); the crime-status flip guards the pack's ordered `status_values` progression; `reset_on_rotation` resets BOTH participants; dead pack keys deleted | causality by construction (RimWorld storyteller = the named anti-pattern); D-021 — the world must act without the PC; small-formula dynamics, not a planner; capture/escape is a binary outcome with no chase sub-system (phase-3+); KI#17–20 found by the owner-requested audit (124 probe runs clean) | director-off A/B mandatory (T8); beat order decay → urgencies → director; autonomous intents enqueue at `entry.tick`, never `beat_tick`, decay commits at `beat_tick`; full detail: `docs/DIRECTOR_SPEC.md` (the runtime contract owner) + `docs/INTENT_SCHEMA.md` + git worklog iter-4/4a |
| D-006/D-007/D-008 | 2026-08-25 | v0 epistemics shape: no group reputation (guard-to-guard transfer models it); rumor = knowledge transfer with fidelity decay; lies = crafted knowledge records | "reputation with the watch" = transfer events at watch change; distortion comes from source incompleteness — no extra system; foundation for phase-4 `believes/lies` | one mechanic covers gossip and hearsay distortion; no truth field on records in v0 |
| D-011/D-012/D-013/D-014 | 2026-08-25 | Repo meta & dependency surface: no CHANGELOG.md; repo name `canonsim` is a placeholder; source concept docs live outside the repo; stdlib-only runtime dependencies | worklog + STATUS + schema_version cover it; no strong naming constraint yet; the English distillation (VISION) is the working truth; determinism + zero supply-chain surface | one less file to bloat; rename before external visibility = one mechanical commit; agents never request the source docs; pytest / ruff stay dev-only deps — anything beyond is the AGENTS §8 stop |
| D-015 | 2026-08-25 | License filter lifted; usefulness & phase is the filter | no monetization planned; open bases adapted freely incl. functional re-invention | donors gated by phase (ROADMAP §4); phase 0 uses zero external code |
| D-016/D-017/D-024/D-026/D-027 | 2026-08-25 | Reference architecture (four places, four jobs): `docs/REFERENCES.md` = the catalog (owner survey rev v2 merged; licenses verified + tagged with check date, unverified quarantined in §11, quarterly review); `docs/ref/<source>.md` = one deep-dive file per source, `docs/REFERENCES_DEEP.md` = index + format template (single-file dives projected 2500–3500 lines, 4–6× the cap); `docs/BLUEPRINT.md` + `docs/blueprint/` = the distillation — the 12-row resolution ledger + cross-cutting laws + build index; the blueprint owns cross-reference resolutions and combinations (mechanics stay in refs, contracts in the specs; single-owner rule preserved — link, never restate, the D-024 anti-drift discipline) | rich but unverified survey (incl. unverifiable, misattributed repos); after 19 reference iterations nothing combined the verdicts — cross-ref tensions (MTTH vs determinism, mutable ECS vs INV-1, GA retrieval vs known_by) each needed one resolved mechanism, recorded once | admission rule: verify via GitHub exact-name search (web SEO misses small repos); donors are inspiration/patterns, never 1:1 copies (`REFERENCES.md` §0.7); a deep dive updates only its ledger rows; a resolution-changing ref needs a worklog note + a superseding D-entry; each per-ref file naturally ≤300 lines (the split-when-bloated law) |
| D-018/D-022/D-029 | 2026-08-27 | Concept absorption & digestion complete: manifestos and owner texts land **surgically into existing docs** (the D-018 pattern — never a new canonical file); one research pass granted then closed (Q4 = no); digestion declared complete at iter-0w — skeleton (phases 0–6, three layers, INV-1..5) confirmed, blueprint = the single build guide | a fifth canonical layer would rot, duplicate owners, and violate the caps; foundation hardening before code; 33 references replaced vague intentions with named donor-backed mechanisms without moving a phase gate | BRIEF/VALIDATION sketches gained sensory-emitter + beat-boundary delta + structural injection-neutralization clauses; P3e `psychological_echo`; sequencing is iteration-counted (calendar dropped); the doc-loop exception law: no doc pass without a fresh owner request |
| D-019/D-020/D-021/D-033 | 2026-08-28 | CORE_DESIGN_RESEARCH proposals accepted and built: M3 (causal chain length) / M4 (novelty) / M5 (non-PC share) into the metric baseline (P1b–P1d); sparse NPC↔NPC pair relations (P2a); minimal goal ticker (P2b); salience-based talk topics (P2c — knowledge *used*, not just stored) | depth-equation factors must be measurable, not asserted; teller↔listener trust needed a data home; the world must act without the PC for T8 to mean anything; the last owner-pending proposal flipped to accepted per its lifecycle rule | no event-schema break (relations are state); full LLM planning — never (Generative Agents anti-precedent); M5 non-trivially non-zero by construction; metric thresholds set from the measured baseline, same protocol as M1/M2 |
| D-025/D-034 | 2026-08-26 | Cap laws: `docs/*.md` ≤600 lines with the substance-over-cruft filter (AGENTS.md §6.1) replacing the rigid 400-line wall; DECISIONS.md ≤30 entries, enforced by collapsing superseded/absorbed entries into consolidated compound-ID rows at phase gates (`ROADMAP.md` §5) — never by mid-iteration cuts | iter-0i trimmed substance (enum lists, field names, pseudo-code) to fit a hard cap — the owner flagged: "hard cap = crutches, not quality"; the gate-collapse keeps the cap's pressure and the append-only invariant intact with a natural archiving rhythm | filler/restatements/linker-chains = cut always; named systems, field lists, enum values, per-source verdicts = never cut to fit; a cap breach triggers a cruft pass first, over-cap substance stays with a worklog rationale; the file may sit over cap between gates; within a phase: supersede, never delete |
| D-030 | 2026-08-27 | Darkness is an architectural property, not content scripts: tone = data asymmetry inside the existing 8 systems; the grim line (romance/intimacy/coercion) is admissible only as a post-gate pack candidate under the `PACK_SPEC.md` trigger | owner directive before any content; an external-LLM content proposal exposed the content-first failure mode: scope creep into a frozen slice, ad-hoc mechanics, a leaked group-reputation field, a spec before its trigger | darkness levers: asymmetric deltas (build slow, break fast), an immediate observable price marker on every socially meaningful behavior, irreversibility, per-NPC rumor lethality (D-006 held); core stays **axis-blind** (a future pack adds axes with zero code change, INV-3 corollary); consent/coercion = crafted knowledge records (D-008 pattern) |
| D-031 | 2026-08-27 | Quality round absorbed surgically (the D-018 pattern): law L13 abstraction cost gate (Rule-of-Three tiers, 4-branch registry threshold) + L14 elegance checklist (`BLUEPRINT.md` §2); type discipline + fail-fast + ActionResolver registry + negative-test principle (`phase0.md` §1/§2/§6); architecture fitness test (`tests/test_architecture.py`); **stack frozen**: stdlib-only through phase 2; mypy parked as owner-gated `qa-1` | the owner fears future work rotting into piled-up "works somehow" complexity; no new canonical files (a fifth law layer rots and duplicates owners); dev tooling is capped at pytest + ruff (AGENTS §8/§10) | INV-1 canon-write-privilege line in AGENTS §4; the code-quality DoD bullet in §9; Go/Rust revisit gated by `perf-1` data, never speculation |
| D-035/D-036 | 2026-08-28 | The commit gate + spread-pass discipline: every event enters through `Simulator._commit` — state deltas validated against the projection BEFORE the write (`validate → append → apply`); the spread pass is a per-layer singleton with a shared cause map (`PassPayload` carries no mutable state; ignitions merge `location → started_id` into the map) | KI#13: write-then-validate pollutes the append-only truth exactly when the code is already wrong (INV-1 makes the log the only truth); KI#16: parallel passes double `chance_per_tick` and lose the cause chain (seed-19 crash) | resolvers still read current values / stay idempotent — the gate is a net, not a license; one chance roll per spot per tick per layer; every spread chains to a cause; system-pass bookkeeping is global per layer, never a frozen queue-payload snapshot |
| D-037 | 2026-08-28 | iter-3 epistemics architecture: (1) knowledge state = a derived per-knower index (`core/knowledge.py` `KnowledgeView`) rebuilt by folding `knowledge` records — never projection props; (2) system reactions are event-driven, dispatched from the commit door (`_commit` → `_react`: crime first, telling second) — no call site can forget them, the cascade terminates; (3) watch rotations fire on clock crossing, never pre-seeded — a run still ends when its script's queue drains; (4) the systems live in `core/`, **not `sim/systems/`** — anything the canon door must dispatch is kernel mechanics (the import-boundary law) | (1) keeps INV-1 pure (memory = fold of the log, T2 applies); (2) removes the "forgot to react" bug class at zero scheduling cost; (3) pre-seeding would extend every run to tick 1080; (4) core never imports sim — law wins over the stale plan note | suspicion reacts to **novel tokens only**; the arrest threshold has a single owner (`crime_watch.arrest.requires_suspicion`); expectation violations chain to the axis-specific mover; `crime_status` lives on the player entity (namespace-collision avoidance); the telling shares the teller's most salient **novel** fact with one-step fidelity decay + an acceptance roll; official briefings transfer unrolled |
| D-042/D-043/D-044/D-047 | 2026-08-28 | The read side is pure functions of the log — render AND brief: every render entry point builds its `Engine` on a fresh `RngBank` from the log HEADER seed (same log → same bytes in any process/`PYTHONHASHSEED`; a growing log keeps its rendered prefix; tracery ~350 stdlib lines, cosmetic stream only, ShufflePool no-immediate-repeat; the importance gate is pack data `tale_gate.min_importance`); M1–M5 + the emergent-chain count are pure functions of `(events, projection)` in `core/metrics.py` (Mesa `DataCollector` inverted — the simulator never knows a metric exists); the assembler is **zero RNG** (dry structured tokens, L2 — `docs/BRIEF_SPEC.md` owns the skeleton: two-level budgets, whole-block eviction in the fixed order, directives never dropped, `[truncated:N]` markers, ranking caps ≠ budget drops, all budgets at `rules.json::brief`); `scripts/balance_harness.py` runs the 1000-sim distribution (contract `docs/TEST_PLAN.md` §6) | render determinism must be env-independent and prefix-stable; a runtime-coupled metric creates feedback risk + hot-path cost; the same purity law extended to the phase-1 mediator input (D-047's clause: deterministic token proxy keeps byte-identity stdlib-only); separating ranking caps from budget drops keeps the anti-silent-drop law about assembly budgets, not retrieval windows | the 1000-seed baseline is the M-threshold reference (M5 p50=0.77, chains p50=20, M3_mean p50=13.81, M1 p50=0.24, M4 p50=0.18; full table reproducible, output gitignored); T1 covers the chronicle via the log fixture; the session = one opened Simulator, byte-identical to the batch run; brief bytes pin-tested (golden fixture); the real tokenizer must not change committed formats without a spec bump (BRIEF_SPEC §8) |
| D-045 | 2026-08-28 | **Phase-0 gate verdict — PASS** (iter-6): all six `MVP_SCOPE.md` §16 exit criteria met; no kill-criteria hit; T6 (1000+ ticks clean), T1 (byte-identical + fixture guard), T8 (26 emergent chains OFF), T7 (chronicle reads as a story), T3 (zero leaks), T5 (impossible stays impossible) | the gate is the evidence checkpoint, not a feature deadline (`ROADMAP.md` §5); the ontology proved combinatorial depth; the OFF run's 26 chains prove the core is not dead; evidence detail: `docs/TEST_PLAN.md` + worklog iter-6 | (a) intake list deferred to iter-7 (none blocked phase 0): `doc-1`, the rest-action candidate (pack data, NOT core), `qa-1`, `ci-1`, `perf-1`, `pack-1`, the DECISIONS collapse (done iter-7); (b) the iter-7+ tuning knob = the IMPORTANCE RULE itself (hooks on story-critical events), NOT `tale_gate.min_importance`; (c) `day1_full.json`/seed 125 = the canonical T8 fixture; (d) phase-1+ metrics compare against the baseline, not against feel |
| D-046 | 2026-08-28 | `scripts/` is operator tooling inside the executable invariants (PACKAGE_DIRS += scripts + a coverage-closure test — a new top-level code dir joins in the SAME iteration it lands); the print exemption is CLI-class tools only (`cli/` + the argparse harness in `scripts/`); the false "AGENTS §9 — Script Persistence Rule" citation superseded (the rule text bled from the session prompt — the owners are TEST_PLAN §6 + D-044); M5 qualifiers: the urgency-driven share is 5–15%, the formal M5 total is 73–83% (both true, different quantities — quoted with qualifiers) | the iter-6a audit reproduced the 1000-sim baseline exactly — the drift was in citing documents, not the numbers; a top-level code dir outside `PACKAGE_DIRS` silently escapes INV-4's executable check | the stoplist scope stays `core/`+`sim/` (engine) with the periphery interpretation test-owned; KI#22/23/24 closed iter-6a |
| D-048/D-049/D-053 | 2026-08-28 | The scene ledger — canon vs texture (design iter-8a, hardening iter-8b, the LLM-free half iter-10; mechanism owner `docs/blueprint/phases.md` §1, spec owners `docs/BRIEF_SPEC.md` §3.3 + `docs/VALIDATION_SPEC.md` §8): narrator-invented texture lives in a session-scoped, append-only, mediator-owned ledger beside the canon log — never folded into state, never committed, never replayed; the determinism quarantine (canon log = deterministic replay; ledger = append-only session render state, auditable never replayable — the assembler's purity pair widens to (log, ledger), same (log, ledger, pack) → same brief bytes); scene = PC-location interval, identity `(location, ordinal)`, derived fold — zero new event types; discrete one-way entry lifecycle `{active, pinned} → {retired, contradicted, promoted}` (in-scene updates = retire+establish pairs in ONE delta — entries never edited in place; the ledger never evicts, ALL boundedness lives in the brief window); canon always outranks texture (deterministic contradiction retirement, cause-linked); ONE validation gateway — inline deltas and extraction output pass the same checks (scope shape/target, scene match, entity presence, canon-slot overlap incl. pack-modeled fields, laundering, `unique_slots`, duplicate, live conflict; the closed 8-reason refusal vocabulary); promotion ONLY through the intent door (one-way; a texture-referencing Intent → committed event = the promotion); no TTL — retirement is an explicit recorded decision; the ledger dies with the session, never summarized | VISION §5 licenses "free texture at low importance" but nothing stored it — a long scene loses the narrator's own established details (the owner's continuity problem); the memory-atlas survey (151 systems, 22 patterns) supplied the shapes and the anti-patterns to refuse (TTL deletion, chained summarization, vectors over dynamic facts); the external design review's real gaps (scene identity, pinning trigger, bound split) hardened in place — every resolution had to reuse existing law, a new mechanism family would have been a redesign smell | the brief's 7th block `scene_texture` with the pinned-first/newest-first window law and tombstones (O(current scene), `max_items` ranking caps — D-047 law); the golden delta fixture pins the refusal vocabulary + full ledger state; the texture-OCC mirror (withdrawals + `mark_promoted`); `apply_delta` auto-syncs the scene (the D-037 no-call-site-forgets discipline); T1/T2 canon replay never touches the ledger |
| D-050 | 2026-08-29 | **Derived runtime indexes live beside their single mutation funnel** (owner-directed perf micro-pass iter-8h; an external patch list was verified against the code before anything landed — the D-049 review discipline): `KnowledgeView` keeps `who → token → source-event-ids` (`add` is its only writer; `holds` is O(1) instead of a row scan, `before_source` semantics preserved — a token learned ONLY from the excluded source is not held; the plain per-knower token-SET variant from the external review would have broken exactly that) and the Simulator keeps `(entity, prop) → tick of the latest committer` (`_commit` is its only writer; the decay pass reads the index instead of scanning the log per NPC-axis — the KI#19 "any committer" baseline unchanged). Same-funnel-law scan eliminations: `_scene_delta_lines` breaks at the beat-window edge (ticks are log-monotonic — the writer invariant, already load-bearing via `reversed()`), `salient()` is a top-1 `max` (first-of-equals ties match the stable reverse sort exactly), `occ_breaking_cause` folds forward ONCE from the proposal point (`fold` is a strict left fold over `apply_event`, so the intermediate states are identical to the per-index refold — O(events), not O(w·events)), director `entropy` is computed once per `releases()` (provably invariant within the call; the eager per-policy-call recomputation was pure waste under a rejecting policy: k+1 identical evaluations per beat) | every patch is a strict function of committed events → rebuildable, T2 applies, INV-1/INV-5 untouched, no new canonical layers (D-018 pattern), no new dependencies (D-012); D-031's data-not-speculation honored with before/after micro-benchmarks (decay pass 45×, occ 40×, holds 664×, releases 24×, scene-delta 5×, salient 1.9× at the measured sizes — and each is an asymptotic win: O(E)→O(1)/O(window)); the reviewer's "index rebuild contract" concern is satisfied structurally — each index has exactly one mutation funnel that sees every event regardless of type (D-037's "no call site can forget"), so a separate `rebuild()` method would be redundant API surface (L13); incremental entropy sums rejected — sync-state for marginal gain | tests 338→340 (+ holds/salient contract pins; the T2 rebuild test extended to the token index); golden fixtures byte-identical; `decay_drafts` signature `events` → `last_change` (runtime + tests only — no doc cites the old shape); one brief-test fixture fixed to be log-shaped (it encoded an out-of-order log the writer invariant forbids — the assertion's semantics unchanged); DECISIONS transiently 34/30 mid-phase (collapse at the phase-1→2 gate per D-034) |
| D-051 | 2026-08-29 | **The bg-1 SQLite sink** (`scripts/df_import.py`, bg-1-sqlite-sink; bg-1 closed): HANDLED records (historical_event/_collection/_figure) load as typed core tables + EAV `*_fields` (every other child tag; repeated tags survive, identical duplicates collapse, nested children as canonical JSON — sorted keys, parse order preserved); `event_participant (hfid, event_id)` lifts every direct child tag ending in `hfid` (the survey's context rule) so bg-3's "figure Y's own records" is a PK prefix scan (measured 4 ms / 1,157 events on the 1.19M-event large world); collection nesting reconstructs from BOTH the parents' `<eventcol>` lists and the `parent_eventcol` up-edge, deduplicated (measured: all 1,510 up-edges agree with parent lists in the large world); every non-noise UNHANDLED record (incl. future UNDOCUMENTED tags — schema drift never breaks an import) lands in one generic `records` table as native JSON — no per-tag schema upkeep (L13); **truncation policy owned: default = flagged partial import** (`meta.partial=1`; the in-flight record at the cut lands with its parsed prefix of fields — KI#36 measured behavior, shared with the survey so sink counts cross-validate against survey counts on ANY export), `--strict` aborts before parsing; art/dance/musical/poetic forms counted-not-stored (coverage-matrix noise law); the plus companion is not imported (selective import, never wholesale — deferred until bg-2/bg-3 need its complementary fields) | the bg-1 AC is "parser loads a world into SQLite" and the AC's consumers (bg-2 taxonomy, bg-3 briefer stress) need exactly participants/grouping/names — typed+participant covers the hot queries, generic JSON covers the long tail without schema maintenance; the sink reuses the iter-8e/8f/8g sanitize+recover+stream core unchanged (one parsing law, zero duplication); stdlib `sqlite3` only (D-012 intact); the DB is a rebuildable index of the export bytes (D-003 analog: fresh rebuild every run, no journal/fsync pragmas, no wall-clock in `meta` — the df_design determinism quarantine: content-deterministic, no golden DF fixtures, no cross-DF-version byte claims) | bg-1 done (AC met + cross-validated on the owner's large world: 2.38 GB → 898 MB DB in 174 s, every table count reproduces the survey exactly); KI#36 fixed on the way (the UNDOCUMENTED marker the matrix promised but the tool never implemented — first real catches: `artifact` and `historical_era`); `tests/test_df_import.py` (11 tests) pins the invariants incl. content determinism and both truncation modes; the plus-companion import is a documented deferral, not backlog scope; DECISIONS transiently 35/30 mid-phase (collapse at the phase-1→2 gate per D-034) |
| D-052 | 2026-08-29 | **The validator contract** (iter-9, `docs/VALIDATION_SPEC.md` — the trigger-fired spec owns the field-level detail; `brief/validator.py` is the LLM-free half): the mediator's gate is a pure function of (proposal, log, pack) — one CLOSED structured proposal document (claims + intents + `expected_event_seq`; no prose field exists, so injection is neutralized structurally, D-018 — loud `ProposalError` on any shape drift, emitter bugs crash); honest verdicts `supported/contradicted/insufficient_data` evaluated against CURRENT canon under closed-world semantics (invented entity/event = contradicted; unmodeled prop = insufficient, never fabricated — UAP); ExpectedVersion OCC = the intent-door semantics reused (INTENT_SCHEMA §4: stale+broken → refused with first-break attribution; stale+intact → rebased; intents pass through with anchors untouched — the door stays the single commit path, D-037); the ≤2-regens protocol owns the call-budget reconciliation (2-call steady state = narrator + extraction OR regen; 3-call worst case = narrator + 2 regens — bounded, counted, exhaustion → dry mode L12) | every law reuses existing machinery (intent OCC, validate_shape, KnowledgeView, fold — D-018/D-035/D-037 discipline); the C-parser (phase 2) and the narrator boundary wire the same shapes; the scene-ledger gateway (iter-10) reuses the refusal shape + regen budget | the golden set (`tests/fixtures/validation_golden.json`) pins verdict semantics as committed data — computed, never LLM-judged; `invented`/`unverifiable`/`regen_count` are the bg-3 harness metrics; DECISIONS transiently 37/30 (collapse at the phase-1→2 gate, D-034) |
| D-054/D-055 | 2026-08-29 | **The narrator boundary — both halves** (iter-11/12): (a) the texture promotion door, the LLM-free half (`core/intent.py` + `resolvers.py::_pickup` texture branch; contract `docs/INTENT_SCHEMA.md` §2/§3/§7; mechanism blueprint §1): a texture-capable action's pack `texture` block (`requires` + `knowledge`, lint-checked, `{texture_slot}` not `{target}`) REPLACES the canon preconditions for intents carrying the mediator-resolved reference `{entry, scope, slot, value}` (`requires_for` — one split at proposal, completion, and OCC attribution; the reference is DATA on the Intent, shape-gated loud; core NEVER sees the ledger, INV-3/D-031); a take-success on the reference IS the promotion — one `StateChange(scope_target, slot, None → value)`, the object's canon birth (the gateway's canon-slot check outranks any texture on the slot from that moment); a failure promotes nothing (live+pinned); ghost interactivity structurally impossible; `unique_slots: ["hearth"]` (cross-scope re-establishment = the laundering-family refusal). (b) agent-in-the-loop, external at dev-time (owner verdict in chat): the call is a FILE contract — the mediator emits `output/mediator/call_<N>.md` (the brief + `narrator_protocol`, BRIEF_SPEC §7.1) and the operator writes `reply_<N>.json`, ONE closed `{prose, texture_delta?, proposal?}` (VALIDATION_SPEC §7.1) with deep shape gates AT the boundary; a refused document never feeds intents — the beat regens whole; the mediator's noun resolution (`feedable_intents`) drops non-player actors / non-live entries / per-document duplicates as WITHDRAWN notes, never events; beat order D-049's; L12 from day one: narrator → template → dry | promotion is the ONLY texture→canon path (zombie texture cannot arise legally; inter-scope movement = take = committed event), reusing the door's whole spine (shape gate, preconditions, checks, OCC, knowledge) instead of a parallel write path (D-037); zero LLM/network/runtime-dependency code — INV-4 unchanged as written, nothing entered the codebase (no §8 stop needed); integration (llama.cpp inside SoW, the dumb-terminal wiring) stayed behind the phase-1 gate exactly as ROADMAP §6 demands; the SoW audit parked as `bg-6` | `brief/mediator.py` (pure document layer) + `cli/mediator.py` (session orchestration, periphery D-046) + the `SceneLedger` wiring; the phase-1 corpus (105 cases) replays the refusal vocabulary, both door outcomes, and the rebased/stale anchor paths through the real cycle; local inference (TECH_NOTES §1) remains the owner-gated runtime-engine candidate (`engine-1`) |
| D-056/D-057 | 2026-08-30 | **Mid-phase-1 structural landings (owner-directed)**: (a) st-1 presence & entity cards (iter-15 — blueprint §1/§5, field owners BRIEF_SPEC §3.4 + INTENT_SCHEMA §7): the brief's 8th block `present_entities` (one dry line per present entity in pack order — `carries=` visibly-carried items on the carrier's line, bare `prop=value` promoted props, directed pair lines; a scene line when the scene location holds promoted props; `max_entities`/`max_pairs` ranking caps, the D-047 law; eviction rank: canon projection outranks narrator texture, D-049) + the write-side twin, the per-present-target `knows` expansion — an actor-held template with `present_at` ∈ {location, destination_location} expands to one record per present entity at the site (pack order, self excluded, items via the carrier closure; the pack's `move` carries it — the mover learns `<id>_present` on arrival); the T1 golden fixture regenerated in the same commit (the iter-3 precedent). (b) the universality pass (iter-20, owner-directed from the iter-19 re-audit): transition layers declare their OWN vocabulary — `spot_state`, `halt_flag`, ordered `follow_ups` (lint joins every kind to a declared event AND knowledge entry at load time) — and `core/transitions.py` iterates the data (no kind string or state value in core; the director's threat sensor reads the declared vocabulary); the scene line gains `brief.present_entities.scene_line_fields` — pack-declared location fields (tavern ships `layout` on all 5 locations) rendered canon-from-birth before promoted props | (a) the quiet-beat hole was twice-confirmed live (iter-13/14: the brief carried no presence fact, so the narrator could not legally name who is in the room) — the fix had to be structural, not recall luck; KI#43's grammar correction obeyed (records ride the move event's templates, no new event types); (b) the re-audit found two universality bottlenecks (hardcoded kind/state branches + a promoted-props-only scene line); KI#48: the gateway's canon_slot reads BOTH prop sources — a pack field is guarded the moment the pack declares it (KI#41's `exits` precedent; `initial_projection` seeding was never needed and stays out) | (a) zero schema change (pack-data growth per EVENT_SCHEMA §11); M1/M3/M5 unaffected; the tavern pack ships 2 new pair seeds (drunk→guard fear 40, maid→barkeep trust 70); (b) byte-identical for the tavern pack (a pure data move; the pack's declared-but-unread `burnout.irreversible` now actually read); generality proven by a synthetic `rot` layer in `tests/test_transitions.py`; the remaining universality bottlenecks (the 12-resolver registry, the closed `core/intent.py` enums) stay phase-6 territory (phases.md §6) |
| D-058 | 2026-08-31 | **Phase-1 gate verdict — PASS** (iter-26, the gate review; owner-directed quality-first close): the exit criterion "0 canon violations per 100 beats (regression set)" met — 109 live beats over 10 narrate sessions (the recorded tally incl. refused-and-caught beat documents; the accepted-beat sum alone is 104), 0 canon violations, the corpus regression set at 105 distilled cases replayed through the REAL mediator cycle (all green in the 610-test suite, ruff clean); no kill-criteria hit (0 breaches in 109 beats of live play — the per-1000 kill reads a rate the current live volume cannot yet reach, honestly noted); the full §5 protocol re-run: T1 byte-identity + fixture guard, T8 single-factor A/B (OFF = 26 emergent chains ≥ 3), M1=0.417 / M2=0.500 on the gate ON-run, T7 — the day1_full chronicle still reads as a story (the decay/wariness repetition stays the noise floor; the tale_gate knob remains the owner-gated tune-1 item); DECISIONS collapsed 41→30 per D-034 in the same iteration | the gate is the evidence checkpoint, not a feature deadline (`ROADMAP.md` §5); the refusal vocabulary in full, both door outcomes, the rebased/stale anchor paths, and the director's live release are all corpus-pinned; the owner's directive made cleanup + re-verification + doc actualization part of the gate work | (a) phase 2 (Parser, mode C) may open — phase-2-vs-more-phase-1-polish stays the owner's call (tune-1/tune-2/tune-3, st-2, corpus consolidation remain owner-gated backlog); (b) the runtime-engine decision (llama.cpp + GBNF, TECH_NOTES §1) and the bg-6 SoW audit unlock with this gate (owner's call when); (c) the next DECISIONS collapse is due at the phase-2→3 gate |
| D-059/D-060/D-061 | 2026-08-31 | **The phase-1 polish menu — three landings on the owner's finish-phase-1 directive**: (a) tune-1 (iter-27): the story-critical importance hook — pack-listed event types (`rules.json::importance.story_critical_events`, lint-closed) score +2 `story_critical_event` (`pack_importance` takes the event type, all emit sites); the tale gate follows low→medium — the RULE owns the signal/noise split per D-045(b), the gate is not the primary knob; + the `recuperate` resolver + `status_effects` pack block (`rest`, the 13th action, 60 ticks, fatigue −30 — the KI#4 counter-play, pack data end to end); day1_full OFF renders 47 events → 14 tale lines, zero decay/wariness noise (pinned); the 1000-sim baseline re-run IDENTICAL. (b) tune-2 (iter-28): `brief.present_entities.card_markers` — the prop-path marker table (threshold rows + value rows, closed marker surface `status.<axis>`/`relations.<axis>`/`crime_status`, lint-checked; the segment is `markers=`); the pack ships `wary` at `relations.suspicion` ≥ 25 (aligned with the `status_suspect_at` flip) + `suspect`/`caught` on the player's card — the narrator's read surface for standing state (the scene_delta half is lawful blindness, blind-NPC D-037: the CARD is the narrator's surface, the delta window the player's). (c) pack-2 (iter-29): the `spot_available` door check — the closed precondition set's 15th test (the target location holds an unburning spot of the declared layer, the exact condition the ignite resolver keys on; the `layer` param lint-checked) — arson on a destroyed or fully-burning location is an `intent_rejected` no-op with `failed_test target.spot_available`, the door-outcome vocabulary's fourth axis | the T7 phase-1 gate re-read flagged the noise floor (27 of 47 day1_full events were decay/wariness repetition — a bare gate raise would have cut story events the raw rule scores low); iter-17's live finding: the suspicion axis and the `crime_status` flip were claimable-but-invisible through the brief; the iter-2a audit note + iter-24's live probe: arson on ashes logged a no-ignition success that pretended the world changed | T1 fixture untouched by (b)/(c); tests 610→629 across the three; the seed-41 corpus probe flipped with (c) (renamed, the old pin lives in git); field-level detail: INTENT_SCHEMA §3 (the requires row), BRIEF_SPEC §3.4/§6 (the marker table law) |
| D-062 | 2026-09-01 | **The phase-2 parser boundary — mode C is EXTERNAL at dev-time over a file contract, mirroring D-055 on the player's free text** (iter-31, the owner's start-phase-2 call — phase 1 closed at D-058, the polish menu done, the remainder owner-gated with no declared candidate): the repo-side half is `brief/parser.py` (pure — the grammar snapshot: the pack's verbs with pack-DERIVED field constraints (ticks positive-int when drawn-`N`, method = `rules.checks.methods` keys, near = the action's ignition-layer spots on the actor's folded position, texture = a live-entry reference) ∪ the addressable nouns (canon entities + live ledger entries — ghost interactivity structurally impossible, blueprint §2); `parse_call` (utterance + grammar + protocol, byte-deterministic); `parse_reply_from_mapping` — the closed {intent \| question \| no_intent} document gated AT the boundary: off-grammar output is a loud ParseError, never a feed — the world never moves on a malformed parse) + `cli/parser.py` `ParserDoor` (the session door: emit (retire window + scene sync + document) → apply (gate against the CURRENT snapshot → pin → feed ONE step through the front door → promotions wired exactly like the narrator path); the gate does NOT duplicate door-owned checks (target-required, the one-path law, preconditions) — one owner per law) + the `say`/`say apply` session commands sharing the session's ONE ledger (D-049). **The pin law lands wired:** `SceneLedger.pin` — blueprint §1(a)'s first consumer (an Intent referencing a texture entry pins it; the reference IS the pin — a door-rejected attempt still pins, a failed attempt keeps it live+pinned, un-pinning does not exist). The trigger-fired `docs/PARSER_SPEC.md` owns the contracts + the ≥90% measurement (parse validity ≠ world legality — attempts are facts; door outcomes never count as parse failures) | the D-055 precedent (zero LLM/network/runtime-dependency code — INV-4 unchanged as written; the runtime engine (llama.cpp + GBNF) stays the owner-gated `engine-1` decision, and grammar-constrained decoding will make off-grammar output structurally impossible at the source — the dev-time gate is its stand-in); field constraints derive from pack data only (INV-3: a second pack requires zero engine changes, the snapshot derivation included); disambiguation is surfaced as questions (uncertainty is asked, never guessed — the exit criterion counts boundary-accepted intents per utterance) | `say <text>` / `say apply <reply>` join the session (MVP_SCOPE §12's phase-0 list untouched — phase-2 commands are spec-owned, the narrate precedent); output/parser/* + output/mediator/* are gitignored runtime artifacts; off-grammar replies leave the cycle open (fix the file, re-apply — dev-time semantics; the runtime re-ask ladder is PARSER_SPEC §7's deferral); tests 629→654 (+25: snapshot derivation incl. the position-tracked near enum, call-document bytes, the gate's refusal vocabulary, pin's law, door e2e — feed/question/no_intent/attempt-is-a-fact/pin-and-promote (seed-probed both ways)/failed-attempt-keeps-pin/the shared-ledger scene-close retirement — + the CLI wiring); DECISIONS transiently 34/30 (collapse due at the phase-2→3 gate per D-034) |
| D-063 | 2026-09-01 | **bg-2 landed — the DF event taxonomy + the sink v2 plus pass** (bg-2-event-taxonomy; the owner supplied the four world exports, byte-identical to the iter-8e survey set — import cross-validation reproduces every §3.2 count): (1) the plus pass — D-051's recorded deferral fired: theft detail (`item_stolen` thief/item/method, 100% coverage measured) and beast-attack victims (`creature_devoured` eater/victim/race) live ONLY in the companion, so sink v2 imports its `historical_events` section into a separate `event_plus_fields` EAV keyed by the same event ids (fields only, no typed duplication, main-file facts never shift; everything else in the companion counted-not-stored; relationships defer until bg-3); (2) `scripts/df_taxonomy.py` — 15 event-type plans + the birth gap + war/beast-attack collection plans, fixed quantile-spread selection (pure function of DB content, no RNG/wall-clock), per-entry participants/place/reconstructed-cause/witness/long-term-consequence; (3) `docs/TAXONOMY.md` — 120 entries across the 16 TASKS bg-2 target types (AC ≥100 MET), the type-map verdicts (E / E+ / R / GAP) against EVENT_SCHEMA, and the measured findings (owner: §4 — the witness column is empty by design, 9/88; the `hfid1`/`hfid2` participant-index blind spot over 25,079 reputation events; murders always carry slayer+site, 5,786/5,786; item-loss mostly terminal, 6/8) | the bg-2 TASKS row demanded exactly this artifact, and its query home is the sink ("participant/grouping/name queries run on the DB, not by re-parsing XML") — the honest notes (causality reconstructed, witness absent, casualties lower-bounded) are baked into the tool's output, not the prose | track B left: bg-3 (briefer spike) — the corpus, the sink v2 query surface, and the §5 consumer caveats are its inputs; the lift-rule extension for `hfid1`/`hfid2` is a measured-law change deliberately deferred; tests 664→673 (+6 taxonomy, +3 import plus-pass), ruff clean |
| D-064 | 2026-09-01 | **Phase-2 gate verdict — PASS** (iter-35, the gate review; the owner's "continue per the plans" session call — the un-gated backlog empty, the phase-2 arc's #1 item, the iter-26 precedent): the exit criterion "≥90% valid intents" (ROADMAP §2) MET — 51 combined utterances across 10 live say-door sessions: 35 fed intents / 10 questions / 4 no_intent, **35/35 boundary validity** (0 honest misfires; 10/10 deliberate off-grammar probes caught loudly; PARSER_SPEC §6 owns the measurement — the fixture's combined tally is the source of record); no kill-criteria hit ("else redo the grammar" is pack-data work, INV-3); the full §5 protocol re-run: day1_full ON seed 125 M1=0.417 / M2=0.500, **identical to the iter-26 phase-1 gate numbers** (phase 2 added the parser without touching a canon path); T8 OFF 26 emergent chains ≥ 3; T1 byte-identity + the fixture-regen guard green in-suite; T7 — the day1_full tale still reads as a story (13 lines, zero noise-floor lines); the corpus regression (105 narrator + 10 parse cases through the REAL stacks) green in the 697-test suite, ruff clean; DECISIONS collapsed 35→30 in the same iteration (D-034) | the gate is the evidence checkpoint, not a feature deadline (ROADMAP §5); the owner's session directive fired the review; honest note (the D-058 family): 51 utterances is dev-time scale — the ≥90% reading holds on the measured volume, not a 10× guarantee; the alternative mix is healthy (the parser commits, 35/51 — not just declines) | (a) phase 3 (Director) opens on the owner's call (the iter-31 precedent; the roadmap never crosses a gate with a dead build, and phase 3 stays parked until called); (b) parse-2, engine-1 and the phase-1 owner-gated remainder (tune-3, st-2, corpus consolidation, bg-6, qa-1, ci-1) stay owner-gated; (c) the dev-time external parser carries mode C until the owner-gated engine decision (D-062); (d) the next DECISIONS collapse is due at the phase-3→4 gate |
| D-065 | 2026-09-01 | **Phase 3 opens + the pacing clock lands (DIR-1, the L4D peak/rest donor)** (iter-36, the owner's "continue per the plan" session call — phase 2 closed at D-064, the un-gated backlog empty, phases open on the owner's call per the iter-31 precedent): `core/director.py` gains the per-run `PacingClock` — a deterministic four-state machine RAMP / PEAK / REST / STAGNATION over narrative entropy, advanced once per beat (guarded against a double advance inside one beat); PEAK = entropy ≥ `director.pacing.peak_floor` (strictly above the stagnation floor, pack lint); leaving PEAK after `min_peak_beats` enters REST for `min_rest_beats` (the L4D `PeakDuration`/`RestMinDuration` anti-flap pair; the `TimeSincePeak`/`TimeSinceRest` two-clock donor shape IS the state machine); stagnation releases fire only outside PEAK/REST — REST is the post-climax breathing room the flat v0.1 detector lacked (it re-injected the beat after a climax, flattening the arc); explicit triggers never consult the clock (D-005: causality is not pacing); a pack without `director.pacing` runs the v0.1 minimal pair byte-identically (the pack's own declaration is the gate, INV-3) | the phase-3 build list (ROADMAP §2 + phases.md §3) names director evolution first, and its exit criterion ("a scene without an event < N beats") is a pacing law — the clock is the substrate every later refinement (L4D2 layered thresholds, PEAK_CLIMAX, multi-channel) layers on; the L4D donor (docs/ref/l4d_director.md) names the peak/rest two-state clock with min durations as its most portable lesson; honest scope note: v0.1 numbers (peak 25 = the suspect-flip band, rest 1 beat = half a day at the pack's beat cadence) are un-tuned — the dir-2 balance-harness A/B owns the measurement before the phase-3 gate | the tavern pack declares `director.pacing` (25/1/1); zero fixture regen — the committed fixtures carry no stagnation releases (the corpus's one live director release is the explicit document_check, ungated by design) and the day1_full ON log is byte-unchanged (all three beats sit in PEAK; the M1=0.417/M2=0.500 phase-2 gate record holds); KI#60 (phases.md §2 stale OPEN header after the iter-35 closure) opened + closed, KI#55–59 deleted per AGENTS §5; tests 697→708 (+11: state machine, REST suppression, post-rest resume, explicit-trigger-mid-rest, the no-double-advance guard, pack-gating, the two lint refusals); DECISIONS transiently 31/30 (collapse due at the phase-3→4 gate per D-034) |
| D-066 | 2026-09-01 | **DIR-2 landed — the eventless-stretch instrument + the balance-harness pacing A/B** (iter-37-eventless-stretch, the phase-3 backlog's #2, the owner's "continue per the plan" session call): (1) `core.metrics.eventless_beat_stretches(pack_rules, events, gate=…)` — the phase-3 exit criterion's measurement ("a scene without an event < N beats", ROADMAP §2) as a pure function of the log + pack data (D-042 family): a scene event = importance at or above the pack's tale gate (`Grammar.tale_gate` — the same reader the chronicle uses, T7's noise-floor law: per-beat bookkeeping and texture never break a stretch), a beat window = `(previous_beat, beat]` over the `urgencies.beat_ticks` axis the loop fires (DIRECTOR_SPEC §7; events at a beat's tick belong to that beat; the trailing partial window dropped — no beat evidence); a stretch = a maximal run of consecutive eventless windows; (2) the harness `--pacing on|off` A/B — the clock-off arm is a PACK VARIANT (the committed pack minus `director.pacing`, materialized under gitignored `output/pack_nopacing/`, linted on load — the pack's own declaration is the gate, INV-3; zero core changes); `--pacing off` requires `--directors on` (a disabled director never consults the clock); (3) `IMPORTANCE_ORDER` moves to `core/log.py` beside the `Importance` literal — the single owner of the ordering, the chronicle imports it (the D-024 dedup: metric and chronicle must agree) | measured (1000 seeds 100–1099, day1_full, director on): **both arms identical — per-seed byte-identical logs (10 probed), max stretch 1, one 1-beat stretch per run (the (360,720] phase-boundary window), quiet 1000/1000**; the mechanism: every run ends in PEAK (the double-steal suspicion holds entropy ≥ peak_floor 25 through the last beat — the stagnation path never fires, so REST never gates anything; the D-065 "v0.1 numbers un-tuned" note gets its answer: on this pack+script the pacing parameters are inert) | honest scope: a measurable clock delta needs a script/pack that walks the world into quiet — a phase-3 gate-protocol script-set question, NOT forced here (the instrument exists and is pinned; forcing a delta would be pack retuning beyond dir-2's task ID); KI#61 (loop comment named never-read `director.stagnation.beat_ticks` — the dead pack key deleted, `urgencies.beat_ticks` is the single owner, zero behavior change) + KI#62 (worklog order/cap drift — bg-3's entry sat below the tombstones, 11 live entries) opened + closed; tests 708→727 (+13 test_metrics.py, +6 test_balance_harness.py incl. the seed-125 D-065-record pin), ruff clean; DECISIONS transiently 32/30 (collapse due at the phase-3→4 gate per D-034) |
| D-067 | 2026-09-01 | **DIR-3 landed — the climax layer (layered thresholds) + `PEAK_CLIMAX` (the boss-beat rule)** (iter-38-climax, the phase-3 backlog's #3, the owner's "continue work per the plan" session call): (1) `PacingConfig.climax_floor` — the optional third entropy layer (`director.pacing.climax_floor`, pack data; strictly above `peak_floor` by lint; this pack's 75 = 3× the peak floor 25, the L4D2 three-intensity ratio the donor names — Boss threshold gates Peak gates Calm); (2) the climax release path — a climax-flagged hook (`director.hooks[tag].climax: true`, pack data) releases ONLY at the end of a peak: clock in PEAK having held `min_peak_beats` (the L4D placement law — boss beats END peaks, never start them) and entropy at the third layer; climax hooks are excluded from the stagnation path (a boss never spawns because the world is boring); explicit triggers stay causal and win the same-beat race (D-005); (3) `PacingClock.PEAK_CLIMAX` — one beat, entered only by a climax release (never by entropy alone: the state marks hook placement, not an intensity band), exited to REST unconditionally (boss beat + reset; a still-loud world breaks the rest per the existing re-spike law, the transition after); (4) `permit_climax()` on the `DirectorPolicy` protocol — a separate policy question because the boss fires at HIGH entropy where the stagnation path fires at LOW (one boolean cannot serve both honestly; Enabled/Disabled the minimal pair) | phases.md §3 names layered thresholds + PEAK_CLIMAX as phase-3 director refinements; the L4D2 donor (docs/ref/l4d_director.md) gives both laws: thresholds are layered (Tank needs 3× the peak threshold sustained) and boss beats are placed at a peak's END; the corpus-preservation discipline drove the pack shape: flagging `possible_document_check` was probed live (the full 727-test suite green with the flag), but its v0.1 wait-stub intent would make a hollow boss beat — the flag lands with the `document_check` action (§11), the owner's content call; the layer without a flagged hook is inert (fixture discipline), the mirror of D-065's inert-on-day1_full pacing numbers | the tavern pack declares the layer (75) as dormant vocabulary — a one-line pack change activates it when the content is ready; a pack without `climax_floor` runs the iter-36 two-layer clock byte-identically, and a flagged hook without the layer is explicit-trigger-only (the nopacing harness variant IS that pack — legal, not drift); the release-path order is now explicit → climax → stagnation (DIRECTOR_SPEC §5/§7 own the law); harness A/B re-measured at iter-38 (10 seeds: both arms byte-identical, the layer inert); KI#60 deleted per AGENTS §5 (closed iter-36, two iterations past); tests 727→739 (+12 test_director.py: the boss-beat exit, the layer/threshold/minimum/placement gates, the quiet-path exclusion, explicit-beats-climax, the no-double-boss guard, director-off, the flagless-layer semantics, the two-layer byte-identity, the two lint refusals), ruff clean; DECISIONS transiently 33/30 (collapse due at the phase-3→4 gate per D-034) |
| D-068 | 2026-09-01 | **DIR-4 landed — the multi-channel quiet split (the L4D three-director family)** (iter-39-dir4, the phase-3 backlog's top un-gated item, the owner's "continue work per the plan" session call): (1) `director.channels` pack data — named pacing dimensions (this pack: threat / social / ambient — the L4D Horde / S.I. / Music family, the names are the pack's own), each an `entropy_floor` plus the observable inputs it binds (`CHANNEL_INPUTS`, the closed vocabulary `suspicion | physical_threats`; the channel's own unreleased hook weights always feed it — a channel never senses another channel's hooks); (2) `SeededHook.channel` + the per-hook quiet gate (`DirectorPolicy.permit_quiet` — the third policy question beside `permit_release`/`permit_climax`; the channel table lives on the Director, the policy only answers): a channeled hook's quiet gate is its OWN channel's floor against that channel's entropy, so a quiet social channel can inject while the threat channel burns — the multi-channel win the single global floor cannot express; (3) what stays global ON PURPOSE: the pacing clock (one drama arc over TOTAL entropy — PEAK/REST suppress every channel), the budget (1 release per beat across all channels, the global lowest-threshold tiebreak unchanged), the climax path (the boss gate reads total entropy), explicit triggers (D-005: causality is not channeling), the per-NPC cooldown, the dead-actor skip; (4) the per-hook opt-in mirrors the climax flag: a channelless hook keeps the v0.1 global-floor question even in a channels pack (mixed mode legal), a channel tag without the block is inert dormant vocabulary, pack lint rejects a tag naming no declared channel when the block exists | TASKS dir-4 + phases.md §3 name multi-channel as the phase-3 arc's next item after DIR-3; the L4D donor's most structural lesson is "three directors, not one" — a single god function mixing pacing dimensions is the named anti-pattern, and the donor's own adaptation note says the split lands as per-channel pack data, not code (INV-3: channel names are pack data, only the input vocabulary is closed) | the tavern pack instantiates the family (threat 3 — an escalation's weight meets its own floor, the quiet path self-blocks: `possible_document_check` fires causally, never from boredom; social 5 — the v0.1 floor carried, suspicion-bound: `guard_suspicious_of_pc`; ambient 2 — inputless noise floor, declared-but-dormant, no hook carries it yet, the owner's content call); fixture discipline holds — T1/T8/corpus byte-identical, a 10-seed day1_full A/B (channels vs no-channels arms, a runner outside the repo per Rule 9) 10/10 byte-identical: the quiet path never fires on day1_full (the D-066 all-PEAK finding; the unit tests exercise the split directly, incl. the quiet-channel-while-another-burns law and the pack's own escalation self-block); KI#63 (phases.md §3 still listed iter-38's landings as still-ahead — iter-38's doc-sync set skipped the file) opened + closed; tests 739→757 (+18 test_director.py), ruff clean; DECISIONS transiently 34/30 (collapse due at the phase-3→4 gate per D-034) |
| D-069 | 2026-09-01 | **drama-1 landed — the Paradox event grammar's predicate + weight layer** (iter-40-drama1-predicates, the phase-3 backlog's top un-gated item, the owner's "continue work per the plans" session call): (1) `core/predicates.py` (new) — the JSON predicate grammar over the folded projection: the three v0.1 leaf kinds (time / place / threshold) evaluate byte-identically through it (the parity pin replays the pack's own trigger spec), compound `all`/`any`/`not` nodes + the implicit-AND list root (the Paradox trigger body), and the generalized `prop` leaf (any entity, any prop path, four comparators; a missing prop answers honestly — False under ordering/equals, True under not_equals; the bool/int conflation guarded); loud ValueError on unknown kinds/shapes (L10: validated content — the pack lint is the first line, the evaluator the backstop); the closed leaf/compound/comparator vocabularies are single-owned there and imported by the lint; (2) `core/director.py` — triggers delegate to the evaluator; the hook weight accepts the `weight_multiplier` object `{base, modifiers[{add|factor, when}]}` (the donor's shape, exactly one of add\|factor per modifier): the entropy sensor and the channel entropies read the **effective weight** per beat (declaration-order application, `add` sums, `factor` multiplies and truncates, 0 legally zeroes; `beat_tick` threaded into both functions — a time-leaf `when` is legal grammar); `first_time_only` (the Wesnoth fire-only-once law) burns the tag after its first release: burned instances stay facts in the buffer (INV-5) but never release and never count toward entropy (un-dischargeable tension is noise, not tension); (3) pack lint: the recursive predicate shape gate (per-kind fields, empty lists rejected as dead vocabulary per L1), the weight object shape, the boolean burn flag | phases.md §3 names the Paradox grammar as the phase-3 backbone and drama-1 was its top un-gated row; honest scope: one iteration lands the grammar's FOUNDATION (predicates + weights + the burn policy) — the option blocks, the immediate/option/after lifecycle, the on_action dispatch, and the ctx scope helpers are the drama-2/drama-3 backlog rows (the underdelivery law: the remainder goes to the next iteration, never into a rushed diff); the predicate layer is the foundation all of them compose on (option availability gates, on_action triggers — one evaluator, many call sites, L13) | the tavern pack instantiates the vocabulary LIVE but inert on the committed runs: the vigil hook carries the escalation modifier (base 2 + add 2 when the watcher's suspicion ≥ 50 — the context-sensitive tension a flat int cannot express) + first_time_only (a second failure never doubles the watch); fixture discipline holds — T1/T8/corpus byte-identical, a 10-seed grammar-vs-flat A/B (125 + the nine corpus seeds, a runner outside the repo per Rule 9) 10/10 byte-identical: the escalation arm needs suspicion ≥ 50 which already saturates every quiet gate, and the vigil never releases on those runs (the D-066 all-PEAK window) — the escalation, the truncation order, and the burn law are unit-pinned instead (TEST_PLAN §6 owns the re-measure); tests 757→794 (+24 tests/test_predicates.py: the leaf truth tables incl. the parity pin + the honest-missing/bool-guard laws, the compounds, the loud backstop, the closed vocabularies; +13 test_director.py: the multiplier wiring + the pack's escalation, the time-leaf threading, the truncation order, the burn law + the repeatable-tag differential, the compound trigger, the six lint refusals), ruff clean; DIRECTOR_SPEC §3/§3a the contract owner (335/300 kept per §6.1 — the grammar tables are substance, the cruft pass ran: two measured-impact dedups to TEST_PLAN §6); DECISIONS transiently 35/30 (collapse due at the phase-3→4 gate per D-034) |
| D-070 | 2026-09-02 | **drama-2 landed — the Paradox event grammar's option layer** (iter-41-drama2-options, the phase-3 backlog's top un-gated item, the owner's "continue working per the plans" session call): (1) `director.hooks[tag].options` — an optional non-empty list of option blocks, each the closed key set `trigger \| weight \| intent \| notes` (an unknown key is a lint error, never a silent ignore): the availability gate (any drama-1 predicate spec), the ai_chance-style weight (the §3a flat/multiplier shapes, default base 1), and the payload override (`{kind?, target?, fields?}` — each declared key WHOLLY replaces the base payload key; Paradox options are complete alternative effect branches, not patches); (2) the choose step at release (`core/director.py::_choose_option` — pure): gated-off options are unavailable, a zero EFFECTIVE weight is never picked (the Stellaris factor-0 zero-out), the heaviest wins, ties break by declaration order — **no RNG**: every director decision stays RNG-free and the cross-run variety the donor's weighted DRAW provides comes from world state (the modifiers read the projection); the draw itself is deliberately excluded, as MTTH is (the TIME-1 family); (3) the deferred-release law: a hook whose options are all gated/zeroed cannot release that beat — nothing hits the door, no budget is consumed, the clock does not mark PEAK_CLIMAX for a closed boss, and the threshold tiebreak orders releasable hooks only; (4) the budget law holds — 1 release per beat, 1 IntentData per release (the chosen option's payload rides the door); (5) the lifecycle mapping: immediate/option/after → seed/choose/apply — NO literal effect blocks land (the door's commit + reaction dispatch is the after, D-037); the ctx scope helpers (`every`/`random`/`any`) did NOT ride — the option gates are single-entity predicates and runtime target picking is DIRECTOR_SPEC §9's named anti-pattern; they wait for drama-3's on_action dispatch where entity-set iteration first earns its keep (L13 honored) | phases.md §3 names the option layer as the grammar's next row over the drama-1 foundation; the deterministic-pick reading follows the repo's established director pattern (the release pick and the threshold tiebreak are already pure functions of (buffer, projection, pack)) — a weighted draw would add a stream consumer whose only gift is variety the modifiers already provide; the whole-key override over a patch-merge keeps each option a self-contained readable branch and dodges the partial-fields ambiguity; pack lint extracted the drama-1 weight block into the shared `_lint_weight_spec` (one owner of the shape, D-024) | the tavern pack instantiates the vocabulary LIVE but inert on the committed runs: the vigil's glance/stare pair (below the document-check band the glance wins the 1-vs-1 tie by declaration order — the v0.1 payload; in the band the stare's escalated 1+2 wins, the release CHOICE hardening with the world exactly as the hook's tension does); fixture discipline holds — the full suite green with zero fixture regen, a 10-seed options-vs-no-options A/B (125 + the nine corpus seeds, a runner outside the repo per Rule 9) 10/10 byte-identical (the choice resolves only at release; the vigil is quiet-path-only in the D-066 all-PEAK window, the document-check carries no options); the pick laws unit-pinned (+18 tests/test_director.py: the v0.1 payload path with the id-sequence pin, the pack wiring + the live glance release, the weight/tie/escalation-flip/zero-out/deferred-release laws, the whole-key override, entropy untouched, the closed-boss no-mark law, the burn through the pick, the seven lint refusals), tests 794→812, ruff clean; DIRECTOR_SPEC §3b the contract owner (400/300 kept per §6.1 — the grammar tables are substance, the iter-40 precedent); DECISIONS transiently 36/30 (collapse due at the phase-3→4 gate per D-034) |
| D-071 | 2026-09-02 | **drama-3 landed — the Paradox event grammar's on_action dispatch** (iter-42-drama3-onaction, the phase-3 backlog's top un-gated item, the owner's "continue work per the plan" session call): (1) `core/onaction.py` (new) — the dispatch: a pack table (`on_action` in rules.json) keyed by COMMITTED event type; event X commits → content reacts, inside `_react` (the canon door, D-037), APPENDED after the hardcoded system reactions (suspicion → arrest → telling) and before the director seeding — the donor's append-not-overwrite composition (vanilla logic runs, custom entries add, never replace; every entry of the keyed LIST dispatches — the list IS the append semantics); (2) each entry the closed key set `scope \| gate \| event \| state \| actor \| target \| notes`: the `witnesses` scope (the event's OWN knowledge records, deduped by first occurrence, event order — the named "every NPC who witnessed X"; the EXPLICIT ctx argument, no implicit `this`, no inherited scope), the per-entity quantified gate ({prop, comparator, value} evaluated with the CANDIDATE as the argument — the spec carries NO entity field; predicates.py's comparison semantics: honest-missing, the bool/int guard), the reaction event, and the alarm-shaped state change ({prop, add}: ONE event per entry, clamped numeric deltas on relations.scale — the one numeric scale; a candidate without a numeric home dropped per the suspicion law; a clamped no-op dropped per KI#13; an empty gated scope emits NOTHING); (3) the termination law: the reaction event carries no knowledge and no hooks (the system reactions skip a knowledge-less event; deferred consequences ride the actions' own hooks, D-005) + the ONE-HOP LINT (no table key may name a reaction event the table emits — a second-order declaration is a load-time error; the cascade terminates by construction, no runtime guard needed); (4) lazy generator: each entry's draft reads the projection as left by the previously committed entries (the KI#13 discipline); pure per INV-2 — a function of (pack data, projection, record), no RNG, no clock, no stored choice | phases.md §3 names the on_action dispatch as the grammar's last row; the scope design follows the phases.md law ("scopes become an explicit context argument minus the implicit-this footguns") — the entity is an ARGUMENT, never a string placeholder or an inherited scope; the one-hop lint over a runtime depth guard: second-order reactions have no v0.1 semantics (no delay machinery — TIME-1), so declaring one is a pack BUG caught at load, loud, zero runtime cost; the alarm shape (one event, many state changes) over the suspicion shape (per-knower events): a leaf reaction needs no per-entity cause chain — its follow-ups would be hooks (D-005) or nothing | the pack's declaration is DORMANT vocabulary, the iter-38 climax-flag pattern: `document_check` → `crowd_wary` (every witness of the public check grows warier — the crowd-reaction layer the token-keyed crime system does not cover) — no action emits the key, so nothing fires on ANY run until the action lands (§11, the owner's content call). The alarm panic echo (witnesses of `alarm_raised` gain fear — the through-the-walls law) was probed LIVE and REVERTED: it fires on the phase-1 corpus's distraction-fire beats, and the corpus's beat anchors and event-count pins are committed fixtures (§8 — a fixture rewrite is owner-gated); recorded in DIRECTOR_SPEC §11 with that content decision. Fixture discipline holds: the full suite green with zero fixture regen, a 10-seed on-vs-stripped A/B (125 + the nine corpus seeds, a runner outside the repo per Rule 9) 10/10 byte-identical (TEST_PLAN §6 owns the re-measure); the dispatch laws unit-pinned through linted pack variants (the iter-36 mutated-pack pattern) (+24 tests/test_onaction.py: the witnesses scope, the clamped delta + no-op drop + numeric-home law, the quantified gate incl. honest-missing/bool-guard, the event shape + the actor/target vocabulary + the location fallback, the crowded-room live path, the append composition + the lazy-group law, the empty-room inertness, the byte-identity arm, the twelve lint refusals), tests 812→836, ruff clean; DIRECTOR_SPEC §3c the contract owner (469/300 kept per §6.1 — the grammar tables + landing records, the iter-40/41 precedent); DECISIONS transiently 37/30 (collapse due at the phase-3→4 gate per D-034) |

| D-072 | 2026-09-02 | **content-1 landed — the document_check action (the §11 content call resolved)** (iter-43-doccheck-live, the owner's blanket quality directive "quality, no crutches, do as best, the fork resolved by the maximum-plus variant"): (1) the action — `document_check` in actions.json over the STANDING `inspect` resolver (a checked interaction producing knowledge, zero state change of its own — the registry grows only with a new mechanic, D-031): the check `perception_vs_social` (pack data in checks.kinds: the actor's scrutiny vs the target's composure), both branch types per the house pattern (take/take_failed — `document_check` / `document_check_failed`; a `{passed?…}` conditional template was probed and REJECTED: the chronicle context is flat, the nested outcome.check.passed is unreachable — the branch types are the honest carrier), co-location + npc-kind preconditions, the success-branch actor record `papers_unsatisfactory` (exact) the ONLY crime-mapped token (EPIST-1: the arresting watcher escalates on what THIS knower holds — +25 in suspicion_sources, the arrest attempt + resolution ride the standing crime machinery), the room's `papers_demanded_of_{target}` sighting deliberately un-mapped crowd memory (rumor material + the on_action witnesses); (2) the watcher PAIR — the institutional consequence is TWO hooks (`possible_document_check` + `possible_document_check_relief`, one per rotation participant), each: the plain threshold trigger (its own watcher's band 50 — the causal condition), the OPTION-GATED confrontation (place: the watcher at the duty post + prop: the stranger present — the drama-2 deferred-release law: a closed option means the hook WAITS, nothing hits the door, no budget, no PEAK_CLIMAX mark), climax: true (the DIR-3 boss path consults the same gate — a closed boss never burns), first_time_only (one check per watcher per run — a talked-down verdict never re-rolls), the real payload {kind: document_check, target: pc_01} (the stub wait is gone); (3) the LIVE crowd reaction — the on_action table now keys BOTH branch types → crowd_wary (+5 per witnessing home-holder; the checked stranger dropped — no home, the suspicion law) | the naive single-hook design was probed and REJECTED before landing: the threshold-only trigger releases the beat the rotation empties the seeded watcher's post — the door would honestly reject the world-impossible attempt and (worse) the climax path is trigger-blind, so the boss flag would BURN on a rejected no-op (the L4D Tank spawning inside a wall); the option gate is the committed D-070 law designed for exactly this — every release path consults the confrontation world, and the relief's half fires AT the rotation beat right after the briefing (the corpus session-10 shape: "a briefing already in them, the look of a man reading a list of descriptions") while Doren's half waits all run (his post emptied the beat his band opened) — the institution, not the man, checks the stranger; the two-hook shape follows D-006 (spread between watchers is transfer events — the briefing opens the relief's band) and §9 (the release targets the hook's pack-declared target_npc, never a runtime pick) | the boss beat is now LIVE canon: measured, seed 19 runs the full ladder (the relief's check → the verdict token +25 → the arrest attempt → caught, irreversible T4 → the crowd +5); seed 2 releases through the CLIMAX PATH with the band never open (the L4D2 three-intensity rule live, PEAK_CLIMAX marked, one rung short of the arrest — the boss without the evidence band); seed 93/125 talk the check down (no escalation, the crowd layer only). Corpus cost paid in-iteration: 4 seed-93 cases re-distilled (the iter-15 regen precedent — the claims re-pinned to the new world, the case `document_check_releases_at_the_rotation` now pins the REAL release incl. the crowd event + the crowd's knowledge token). A/B re-measured (TEST_PLAN §6): 1/10 day1_full seeds fire the check (the `take` shifts each seed's stream — seed 125 the one), diverging by exactly the crowd_wary events, 9/10 byte-identical. ZERO engine edits for the whole boss beat — the grammar's three layers (predicates/options/on_action) + the crime machinery + the climax layer all earn their first LIVE consumer in one landing (D-069/D-070/D-071/D-067 composed); the only core change is the hook-payload target lint (pack.py, mirroring the option lint — the new payload shape earns its lint). +10 tests/test_doccheck.py (the ladder both branches, the deferred + the climax-path releases, the pack declarations, the door rejections, the chronicle lines), tests 836→846, ruff clean; DECISIONS transiently 38/30 (collapse due at the phase-3→4 gate per D-034); DIRECTOR_SPEC §3c/§5/§11 + MVP_SCOPE §7 synced |
| D-073 | 2026-09-03 | **social-1 landed — secrets & leverage as fact clusters** (iter-44-social1-leverage, the phase-3 backlog's top un-gated item, the owner's "continue work per the plan" session call): (1) `core/leverage.py` (new) — the secrets reaction: a committed event's knowledge records ride the standing reaction cascade; a record whose token the pack's `rules.json::secrets` registry declares, held by a NOVEL knower (the crime reaction's novelty law, `view.holds(..., before_source=...)`), kind npc, holder != subject, mints ONE `leverage_gained` fact event per (knower, token) pair — actor = holder, target = subject, the cause chained to the event that taught them, the acquisition fidelity riding the cluster, `expires_at = t + expires_ticks`; the loop wires it in `_react` after the on_action dispatch (the same append composition), before the director seeding; (2) the fact-cluster laws: NO knowledge (the social fact, not the epistemic one — the subject never learns of it), NO hooks (D-005), NO state changes — the reaction systems skip it by construction (the one-hop law's sibling, the cascade terminates); the director buffer untouched (L6); the EXPIRY is a read-side window, never an event (INV-1 — the fact is immutable, its liveness is a fold: `live_leverage` the single read owner, live strictly before `expires_at`; the SPEND joins with the first consumer as a new event naming the cluster's id — social-1b, the fold's `source` already supports it); (3) the name decision: the repo's "hook" word is the director's (SeededHook, D-005 — a deferred consequence tag); the CK3 hook is our LEVERAGE — a social fact, never a release; (4) the pack registry: the closed key sets (`event\|tokens\|notes`; `subject\|type\|expires_ticks`), the subject must be an npc id, and the token must be MINTABLE — a literal `knows` value declared at one of the three birth sites (actions/expectations/transition layers; the reaction systems only transfer existing tokens, so that IS the vocabulary; templated tokens ineligible by construction: a secret's subject is a fixed entity, a templated token's subject varies with the world); (5) ONE live token: the theft secret `figure_reaching_for_purse` over pc_01, type blackmail, 720 ticks — the steal failure's total branch mints the whole room, the partial branch the mark; the document_check verdict (`papers_unsatisfactory`) deliberately un-declared (institutional — the crime ladder owns that rung; a personal-leverage twin would double-count the same knowledge); `leverage_gained` joins story-critical events (the tale line renders: "X now holds something over Y") + `metrics.system_of_type` (knowledge+relations) | phases.md §3 P3a names the CK3 `add_hook` precedent ("a hook IS an event with target, type, expiry tick, cause — not mutable state"); the adaptation keeps the cluster SHAPE and swaps the mutable hook-list for event-sourced facts — exactly INV-1's shape (the expiry-as-fold mirrors D-007's fidelity-as-data: derived, never stored); the novelty law reuses the crime reaction's own `before_source` semantics (one owner of the concept, D-024); the told-secret conferral is free (the transfer event's records are knowledge like any other — the briefing mints the relief's cluster, the institution's knowledge becoming its leverage); the ambient content-3 row did NOT ride (DIRECTOR_SPEC §11's "with social-1 or its own row" — a director-hook content decision is not fact-cluster work; its own row, the iter-44 call) | measured live: seed 93 — the total failure mints the room (4 clusters cause-chained to the pickpocket, fidelity partial) + the rotation briefing mints the relief (vague, two decay steps down); seed 19 — two failures against the same mark, ONE mint round (the novelty law live); the 10-seed day1_full A/B (TEST_PLAN §6 protocol, 125 + the nine corpus seeds): 1/10 seeds mint (125 — the same hot seed as iter-43's check; the same steal failure drives both), the divergence EXACTLY the leverage events, 9/10 byte-identical; corpus regen paid in-iteration (the iter-15/43 precedent): 9 narrator-beats cases re-distilled (the seed-93 room + the seed-15 second-steal family — the pinned claim ids re-pinned to the shifted stream, a re-distill runner outside the repo per Rule 9); KI#64 opened+closed (KI#63's five-iteration deletion drift + the FAQ 21/20 §6.1 keep); +24 tests/test_leverage.py (the cluster shape, the novelty/holder/dedup/cascade laws, the told-secret path, the expiry boundary, the chronicle line, both A/B arms, ten lint refusals), tests 846→870, ruff clean; DECISIONS transiently 39/30 (collapse due at the phase-3→4 gate per D-034); MVP_SCOPE §5/§6 + AGENT_NAVIGATION §1 + TASKS + STATUS + worklog synced |
| D-074 | 2026-09-03 | **social-1b landed — the coerce door, the leverage spend** (iter-45-social1b-coerce, the phase-3 backlog's top un-gated item, the owner's "continue work per the plan" session call): (1) the spend law (INV-1): the spend is a NEW EVENT naming the spent cluster's id in `outcome.cluster` — never a mutation; the fold kills the cluster AT the spend's tick (one secret buys one play — the twin of the expiry boundary law), a spend naming an unknown id is inert log noise, and a `secrets` block without `spend_event` folds spends away entirely (the declaration is the gate, INV-3); (2) the door: `leverage_over` — the intent door's FIRST fold-reading precondition; the facts are `live_leverage` read AT THE CALLER'S OWN TICK (the door at the entry tick, the urgency gate at the beat, the OCC re-check at completion — a tick-windowed precondition is never evaluated on stale facts), threaded as duck-typed data (`first_failing(..., facts=...)`) so the import direction stays one-way (leverage → intent — `LEVERAGE_TEST` lives with the closed test set, core.intent never imports core.leverage); (3) the window law: the OCC re-check runs UNCONDITIONAL for leverage-carrying intents — the projection is event-driven but the window is tick-driven (it can close between accept and completion with no event committed, which the `event_count > based_on` guard would miss); the rejection keeps the standing `projection_moved` reason (the derived liveness moved) while `occ_breaking_cause` EXCLUDES the window test — a window close never attributes a breaking event the log does not hold (cause = the last committed id); (4) the loop stamps `cluster\|secret\|type` onto the spend event from the live fold — the arrest-resolution precedent (loop-side, event-type-keyed, pack-declared decoration via `secrets.spend_event`), the resolver stays fold-blind (no ResolverFn signature change for 13 resolvers — the L13 cost gate); (5) the balance: pack data on the action (the status_effects precedent — "the delta lives here beside its action"), subject-directed pair-axis shifts ({axis, delta} — an absent axis materializes at neutral+delta, the `trust_toward` read law made a write; clamped to the scale; a clamped-to-unchanged delta the quiet beat); v0.1: trust −25 (D-030's break-fast) + fear +25; a status/urgency nudge stays recorded-not-built (the first-consumer law, content-4's call); (6) no check (the leverage IS the power — a resisting subject is the option layer's future), no knowledge (the corner is quiet — the cascade terminates exactly as the mint's does) | the resolver/signature alternative was weighed and rejected: threading the facts into `ResolverFn` costs 13 signature edits for one consumer, while the loop already owns the log (the arrest-resolution precedent is the exact shape — event-type-keyed, pack-declared, stamped at commit); the door-resolved cluster reference (the texture pattern) was rejected too: author-provided ids invert the trust model and the OCC stamping would trust them blindly; the dormancy call: the committed pack declares the action but NO driver carries it (the iter-38 climax-flag / iter-42 on_action pattern) — landing an urgency entry adds a per-beat d100 draw and shifts the SUBSTANTIVE stream for every run, re-distilling all 105 corpus cases for one content row; the corpus staying green IS the byte-identity proof, and the live-fire tests run on crafted pack copies (the hard_pack precedent) | measured live (crafted driver, probability-100 urgency, seed 93): the room failure mints at t=5, the beat at 360 enqueues the drunkard's coerce at the entry tick 405, the door accepts (fold live), completion at 408 — `outcome.cluster` names the drunkard's own cluster, pair trust None→25 / fear None→75 on pc_01, importance medium, one coerce, the drunk dead in the fold while the room's four clusters live on; the second beat stays silent (the gate sees nothing live); the window-close probe (crafted expires_ticks = accept−mint+2): the completion rejects `projection_moved / actor.leverage_over` with the cause chained to the last committed canon; the player's leverageless coerce on the UNMODIFIED pack: a plain door rejection (the dormancy proof at the door level); +25 tests/test_coerce.py (the fold's spent law, the door unit, the resolver's balance/clamp/quiet-beat, the urgency gate, the e2e spend + the one-play law + the window close + the chronicle line, the declarations, 8 lint refusals incl. the spender-must-gate-on-leverage contract), tests 870→895, ruff clean; KI#66 opened+closed (cli/main.py's stale "13 actions" — the iter-43 §2 miss family); DECISIONS transiently 40/30 (collapse due at the phase-3→4 gate per D-034); INTENT_SCHEMA §3/§4 + MVP_SCOPE §2/§5/§6/§7 + TASKS + AGENT_NAVIGATION §1 + STATUS + worklog synced |
| D-075 | 2026-09-03 | **social-2 landed — the psychological echo, the residue read model** (iter-46-social2-echo, the phase-3 backlog's top un-gated item, the owner's "continue work per the plan, quality over speed" session call): (1) `core/echo.py` (new) — the P3e sketch's law held literally ("not new data, a behavior modifier computed from the existing log"): a PURE read-side fold over the knowledge view, score(who, axis) = Σ over the knower's records of pack-declared token valence × (fades_ticks − age) // fades_ticks × the fidelity percent — integer, floored, dead AT the boundary tick (the leverage expiry law's twin, D-073), a re-learned token RENEWS (records sum: heard twice, felt twice), clamped to the pack scale, zero pairs absent from the tuple (a missing pair IS zero — the world's honest answer, never an error); the echo writes NOTHING (no events, no knowledge, no hooks, no state, no templates, no metrics — INV-1 by construction, the social family's cleanest landing: D-073 minted facts, D-074 consumed them, the echo never touches canon) and runs ONLY for intents whose preconditions carry the echo test (the iter-45 laziness law — every other action pays nothing); (2) the consumer: `echo_at_least` (axis, value) — the closed test set's 17th, the P2b behavior gate (the sketch's own dependency: "the goal/urge ticker acts on the echo"); the scores are duck-typed data (`first_failing(..., echoes=...)` — who/axis/score attributes, the import direction one-way: core.intent never imports core.echo); (3) the window law GENERALIZED: `WINDOWED_TESTS` = (leverage_over, echo_at_least) — truth driven by TIME, not event application; the OCC re-check runs unconditional for the family and `occ_breaking_cause` excludes the family (a decay close never attributes an event the log does not hold); the loop computes each fold lazily per evaluation point (`_fold_reads`: the door at the entry tick, the beat gate at the beat tick, the completion at its own — the two channels side by side, each paid only when asked for); (4) the pack: `rules.json::echo` — scale [0,100], fidelity_weight 100/50/25 closed against the pack's own fidelity chain (a missing step would KeyError at the fold — the KI#15 family, refused at load), four tokens = the day-1 walkthrough's residue (the theft sighting dread 25/wariness 35 fades 720; the fire-starting sighting dread 60/wariness 30 fades 1440 — "the watcher who saw it is jittery next morning"; the inferred absence wariness 30; the bar noise wariness 15 fades 360), papers_unsatisfactory UNdeclared (the D-073 institutional call — the crime ladder owns that rung's behavioral escalation); lint `_echo` (the closed key sets, the chain coverage, the mintable-token law reused from the secrets lint, non-zero valence — a sign is legal, the pack declares which) + the requires cross-lint on BOTH sides (actions + urgencies: axis must be declared, value strictly above the scale floor and at or below the ceiling — a gate that can never pass or never fail is dead vocabulary, refused at load); (5) the L6 fence (the design's hard edge): the echo is per-NPC valence over the NPC's OWN records, never player-adapted, and NEVER an entropy input — DIRECTOR_SPEC §4 reads observable state only, a knowledge-derived score is not observable state; the director and the chronicle are untouched by construction (the FAQ carries the pitfall entry — wiring echo→entropy would be an invariant-grade bug dressed as a tuning change) | the check-modifier alternative (skill_total reading the echo) was weighed and rejected: threading the fold into every skill_total call site is the L13 cost with a hypothetical consumer, and it would double-count the alarm's own fear spike (status fear already decays canon-side — the echo is deliberately NOT status); the director-entropy alternative is the L6 violation above, rejected flatly; the state_changes sourcing of the sketch ("knowledge.records + state_changes") stayed recorded-not-built (the first-consumer law: both P3e examples are knowledge-driven, and every v0.1 socially-loaded state change also births knowledge records for the affected party — the state axis is redundant until an event that changes an NPC without teaching anyone anything); the dormancy call follows iter-45 exactly: no action or urgency carries echo_at_least in the committed set (an urgency entry would shift the SUBSTANTIVE stream and re-distill all 105 corpus cases — the live driver is the content-5 row), and unlike D-073 the declaration itself is INERT (the mint was event-driven and paid 9 re-distilled cases; a read model costs nothing) | measured live (crafted driver, probability-100 urgency, seed 93, tuned token dread 100/fades 720): the drunk's partial sighting at t=5 reads dread 25 at the beat-360 gate → the driver's look_around fires ONCE (t=406, the entry-tick enqueue law); the beat-720 read is 0 — the roll silent ("events that happened but no longer matter" stop driving behavior, the P3e headline live); the action-gated window probe (look_around carrying the gate, ticks 400 — the coerce shape: the door and the OCC re-validate what the ACTION declares; an entry-only gate stays silent at the beat while an action gate gets the full window law, the pack author picks per gate): the door accepts at 405 (residue 22 at the caller's own tick), the completion at 805 rejects projection_moved / actor.echo_at_least with the cause chained to the last committed canon; the committed valence reads real residue (the drunk: dread 6 / wariness 8 at the beat-360 read — the table is live-readable while dormant); the 10-seed day1_full A/B (echo block vs a copy without it, a runner outside the repo per Rule 9): 10/10 byte-identical, ZERO corpus regen (the first social-family landing without one); +29 tests/test_echo.py (the fold laws: linear fade/boundary death/fidelity scaling/renewal/clamp/zero-absence/order; the door unit + the attribution exclusion; the urgency gate's silence; the e2e driver + the fade law + the window close + the committed residue pin + the byte-identity arm; eleven lint refusals incl. the dead token, the dead gate both sides, and the no-block gate), tests 895→924, ruff clean; KI#67 opened+closed (README's Status froze at iter-43 — the KI#66 family, iter-44/45 skipped the README sync), KI#65/KI#64 deleted per AGENTS §5; DECISIONS transiently 41/30 (collapse due at the phase-3→4 gate per D-034); INTENT_SCHEMA §3/§4 + MVP_SCOPE §5/§6 + phases.md §3 + TASKS (social-2 done, content-5 opened) + AGENT_NAVIGATION §1 + README + STATUS + worklog synced |
| D-076 | 2026-09-03 | **arc-1 landed — arcs & tension shaping, the release-chain layer (P3c's engine half; iter-47-arc1-chains, the phase-3 backlog's top un-gated item — the LAST engine row of the phase-3 build column "stagnation detector, complication buffer, arcs")**: (1) the pack shape — `director.arcs.<name>` = {members (>= 2 unique DECLARED hook tags), min_gap_beats (int >= 2 — the 1-per-beat budget already enforces 1, a smaller gap is dead vocabulary, refused at load), notes}; membership is ONE-SIDED — the members list IS the declaration, a hook spec carries no arc key (the single owner of the fact, D-024; the smallest possible diff: no SeededHook change, no loop change, no schema change, no new events); pack lint: the closed key set (ARC_KEYS, the option-block precedent), a member naming no declared tag, a doubled member, a tag in two arcs (ambiguous order) — all load-time refusals; (2) the ORDER law — a member tag is a release candidate only while it is its arc's CURRENT member (a per-run cursor: the first member not yet released, folded state like the drama-1 burn set, INV-2); the chain gates candidacy on ALL release paths, EXPLICIT TRIGGERS INCLUDED — an arc is pack-declared CAUSALITY (a second beat that presupposes the first), not pacing; D-005's ungated-explicit law is about the CLOCK, and the chain is not the clock (the distinction is the design's load-bearing wall: without it the arc degenerates into another intensity band and chains nothing); (3) the GAP law (the tension-shaping half) — the current member may not release via the quiet or climax path within min_gap_beats of the arc's previous member's release (the chain's beats march, they do not dump); explicit triggers bypass the gap (causality is not pacing — the mid-rest law's twin); (4) the ENTROPY MIRROR — instances of PASSED members (position < cursor) never release and stop counting toward entropy (and the channel entropies — one law, both views), the first_time_only burn law's twin: one play per arc beat, un-dischargeable tension is noise; CURRENT and FUTURE members count normally — the seeded buffer's meaning is UNCHANGED (a fully-seeded chain reads its whole weight until it marches; the D-065..D-072 tuned numbers stay valid); a stalled arc's future members keep counting exactly as any permanently-blocked hook does today (no new hazard, honestly recorded); (5) the cursor advances at emit-time inside _mark_released (the budget/cooldown law: a door rejection still spends the member's beat — attempts are facts); composes with channels (AND), the climax flag (boss = current + climax path + gap open), options (the pick rides the member's release), first_time_only (orthogonal) | the two-sided membership alternative (a hook-level `arc` key) was rejected: two declarations of one fact drift (the KI#66/KI#67 family — every count restated in a second place went stale); the "gap gates explicit too" alternative was rejected on the D-005 reading (causality is not pacing — a trigger-driven successor firing mid-gap is the world forcing the issue, the exact law that lets consequences fire mid-rest); the "passed members keep counting" alternative was rejected as the burn-law inconsistency (un-dischargeable tension is noise — the v0.1 law already says so for burned tags); the "exclude future members from entropy" alternative was rejected because it silently re-tunes every standing floor (peak 25 / climax 75 read hook weights — the seeded buffer's meaning is not this layer's to change); the STALL design: a member that can never release parks the chain (later members never fire, their weights stay in entropy like any dead-target hook today) — chosen over auto-skip/re-seed because silent recovery is the L4D soften-the-canon anti-direction and the Generative Agents re-plan shape needs a content consumer first (recorded-not-built, DIRECTOR_SPEC §11, the first-consumer law); the dormancy call follows iter-38/42/45/46 exactly: NO chain in the committed pack — the naive watcher-pair chaining was probed live and REFUSED as the live driver (the D-076 e2e: on seed 125 day1_full the corpus-pinned relief release would be held forever by a predecessor that never fires — "his post emptied the beat his band opened", iter-43; the driver needs a first member that is live on the runs it targets — content-6's call, with the corpus regen any release-stream change implies) | measured: the 10-seed day1_full A/B (HEAD vs the arc machinery on the committed pack, a runner outside the repo per Rule 9) 10/10 byte-IDENTICAL, zero corpus regen (a pack without director.arcs runs the v0.1 release path — the guarded no-op view split keeps the hot path untouched); the e2e probe (crafted pack copy, the watcher pair chained gap 3, the REAL loop): seed 125 plain = director_0000 at t=734 (the corpus pin) vs chained = NO release all run (the order law holding a firing explicit trigger through the real beat cycle — the stall semantics live); the unit suite pins every law on mutated packs: the reordering differential (chained vs unchained, same hooks), the explicit hold + the mid-gap bypass, the gap spacing, the future-counts (a successor's 25 alone holds PEAK — the clock pin) and the passed-excluded (the quiet-path probe release — the arithmetic counterfactual), one-play-per-beat, the climax path (non-current boss held, explicit-first, gap inside the peak, PEAK_CLIMAX mark), director-off, the empty-block inert law, eight lint refusals; +19 tests/test_director.py, tests 924->943, ruff clean; the Alien three-axis unknown-axis conflict (phases.md "may pace against the gap" vs DIRECTOR_SPEC §4 "observable state only, L6" — the iter-46 echo FAQ's exact fence) recorded in §11 for the owner to resolve, NOT silently; DECISIONS transiently 42/30 (collapse due at the phase-3->4 gate per D-034); DIRECTOR_SPEC §3d + §11, MVP_SCOPE §5/§6, TASKS (arc-1 done, content-6 opened), phases.md §3, README, AGENT_NAVIGATION §1, STATUS, worklog synced |
| D-077 | 2026-09-03 | **content-2 landed — the alarm panic echo, the through-the-walls law LIVE** (iter-48-content2-alarm-echo, the phase-3 backlog's top un-gated item, the owner's "continue work per the plan, quality over speed" session call — the pre-iter-48 DIRECTOR_SPEC §11 first row closing): (1) the landing shape — ZERO engine edits, one `on_action` entry (`alarm_raised` → `panic_ripple`, scope `witnesses`, state `status.fear` +10), a story-critical event type with its own chronicle line ("Panic ripples through the walls of {location}." — static, no cosmetic-RNG draws), and the 7-case corpus re-distill the row always implied (the content-1/iter-43 precedent — the last content row that owed one); (2) the DELTA calibration: +10 is the CONTAGION half, deliberately a quarter of the hardcoded +40 direct spike (the alarm's own fear_spike — the transition layer's number, untouched); the occupants compound 40→50 (the fire SEEN and the panic HEARD are two stimuli, second-order contagion on top of first-order shock — the L4D panic-event family's intensity escalation), the cause actor 0→10 (he hears his own shout — the corpus's own the_alarm_is_heard_by_the_cause_actor pin made a fear fact), the distant adjacent hearers would start at 10 (structurally empty on day-1 runs per the tune-3 static-placement finding — the through-the-walls half is declared law, dormant until NPC movement exists); (3) NO gate (the design call): a shout of fire unsettles everyone who heard it — the alarm's own knowledge resolution already decides who that is; a position-keyed exclusion (only-the-adjacent) would hardcode the location into pack data (pack-brittle — a second pack, another fire, the entry misfires) and an at_most-0 fear gate would conflate "already afraid" with "at the fire" (a crutch that misbehaves the first time an NPC carries unrelated fear); (4) the decay law: the echo commits at the alarm's OWN tick — the fear-axis decay baseline stays where the alarm set it (measured: beat-360 decay 50→46 / 10→6, the 354-tick proration — never a re-based double decay); (5) the regen protocol (the iteration's engineering core, reusable for content-4/5/6): a fixed-point runner outside the repo (Rule 9) re-distilling through the REAL mediator cycle — the old reference = the HEAD-pack green replay (kept per-beat pre-reply event snapshots + the STATUS LADDER: a corpus beat may be a DESIGNED refusal — only the last beat is pinned by the corpus test; outgoing_guard beat 0 is the blind-guard probe and stays 'regen' by design), sweeps re-pin state claims (the fold at each beat's reply gate, trustworthy only when the beat's predecessors landed their HEAD statuses) and event ids (the pristine-old → current-new alignment, idempotent — re-pinning from a re-pinned id never settles), the deliberate echo claims added AFTER convergence (they reference NEW-stream events the old-id alignment must never touch), a confirmation sweep, then the verify pass harvesting the BEAT needles/last_event_type/call_contains | the double-count alternative (gate out the occupants) was rejected above (position gates hardcode the location, fear gates conflate unrelated state); the delta-0-for-occupants "pure through-the-walls" alternative was rejected because the v0.1 gate grammar cannot express "not at the source's location" without static values — the honest reading of §11's own wording ("witnesses of alarm_raised gain fear") is the un-gated scope; the chronicle-importance alternative (leave panic_ripple out of story_critical_events — invisible, low) was rejected on the crowd_wary precedent: the panic IS a tale beat, the day's arc reads "Someone shouts: fire! → Panic ripples through the walls → The fire spreads" (T7's readability bar); the system_of_type entry was deliberately NOT added (the crowd_wary precedent — the state changes already classify the event to one system, M1 untouched by the reaction layer) | measured: the corpus scope exactly 7 diverging cases (the probe: 118 committed replays, 9 fire-family streams diverge, 7 pin-break — the 2 designed-refusal cases stay green untouched); the fixed-point regen converged in 2–3 sweeps per case, ladder shapes preserved; the day1_full 10-seed A/B (committed vs the alarm-entry-stripped linted variant, a runner outside the repo per Rule 9) 10/10 byte-IDENTICAL with zero alarm events in any log (the empty-backyard law: the gate script's arson burns the yard the PC stands alone in — the iter-42 dormant-arm shape, now for a LIVE entry; the echo's live divergence lives in the corpus's own seed-33/93 occupied-room scripts); the live behavior pinned in +8 tests/test_panic.py (the compounding + the cause actor + the one-hop termination + the decay baseline + the chronicle line after the shout + the empty-room law + the stripped-entry byte-identity + the declarations + the corpus-evidence guard), tests 943→951, ruff clean; test_actions' folded-fear pin updated 40→50 (the intentional world change, the iter-43 test-actions precedent); DECISIONS transiently 43/30 (collapse due at the phase-3→4 gate per D-034); DIRECTOR_SPEC §3c + §11 + the header, TEST_PLAN §6, TASKS (content-2 done), phases.md §3, AGENT_NAVIGATION §1, README, STATUS, worklog synced |
| D-078 | 2026-09-03 | **content-4 landed — the coerce driver, the drunkard's roll re-armed** (iter-49-content4-coerce-driver, the phase-3 backlog's top un-gated item, the owner's "continue work per the plan, quality over speed" session call — social-1b's live content set, the fact cluster's first LIVE runtime consumer): (1) the landing shape — ZERO engine edits, the drunkard's EXISTING urgency entry becomes the coerce carrier ({kind: coerce, target: pc_01}, requires same_location + leverage_over, weight 40 kept); the idle-wait nudge dies (his static placement is what kept him in-scene all along — the tune-3 law; the wait was noise-floor events only); (2) the REPLACEMENT law (the iteration's load-bearing discovery): urgency rolls share the substantive stream with the checks, so the per-beat draw COUNT is what positions every later check draw — an ADDED entry shifts them all (measured: 3 corpus ladders flip, the document_check fail branch — the corpus's ONLY pin of it — among them; a ladder flip is a coverage LOSS, not a re-pin); keeping the entry's slot and weight keeps the draw count and the checks' positions — the corpus's designed ladders hold by construction, and the driver's divergence is exactly its own events (the disappeared waits + the coerces + the seed-125 expired-card door rejections — the tick-window law live: the cluster dies at 729 between the beat-720 gate pass and the door's re-read); (3) the corpus re-distill through the rebuilt fixed-point runner (identity-proved FIRST — 105 cases, the pristine corpus its own fixed point, zero false re-pins; the corpus test green after the write is the fixed-point verdict): the doccheck case's 2 id re-pins, the silent_second tail re-pin (crowd_wary→coerce — the spend lands inside the final door batch, unclaimable at its own gate), the deliberate pins on outgoing_guard beat0 (the coerce by id + pc_01's pair axes trust 25 / fear 75 — what the spent card bought, the iter-48 pattern), 4 seed-93 cases coerce in total; (4) the live-fire tests moved onto the committed pack (the iter-45 armed_pack append pattern retired — the committed entry IS the driver; the shrunk-expiry window probe keeps a minimal one-knob pack copy) | the ADDED-entry alternative was measured and REFUSED (the 3 flipped ladders — losing the doccheck fail-branch corpus pin to save one idle wait was the exact anti-quality trade); the engine-side urgency-stream split (a third named stream, the assure scoping law reworked) was recorded-not-built as the engine-2 infra row: it buys permanent add-safety but pays a one-time migration flip anyway (the checks shift by the removed draws) and touches INV-2's machinery — the owner's call, not smuggled into a content row; the director-hook alternative was rejected (the drama-1 predicates read the projection, the leverage fold is not projection state — a hook cannot gate on the card, and an ungated hook release would burn door rejections as noise); the status/urgency nudge (the row's optional half) was rejected on the action's own v0.1-buy note — the pair shifts carry the drama, the first-consumer law stands | measured: the identity check 105/105 zero-change; the corpus re-distill 2 mechanical re-pins + 1 tail re-pin + the deliberate pins, ZERO ladder flips (vs the add-arm's 3); the corpus test 127/127 green through the real cycle; the day1_full 10-seed A/B 2/10 byte-identical (15, 85 — the drunkard's roll misses at every crossed beat), the 8 diverging seeds change by exactly the drunkard's waits gone (id shifts only, zero outcome flips) + seed 125's two intent_rejected (failed_test actor.leverage_over — the expired card, the world's honest record); +1 corpus pin test (the spend by id + the pair axes + the silent_second tail), tests 951→952, ruff clean; the world-change pins re-pinned: test_leverage boundary 5→4 (the drunkard's cluster spent before expiry) + the births A/B filtering the coerce (the facts' divergence now includes their consumption — the INV-3 gate narrowed honestly), the urgency tests' seeds re-probed (the maid carries the plain-roll pins; the drunkard's roll measured through the gate with/without facts), the parse corpus's wait-720 count 16→15; DECISIONS transiently 44/30 (collapse due at the phase-3→4 gate per D-034); TASKS (content-4 done, engine-2 opened), TEST_PLAN §6, phases.md §3, README, STATUS (header + the FAQ regen entry actualized with the iter-49 laws), worklog synced |
| D-079 | 2026-09-03 | **engine-2 landed — the urgency-roll stream split, per-entry streams** (iter-50-engine2-stream-split, the owner's "quality over speed" fork call resolving the engine-2 owner-gate: spend the iteration now, buy the permanent add-safety — the content-5 fork "replacement law vs engine-2 first" taken the quality way): (1) the LAW (INV-2, the add-safety clause): every pack urgency entry rolls d100 on its OWN content-addressed stream `urgency:<npc>:<kind>` (`core/rng.py::urgency_stream_name` — the single owner of the naming grammar; lazily registered on first use, so the closed-set tripwire survives verbatim for every non-family name; the (npc, kind) pair is pack-linted unique, making the name injective) — an added or removed entry shifts NEITHER a later check draw (the substantive stream is untouched) NOR another entry's rolls (each entry's stream is its own). The nesting law reworked: an urgency-family stream may shadow the assured `substantive` run scope (`assure` guard), the ONE legal nesting — cosmetic-inside-substantive stays the loud RNG-1 desync tripwire, and substantive-inside-urgency raises (a bug, not a feature); (2) the DESIGN MEASUREMENT (the record of the refused alternative): the engine-2 row recorded "a third named stream" (singular); the single shared `urgency` stream was landed as a candidate and REFUSED by its own A/B — add-safety 4/10 day1_full byte-identical (the iter-49 refused scenario: an added p=40 entry, silent by gate): the entries COUPLE by draw position (the added entry's per-beat draw shifts every later beat's roll — the maid's urgency stops firing), i.e. the single stream delivers check-add-safety but leaves the per-landing corpus-regen burden in place, which is exactly what the row exists to remove; the per-entry design re-measured 10/10 byte-identical. A firing added entry is NOT a violation: it lands new canon (its own events + legitimate downstream), the content-landing protocol's territory; (3) the one-time migration flip (the row's recorded price): the flip A/B 0/10 byte-identical — every seed diverges at the beat-720 region (the checks shift by the removed draws; fingerprints 15→6 / 17→10 / 20→14 / 38→11 / 35→13; seed 125's doccheck ladder flips), paid in-iteration through the fixed-point corpus runner (identity-proved first on the unshifted stream, 105/105 zero-change; the iter-50 FAQ laws 7-8: the write-back into the fixture, and the pristine-restart rule — a measured-and-refused candidate never lands in the fixtures); (4) the pins: 4 bank-family laws (nesting, the reverse-nesting refusal, lazy registration + per-seed derivation, the name grammar) + the e2e add-safety byte-identity (the steal's check AFTER the beats — six rolls precede its dice, so a surviving draw position proves the decoupling) + the duplicate-pair lint refusal; the corpus: 2 narrator cases re-distilled (the flee-pursuit check flipped; `caught_at_the_door_of_his_own_distraction` migrates to the refusal family — its caught-fleeing knowledge claims die with the vanished flee_caught event, the deliberate `token_absent` needle; the caught path stays pinned by `caught_fleeing_the_burning_tavern`), 1 parse pin (s7's wait-720 batch 15→14), 2 unit seed re-probes (the coerce gate 7→4, the KI#17 order 2→3) | the per-(site) hash alternative (a d100 derived from stable_hash(seed:npc:beat) without the bank) was rejected flat: the RngBank is the only door to entropy (L5) — a per-site hash bypasses the draw counters, the audit guard, and the fingerprint, an INV-2 violation dressed as a simplification; the constructor-registration alternative (the Simulator passing the pack-derived stream set at bank construction) was weighed and rejected on ripple cost (loop.py + every unit test's bank construction) against the lazy family registration, which keeps the tripwire for all non-family names while the family names are built by one function and pack-linted — the typo surface does not exist; the replacement law is RETIRED as a requirement (a re-arm stays an available technique when the table's semantics prefer it — content-5/6 land by ADDING entries now, the regen covering only the landing's own events, the iter-48 scale) | tests 952→959, ruff clean; TASKS (engine-2 done; content-5/6 rows relieved), TEST_PLAN §6 (the two A/B arms recorded), BLUEPRINT RNG-1 + phase0 §1 (the family law), DIRECTOR_SPEC §7, README, STATUS (header + FAQ laws 7-8 + Next step), worklog synced; DECISIONS transiently 45/30 kept per §6.1 (collapse due at the phase-3→4 gate per D-034) |
| D-080 | 2026-09-04 | **content-5 landed — the echo driver, the jittery-watcher beat LIVE** (iter-51-content5-echo-driver, the phase-3 backlog's top un-gated item, the owner's "continue per the plan, quality over speed" session call — social-2's live content set, the read model's first LIVE runtime consumer): (1) the driver — the guard's urgency entry (an ADDED entry, the engine-2 relief delivered: `look_around` over `echo_at_least dread >= 15`, p=100 the compulsion semantics — a symptom, not a decision, the residue IS the gate; a sub-100 roll would only mask the fade law with silent misses; the drunkard's 2-in-5 stays the deliberate-act idiom). The bar's reading: 15 = the partial sighting of the 60-weight fire token at exactly half its 1440 fade window, boundary inclusive (measured: dread 22 at beat 360, 15 at 720, 7 at 1080 — two scans then the silence); (2) the AXIS call — dread, never wariness: the fire-fear is the echo's own channel; the purse wariness double-counts the crime ladder's channel (the iter-44 institutional call's family — papers_unsatisfactory undeclared for the same reason); (3) the measured-and-refused design arm: the wariness gate (guard_01's purse_missing inference, live 30/15 on ALL 10 day1_full seeds + 31 corpus cases) — 34/105 cases diverge, the document-check release case's arson beat starves on the anchor-shifted event ids (the stream count moves the reply gate; the designed ladder's survival becomes re-pin luck), and the rotation briefings RENEW the purse residue every 720 — the jitteriness would never fade on multi-day runs (the fade story IS the echo's headline law). Refused: the v0.1 demo footprint did not justify breaking a designed corpus ladder family; (4) the landing's honest footprint (measure-first, both arms): day1_full 10/10 byte-identical (the dread-silent law — the reach family's partial reads 6, the purse inference is wariness-only, the empty-backyard fire mints no sighting); the corpus 1/105 — the watch-change case diverges by exactly the guard's scan (ev_0020, t=374, the guardroom) + the trailing wait's id +1, ZERO broken pins (the scan rides AFTER the case's claimed ids — the first content landing with no forced re-distill: engine-2's add-safety delivered at the corpus level), the deliberate pins alone (the scan by id + the scene snapshot knowledge, the case's needle 10→12 — the corpus now regression-pins the driver's committed-run firing); (5) the live-fire tests on the COMMITTED pack (the iter-49 pattern): the fade arc (two scans at 374/774, the 1080 silence, the snapshot mint, the silent-skip law, the monotone decay — the witness's briefing passes nothing back under the novelty law); the driver-stripped fingerprint identity (live minus the scans IS the stripped run — the iter-46 dormancy test retired and replaced by the load-bearing law: the echo block now GATES live behavior); the iter-46 crafted-copy probe family stays the mechanism isolation (tuned_pack strips the committed driver) | the action-carried alternative (an action whose requires carry echo_at_least — the row's "an urgency entry OR action" fork) was rejected: the P2b consumer is autonomous behavior selection (the sketch's dependency); a player-gated echo door is the parse/door families' territory and would need the window law's unconditional OCC re-check — the urgency gate reads at the beat, the door re-validates the ACTION's own requires (look_around carries none, no window opens); the fire-family NPC alternatives (barkeep/maid/drunk — all four hold the partial sighting) were weighed and refused for guard_01: the literal watcher (the sketch's word), no urgency entry yet (the cleanest stream addition), and the corpus's watch-change case IS his story (the handover + the jitteriness through it); the gate-value alternatives (>=8 to catch the reach family's dread 6 was refused as corpus-overfit tuning; >=16 one-scan-only was refused — the two-beat fade arc IS the law's visibility) | tests 959→960 (+2: the jittery-watcher fade arc, the load-bearing table flip; the dormancy/declarations/costs tests flipped to the consumer law; the lint tests isolated from the committed driver's cross-lint), ruff clean; TASKS (content-5 done), TEST_PLAN §6 (the iter-51 A/B record + the refused wariness arm), phases.md §3 (the P3e row: the driver landed), README, STATUS (header + KI#68 deleted per AGENTS §5 + Next step), worklog synced; DECISIONS transiently 46/30 kept per §6.1 (collapse due at the phase-3→4 gate per D-034) |
| D-081 | 2026-09-04 | **content-6 landed — the arc driver, the aftermath chain LIVE** (iter-52-content6-arc-driver, the phase-3 backlog's top un-gated row — arc-1's live content set, the release-chain layer's first LIVE consumer, the D-076 dormancy ending): (1) the measured-first survey (a runner outside the repo per Rule 9, both arms, day1 x10 + the 105-case corpus): the relief is the ONLY hook tag releasing on ANY committed run (the vigil never fires — the all-PEAK quiet window; Doren's half never fires — his post empties the beat his band opens) — so NO existing pair co-releases, and the chain's FIRST member must be the relief (the D-076 naive watcher-pair chaining refused: it would hold the corpus-pinned relief behind Doren's never-firing half forever); (2) the successor — `barkeep_wary_sweep`, the room's reckoning (the day's closing beat): trigger-less (the climax path is its only road — the vigil's stagnation-only shape, inverted), climax-flagged (the closing beat that ENDS the peak: PEAK_CLIMAX + the unconditional REST, the denouement the clock already defines — the D-067 "high-severity" wording is the flag's donor description, the pack's declaration is the law; the tension recorded here for the owner's review, veto-able), weight 0 (a closing beat carries no tension — the entropy footprint exactly zero, the whole +W gate-flip class dead by construction), first_time_only, seeded on the steal-failure list APPENDED LAST (the buffer order can never steal the relief's budget slot — the corpus pin's belt-and-suspenders, the order law holds it non-current there anyway); (3) the LOAD-BEARING law (the iteration's core finding): the gap law is what makes the arc non-decorative — the UNCHAINED sweep releases at beat 720, one beat past the check's release, and both intents ride the same entry tick (t=732) where the queue's actor_id tiebreak pops the barkeep BEFORE the relief guard: the sweep's event lands at t=733, BEFORE the check's own event at t=734 — a second beat landing before its predecessor is a causality lie in the canon, and min_gap 2 is exactly what defers the sweep to beat 1080 (t=1456, the day's LAST event, appended after everything HEAD held, zero id shifts); the alternative designs measured and refused: the trigger-gated successor (decorative — the option-gate/trigger does all the deferral, removing the arc changes nothing), the check-seeded successor (decorative — born current, the gap law has nothing to defer), the early-trigger + option-gate pair (the same-tick inversion hazard returns), and the NPC-move successor (the relieved watcher walking back — the first NPC movement in the corpus's history: the tune-3/st-6 owner-gated fork, not a content row's to open); (4) the footprint (both arms, TEST_PLAN §6): day1_full 9/10 byte-identical (the quiet seeds' steals succeed — no failure, no seeding, not even the hooks field diverges), seed 125 +1 appended event; the corpus 105/105 pin-green, ZERO re-distill — the FIRST content landing with none (the 14 theft-failure cases diverge by exactly the seeding event's own `hooks` field, the birth record — the FAQ law 9); the nopacing arm now differs by exactly the sweep (the D-065 "both arms identical" record superseded in part: the clock's PEAK/REST bands still gate nothing, but the closing beat rides the climax path, which the clock-off pack removes — a trigger-less climax hook dies without the layer; re-pinned in test_balance_harness) | the corpus-regen debt retired: the successor-stripped arm is HEAD-shaped (the fingerprint identity — the sweep adds no draws, pinned), so the landing ships pin updates alone (the seeded-hooks set in test_actions + test_doccheck, the dormancy pin → the live-declaration pin in test_director, the D-065 re-pin) — no fixed-point runner needed this iteration; the successor's social channel tag is inert vocabulary on a climax hook (the lint's closed set demands a tag; content-3 [ambient] stays the row that actually carries the dimension — NOT this landing); the arc driver's re-plan question (DIRECTOR_SPEC §11) answered: no recovery needed — the live chain stalls only when its predecessor's world honestly died (the defused-relief pin), the row stays parked | tests 960→966 (+6 tests/test_arc_driver.py: the march with the gap arithmetic and the canon order, the arcs-stripped inversion A/B, the defused-relief order-law stall, the fingerprint identity, the quiet-seed byte-identity, the declarations), ruff clean; TASKS (content-6 done — the phase-3 build column now fully live except content-3), TEST_PLAN §6 (the iter-52 record), DIRECTOR_SPEC §3d+§11, phases.md §3, README, AGENT_NAVIGATION §1, STATUS (header + the regen FAQ law 9 + Next step + the owner's two review questions: the Alien unknown-axis conflict, the climax-flag-on-weight-0 question), worklog synced; DECISIONS transiently 47/30 kept per §6.1 (the collapse owes at the phase-3→4 gate, the KI#64 note) |
