# DECISIONS.md — ADR-lite

> Append-only **within a phase**; each entry: decision → why → consequence.
> Supersede with a new entry, never delete mid-phase. Cap: 30 entries,
> enforced by collapsing at phase gates (D-034): family merges write compound
> IDs (`D-018/D-022/D-029`) so external citations still resolve; compressed rows
> keep the decision and link the single owner of the detail (D-024 anti-drift
> — spec-restatement inside a decision row is duplication, not substance).
> Pre-collapse history lives in git. Collapse ledger: 46→30 (iter-7), 41→30 (iter-26), 35→30 (iter-35), 48→30 (iter-54), 71→30 (iter-102), 44→30 (iter-116), 51→30 (iter-140-memgc — clobbered by a stale-base archive apply at iter-140-intake27, the files re-expanded to 63 rows), 63→30 restored (iter-151, D-185), 31→30 (iter-176, doc-3 — the owner's call). Collapses fire on the owner's explicit call (D-034/D-185).

| ID | Date | Decision | Why | Consequence |
|---|---|---|---|---|
| D-001/D-009 | 2026-08-25 | Repo language law: docs, code, identifiers, commits in English; owner chat in Russian; prototype pack display names in English | token economy, owner request; consistency with logs/tests | all agents write English; final-product localization is pack data, zero code |

| D-002/D-010/D-032 | 2026-08-25 | Data contracts: JSON / JSONL + JSON Schema, no XML tooling (XML only as DF Legends *input*); EVENT_SCHEMA.md ↔ `schemas/event.schema.json` 2-place sync, test-enforced (the doc example is a test fixture); schema validation = the stdlib mini-validator `core/schema.py` for the used subset (`type`/`properties`/`required`/`enum`/`pattern`/`additionalProperties`/`items`/`minimum`/`$defs`/`$ref`) — any other keyword fails loudly | Python-stdlib native — the log is JSONL; `jsonschema` is a runtime dependency (D-012 forbids); hand-rolled structural checks duplicate the contract (the exact drift D-010 prevents) | `schemas/event.schema.json` is the machine contract; the log writer is the enforcement point — no invalid line can enter a log; T0 re-validates the doc examples; pack-file JSON-Schemas stay a phase-6 rung |

| D-003/D-023 | 2026-08-25 | Event-sourcing shape: JSONL log = truth, SQLite = derived index (INV-1); runtime state = the incremental projection `state_new = state_old.apply(event)`; `fold(log)` is for T2 replay and rebuild only — never the startup hot path | the ambiguity risks O(N²) at startup if fold is misused as the runtime state path (KI#5) | replay = fold(log); the DB can be dropped and rebuilt at any time; fold stays the truth-test (T2) and the rebuild path |

| D-004/D-028/D-079 | 2026-08-25 | Determinism law: no wall-clock anywhere, including the log header (header carries seed, python, schema_version, commit, pack — no timestamps); INV-2 = single point of randomness control — one master seed, named streams deterministically derived via the RngBank authority (`stable_hash(s) = int.from_bytes(hashlib.sha256(s.encode()).digest()[:8], "big")`, environment-independent); cosmetic draws can never desync canon replay; D-079 (iter-50, engine-2): every pack urgency entry rolls d100 on its OWN content-addressed stream `urgency:<npc>:<kind>` (`core/rng.py::urgency_stream_name` the naming grammar's single owner; the (npc, kind) pair pack-linted unique, the name injective; lazily registered on first use — the closed-set tripwire survives verbatim) — an added or removed entry shifts NEITHER a later check draw NOR another entry's rolls (INV-2's add-safety clause; the single shared `urgency` stream was landed as a candidate and REFUSED by its own A/B — add-safety 4/10 day1_full byte-identical); the nesting law reworked: an urgency-family stream may shadow the assured `substantive` run scope (the ONE legal nesting), cosmetic-inside-substantive stays the loud RNG-1 desync tripwire, substantive-inside-urgency raises (a bug, not a feature) | byte-identical determinism; the old "one Random instance" wording contradicted RNG-1 in three load-bearing places and was stricter than its own multi-stream donors; a built-in `hash()` derivation would silently depend on `PYTHONHASHSEED` | AGENTS.md §4 is the single reading owner; the iter-0v audit sub-clauses live as blueprint/spec sub-clauses (list in git worklog iter-0v) |

| D-005/D-038/D-039/D-040/D-041 | 2026-08-25 | Director & autonomous mechanics (iter-4/4a): the director is a consequence planner, never an improviser — complications are seeded at event time in a per-run buffer, never invented later; narrative entropy = observable state only (hook weights + global suspicion + visible threats); explicit triggers (time/place/threshold) fire causally; releases ride the intent door as `director_<N>` (the director never writes canon); director-off keeps seeding, suppresses releases (T8 baseline); crossings fire in TICK order. Autonomous: P2b goal ticker — per-NPC urgency specs roll d100 on the substantive stream; a precondition failure stays SILENT (the world's noise floor absorbs autonomous attempts); arrest resolution cause-chained to the attempt, `caught` irreversible (T4). Audit laws (iter-4a): the playscript feed is player-driven only (an autonomous ending NEVER advances the script); the crime-status flip guards the pack's ordered `status_values` progression; `reset_on_rotation` resets BOTH participants; dead pack keys deleted | causality by construction (RimWorld storyteller = the named anti-pattern); D-021 — the world must act without the PC; small-formula dynamics, not a planner; capture/escape is a binary outcome with no chase sub-system (phase-3+); KI#17–20 found by the owner-requested audit (124 probe runs clean) | director-off A/B mandatory (T8); beat order decay → urgencies → director; autonomous intents enqueue at `entry.tick`, never `beat_tick`, decay commits at `beat_tick`; full detail: `docs/DIRECTOR_SPEC.md` (the runtime contract owner) + `docs/INTENT_SCHEMA.md` + git worklog iter-4/4a |

| D-006/D-007/D-008 | 2026-08-25 | v0 epistemics shape: no group reputation (guard-to-guard transfer models it); rumor = knowledge transfer with fidelity decay; lies = crafted knowledge records | "reputation with the watch" = transfer events at watch change; distortion comes from source incompleteness — no extra system; foundation for phase-4 `believes/lies` | one mechanic covers gossip and hearsay distortion; no truth field on records in v0 |

| D-011/D-012/D-013/D-014/D-046/D-183 | 2026-08-28 | **Repo meta & the executable surface**: no CHANGELOG.md (git + STATUS own history); the repo name a placeholder; the concept docs live outside the repo; stdlib-only runtime (D-012, AGENTS §5/§8 — any dependency addition stops for owner confirmation); D-046 `scripts/` is operator tooling INSIDE the executable invariants (PACKAGE_DIRS += scripts + the coverage-closure test; the print exemption CLI-class only; the INV-4 import check runs against every package dir); D-183 the AGPLv3 relicense (iter-150 part 1, the owner's call): `LICENSE` at the root (the canonical AGPL-3.0 text, the verbatim SPDX copy) + the README's License section re-pointed (SPDX `AGPL-3.0-only`); the donor stance (D-015/ROADMAP §4) untouched by design — what the repo TAKES vs what it PUBLISHES UNDER are separate questions; `pyproject.toml` carries no license field before or after (the owner's call if wanted) | a top-level code dir outside `PACKAGE_DIRS` silently escapes INV-4's check; dev tooling stays capped at pytest + ruff; the publishing license the owner's explicit choice, never a default | the stoplist scope (INV-3) stays engine-side (core/ + sim/ + brief/) with the periphery interpretation test-owned |

| D-015/D-016/D-017/D-024/D-026/D-027 | 2026-08-25 | Reference architecture (four places, four jobs): `docs/REFERENCES.md` = the catalog (owner survey rev v2 merged; licenses verified + tagged with check date, unverified quarantined in §11, quarterly review); `docs/ref/<source>.md` = one deep-dive file per source, `docs/REFERENCES_DEEP.md` = index + format template (single-file dives projected 2500–3500 lines, 4–6× the cap); `docs/BLUEPRINT.md` + `docs/blueprint/` = the distillation — the 12-row resolution ledger + cross-cutting laws + build index; the blueprint owns cross-reference resolutions and combinations (mechanics stay in refs, contracts in the specs; single-owner rule preserved — link, never restate, the D-024 anti-drift discipline) | rich but unverified survey (incl. unverifiable, misattributed repos); after 19 reference iterations nothing combined the verdicts — cross-ref tensions (MTTH vs determinism, mutable ECS vs INV-1, GA retrieval vs known_by) each needed one resolved mechanism, recorded once | admission rule: verify via GitHub exact-name search (web SEO misses small repos); donors are inspiration/patterns, never 1:1 copies (`REFERENCES.md` §0.7); a deep dive updates only its ledger rows; a resolution-changing ref needs a worklog note + a superseding D-entry; each per-ref file naturally ≤300 lines (the split-when-bloated law) **+D-015 the license filter (usefulness & phase is the filter — donors gated by phase, ROADMAP §4; proprietary universes pattern rows, never committed content; merged here at iter-140).** |

| D-018/D-022/D-029/D-186/D-198 | 2026-08-27..09-24 | Concept absorption & digestion complete: manifestos and owner texts land **surgically into existing docs** (the D-018 pattern — never a new canonical file); one research pass granted then closed (Q4 = no); digestion declared complete at iter-0w — skeleton (phases 0–6, three layers, INV-1..5) confirmed, blueprint = the single build guide; **D-186 (iter-153-worldbuild1, the owner's archive-intake call): the worldbuild-surface EXCEPTION — the authored-setting corpus lands as its own directory `docs/worldbuild/` (10 files, `README.md` the index + the terminology fence), never surgically into existing docs: world canon has no existing owner (VISION frozen, ROADMAP owns phases, specs own contracts) and ~2000 lines would cap-blow any surgical target; the surface owns the authored-setting model ONLY — never runtime contracts, pack schemas, or build order (the wiring: NAV §1/§2/§3, the repo README map, TASKS' standing-rows preamble the separate-track pointer); the terminology fence: worldbuild "canon"/"Echo" ≠ INV-1's log canon ≠ `core/echo.py`'s read model; the legacy source archive stays outside the repo (D-011's line stands); the world track's plan (`WORLD_WORKPLAN.md`) a separate track from the engineering backlog, never a second queue; **D-198 (iter-211-doc4, the owner's control-plane rework call over the external CanonSim Agent Control Plane document): the document itself stays outside the repo (the convenience-copy law); the durable layer extracted surgically — task intake + the design-fork preference + infrastructure admission + the four stop conditions → AGENTS §2, authority & conflict resolution (the 8-step protocol, no silent reconciliation) → the new AGENTS §11, handoff & reproducibility + the exact check triple → the new AGENTS §12/§10 (D-113's verify-seed + archive-protocol chat-side placement SUPERSEDED — the in-repo home; the marker rides the D-113 row), context closure + question classes + the change-impact gate → NAV §2 (+ `impact` named in NAV §5); the research vocabulary → TEST_PLAN §9 (iter-212-doc5: the claim-side classes FACT/INFERENCE/HYPOTHESIS/PROPOSAL/UNKNOWN named beside the standing disposition vocabulary — the class gates what a disposition may license) + BLUEPRINT §2 L15 (iter-212-doc5: the five-part design test beside L13/L14 — a genuine design fork, never a ritual); the invocation target = repository + task words — mode/revision/owner/scope/verification resolve from the repo, never a prompt template; the family CLOSED at iter-213-doc6 (doc-4 AGENTS/NAV + doc-5 TEST_PLAN §9/BLUEPRINT §2 L15 + doc-6 the acceptance collapse — the minimal-invocation artifact delivered chat-side, never a repo file); the refusals stand: no agentcheck.py, no .agents/skills/, no second project memory, no nested AGENTS files (no consumer/problem/owner)** | a fifth canonical layer would rot, duplicate owners, and violate the caps; foundation hardening before code; 33 references replaced vague intentions with named donor-backed mechanisms without moving a phase gate; **D-186's why: the surgical law targets concept manifestos feeding engineering docs — a first-class setting model with no existing owner is a different content class, and the owner explicitly called the directory** | BRIEF/VALIDATION sketches gained sensory-emitter + beat-boundary delta + structural injection-neutralization clauses; P3e `psychological_echo`; sequencing is iteration-counted (calendar dropped); the doc-loop exception law: no doc pass without a fresh owner request; **future owner-text intakes route by content class: engineering concepts → surgical (D-018), an owned domain model → its own surface under NAV §3's ownership law (D-186 the precedent)** |

| D-019/D-020/D-021/D-033 | 2026-08-28 | CORE_DESIGN_RESEARCH proposals accepted and built: M3 (causal chain length) / M4 (novelty) / M5 (non-PC share) into the metric baseline (P1b–P1d); sparse NPC↔NPC pair relations (P2a); minimal goal ticker (P2b); salience-based talk topics (P2c — knowledge *used*, not just stored) | depth-equation factors must be measurable, not asserted; teller↔listener trust needed a data home; the world must act without the PC for T8 to mean anything; the last owner-pending proposal flipped to accepted per its lifecycle rule | no event-schema break (relations are state); full LLM planning — never (Generative Agents anti-precedent); M5 non-trivially non-zero by construction; metric thresholds set from the measured baseline, same protocol as M1/M2 |

| D-025/D-034 | 2026-08-26 | Cap laws: `docs/*.md` ≤600 lines with the substance-over-cruft filter (AGENTS.md §6.1) replacing the rigid 400-line wall; DECISIONS.md ≤30 entries, enforced by collapsing superseded/absorbed entries into consolidated compound-ID rows at phase gates (`ROADMAP.md` §5) — never by mid-iteration cuts | iter-0i trimmed substance (enum lists, field names, pseudo-code) to fit a hard cap — the owner flagged: "hard cap = crutches, not quality"; the gate-collapse keeps the cap's pressure and the append-only invariant intact with a natural archiving rhythm | filler/restatements/linker-chains = cut always; named systems, field lists, enum values, per-source verdicts = never cut to fit; a cap breach triggers a cruft pass first, over-cap substance stays with a worklog rationale; the file may sit over cap between gates; within a phase: supersede, never delete The post-ladder rhythm: the phase gates retired with the completed ladder — collapses fire on the owner's explicit housekeeping call (D-185, the iter-140/iter-151 precedent). |

| D-030/D-031 | 2026-08-27 | **The owner-directed principle rounds absorbed surgically** (the D-018 pattern — never a new canonical file): D-030 darkness is architecture — tone = data asymmetry inside the existing 8 systems (asymmetric deltas, an observable price marker on every socially meaningful behavior, irreversibility, rumor lethality per D-006); the core stays axis-blind (a future pack adds axes with zero code change); the grim line admissible only as the post-gate `pack-1` candidate under the PACK_SPEC trigger; D-031 the quality round — L13 the abstraction cost gate (Rule-of-Three, the 4-branch registry threshold) + L14 the elegance checklist (BLUEPRINT §2), type discipline + fail-fast + the registry + negative tests (phase0 §1/§2/§6), the architecture fitness test, the stack frozen stdlib-only through phase 2, mypy parked as `qa-1` | the owner fears rot into piled-up complexity; content-first proposals exposed the scope-creep failure mode (a spec before its trigger, ad-hoc mechanics) | AGENTS §4 canon-write-privilege line + §9 the quality DoD bullet; dev tooling capped at pytest + ruff |

| D-035/D-036/D-050 | 2026-08-29 | **The write-path discipline** (engine internals): D-035/D-036 the commit gate + the spread-pass discipline — every event enters through `Simulator._commit` (state deltas validated against the projection BEFORE the write, INV-1's privilege line D-031), the per-layer spread singleton (one live pass per layer, shared causes, KI#16); D-050 the derived indexes live beside their single mutation funnel (iter-8h): `KnowledgeView`'s `who → token → source-ids` (add is the only writer; `before_source` semantics preserved) and the Simulator's `(entity, prop) → latest-committer-tick` (`_commit` the only writer; the decay baseline reads the index — KI#19); plus the same-funnel scan eliminations (the beat-window break, the top-1 salience, the forward-once OCC fold, the once-per-call entropy) | each index has exactly one writer — the invariant is checkable by reading one function; the golden fixtures byte-identical through the pass | the perf numbers: profiled at iter-30 (TECH_NOTES §8 — event-linear, schema validation the hot spot); tests: the fold/replay suites |

| D-037 | 2026-08-28 | iter-3 epistemics architecture: (1) knowledge state = a derived per-knower index (`core/knowledge.py` `KnowledgeView`) rebuilt by folding `knowledge` records — never projection props; (2) system reactions are event-driven, dispatched from the commit door (`_commit` → `_react`: crime first, telling second) — no call site can forget them, the cascade terminates; (3) watch rotations fire on clock crossing, never pre-seeded — a run still ends when its script's queue drains; (4) the systems live in `core/`, **not `sim/systems/`** — anything the canon door must dispatch is kernel mechanics (the import-boundary law) | (1) keeps INV-1 pure (memory = fold of the log, T2 applies); (2) removes the "forgot to react" bug class at zero scheduling cost; (3) pre-seeding would extend every run to tick 1080; (4) core never imports sim — law wins over the stale plan note | suspicion reacts to **novel tokens only**; the arrest threshold has a single owner (`crime_watch.arrest.requires_suspicion`); expectation violations chain to the axis-specific mover; `crime_status` lives on the player entity (namespace-collision avoidance); the telling shares the teller's most salient **novel** fact with one-step fidelity decay + an acceptance roll; official briefings transfer unrolled |

| D-042/D-043/D-044/D-047 | 2026-08-28 | The read side is pure functions of the log — render AND brief: every render entry point builds its `Engine` on a fresh `RngBank` from the log HEADER seed (same log → same bytes in any process/`PYTHONHASHSEED`; a growing log keeps its rendered prefix; tracery ~350 stdlib lines, cosmetic stream only, ShufflePool no-immediate-repeat; the importance gate is pack data `tale_gate.min_importance`); M1–M5 + the emergent-chain count are pure functions of `(events, projection)` in `core/metrics.py` (Mesa `DataCollector` inverted — the simulator never knows a metric exists); the assembler is **zero RNG** (dry structured tokens, L2 — `docs/BRIEF_SPEC.md` owns the skeleton: two-level budgets, whole-block eviction in the fixed order, directives never dropped, `[truncated:N]` markers, ranking caps ≠ budget drops, all budgets at `rules.json::brief`); `scripts/balance_harness.py` runs the 1000-sim distribution (contract `docs/TEST_PLAN.md` §6) | render determinism must be env-independent and prefix-stable; a runtime-coupled metric creates feedback risk + hot-path cost; the same purity law extended to the phase-1 mediator input (D-047's clause: deterministic token proxy keeps byte-identity stdlib-only); separating ranking caps from budget drops keeps the anti-silent-drop law about assembly budgets, not retrieval windows | the 1000-seed baseline is the M-threshold reference (M5 p50=0.77, chains p50=20, M3_mean p50=13.81, M1 p50=0.24, M4 p50=0.18; full table reproducible, output gitignored); T1 covers the chronicle via the log fixture; the session = one opened Simulator, byte-identical to the batch run; brief bytes pin-tested (golden fixture); the real tokenizer must not change committed formats without a spec bump (BRIEF_SPEC §8) |

| D-045/D-058/D-064/D-083/D-094/D-136/D-146/D-149/D-151/D-170/D-172 | 2026-09-06 | **The phase-0..6 gate verdicts — PASS, the ladder complete** (iter-6/26/35/54/65/102/116; the phase table owns closed/open — these rows are the verdict records): D-045 phase 0 — all six MVP_SCOPE §16 exit criteria met, no kill-criteria, day1_full/seed 125 the canonical fixture, the metrics baseline recorded; D-058 phase 1 — 0 canon violations over 109 live beats (the exit criterion), the corpus 105 replayed through the real mediator cycle, DECISIONS 41→30; D-064 phase 2 — 35/35 boundary validity over 51 combined utterances, the §5 re-run IDENTICAL to iter-26's numbers (the parser never touched a canon path), DECISIONS 35→30; D-083 phase 3 — the §5 re-run green (ON seed 125 M1=0.509/M2=0.333, OFF T8 26 chains ≥ 3, stretch max 1), DECISIONS 48→30; D-094 phase 4 — 0 leaks on the blind-NPC suite (the exit criterion), the §5 re-run identical to iter-54's numbers, the mode-F chronicler acceptance green; D-136 phase 5 — the exit criterion 'an emergent chain of 3+ events without the player' MET in its worldgen-fed form (T8 OFF 24 chains — the iter-65 record's −2 the DESIGNED day1_full LOD price, paid on record; the 60-seed band 15–24, M5 p50 0.79), DECISIONS 71→30, TASKS collapsed; D-146 the phase-6 OPENING — the gut-check NO CUTS (the 20-seed ablation + the named-consumer trace), the second-setting shape TRAVEL; D-149 the reskin day — the gate instrument MET BY MEASUREMENT (`content/road_pack/` the TRAVEL-loop reskin, 14m24s, ZERO core edits git-verified); D-151 the phase-6 verdict — PASS (the §5 re-run: the seed-125 pair ON M1=0.518/M2=0.200 reproducing iter-102's numbers exactly / OFF T8 24 chains; T7 both chronicles read as stories; ROADMAP §2 CLOSED — the ladder complete, the open build rows stay owner-gated backlog rows; DECISIONS 44→30); D-170 the L2 depth audit — the verdict material (the three lenses, the A–H ledger, PARTIALLY CONFIRMED as material; phases.md §6's depth-audit block the owner); D-172 the L2 verdict — DELIVERED at the measured band, the two-level gate COMPLETE (world-2 closed, never reopening for slices) | the roadmap's ladder owner-gated at every step; the verdict rows record each gate's evidence checkpoints ONCE — the full protocol ROADMAP §5, the per-gate runs worklog + git | the standing work: the owner-gated backlog rows + the SoW horizon (ROADMAP §6) |

| D-185 | 2026-09-20 | **The memory-GC law + the semantic documentation compaction pass (iter-151-docscomp1, the owner's ownership-matrix session + the «вперёд, приступай к работе» call; the iter-140-memgc precedent — its D-173 row was clobbered when a stale-base archive apply landed iter-140-intake27 wholesale over the GC, and is restored here under a collision-free ID): the post-ladder collapse trigger = the owner's explicit housekeeping call (the D-034 gate rhythm retired with the completed ladder); the pass: phases.md §6's closed research archive kept as finding → consequence → consumer compact blocks (the consult cards, the slice blocks, the depth audit, the L2 verdict intact; the one-pass journals in git at the D-rows' commits); TASKS re-collapsed per the iter-102/iter-140 precedent (the phase-6-era ledger iter-116..150) + the thirteen standing rows extracted to the live queue head; DECISIONS re-collapsed 63→30 (the family pattern); SPECS_BACKLOG's fired-and-absorbed triggers re-pointed (GROUP_SPEC → depth-7, CORE_ONTOLOGY → PACK_SPEC — restored from the clobbered GC); AGENT_NAVIGATION restored to a state map (one line per path, no narrative); REFERENCES_DEEP's verdict column compressed to one-line purposes (D-026 — the mechanics live in the ref files); TEST_PLAN retitled the Verification Stack (the historical per-landing acceptance records → the D-rows + tests + git; the instrument definitions kept); the CORE_DESIGN_RESEARCH inbound collapsed (the file's deletion itself the owner's next call after a zero-inbound check; executed iter-152 — the file deleted, the evidence-class citations swept to their live owners) | the caps' enforcement rhythm died with the completed ladder — the over-cap files had no future collapse point; the matrix session measured the corpus at 13,894 lines / ~1.06 MB with the same facts owned 2–3 times (phases↔TASKS↔DECISIONS the worst); the stale-apply clobber proved an archive built against a pre-GC BASE can silently erase a landed GC | the reading gradient unchanged (STATUS → TASKS → the owning doc → a §6 card only when the row names it); future collapses fire on the owner's explicit call, recorded in the collapse ledger; nothing in the build queue moved — the ORDER owner's rows untouched; the delivery lesson: never apply a delta archive without checking its BASE_COMMIT against HEAD |

| D-048/D-049/D-053/D-056/D-057 | 2026-08-31 | **The scene/brief read-side stack** (the phase-1 structural landings, owner-directed): D-048/D-049/D-053 the scene ledger — canon vs texture (the design iter-8a, hardening iter-8b, the LLM-free half iter-10: the scene lifecycle, the pin/retire vocabulary, the laundering refusals, the D-049 determinism quarantine — the ledger is session render state, never replayable, T1/T2 never touch it); D-056 st-1 presence & entity cards (iter-15: the arrival snapshot, the present set, the card surface — BRIEF_SPEC §3.4); D-057 the pack-declared `layout` (iter-20: canon-from-birth on the scene line, no initial_projection seeding — KI#48's correction: canon_slot reads BOTH prop sources) | the read side is pure functions of (log, ledger) with zero assembler RNG (L2); the projections are folds, the cards are the narrator's surface | contracts: BRIEF_SPEC §3.3/§3.4 + blueprint §1; tests: the ledger/presence/brief suites (iter-10/15/20) |

| D-051/D-063 | 2026-09-01 | **The track-B DF pipeline closed** (bg-1 + bg-2, the owner-supplied world exports): D-051 the SQLite sink — HANDLED records load as typed cores + EAV + `event_participant` + generic JSON, truncation flagged PARTIAL by default (`--strict` aborts), the DB a rebuildable content-deterministic index of the export bytes (INV-1's shape on foreign canon; no wall-clock in `meta`); D-063 the taxonomy + the sink v2 plus pass — `docs/TAXONOMY.md` (120 entries over the 16 target types, measured findings incl. the hfid1/hfid2 participant-index blind spot), the companion's `historical_events` in `event_plus_fields` (same ids, fields only) | the epistemic closure IS the participant index (blind spot inherited — DF history is canon-dense, epistemology-empty: the legends structure donates, knowledge propagation stays ours to design, phases.md §4) | recipes + numbers: TECH_NOTES §3.1-§3.3; tools: scripts/df_{survey,import,taxonomy,briefer}.py; tests: the bg suites (bg-1/bg-2) |

| D-052/D-054/D-055/D-062 | 2026-08-29 | **The two external dev-time doors + the validator** (iter-9/11/12/31): (a) D-054 the texture promotion door — the LLM-free half (`core/intent.py` + `resolvers.py`'s texture branch; contract INTENT_SCHEMA §2/§3/§7): a texture-capable action's pack `texture` block REPLACES the canon preconditions for intents carrying the mediator-resolved `{entry, scope, slot, value}` reference; a take-success IS the promotion — one StateChange, the object's canon birth; a failure promotes nothing; core NEVER sees the ledger (INV-3/D-031); ghost interactivity structurally impossible; `unique_slots` the laundering-family refusal. (b) D-055 the narrator door — agent-in-the-loop, EXTERNAL at dev-time: the FILE contract `output/mediator/call_<N>.md` ↔ `reply_<N>.json`, ONE closed document shape with deep shape gates AT the boundary; a refused document never feeds intents (the beat regens whole; L12 from day one: narrator → template → dry). (c) D-062 the mode-C parser boundary — the same file-contract pattern on the player's free text (iter-31): the grammar snapshot as DATA (pack verbs with pack-derived field constraints ∪ addressable nouns — canon entities + live ledger entries), `parse_call` byte-deterministic, the closed {intent | question | no_intent} reply gated at the boundary (off-grammar = a loud ParseError, never a feed), the pin law (`SceneLedger.pin` — the reference IS the pin; a PROMOTED entry terminal), promotions wired exactly like the narrator path. (d) D-052 the validator contract (iter-9, VALIDATION_SPEC the field-level owner, `brief/validator.py` the LLM-free half): the mediator's gate a pure function of (proposal, log, pack) — one CLOSED verdict vocabulary, ExpectedVersion OCC, the fact transaction, honest verdicts against CURRENT canon under closed-world semantics | the LLM boundary (INV-4) holds by keeping every external side behind a file contract with shape gates at the boundary — no post-hoc text sanitization; prose is never a fact proposal (injection neutralized structurally) | contracts: INTENT_SCHEMA + PARSER_SPEC + VALIDATION_SPEC + BRIEF_SPEC §7.1 (the single owners); the phase-1 corpus (105 cases) replays the refusal vocabulary + both door outcomes through the real cycle; local inference stays the owner-gated engine-1 candidate; per-clause landing detail: the specs + git |

| D-059/D-060/D-061 | 2026-08-31 | **The phase-1 polish menu — three landings on the owner's finish-phase-1 directive**: (a) tune-1 (iter-27): the story-critical importance hook — pack-listed event types (`rules.json::importance.story_critical_events`, lint-closed) score +2 `story_critical_event` (`pack_importance` takes the event type, all emit sites); the tale gate follows low→medium — the RULE owns the signal/noise split per D-045(b), the gate is not the primary knob; + the `recuperate` resolver + `status_effects` pack block (`rest`, the 13th action, 60 ticks, fatigue −30 — the KI#4 counter-play, pack data end to end); day1_full OFF renders 47 events → 14 tale lines, zero decay/wariness noise (pinned); the 1000-sim baseline re-run IDENTICAL. (b) tune-2 (iter-28): `brief.present_entities.card_markers` — the prop-path marker table (threshold rows + value rows, closed marker surface `status.<axis>`/`relations.<axis>`/`crime_status`, lint-checked; the segment is `markers=`); the pack ships `wary` at `relations.suspicion` ≥ 25 (aligned with the `status_suspect_at` flip) + `suspect`/`caught` on the player's card — the narrator's read surface for standing state (the scene_delta half is lawful blindness, blind-NPC D-037: the CARD is the narrator's surface, the delta window the player's). (c) pack-2 (iter-29): the `spot_available` door check — the closed precondition set's 15th test (the target location holds an unburning spot of the declared layer, the exact condition the ignite resolver keys on; the `layer` param lint-checked) — arson on a destroyed or fully-burning location is an `intent_rejected` no-op with `failed_test target.spot_available`, the door-outcome vocabulary's fourth axis | the T7 phase-1 gate re-read flagged the noise floor (27 of 47 day1_full events were decay/wariness repetition — a bare gate raise would have cut story events the raw rule scores low); iter-17's live finding: the suspicion axis and the `crime_status` flip were claimable-but-invisible through the brief; the iter-2a audit note + iter-24's live probe: arson on ashes logged a no-ignition success that pretended the world changed | T1 fixture untouched by (b)/(c); tests 610→629 across the three; the seed-41 corpus probe flipped with (c) (renamed, the old pin lives in git); field-level detail: INTENT_SCHEMA §3 (the requires row), BRIEF_SPEC §3.4/§6 (the marker table law) |

| D-065/D-066/D-067/D-068/D-069/D-070/D-071/D-072/D-073/D-074/D-075/D-077/D-078/D-080/D-082 | 2026-09-01 | **The phase-3 (Director) build column landed — the four columns, condensed at the phase-5→6 gate (iter-36..53; the per-column compound rows live in git + TASKS' phase-3 ledger, the mechanism detail in DIRECTOR_SPEC §3–§5 + TEST_PLAN §6 — the single owners): the pacing stack (D-065 the RAMP/PEAK/REST/STAGNATION clock over narrative entropy, D-066 the eventless-stretch instrument + the DIR-2 A/B harness, D-067 the climax layer + PEAK_CLIMAX, D-068 the multi-channel policies + the per-hook quiet gate), the Paradox event grammar (D-069 predicate/weight + the burn law, D-070 the option layer, D-071 the on_action dispatch), the content column (D-072 document_check, D-075 the echo engine, D-077 the panic ripple, D-080 the echo driver, D-082 the ambient driver), the social stack (D-073 secrets/leverage, D-074 the coerce door + the spend, D-078 the coerce driver). The exit criterion's measurement (max stretch 1) and the gate verdict live in the gate-verdict row (D-083).** | phase 3's declared build column, ROADMAP §2; the owner's per-iteration "continue" calls | the pacing/grammar/content/social four-column shape is the director law's standing form; per-mechanism owners: DIRECTOR_SPEC + TEST_PLAN §6 |

| D-076/D-081/D-138 | 2026-09-03 | **The release-chain layer + its driver + the climax-flag verdict (arc-1 + content-6, iter-47/52 + iter-105)**: D-076 the engine — `director.arcs` pack chains: the ORDER law (a member tag is a candidate only while it is its arc's CURRENT member, all release paths), the GAP law (`min_gap_beats` spacing on the quiet/climax paths only), the entropy mirror (passed members' instances stop counting — the burn law's twin), the one-sided membership lint; D-081 the driver — the aftermath chain `[possible_document_check_relief, barkeep_wary_sweep]` gap 2 LOAD-BEARING (the unchained sweep would land its event BEFORE the check's own — a causality lie; the march holds the order), the successor trigger-less + climax-flagged + weight 0 (the entropy footprint exactly zero), measured 9/10 byte-identical + seed 125's one appended event, 105/105 pin-green ZERO re-distill (the seeding event's hooks field the birth record); the NPC-move successor arm refused (the tune-3/st-6 owner-gated fork); D-138 the climax flag = the peak's END marker (PEAK_CLIMAX then REST unconditionally; a non-boss beat that must fire regardless of the clock carries an explicit trigger instead — D-005's causality law) | the DF event_collections / Paradox event-chain precedent: release-side causality must be pack-declared, never pacing-invented | contracts: DIRECTOR_SPEC §3d/§5/§11; tests: test_director + test_arc_driver; the D-065 nopacing record superseded in part (the closing beat rides the climax path, which the clock-off pack removes) |

| D-084/D-085/D-086/D-087/D-088/D-089/D-090/D-091/D-092/D-093/D-095..D-104 | 2026-09-05 | **Phase 4 (Knowledge & scene) build column + the v0.2 refinement lance (iter-55..72, condensed at the gates)**: the trait crystallization fold (D-084 — `core/traits.py`: threshold DISTINCT tokens of a pack-declared family crystallize the belief, the contributing records' source ids riding the fold); the brief's derived-trait read (D-085 — belief lines lead recalled_facts); reflection-on-recurrence through the canon door (D-086 — LEGEND_SPEC's compression half, written just-in-time at the row); the tavern reflection arming (D-087); the retrieval ladder (D-088 — FTS5 + the vec probe/scan/floor chain + the re-ranker; `knower` IS known_by); the mode-B scene stack (D-089/D-090 — the chorus queue + the knower parameter, the session wiring); the texture identity tier + quotas (D-091 — `identity_slots` + `per_entity_max_items`); the blind-NPC leak suite's phase-4 extension (D-092); the mode-F offline chronicler (D-093 — `scripts/chronicle.py`, the [chronicler] extra the approved D-012 exception, the attach→stdlib ladder, autoinstall off, the manifest content-derived); the v0.2 refinements D-095..D-104 (prosefloor, beliefwire ×2, rumordrift, suspectaxis ×2, testproto, prosefloor-2 — VALIDATION_SPEC §2.1 + TEST_PLAN §8 the owners) | every landing owner-called per the phase-4 wave plan; the exit criterion '0 leaks on the blind-NPC suite' the column's spine | the phase-4 surfaces complete (traits feed BRIEF_SPEC §3.5, reflection mints canon events, retrieval rides known_by, mode B live); gate PASS iter-65 (D-094); per-row detail: TASKS' phase-4 backlog + the owning specs + git |

| D-098/D-109/D-110/D-111/D-112/D-113/D-118/D-129 | 2026-09-07 | **The protocol-law & testproto family (iter-68..94, condensed at the phase-5→6 gate; per-row detail in git + TEST_PLAN §8 + the owning specs): D-098 the intermediate-build LLM-integration protocol — the three-layer synthesis (the contract simulacrum / the live probes / the gate heartbeat) over one pinned corpus + one metric vocabulary, the decomposition not an election; D-109 bg-8 its live half — the deviation corpus F1–F6 through the REAL mode-C door, the world-answer law's first live numbers (coverage 34/34, the ≤2-regen ladder); D-110/D-111 the predicate contract restored — missing answers False under EVERY comparator, and a missing key is a ValueError naming the field (the loud-backstop twin); D-112 the iter-11b stress-test resolutions RATIFIED (the D-056-amended arrival snapshot, the containers law); D-113 the doc-law verdicts (Rule 9's in-repo home AGENTS §7; ORDER → STATUS Next, composition → TASKS; verify-seed + archive-protocol chat-side — superseded by D-198: the in-repo home AGENTS §10/§12); D-118 the shadow-replay introspection law (mech-1: agent-facing CLIs replay through the PUBLIC pipeline only, never runtime instrumentation); D-129 the effect-line habit — protocol-law D-rows state their expected observable effect, checked within 1–2 iterations, reported at each gate's §5 staleness pass.** | the protocol laws grew monotonically between gates — the family holds the cross-phase guardrails | the pruning pair is D-128's gate staleness pass (ROADMAP §5 step 6) + D-129's effect lines; owners: TEST_PLAN §8, AGENTS §7, core/predicates.py, scripts/mechanics.py |

| D-105/D-106/D-107/D-108/D-114/D-115/D-116/D-117/D-120/D-121/D-122/D-123/D-124/D-125/D-126/D-127/D-131/D-132/D-133 | 2026-09-08 | **The phase-5 (Depth) build column landed (iter-73..98, condensed at the phase-5→6 gate; per-row detail in git + TASKS' phase-5 ledger + phases.md §5 — the architecture owner): the acquisition gate + arming (D-105/D-106), the lazy-detail family + arming (D-107/D-108), the fold checkpoints (D-114), the ordered worldgen passes + the committed arming (D-115/D-117; D-116 the generator-concept verdict set — the 13 questions resolved, the W0..W4 wave order, the spatial model pinned as phase law), the four W1 bridges (D-120 the scene-line projection pipe, D-121 the DF-legends history bridge + the cause tree, D-122 the placement discipline, D-123 the geometry rework — the grid-hash walk, 10k sites in 1.19 s), the macro clock + its consumers (D-124 the cadence law + the aggregate emission surface, D-125 the scene LOD + the one-gate law, D-126 factions with goals — the KeeperRL small formula, D-127 the write-side group LOD — member_of + the population tier + condensation), the W2 tail (D-131 name-1 the lazy name generator + the condensation consumer, D-132 st-6a travel with the edge price law, D-133 weather-1 the ambient chain + canon erosion + the committed macro arming — the primitive's first consumer). Every landing measured its corpus price both arms before paying it; the unarmed/68a pattern + the one-gate law kept the committed pack's price surface designed, never accidental.** | phase 5's declared build column, ROADMAP §2; the wave order D-116 | the exit verdict: the gate-verdict row (D-136); the phase law: phases.md §5 |

| D-119/D-128/D-129/D-130/D-134/D-135/D-147/D-148/D-150/D-155..D-167/D-171/D-173/D-174/D-175/D-184/D-187/D-189/D-190/D-191/D-194/D-195/D-196/D-197/D-199 | 2026-09-10..24 | **The research-intake verdict sets (intakes 6..38 + the queue's own revalidation, iter-85..195 + iter-214, doc-only — every external text verified against HEAD before routing, the convenience-copy law): every verdict owned-by-standing-law or routed to a named row/card, zero build-grade items; the ADMISSION RULE (D-175): a new external intake convenes only with a named open row it feeds + a stated falsifier — new knowledge comes from building and measuring. The per-intake compact records: `docs/blueprint/phases.md` §6 (the declared owner — finding → consequence → consumer per intake); the full one-pass tables: git at the D-rows' commits. D-194 (intake-34, iter-181 — the ultimate research corpus): the drop-in tree refused (a parallel layer over owned domains; the D-186 exception inapplicable — research residue, not an ownerless domain), the capability/progression consult card + the math-mechanism index card parked behind the owner gate (Experiment 0 the falsifier), the doctrine confirmed as the standing discipline's own live form. D-195 (intake-35, iter-182 — the corpus's second pass, the owner's call firing the deferred trigger): the family dispositions independently re-confirmed at HEAD, zero new consumers, the ref-file deep record still deferred behind a named-family row, the four precisions (Experiment 0's landed-folds reframe, R1's M3 argument, the §9 deletion-criterion candidate, the generalized-controls hypothesis) riding the intake block D-196 (intake-36, iter-193 — the curated unit archive, the ULTIMATE v2 packet: 39 units / 10 families, every body read + code-verified at HEAD): 34/39 CONFIRMED-owned — the largest single confirmation batch (D01 = the intent door whole; M01–M07 = the standing doctrine's own atoms; P02 rejected twice over; the WFC fence stands), the THREE fresh routings each joining a named consumer — C03+F02 → the W5 heartbreak station (the relation-formation triple + the option-topology measurement vocabulary), P01+P03 → the road-traffic depth-7 rider (capacity ≠ existence + the fracture discriminant), C05 → the group-stock lint gap (the honest-denominator law) — zero new rows (D-175). D-197 (intake-37, iter-195 — the agent-bottleneck research note, the owner's mandatory-artifact call): PARTIALLY CONFIRMED — the formats + tooling direction confirmed (zero format change), the full-file-rewrite claim REFUTED by the measured history (97% of 120 pack-JSON touches surgical, AGENTS §2 the standing defence), the mutation-blindness MEASURED (value mutations pass lint + goldens when uncovered); the research/intervention documentation schema (residue → phases.md §6, interventions → `scratch/` gitignored, adoptions → DECISIONS, tasks → TASKS — no parallel tree, D-194's refusal); one row convened on the owner's call (mech-2 — the agent impact surface, R03's first named consumer) + TEST_PLAN §9's mutation-adequacy prism; the record: the intake-37 block. D-199 (intake-38, iter-214-doc7 — the cross-domain principle document, the owner's «сажай intake-38» call): CONFIRMED-owned — the doctrine the standing discipline's own live form (§18's five-part test = BLUEPRINT §2 L15 verbatim, §19 the promotion gate = AGENTS §2.8, §20 the anti-patterns = the binding do-not-import list, §21 the same owners named), the §8/§9 method families landed or parked through intakes 34..37 (ablation/empty-ablation, the mutation probe, CEGAR→mech-2, delta-debug PROPOSED, sham HYPOTHESIS, CRN CONFIRMED); the fresh residue parked per D-175 with ZERO new rows — held-out transfer (HYPOTHESIS, engine-1's {3–8B} corpus the named first consumer), the experiment-contract fields stopping-rule/spillover/blocked-nuisance (PROPOSAL/UNRESOLVED beside the deletion criterion, the first-consumer law), evidence-independence + metric anti-Goodhart (HYPOTHESIS/PROPOSAL, each behind its first consumer), §12's residual relation-lens as consult vocabulary; a second agent's independent 20-file corpus pass recorded as the corroboration (intake-35's precedent — its ADOPT list predating the intake-34..37 absorption, its 15-question filter restating D-024+D-175, not planted); the record: the intake-38 block** | the owner's research calls over uploaded external texts; the repo's own law the arbiter (the transplantation test, D-024) | the intake rhythm now admission-gated (D-175); the revalidation table's essence: TASKS' standing rows + git; intake-37's live residue: the mech-2 row + the §9 prism + the scratch/ law; intake-38's live residue: none — the fresh atoms all parked behind named first consumers |

| D-137/D-139/D-140/D-141/D-142/D-143/D-144/D-145 | 2026-09-12..13 | **The iter-104..108 research-verdict + fixation family (the owner's direct calls on the iter-105 chat's priority list, condensed at the phase-6 gate)**: D-137 the Alien unknown-axis resolution — the OBSERVABLE-answers law (the `unknown` axis, whenever it lands, paces against the world's OBSERVABLE answers to ignorance — logged refusals, failed checks, no-knowledge events — never the private actual-vs-perceived gap; the L6/EPIST-1 observable-only fence stands); D-139 the resume door — the INVISIBLE-TO-THE-LOG law (`--resume` opens the session OVER an existing log; a clean-boundary resume is byte-identical to the uninterrupted run — only steps are inputs; the run cursor derived-of-the-run, never canon, never log-derivable); D-140 the payoff-latency + tension-profile riders (the `provenance.cause_hook` pairing, the schema 0.2 additive bump); D-141 the {3–8B, GBNF} gap rows (engine-1's decision inputs); D-142 the CORE-ENDS/PACK-BEGINS admission law (a change lands in `core/` iff it alters WHAT mechanics exist; in pack JSON iff it tunes HOW declared mechanics behave — the test: 'must a second pack carry this for the mechanic to exist at all?'; enforcement the three-layer fence: INV-3's stoplist + the pack lint + test_drift's wiring contract); D-143 the intent_rejected health-rate (≥90% delivery in live traffic, both pure log reads); D-144 the per-component latency budget; D-145 the phase-6 opening-day questions + the derived one-pager `scripts/digest.py` | the research session's accepted list fixed where it lives (the owner's «зафиксируй где нужно» call); the risk-synthesis riders the iter-105 priority list | DIRECTOR_SPEC §3d/§11 + TEST_PLAN §6 the riders' owners; the admission law the phase-6 packs' own gate (pack-1/pack-4 built inside it, zero core change held); per-clause detail: git |

| D-152/D-153/D-154/D-168/D-169 | 2026-09-13..19 | **The phase-6 world-2 build column landed (the two-level gate's material; the mechanism law's owners: AGENT_NAVIGATION §1's `core/` rows + phases.md §6's slice blocks + the owning specs; the landing evidence: TASKS' phase-6-era ledger + worklog + git — the per-slice D-rows' full text in git, collapsed at the owner's memory-GC call iter-140)**: D-152 the admission-lint rungs LIVE (the teleology gate + the live-char crosswalk + the price-marker lint in `core/pack.py` — PACK_SPEC §5/§6 the single reading owner, the 68a twins' completion); D-153 the province skeleton (the Sarrow Vale, 324 sites on the travel lattice, the spine records — the AP crosswalk's first committed consumer, original nouns per D-130, the T1 twin); D-154 the cultures half (the two name-1 phonotactic profiles — name-1's first committed arming; the condensation travelers; the cultures block + prohibition sets); D-168 the triangle (the factions armed 0→1 — the census's only zero-consumer family; the fear ladder + the grievance deadband; the deep feud chronicle re-cut; the composed outcome-divergence experiment; KI#85; the golden regen 39→44); D-169 the calendar (the sub-year cadence family — market days 14400 / fairs 43200 / seasons 129600 + the four-phase cycle, fired coarsest-first; the weather's seasonal ride on the D-030 asymmetric weights; the composed YEAR experiment; KI#86; +16 tests, PROVINCE_STOPLIST +2, the budget's second re-declare) | the phase-6 gate's two-level instrument: L1 the reskin day (D-149), L2 the deep second world — the wave plan D-153's («each slice a row, the ORDER owner decides»); every slice owner-called per STATUS Next step's readiness pick | the two-level gate COMPLETE (L1 PASS iter-116/D-151; L2 DELIVERED iter-139/D-172); world-2 closed, never reopening for slices — the residue routed to the content-side rows (F4's promotion events: pack-1/pack-4) |

| D-176/D-177/D-178/D-179/D-180/D-181/D-182/D-188/D-192/D-193/D-200/D-201/D-202/D-203/D-204/D-205/D-206/D-207/D-208/D-209/D-210/D-211/D-212/D-213/D-214/D-215/D-216/D-217/D-218/D-219/D-220/D-221/D-222/D-223/D-224/D-225/D-226/D-227 | 2026-09-19..26 | **The standing-row landings (the owner's delegation/engine calls, every build riding its pre-implementation contract — CONTRACTS.md the boundary pattern)**: D-176 ci-1 the GitHub Actions runner LIVE (`.github/workflows/ci.yml`: pytest + ruff on push/PR to `main`, PYTHONHASHSEED=0 job-level, Python 3.12.14 pinned EXACT — the golden byte-compare law, a bump rides a fixture regen never alone); D-177 the three contract writes (roads-1/res-1/since-1: pinned decisions, invariant sets, §9 claim packets, minimal test sets — a contract is the pre-implementation boundary, never the row's spec); D-178 roads-1 the generated-exits pass (the MST backbone + the pack-declared MUTUAL k-nearest overlay over the CLAIMED locations — per-node overlay degree ≤ k; the ONE shared read `core/roads.py::exits` — authored non-empty wins, empty-authored the generated-world marker; the loud WorldgenError conflict rule; the committed packs armed k=0, zero corpus price; the pack.py split rider → `core/packlint/`); D-179 res-1 the economy substrate (the `account.<kind>` primitive, the three verbs `account_sourced`/`account_transferred`/`account_consumed` through the canon door, integer-only, the flows on the maclock cadence, the derived prices `base + per_unit × level`, the underflow floor's soft-door/loud-gate two arms; UNARMED on the committed packs — zero corpus price); D-180 since-1 the re-encounter delta (the per-(reader, entity) encounter-epoch fold — presence per present_entities + the carried-item closure; the cards' `since=` segments atomic with the card; read-side only, zero streams, unarmed); D-181 pack-1 the grim tavern (the FOURTH pack — the dark line as pure pack data: the attraction/intimacy/loyalty + shame/anger axes, the flirt→proposition ladder with the world's reply (never a roll), the consent split structural (the canonical event vs the crafted belief record + the fact/belief vocabulary-split lint), the coerced branch the ONE spend door, the pawn-ticket hinge; res-1's + since-1's first consumer armings); D-182 pack-4 the pressure city (the FIFTH pack, the T1 slice — the displacement law as pure pack data, ZERO CORE CHANGE held exactly: the gauge = res-1's one-meter read surface, the Cooling Debt = the thermal_shock layer, the factions = depth-6's objective-function triangle, the closed cycle = res-1's second consumer; the FIRST authored non-scaffold pack; the engine findings — the group-stock lint gap, the co-due same-account flow limit — recorded, never patched in-core); D-188 companion-1 + tune-3 the companion arming, armed together as the row's own law (iter-157, the owner's «довести до ума сам генератор» call): the crossing household's second hand embodied as pure pack data (Dellan — ANCHOR_REGION §6.1's first meso unit, the pack embodiment §9 deferred to the engineering side; the spine, the kin/runner pair axes, the punt pole committed — KI#87 CLOSED), the FIRST committed NPC movement source (the road-leg urgency beat, kind=move, target the artery, gated adjacent — the HEAD census flip), the traveling-knower probe CONFIRMED per the row's own deciding shape (the crafted twin: alarm_adjacent heard+vague on the MOVER one leg out, the panic echo +10 reaching him, the telling carrying the shout to the player — the movement-driven adjacency distinguished from the worldgen-static arm), and the escort halves over the EXISTING doors (the mode B actor-step voice, the paired move legs — the no-teleport law's letter; the scene-LOD law shapes the split: the beat fires co-located, the warm ring waits for the reader); zero core change — the mid-travel beat-carry duplicates die at the door/OCC as bounded intent_rejected no-ops (attempts are facts), the golden regen's delta exactly the companion's own events (the D-079 add-safety held in the wild); D-192 engine-1 DECIDED (iter-170, the owner's real-backend convening call: llama.cpp installed + the RTX 3080 Ti station + the three-model set + the real `llama-server` test evidence): a long-lived llama-server behind an EXPLICIT ADAPTER (transport/serialization/errors/capability-translation/lifecycle the adapter's, orchestration/acceptance/operations the application's) + GBNF structured output, the D-055 file-contract frame the insertion point (the doors' gates run on the reply document, engine-agnostic by construction); the build's boundary CONTRACTS §4 (the D-177 pattern): semantic typed policies NEVER raw llama.cpp flags, the three reproducibility tiers, structured output ≠ validation, the client's home the Rule-9 runner outside the repo (INV-4 lifts only at the build's own iteration, the AGENTS §4/§8 edits riding it) | the queue's own revalidation (D-184) had named the order; every row rode its contract's falsifiers (the §9 claim packets CONFIRMED at the measured bands); the zero-core law (D-142's admission law) held through both packs; D-192: the owner's measured station evidence (TECH_NOTES §13) — the boundary survives the real consumer, both session documents route exactly this landing | the substrate primitives arm by pack data alone (the unarmed landings proven); the post-T1 residues (the legal exclusion D-134, the cultures name-1, the road traffic depth-7, the lore hooks D-140) the future slices' material; per-landing detail: TASKS' ledger one-liners + CONTRACTS + the packs' own notes + git |; engine-1's remaining measurement the experiment's (CONTRACTS §4.3 — the {3–8B, GBNF} corpus + the four new arms), results feeding presentation-1 (D-022/D-148); D-193 engine-1's BUILD LANDING (iter-177, the owner's batched call with presentation-1's write): the GBNF mapping repo-side (`brief/gbnf.py` — the snapshot's engine-facing serialization, the door's shape laws encoded at the source: one-path variants, canon-nouns-only targets, the drawn-N ticks required, non-empty strings; the golden grammar fixture + `grammar_fingerprint` = the D3 manifest's grammar id), the explicit adapter `cli/engine.py` (INV-4's executable form: the network surface is EXACTLY ONE module — the architecture test narrowed to it, AGENTS §4/§8 reworded with the lift), the door wiring (`--engine`: the file contract preserved — the runtime engine is "the operator", the gates byte-identical, zero gate edits), the failure→ladder mapping (the runtime re-ask ×1 with the menu as the repair turn; D7's rungs — the parse cycle open / the narrator's template rung; the door's RunnerError TERMINAL, the s2c1 lesson), the provenance manifest (model+sha256 + build_info + params + grammar id + seed, output/engine/), the adapter contract tests over a stub llama-server + the mapping pins with a regex oracle (independent re-derivation); the station remainder (the 27B arm, the live compile check) rides TEST_PLAN §8.5's gap rows; D-200 the WORKBENCH ADMISSION (iter-215, the owner's 2026-09-24 «начать разработку» call over the external v5.2 Redot brief — the convenience-copy law, the brief stays outside the repo): the Workbench family opened as standing rows (TASKS wb-1..N, owner-gated per row) with CONTRACTS §5 its pre-implementation contract — Redot 26.2 LTS pinned (redot-26.2-stable, Compatibility renderer, GDScript; the binary an external toolchain, one REDOT_EXE resolution point, the project at workbench/presentation/redot/ committed, .godot/ ignored), the seam chain fixture → typed read model → Visual Scene IR → Redot composition → screenshot artifact (renderer-neutral IR, the v5.2 identity closure, the status laws CANONICAL/DERIVED/OBSERVED/UNKNOWN/HIDDEN/VISUAL with no silent collapses), workbench/ periphery per D-046 (read-side core imports only — read_log/fold/present_in_order, zero canon writes, zero new network surfaces; the future gateway an owner-gated exception with its own contract), the determinism laws (byte-identical IR JSON, byte-identical double-run PNG in-environment, sha256 stable hashes, no RNG draws), the artifacts gitignored with tests regenerating in-run (REDOT_EXE-gated skip — the D-093 pattern); D-201 the WB-4 GATEWAY EXCEPTION (iter-219, the owner's «продолжай работу» continuation call over the v5.2 brief, the family's fourth row, the contract written first at commit 23a3251): INV-4 narrowed to the TWO-SURFACE form — the outbound engine adapter cli/engine.py (D-193, unchanged) + the inbound gateway binding workbench/api/transport.py (loopback hosts only; the non-loopback-no-auth refusal executable at construction, §30/§33) — the semantic core workbench/api/gateway.py + contract.py SOCKET-FREE (§4.1: protocol/schema/auth/session translation only; §6.2's forbidden edges pinned in the packet); one dispatch core (auth → idempotency → revision/lease → operation → outcome), the §8 rejection vocabulary closed and enforced where the in-memory session-translation seed exercises it (the recorded-outcome replay = one effect, DUPLICATE_REQUEST the conflicting reuse; the §12.1 law — a mutating raise after admission is SENT_OUTCOME_UNKNOWN, a read raise RUNTIME_FAILED, every rejection NOT_SENT); the §13 minimal ordered-event form (per-session sequence, no clock in identity, RESYNC_REQUIRED beyond the bounded retention); the GUI/CLI/API parity = the same core called direct and through the transport, byte-identical; AGENTS §4/§8 reworded with the exception riding; D-202 the WB-5 OPERATIONS LANDING (iter-220, the owner's «продолжай работу» continuation call over the v5.2 brief, the family's fifth row, app §32 step 5): the minimal application operations over the wb-4 registered surface — workbench/application/operations/ the Python-side package (lifecycles.py §11's four closed state machines with the terminal semantics = the empty successor set + §25's FAILED_SHUTDOWN/process-loss UNKNOWN branches; execution.py §12's absolute deadline frozen at admission — the remaining budget the only lower-layer view, the cooperative cancellation + the in-memory run registry admitting over the §10 artifact-before-side-effects freeze with the truthful terminal close — the late result FAILED_TO_CANCEL, the checkpoint abort CANCELED, mark_unknown the §25 surface; models.py §20's discovery half + the one real work kind (the chunked digest, §20's computed-when-needed arm); composition.py §6.1's single wiring owner registering run.start/get/cancel + model.list/inspect, chat.send honestly NOT registered — the backend row's consumer, the admission law) + gateway.py's two targeted edits (OperationRejected the public rejection carrier with the closed vocabulary enforced at raise time; OperationEffects the session-scoped OPERATION_EFFECT surface — the wb-4 composition point's designed growth); the 46-test claim packet incl. the spawn-failure SENT_OUTCOME_UNKNOWN integration, the independent-sha256 oracle, the direct-vs-HTTP parity over the new surface, the cross-seed pair, the AST import-closure scan; zero core change, INV-4's two-surface form unchanged; D-203 the WB-6 BACKEND LANDING (iter-221, the owner's «подключи llama.cpp» call — the backend row, app §32 step 7, chat.send's admission-law deferral closed): the llama.cpp backend wired as ONE typed port injected at the composition root — workbench/application/operations/backend.py (BackendPort = props/chat/load_model/unload_model, every member with a registered consumer; ModelLoadStates the Model ladder walked on OBSERVED outcomes — the application's observation surface folds the backend's replies, LOADING/LOADED/UNLOADING transit states never resting ones; the load-failure law: the observed refusal http/malformed → LOADING → FAILED, the unknown «unavailable» outcome rests SELECTED — §12.1's sibling at the model layer, the re-load legal; the unload failure NO walk — the still-loaded truth; chat.send the §19.1 walk over the wb-5 run registry, identity-then-poll, the caller-tunable §12 deadline, the props probe evidence-not-gate) + cli/engine.py's model-management half (POST /models/load|unload — BUILD-SENSITIVE per app §20: the b11064 station's model-management reality is the ROUTER, the dedicated endpoints the newer upstream line, the wire shapes stub-pinned, the live re-verification a station row; single-try no-ladder — §29's "bounded retry if allowed" answered no for management calls) + the composition's backend= wiring (the three operations only when the port is injected — the admission law's honest form continued); THE LADDER GAP RECORDED (never silently reconciled, AGENTS §11): §11's Model ladder has FAILED terminal with no re-selection path (only EVICTED → SELECTED exists) — a FAILED model rejects re-load loudly (DOMAIN_REJECTED with the gap named); the owner's ladder amendment is the open note; the MANAGED half (§11.1 process machinery), the replacement path (prepare-new → swap → retire-old), and the load-state READ operation (the frontend's consumer) are honestly deferred to their rows; the 28-test claim packet incl. the REAL adapter's port conformance over the live stub server; zero core change, INV-4's two-surface form unchanged — the port injected, never imported; D-204 the WB-7 LIVE CHAT LANDING (iter-222, the owner's «продолжай работу по wb 7» + «могу ли я подключить llama.cpp к реготу» call — frontend §46 Phase A's Chat row + app §22's CLI/batch delivery surface): the row that makes llama.cpp REACHABLE from the Redot Chat surface — scripts/workbench_app.py the composition root + loopback serve (the ONE launcher: Gateway + compose_workbench_operations(backend=LlamaServerClient) + LoopbackHttpTransport — the ONLY place INV-4's two sanctioned surfaces meet, the port injected never imported; --no-backend the honest admission form; the startup health probe EVIDENCE-NOT-GATE — the app serves with the backend down, chat runs closing FAILED with the observed cause, never a hang never a fake) + the Redot frontend half (gateway_client.gd the typed POST /op client — the sequential queue, NO endpoint of its own, G8's executable half: the frontend dials the GATEWAY only; shell.gd's live circuit — the URL resolution order --gateway-url > CANONSIM_GATEWAY_URL > the committed canonism_workbench/gateway/url setting, app.status → session.create (the per-process idempotency key) → chat.send → the run.get poll to the truthful terminal with every honest note, Stop = run.cancel, the bounded §15 message list, the proof mode staying the STATIC deterministic form — no network in the capture, D4 preserved); the 8-test claim packet incl. THE END-TO-END chat over real loopback HTTP against the live stub llama-server (the scripted reply observed COMPLETED with the backend identity) + the dead-endpoint FAILED close; zero core change, zero pack change, the LOG untouched, INV-4's two-surface form unchanged; D-205 the WB-8 MANAGED-MODELS LANDING (iter-223, the owner's «выбирать модель я должно из интерфейса» + «llama.cpp тоже запускаться при загрузке модели» + «настройки подтягиваться и самые нужные флаги» calls over the v5.2 brief — frontend §46 Phase A's Models row + app §11.1's MANAGED half): workbench/platform/llama_process.py the process mechanics (§2's platform/process row — build_server_command the typed honest default flag set (-m/--host/--port/-ngl/-c/-fa on/-a/--jinja/--no-webui + the operator's extras verbatim, re-verified 2026-09-25 against the current llama.cpp server surface; the command lead one token or a short prefix), LlamaServerProcess spawn/exit-observation/the §11.1 bounded graceful stop TERM→deadline→kill with the OBSERVED exit code, the readiness wait over an INJECTED probe — ZERO network imports, the engine adapter stays the one outbound surface) + scripts/workbench_app.py's --managed form (§11.1's lifecycle policy at the composition root: _ManagedBackend the port-face wrapper — a HEALTHY server delegates unchanged (the ATTACHED behaviour preserved), model.load on a DOWN server spawns llama-server WITH the caller's model (prepare → validate → ready through the adapter's own health() probe, the honest readiness budget 300s), the spawned model's unload STOPS the process, Ctrl+C stops only OUR process (§25: an ATTACHED server never terminated by us), the spawn/ready failure the honest 'unavailable' cause — §12.1's sibling at the managed boundary, the model rests SELECTED, the deliberate re-load legal; --llama-server-exe/--llama-ctx/--llama-ngl/--llama-args the operator's override surface; the managed ABSENT/LIVE evidence line, §18) + backend.py's model.states READ operation (the load-state read surface's registered consumer — the same document, never a second owner) + the Redot half (shell.gd's Models surface — the discovered list over model.list + the per-model lifecycle states over model.states + Load/Unload over model.load/model.unload with the honest minutes-class per-call timeout (gateway_client.gd's timeout_s envelope — the Redot 26.2 `timeout` property), the truthful UNKNOWN/refusal/transport notes, FAILED terminal shown never hidden (D-203's gap displayed, not collapsed); the owner-reported launcher warnings fixed — the UNUSED tag → _tag, the SHADOWED _text → _body, the hygiene pinned in the contract test) + the 17-test claim packet (tests/test_managed_backend.py: the flag set, the process mechanics with the OBSERVED stop code (a POSIX TERM reports the signal — the truth, never a guessed 0), the lifecycle policy incl. THE REAL SPAWN over the stand-in server tests/_managed_fake_server.py (readiness through the adapter's own probe, the graceful stop's observed code) + the honest unavailable causes + the launcher wiring/precedence (--no-backend wins over --managed)) + test_shell_contract.py's three wb-8 pins; zero core change, zero pack change, the LOG untouched, INV-4's two-surface form unchanged — the wrapper only decides WHEN a process must exist, the HTTP stays cli/engine.py's; D-206 the DELIVERY PROTOCOL'S STANDING FORM (iter-224, the owner's 2026-09-25 «тебе ничего комитить и пушить не надо» directive over the wb-8 handback): the agent has NO push access to the owner's repository and NEVER runs git commit/push against it — the owner does all own-side git personally; the sandbox git is bookkeeping only (BASE_COMMIT + the changed-path enumeration, never a deliverable); every file-touching iteration delivers the delta archive on BOTH channels (the chat attachment + the tmpfiles.org direct link with md5 and byte size) and closes with the owner-side Git Bash block — cd the owner root (/c/Users/fallo/OneDrive/Desktop/repo/canonsim), git status --short, git add <explicit paths>, git status --short, commit, push; re-listing an already-committed path stages only the real diff, the block re-runnable (AGENTS §9/§12 the law owner, the §12.1–12.3 recipe); D-207 the ENGINE REFERENCE INDEX ADMISSION (iter-225, the owner’s 2026-09-25 «отразить в репозитории» call over the external Redot/Godot agent reference index — the tmpfiles hand-off ephemeral, the engine routing homeless): the owner’s external artifact admitted as docs/REDOT_ENGINE_INDEX.md — the version firewall (Redot 26.2 LTS pinned, Godot secondary cross-reference only; Redot 26.2 has NO HTTPServer class — the app gateway stays Python-side) + the routing map for every engine/API/UI/networking/performance/export question on the Redot surface (wb-9+’s engine-facts owner); admission deltas only (§2.2/§2.3 reconciled to the landed wb-4..wb-8 surface + the v5.2 docs’ external-by-law status (D-024/D-200), §10’s skills pinned agent-side per AGENTS §2.8, the external changelog cut — history to git/worklog); docguard’s allowlist carries the 600-cap’s §6.1 rationale; the routing surfaces: NAV §1/§2/§3 + STATUS FAQ + TASKS’ wb-9+ pointer + CONTRACTS §5 + README’s agent list; the index rots by design — the REFERENCES quarterly pass its refresh; D-208 the WB-9 MODEL-FLOW LANDING (iter-226, the owner's 2026-09-26 «открыл воркбенч, зашел и загрузил модель» + «подтянуть модель откуда угодно» + «настройки запуска llama.cpp... сэмплеры всякие» calls over the v5.2 brief — the model flow end to end): INV-4 gains its THIRD sanctioned surface, owner-gated — workbench/platform/model_fetch.py the OUTBOUND model-assets fetch (HTTP GET downloads only: the operator URL vocabulary direct/HF-resolve/HF-blob→resolve/hf:namespace-name/file → the .part stream + the atomic rename into the §16 MODELS_ASSETS root; the §12 checkpoint as the injected cooperative seam — the caller's WorkCancelled/DeadlineExceeded carriers propagate untouched; the D1-family failure vocabulary; AGENTS §4/§8 + the architecture pin reworded, the same D-201 admission form) + the model.fetch WORK KIND (models.py over the INJECTED fetcher — the BackendPort pattern: admission gates BEFORE the run exists (the occupied name, the .part residue, the non-plain name — NOT_SENT, never a post-admission failure), the run registry's wb-9 progress surface (WorkContext.progress → the run.get document's live field, JSON-safe-gated) + the kind's own minutes-class default deadline 3600s (the WorkKind field, chat.send's own-deadline pattern)) + the LAUNCH-SETTINGS FAMILY (workbench/application/settings.py: the typed LaunchSettings document — exe/context/ngl/fa/jinja/no-webui/temp/top-k/top-p/min-p/repeat-penalty/extra_args, closed-set validated loud never clamped; the atomic schema-tagged persistence at workbench/runtime/settings.json (§16's USER_CONFIG role, gitignored); the loud load (corrupt/foreign-schema/unknown-field refuse, never a silent reset); backend.settings + backend.settings.update over the gateway, the command preview + managed-liveness INJECTED at the composition root (the store never imports the platform — §6's direction law); chat.send's absent-temperature default resolves from the store (§19.1's BASE layer, the explicit call-local value still wins)) + the RUNTIME LAYOUT DECISION (the owner's «определись где будет папка» call: workbench/runtime/ the gitignored root — models/ (auto-created, §16's recovery-by-creation), llama.cpp/ (the drop folder, the launcher's deterministic discovery scan: root level then the sorted subfolders, then PATH), settings.json) + scripts/workbench_app.py's MANAGED DEFAULT FLIP (--attached restores the wb-8 observe-only form; the settings load + the CLI-over-store per-field overrides read at each spawn; the models-dir auto-create for the DEFAULT path only — a custom missing dir stays the loud typo guard) + scripts/workbench_launch.py the ONE-COMMAND LAUNCHER (the gateway child's stdout bind-line watch — the readiness evidence WITHOUT a launcher-side socket; the Redot child after the bind; either exit or Ctrl+C stops both, the gateway first so the managed llama-server rides its own graceful path; SIGBREAK on Windows) + the Redot half (shell.gd: the Settings surface goes REAL — the typed launch fields + the collapsed advanced extras + the command preview + the honest next-spawn/LIVE note; the Models surface's MANAGER — the URL row, Fetch/Cancel over run.start/run.cancel, the distinct fetch-get poll tag, the live progress note, the honest terminal notes; the offline hints name workbench_launch.py); the 35-test claim packet (tests/test_model_fetch.py 11 + tests/test_settings.py 10 + the workbench_app/managed/shell_contract extensions — the REAL end-to-end fetch against a live loopback HTTP file server, the settings roundtrip over served HTTP, the chat-temperature BASE resolution, the exe resolution order); zero core change, zero pack change, the LOG untouched; D-209 the WB-10 OWNER-EXPERIENCE LANDING (iter-227, the owner's 2026-09-25 fix list over the wb-9 handback — the launcher's Popen `buffering` TypeError + «где найти redot.exe и как его подключить?» + «просто открывающийся проводник и выбор уже скаченных локальных моделей» + «пользователь не должен вводить команды чтобы запустить или скачать что-либо!» + «интерфейс вверх убожества»): the ZERO-COMMAND owner experience — scripts/workbench_launch.py reworked (the bufsize fix; the FOLDER-AWARE Redot resolution — a release FOLDER scans for the engine executable (the known names in preference order, redot.windows.editor.x86_64.exe first, then the sorted redot* glob, root + one folder deep); the chain --redot-exe (exe or folder) > the persisted workbench/runtime/launcher.json > REDOT_EXE > the Desktop-shaped common-roots auto-scan > the native tk folder picker ONCE; the explicit CLI/env forms strict-verbatim (the spawn's own loud error, workbench_app's resolve_llama_exe law), the persisted pick recovering when stale; the OBSERVED bind URL forwarded to the Redot child as --gateway-url; the bare "--" separator stripped before the child's parser — the latent wb-9 gateway-arg bug) + the model.import WORK KIND (models.py: a LOCAL copy into the models root — one or more absolute paths, the fetch kind's admission gates (exists/plain/unoccupied/no-.part/no-duplicates — NOT_SENT), the .part stream + the atomic rename, the per-file live progress + the cooperative §12 checkpoint per chunk, the best-effort .part cleanup; NO network anywhere — INV-4's three-surface form UNCHANGED, the kind wired into the default composition unconditionally) + discover()'s models_root (the frontend's open-folder answer, never a local guess) + the Redot half (shell.gd's native-picker Models manager — Add local models…/Add folder… over FileDialog.use_native_dialog + ACCESS_FILESYSTEM (the engine surface verified against redot-26.2-stable's scene/gui/file_dialog.cpp), Open models folder via OS.shell_open on the gateway's own models_root, the URL fetch demoted to the collapsed advanced row; the theme@0.2 visual pass) + Workbench.bat/Workbench Setup.bat the repo-root double-click entries (the python/py resolution, the honest pause on failure — §22's "a .bat/launcher may select runtime paths but must not embed a second backend client" held) + the 23-test claim packet (test_model_import.py 8 + test_workbench_launch.py 12 incl. THE REAL SPAWN integration — the buffering crash pinned dead — + the shell_contract/workbench_app/operations pins); zero core change, zero pack change, the LOG untouched; D-210 the WB-11 TRANSPORT-CHAIN LANDING (iter-228, the owner's 2026-09-26 report over the wb-10 handback — «молча висят + транспорт результ 13» + «модель выбрать не могу, там пусто, моделей не видно, кнопка выбрать модель не работает (проводник не открывается)» + the evidence «llama.cpp и модели запускаются штатно если отдельно запускать»): the transport-13 freeze chain pinned dead — model.load/model.unload become RUNS (app §8's own law, the chat.send pattern: the dispatch validates + walks the ladder + admits + launches under milliseconds, the minutes-class managed spawn/readiness walk and the graceful stop ride the registry's worker thread OFF the coarse dispatch lock; the WIRE SHAPE CHANGE — model.load/model.unload now return the execution document {execution_id, state: STARTING, work, deadline_seconds}, the terminal truth rides run.get, the FAILED diagnostics carry the observed cause str(exc) — §21's honest surface, never an error_type-only blackout; MODEL_LOAD/UNLOAD_DEFAULT_DEADLINE_SECONDS the rows' own §12 budgets; the single-slot + in-flight admission guards — ModelLoadStates.active's own documented law, enforced) + llama_process.py's _PipeDrain (one daemon reader per captured pipe into a bounded 64KB tail ring: the Windows pipe-buffer wedge — an undrained PIPE fills, llama-server blocks mid-write, never answers /health, the readiness walk burns its budget — pinned dead; stderr_tail reads the ring, cross-platform, the os.set_blocking-on-Windows '(empty)' bug dead) + shell.gd's run-poll Models circuits (model-load-get/model-unload-get on the shared tick; the honest picker/load guards — never a silent return, §18; the failed scan re-arm so the empty list never sticks) + the regression pin: model.list ANSWERS while a gated load stands in the port call (THE DISPATCH-LOCK test); zero core change, zero pack change, the LOG untouched, INV-4's three-surface form unchanged; D-211 the VISUAL SYSTEM ADMISSION (iter-229, the owner's 2026-09-26 «вот и отлично, зафиксируй где нужно и обращайся при дальнейшей работе! нынешний ui - ужасен! поэтому всеми тремя руками и ногами - за!» call over the external 24-section Visual System and UI Engineering instruction document — the D-207 engine-index admission pattern): docs/VISUAL_SYSTEM_UI.md ADMITTED as the visual rows' LAW owner — the surface-driven grammar (CANVAS/SURFACE/CONTAINER/CONTENT/ACTION, not card-driven; the native dialog preferred over any in-engine imitation), the semantic token taxonomy (SURFACE_*/CONTENT_*/ACTION_*/BORDER_*/STATE_*/AUTHORITY_* — the theme file the single source, a color outside a token forbidden), the color law (a neutral ramp + exactly ONE accent; status colors semantic STATE_*; never color-only state), the per-surface visual state matrix (Chat EMPTY/ACTIVE/GENERATING; Models EMPTY/DISCOVERED/ACTIVE/LOADING/FAILED; Settings DEFAULT/EDITING/DISABLED/ERROR — a state outside the matrix is a bug, a matrix state without a visible rendering is a bug), the transplantation protocol (SOURCE→TECHNIQUE→MECHANISM→INVARIANT→ADAPTATION→CONSEQUENCE — mechanisms, not looks), the reference pack under AGENTS §2.8 (Godot Modern/Minimal, ThemeGen a TOOL never a decision source, LobeHub/Jan/LM Studio/Open WebUI, dot-ui/Iso-Themes, Godot Theme Explorer, editor-icons, SillyTavern LICENSE-SENSITIVE mechanisms-only), the §8 per-row UI report (TARGET PROBLEM/CURRENT PRIMITIVE/TRANSFERRED MECHANISM/INVARIANT/FILES AFFECTED/EXPECTED CONSEQUENCE/REJECT CONDITIONS/VERIFICATION STATE — every visual row closes with one), the anti-patterns, the wb-12+ owner-gated candidate queue (the token audit, then Models, Chat, Settings, the later surfaces); THE STANDING RESOLUTIONS: functional rows first (a surface that lies is worse than a surface that is ugly), the doc is the LAW for each row NEVER a parallel standing program (the external document's program framing resolved), the app spec's §18 evidence law wins over quiet chrome (the badge/status/effective state never hidden for visual calm); routing surfaces: NAV §1 + TASKS wb-9+ pointer + STATUS FAQ; doc-only, zero code change, the LOG untouched; D-212 wb-12 the visual token audit LANDED (iter-230, the owner's «тема и UI все так же убоги» + «не происходит плавной прокрутки вниз» calls — VISUAL_SYSTEM_UI §10's queue head, the law D-211 + this landing): theme@0.3 — the neutral ramp re-pinned over the Catppuccin Mocha VALUE reference (§6's transplantation applied to VALUES: the mechanism named — the tuned layered luminance separation; the provenance noted in the theme header, no assets no code), exactly ONE accent (the #89b4fa family: accent/accent_soft/focus_ring; the warm-orange family + its brown-tinted pressed/selected surfaces retired, accent_deep retired as token-without-consumer), the NavButton type variation + styles/chip_busy; the chat follow law rides the same row (the tween over the scrollbar's float value never the int scroll_vertical jump, the layout-settle await before the target read, the near-bottom gate so a reading owner is never yanked, the follow on every role, the bounded late-layout re-settle) + the GENERATING chip (§5's Chat matrix state made visible: a pulsing dot AND a text label); D-213 the theme@0.4 OLED re-pin LANDED (iter-232, the owner's «цвет лучше взять темный под oled мониторы, но не синий такой убогий» call — a wb-12-family values row, VISUAL_SYSTEM_UI §3 the law): the neutral ramp re-pinned TRULY neutral (R=G=B, the Catppuccin navy tint retired) over the near-black #050505 base — the external brief's "never pure black" doctrine OVERRIDDEN by the owner's explicit OLED call (AGENTS §2.6, the session's owner request IS the task; the layered-luminance mechanism survives as pure lightness steps) + the ONE accent moved to teal #4cc9a6 (the owner's two named rejections: the iter-230 blue #89b4fa AND the older warm-orange family); values-only — every token name, size, spacing and pin unchanged, 21 contrast pairs measured (scripts/contrast_check.py); D-214 the frontend UI/UX law ADMITTED (iter-233, the owner's tmpfiles hand-off of the external Ultimate Frontend/UI/UX/Visual Architecture consolidated pack + the «начать работать в этом направлении» direction call): docs/FRONTEND_UIUX_LAW.md the repo-side binding distillation (the D-024/D-200 no-vendoring law) — the interaction law owner over the WHOLE frontend domain: the executive doctrine (the UX invariant + the implementation invariant + the missing-middle-layer diagnosis), the law chain (spec integrity), the IA (WORK/RESOURCES/SYSTEM), the workspace grammar, the shared selection model, focus+context, semantic zoom, compare, timeline, graph policy, the evidence ladder, the epistemic grammar, the query lifecycle, empty semantics, drill-down, the Evidence Capsule, T1–T8, the cost budgets, the accessibility/reduced-motion/focus-keyboard/responsive/DPI/localization contracts, the component law, Scene IR integration, bounded rendering, surface hygiene, the verification matrix (static≠runtime≠task; the A–I gates), the anti-patterns, the agent rules, the P0–P4 order, the contradictions register, the 20 invariants; VISUAL_SYSTEM_UI §11 the companion-requirements routing (the pack-diagnosed drift repair) + §10 re-pointed (ux-1/obs-1 before indefinite peripheral polish); the ux-1 + obs-1..N rows registered; 627 lines after three §6.1 cruft rounds (the enum/invariant lists the substance — docguard allowlisted); D-215 ux-1 the P0 minimums (iter-234, FRONTEND_UIUX_LAW §25's own order — the boundary BEFORE surface count grows): the localization boundary's committed form is scripts/strings.gd (preload + static `lookup`, NEVER `tr` — Object's native signature makes `static func tr(` a parse error; the honest fallback returns the key; the canonical protocol vocabulary renders verbatim, never re-worded) with locale resolution --lang > CANONSIM_LANG > OS and proof runs explicit-only (byte-stable captures, ru included); reduced motion is a UI-local persisted setting (workbench/runtime/ui_state.json, proof-untouched) with static equivalents for every tween/pulse; the viewport policy is min 1152x700 + stretch canvas_items/expand at base 1440x900; the keyboard baseline is Esc/Ctrl+. stop + task-aware focus entry per surface; KI#97's law — the REDOT_EXE-gated pins ride the live shell (a skip is not a pass), a verification harness pins its own window and never inherits the product's resize contract; D-216 obs-1 the Observatory vertical slice (iter-235, the LAW's §50 standing resolution — the grammar BEFORE the backend, never the 1800-line jump): the nav adopts the §4.1 IA as SURFACE_GROUPS (WORK/RESOURCES/SYSTEM intent groups, never a flat catalog — Runs joined SYSTEM-planned per the LAW's map, Inference kept, nothing silently dropped); the slice's committed form is scripts/observatory.gd — the LAW §18 split seed: the surface composes ITSELF through compose(theme, translator) with the shell injecting its own _tr Callable (ONE boundary, ONE token source; the observatory never opens the catalog or the theme directly); the regions are the LAW §5.1 grammar (breadcrumb/context/question/primary view/inspector/evidence ladder) with honest-empty values (no-session, not-set, NO DATA — the distinct empty semantics), the DRAFT lifecycle as the read-only truth, and the evidence rungs' unknown as text (§13's not-color-only); the runner law — visual_proof.py's --surface never gates the vocabulary (the shell owns it and validates with exit 2; the choices= restriction was the same KI#97 drift family); D-217 obs-2 the live run over the READ-side seam (iter-236, the LAW §25 P1 continuation + the selection model's first consumer): the seam's one home is workbench/observatory_read.py (the bounded read model over core.log.read_log — the scene_build read-side edge, the application-operations package stays CanonSim-free per its dependency envelope; the operations layer workbench/application/operations/observatory.py owns only the closed argument sets + the DOMAIN_REJECTED translations) serving observatory.runs (the discovery scan, honest degradation per corrupt header, content truths only — no mtime) + observatory.read (the after-cursor window over event IDs, default 50/cap 200 as the ONE boundedness ceiling, authority CANONICAL rendered never guessed, duplicate-id logs refused); the composition root wires both over the injected runs root + schema (half-wired refused loudly) with workbench_app.py passing the cli's own logs/ law (INV-1's source, never a second copy); the surface form — observatory.gd v0.2 speaks ONLY through runs_requested/read_requested (the shell owns the transport, LAW §18) with feed_runs/feed_read/feed_rejection/feed_transport_failure in, the picker + Refresh + Earlier/Later pagination, the selection as the event ID on button metadata (set_meta/get_meta, never a row index) restoring by ID across re-reads (§6.1), the inspector scrolling (the boundedness law) with the declared cause + state changes as DATA (a cause_id never confirms the BRANCH rung — only READ confirms under a selection), the distinct empties probing/NO RUNS/NO EVENTS/refused, and the breadcrumb Observatory → run → event · kind; the Redot 26.2 lessons pinned in code — JSON.parse is an instance method (parse_string the static form), JSON numbers parse as FLOATS (integer-valued floats normalize back via _int_text/_normalize_numbers — the canonical ints never render "42.0"), a GridContainer value column needs SIZE_EXPAND_FILL (clip_text alone yields a zero-width column), and JSON null is a PRESENT key (get()'s default never fires; String(null) is a runtime error); the proof law — --obs-document is the harness's ONLY injection (a REAL op-produced document through the same feed path, proof-mode only, exit 4 its refusal code, the meta's observatory_run keeps a loaded capture honest); D-218 the V5.2 CORPUS RE-HOMING (iter-237, the owner's 2026-09-25 «переработай документацию нормально и мне не приходилось её загружать постоянно» call over the re-delivered 8-file corpus): the loss/ownership audit first (never a merge) — verdicts: the engine index FULLY absorbed (D-207), the integration/migration docs SUPERSEDED, the frontend spec ~75% via the consolidation with 10 MATERIAL contract losses in the LAW distillation (dense-data/forms/search, task modelling, chat ergonomics, the AI-interaction contract, typography/dimension tokens, Gestalt/density, usability evaluation + metrics, the acceptance benchmark + G1–G12, budget categories, two-layer identity), the app spec ~60% implemented with its un-landed contracts DANGLING on unresolvable §-references (26 sites), the Observatory analytical layer + world-presentation contracts UNOWNED; the repair — THREE new law owners admitted on the D-207/D-211/D-214 pattern: docs/WORKBENCH_APP_LAW.md (the app spec's binding distillation, §-numbering preserved 1:1 so every existing `app §N` citation resolves with zero edits), docs/OBSERVATORY_LAW.md (the control plane; §-numbering preserved; the research findings stay EVIDENCE SNAPSHOTS per the source's own PROPOSAL status), docs/WORLD_PRESENTATION_LAW.md (the P4 family's scene/asset/manifest/LOD/degradation contracts); the existing owners REPAIRED not duplicated (FRONTEND_UIUX_LAW +§11.1/§14.1/§20/§21.1/§21.2/§22.1/§22.2 + the anti-pattern completions; VISUAL_SYSTEM_UI +§1.1/§2.1/§2.2/§12; REDOT_ENGINE_INDEX +§20.1 the version-admission gate); the §-reference drift itself repaired (CONTRACTS §5's mapping note + the TASKS/STATUS citations re-pointed — the drifted 'LAW §50/§51/§39/§42' numbers were the consolidation's, not the LAW's); the standing resolutions: the external corpus stays EXTERNAL-ONLY source material (D-024/D-200 unchanged — never vendored), no repo work requires it anymore, documentation ≠ implementation (each new-law section carries LANDED/CONTRACT markers); D-219 inf-1 the llama.cpp semantic inference-control landing (the owner's 2026-09-25 chip-workspace hand-off: Settings ≠ Inference Control, ONE semantic authority — docs/LLAMA_CPP_INFERENCE_CONTROL_LAW.md the binding owner; workbench/application/inference.py the semantic core — the control library, the profile store, the resolver, the compiled spawn surface; the settings store slims to DEPLOYMENT schema/2 with the value-preserving one-way migration; chat's BASE temperature re-points to the resolver; the extra_args hatch never shadows a semantic control — the compile-time conflict; the Inference surface under WORK, Chat carries the projection only; D-220 inf-2 the full chip library + the workspace law (the owner's «доделывай по-человечески» call): the library carries the WHOLE reviewed surface (85 controls over the 15 categories, the complete sampling family, every disabled/AUTO form the runtime's own literal; the profile FIELD-STABLE — the inf-1 fields unchanged, the chain upgrading 5→9 at load with the operator's order preserved; the pinned ids ride the same document's workspace section, never a second store); the relations are typed data (the mirostat noop pinned from the --help's own words), never a rule engine; the compile seam splits field-keyed values (compile_semantic) from flag syntax (SEMANTIC_FLAG_TABLE + build_semantic_command, unknown fields LOUD) with build_server_command byte-stable and the 138-token guard; the Redot workspace is DATA-DRIVEN over the read document's own metadata and builds ON the read (inf-1's compose-before-read empty-groups bug repaired), the §30 presets transparent diff-previewed, the pin chips quick-access reveals (owner: LLAMA_CPP_INFERENCE_CONTROL_LAW §21; the packet tests/test_inference.py)); D-221 ssi-1 the ARCHITECTURE FREEZE (Phase 0 of the owner's 2026-09-26 SSIEC-v3 control-plane call — the external governance package as a CONTROL PLANE over the repo, never a runtime dependency, no big-bang rewrite): for the ssi initiative's duration the public-contract surfaces are FROZEN — the event schema (schemas/event.schema.json + docs/EVENT_SCHEMA.md, INV-1's 2-place sync), the canonical log format (core/log.py the one canon-write path + the header fields, INV-1/INV-5 + AGENTS §8's standing stop-rule), the fold semantics (state = fold(log), INV-1), the scheduler order (the (tick, sub_order, actor_id) queue key, INV-2), the RNG semantics (core/rng.py the RngBank authority, D-028), the pack format (core/pack.py + PACK_SPEC + core/packlint/), the checkpoint semantics (core/checkpoint.py), and the Workbench-gateway public operations (workbench/api/gateway.py + contract.py over WORKBENCH_APP_LAW's §-map) — confirmed where they already stand (INV-1..5, EVENT_SCHEMA.md, PACK_SPEC.md, TEST_PLAN §1.1 — never re-stated, D-024); the strangler phases (ssi-4..ssi-6) move INTERNAL ownership only, a public-surface change needs the owner's explicit call outside the initiative; the standing no-touch floor named: core/log.py, core/rng.py, core/schema.py, the fold mechanics (R3) [PCC: intent=freeze the initiative's blast radius at zero public-contract drift before any SSI machinery lands; invariants=INV-1..5 hold untouched through every ssi phase, the 2344+9 suite green without edits; delta=doc-only — zero code, zero schema, zero pack, zero LOG change (docs/TASKS.md the ssi track + this row + STATUS KI#99/worklog); verification=the full suite + ruff + docguard green at BASE_COMMIT 4a1eb8c before any edit, re-run green after; provenance=the owner's 2026-09-26 request + the external SSIEC-v3 package (md5 305952c10b0997236b93d3c77c2b7681, 147249 bytes, docs/ssi/ per D-222); runtime=none — a documentation freeze]]; D-222 ssi-2 the SSI FOUNDATION (Phase 1, the owner's same call): the placement decided on D-198's admission ladder — existing mechanism first (docs/ already hosts the admitted external law packs D-214/D-218/D-219 and the subdirectory precedents worldbuild//ref//blueprint/), so the READ-ONLY reference copy lands docs/ssi/ (the 34-file package verbatim, md5-pinned in the overlay's header; the overlay the ONLY authored file — a top-level ssi/ REJECTED: documentation, not code, and the minimal-shift law; core/ssi.py and any runtime dependency REJECTED standing: the control plane is docs + lint, never imports) with the two over-cap doctrine files (unified-v3 1946 lines, unified-v2 1420) joining docguard's allowlist on the phases.md research-archive precedent; docs/ssi/SSI_OVERLAY.md the overlay — the A–L block matrix mapped onto the repo's existing mechanisms (AGENTS §4/§2, the STATUS KI lifecycle, DECISIONS, test_architecture + the stoplist test, TEST_PLAN's stack) as a table, never a rewrite (D-024); the risk ladder R0–R5 (controls/risk-classes.yaml) admitted as AGENTS §2.9 — a refinement of the 3–5-file soft limit, never a replacement: R0–R2 zero added bureaucracy, R3+ self-declares the class tag and carries the compact PCC record in the DECISIONS row (the owner's no-separate-file-per-change call); docguard gains the SSI shape families (the overlay's block matrix complete + the closed state vocabulary + the explicit waive/skip laws + the declared eight-rule subset with instruments-on-disk + the phase ladder owner-gated + N018's evidence bar + the R3+ PCC fields + the AGENTS ladder pin) with the crafted-breach tests; (R3) [PCC: intent=the external governance package becomes an executable control plane without becoming a runtime dependency or a second source of truth; invariants=INV-4's network surface untouched (docs + lint only, zero engine imports), D-024's one-owner law (the overlay maps, never restates), the docguard law (lint = counts over shapes, never taste) held by the new families; delta=docs/ssi/** (34 reference files + the overlay), AGENTS.md §2.9, scripts/docguard.py (+3 check families + 2 allowlist rows), tests/test_docguard.py (+7 crafted-breach tests + the fixture), docs/AGENT_NAVIGATION.md (§1/§2/§3 routing) + the state docs; verification=17 docguard tests green (each crafted breach trips exactly its family) + the real-repo drift pin clean + ruff clean + the full suite green without functional change; provenance=the external SSIEC-v3 package (md5 305952c10b0997236b93d3c77c2b7681, 147249 bytes) read-only under docs/ssi/; runtime=none — the lint runs in CI only]; D-223 ssi-3 the EXECUTABLE NEGATIVE-RULES SUBSET (Phase 1's close — the owner's chosen eight, never all twenty): SSI-N001 no-hidden-effect + SSI-N002 no-new-primitive-without-gap land in tests/test_architecture.py as the stdlib-ast family's widening — N001 the wall-clock/entropy import ban (time/datetime/secrets/uuid across the canonical kernel core/sim/render/brief/cli; INV-2's static surface widened from the bare `random` monopoly — the entropy authority stays core/rng.py, workbench/ outside the envelope per TEST_PLAN §8.4), N002 the admission-closure check (every PACKAGE_DIR carries its NAV §1 owner row; the sim/ row landed with this — the admission-completion fix, not a waiver); SSI-N006/N007/N010/N017/N018 already executable in docguard per D-222; N018/N020's full executable forms stay OPEN by design — N018's trajectory audit fires with ssi-3 (Phase 2), N020's gc-gate with ssi-8 (Phase 7), each OPEN in the overlay's rule table, never a silent skip; the other twelve rules stay reference-only on the owner's call (AGENTS §2.8 — no machinery without a consumer); (R3) [PCC: intent=the control plane's claims are executable, green at HEAD, and honest where they are not yet executable; invariants=the eight rules ride the TWO existing instruments (test_architecture + docguard) — no new tool, D-198's admission ladder held; delta=tests/test_architecture.py (+2 checks, the family's docstring (f)/(g)), docs/AGENT_NAVIGATION.md (the sim/ row) + the state docs; verification=8 architecture tests green (N001/N002 included) + the 17 docguard tests + the full suite green without functional change + ruff clean; provenance=negative-rules.yaml's subset verbatim in the overlay's rule table (docs/ssi/controls/negative-rules.yaml) + the owner's chosen-eight list; runtime=none — CI only]; D-224 ssi-3 the OWNERSHIP/TOPOLOGY AUDIT (Phase 2's close — the owner's 2026-09-26 go-ahead over the ssi-3 row): the machine-readable map at docs/SSI_TOPOLOGY.md (the 82-module owner/reads/writes/emits table over core/ + workbench/ Python — the mechanical columns derived by the NEW scripts/topology.py, the owner column authored from NAV §1 + the spec docs; --audit regenerates the full JSON at any HEAD, --check drift-pins the inventory + the owners + the watchlist rows' reads/emits so a seam-relevant change without a map update goes RED, tests/test_topology.py the 7-test claim packet — the instrument admitted per D-198's ladder with ssi-4/ssi-5 + the N018 trajectory audit as its named consumers, the audit inputs being code + git history, outside both docguard's and test_architecture's charters) + the co-change/trajectory audit over the last 150 commits (the window's code pairs ranked: loop.py↔pack.py the #1 at 11 co-changes/lift 9.07/Jaccard 0.69 — numerically the external hypothesis CONFIRMED, but ALL 11 commits in iter-90..168 (the add-a-system era), both files dormant since, pack.py already decomposed 3990→290 via core/packlint/ — the coupling HISTORICAL, largely dissolved, DORMANT at HEAD) + the verdict table (the five line counts CONFIRMED exactly; the implied live core coupling REFUTED — the four god-objects + pack untouched since iter-107..169 (75+ iterations; core/'s only post-iter-169 touches: the iter-191/199 economy-gloss constants + the iter-200 packlint rename guard, none a god-object — the iter-245 verification's precision); inference.py CONFIRMED the active hotspot: born 2511 lines in two iterations, single in-repo dep) + the phase consequences recorded in the map's §5 (ssi-4 CONFIRMED the first strangler step with the LAW's three-layer split naming the semantic owners; ssi-5's core scope SHRUNK per the pre-registered rule — refuted live coupling = shrink, the phase stays owner-gated; ssi-6's read-seam consumer set named from the map: scene_build/observatory_read/scene_ir) + the overlay updates (block B VERIFIED, N018 VERIFIED — the trajectory audit is now the executable, never a snapshot, the phase ladder's Phase 2 CLOSED); (R3) [PCC: intent=the Phase 2 audit delivers the N018 evidence base — co-change/trajectory over git history, never a line-count snapshot — and the living map the ssi-4/ssi-5 splits consume; invariants=N018 upheld (the external numbers become CONFIRMED-numeric/REFUTED-as-live-coupling, the recorded figures BASE_COMMIT-pinned with the one-command regeneration), D-024's one-owner law (the map's owner column links the owning specs, never restates them), the stdlib-only + sorted-iteration discipline in the instrument, zero runtime change (no core/ or workbench/ code touched); delta=docs/SSI_TOPOLOGY.md (new, 240 lines), scripts/topology.py (new, the audit instrument), tests/test_topology.py (new, the drift pin + 6 crafted breaches), docs/ssi/SSI_OVERLAY.md (block B/N018/phase ladder), docs/AGENT_NAVIGATION.md (§1/§2/§3 routing rows) + the state docs; verification=2360 passed + 9 skipped + ruff clean + docguard clean + topology --check clean at HEAD (each crafted breach trips exactly its family: dropped row, stale row, ownerless row, watchlist reads/emits drift, missing map loud); provenance=the owner's 2026-09-26 go-ahead over the ssi-3 row + the external analysis's hypotheses re-verified independently (the tooling: git log --name-only co-change over the exact last-150-commits window, git numstat trajectories with the boundary-commit fix — `git log -N -- path` limits the output list, not the window, the trap the instrument's own docstring pins); runtime=none — the check runs in CI only, the audit regenerates on demand]; D-225 ssi-4 step 1 (iter-247, the owner's «ssi4 вроде открыть можно» go-ahead): workbench/application/inference.py (2511 lines) became the workbench/application/inference/ PACKAGE — the semantic-control DATA half extracted verbatim (library.py the control model + the 85-control library + the indexes; chain.py the §11 membership/order semantics; presets.py the four §30 presets; relations.py the typed relation data) + the facade __init__.py (the behavior halves pending their own steps + the re-exports; __all__ completed with the de-facto public names — SemanticControl, DEFAULT_CHAIN, CHAIN_FAMILIES, STATES, the relation/preset types); the public import surface `workbench.application.inference` UNCHANGED (every consumer and test imports exactly as before — tests/test_inference.py untouched), the gateway operations inference.read/update byte-stable; pyproject's explicit packages list + the WATCHLIST successor = the package facade (scripts/topology.py, the map rows refreshed at the split, the drift-pin test re-anchored to the facade row prefix); the LAW/APP_LAW/NAV/STATUS path syncs; (R3) [PCC: intent=the audit-CONFIRMED active hotspot (D-224) straightened at its cheapest moment — the split follows the LAW's own §2 semantic owners, never an external template, the strangler method the packlint decomposition proved (D-175); invariants=INV-4 untouched (zero new network/engine/platform imports — the package imports stdlib + the gateway types only), D-221's freeze untouched (zero public-contract drift — the ops byte-stable, the profile schema unchanged), D-024's one-owner law (the data modules' docstrings point at the LAW, never restate it); delta=code-class only — a module-to-package move, zero behavior change, zero test-file change (the 34-test claim packet green UNCHANGED over the moved code); verification=2361 passed + 9 skipped + ruff clean + docguard clean + topology --check clean at HEAD (the drift pin green over the new watchlist facade row); provenance=the owner's 2026-09-26 call + the ssi-3 map's CONFIRMED hotspot verdict + the packlint precedent; runtime=none — a pure code move, the rollback is the reverse move (git), no persisted-artifact migration]; D-226 ssi-4 step 2 (iter-248): the RESOLUTION half out of the facade — profile.py (the InferenceProfile document + the validation family, §19.1's ACCEPTED laws: rejected LOUD, never a silent clamp, data-driven over the library's own metadata) + resolver.py (STATES + resolve + _chain_item_state, the law's §6/§8 effective-state resolver — a pure function of the profile, no I/store/gateway) extracted verbatim; the facade keeps the store/migration/operations/compiled halves + the re-exports (__all__ completed with ChainItem/RELATIONS/effective_temperature, the de-facto public names); the map rows refreshed in the SAME iteration — the drift pin went RED on the facade's reads + the two missing inventory rows (the designed loudness), then green; (R3) [PCC: intent=the resolution half at its semantic owner, the facade shrinking 1157→700 toward the pure re-export shell, the split's middle move on the same method as D-225; invariants=same as D-225 — INV-4 untouched, D-221's freeze untouched (the ops byte-stable, the profile schema unchanged), D-024 (the modules' docstrings point at the LAW/APP_LAW owners, never restate); delta=code-class only — two new modules + the facade trim, zero behavior change, zero test-file change; verification=2361 passed + 9 skipped + ruff clean + docguard clean + topology --check clean at HEAD (the RED→GREEN drift-pin cycle observed in-iteration); provenance=the owner's 2026-09-26 go-ahead + D-225's method; runtime=none — a pure code move, git the rollback]; D-227 ssi-4 CLOSE (iter-249, the phase's final step): the PERSISTENCE/OPERATIONS half out of the facade — store.py (SCHEMA + InferenceStore: load-loud/current/update-atomic/persist), migration.py (the one-way settings/1→settings/2+inference/1 migration), operations.py (the gateway family inference.read/update + effective_temperature — the ops now deriving at their TRUE owner, the map shows them there), compiled.py (compile_semantic, the platform seam's field-keyed input) extracted verbatim; the facade = the PURE re-export shell (176 lines: the docstring + the re-exports + __all__); PHASE 3 CLOSED — the split complete: 2511 lines → 10 owner modules + the facade, zero behavior change across all three steps, the public import surface + the gateway ops + the test claim packet byte-stable throughout (tests/test_inference.py untouched since iter-240); the overlay's phase row → CLOSED; the drift-pin test re-pinned to the emission-free facade (an op re-added to the facade = RED); (R3) [PCC: intent=the final half at its semantic owners, the facade converging on the pure re-export shell — the phase's acceptance bar met: the audit-confirmed hotspot decomposed by the LAW's own §2 semantic owners at zero public-surface drift; invariants=INV-4 (operations.py imports the gateway types only, the injected-preview seam law preserved), D-221's freeze (the gateway ops byte-stable — the registration moved, the op names/kinds/documents unchanged; the profile schema unchanged), INV-2 untouched (workbench outside the determinism envelope, zero core change across the whole phase); delta=code-class only — four new modules + the facade's final trim, zero test-file change across the phase (2361+9 at every step); verification=2361 passed + 9 skipped + ruff + docguard + topology --check clean at HEAD (the drift pin RED→GREEN each step — the in-iteration map refresh the watchlist's own law; the facade-completeness import check over the 24 public names); provenance=the owner's 2026-09-26 go-ahead + D-225/D-226's method + the packlint precedent (D-175); runtime=none — pure code moves, git the rollback, no persisted-artifact migration]
