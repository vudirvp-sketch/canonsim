# DECISIONS.md — ADR-lite

> Append-only **within a phase**; each entry: decision → why → consequence.
> Supersede with a new entry, never delete mid-phase. Cap: 30 entries,
> enforced by collapsing at phase gates (D-034): family merges write compound
> IDs (`D-018/D-022/D-029`) so external citations still resolve; compressed rows
> keep the decision and link the single owner of the detail (D-024 anti-drift
> — spec-restatement inside a decision row is duplication, not substance).
> Pre-collapse history lives in git. Collapsed 46→30 at the phase-0→1 gate
> Pre-collapse history lives in git. Collapse ledger: 46→30 (iter-7), 41→30 (iter-26), 35→30 (iter-35), 48→30 (iter-54), 71→30 (iter-102), 44→30 (iter-116), 51→30 (iter-140-memgc — clobbered by a stale-base archive apply at iter-140-intake27, the files re-expanded to 63 rows), 63→30 restored (iter-151, D-185). Collapses fire on the owner's explicit call (D-034/D-185).

| ID | Date | Decision | Why | Consequence |
|---|---|---|---|---|
| D-001/D-009 | 2026-08-25 | Repo language law: docs, code, identifiers, commits in English; owner chat in Russian; prototype pack display names in English | token economy, owner request; consistency with logs/tests | all agents write English; final-product localization is pack data, zero code |

| D-002/D-010/D-032 | 2026-08-25 | Data contracts: JSON / JSONL + JSON Schema, no XML tooling (XML only as DF Legends *input*); EVENT_SCHEMA.md ↔ `schemas/event.schema.json` 2-place sync, test-enforced (the doc example is a test fixture); schema validation = the stdlib mini-validator `core/schema.py` for the used subset (`type`/`properties`/`required`/`enum`/`pattern`/`additionalProperties`/`items`/`minimum`/`$defs`/`$ref`) — any other keyword fails loudly | Python-stdlib native — the log is JSONL; `jsonschema` is a runtime dependency (D-012 forbids); hand-rolled structural checks duplicate the contract (the exact drift D-010 prevents) | `schemas/event.schema.json` is the machine contract; the log writer is the enforcement point — no invalid line can enter a log; T0 re-validates the doc examples; pack-file JSON-Schemas stay a phase-6 rung |

| D-003/D-023 | 2026-08-25 | Event-sourcing shape: JSONL log = truth, SQLite = derived index (INV-1); runtime state = the incremental projection `state_new = state_old.apply(event)`; `fold(log)` is for T2 replay and rebuild only — never the startup hot path | the ambiguity risks O(N²) at startup if fold is misused as the runtime state path (KI#5) | replay = fold(log); the DB can be dropped and rebuilt at any time; fold stays the truth-test (T2) and the rebuild path |

| D-004/D-028/D-079 | 2026-08-25 | Determinism law: no wall-clock anywhere, including the log header (header carries seed, python, schema_version, commit, pack — no timestamps); INV-2 = single point of randomness control — one master seed, named streams deterministically derived via the RngBank authority (`stable_hash(s) = int.from_bytes(hashlib.sha256(s.encode()).digest()[:8], "big")`, environment-independent); cosmetic draws can never desync canon replay; D-079 (iter-50, engine-2): every pack urgency entry rolls d100 on its OWN content-addressed stream `urgency:<npc>:<kind>` (`core/rng.py::urgency_stream_name` the naming grammar's single owner; the (npc, kind) pair pack-linted unique, the name injective; lazily registered on first use — the closed-set tripwire survives verbatim) — an added or removed entry shifts NEITHER a later check draw NOR another entry's rolls (INV-2's add-safety clause; the single shared `urgency` stream was landed as a candidate and REFUSED by its own A/B — add-safety 4/10 day1_full byte-identical); the nesting law reworked: an urgency-family stream may shadow the assured `substantive` run scope (the ONE legal nesting), cosmetic-inside-substantive stays the loud RNG-1 desync tripwire, substantive-inside-urgency raises (a bug, not a feature) | byte-identical determinism; the old "one Random instance" wording contradicted RNG-1 in three load-bearing places and was stricter than its own multi-stream donors; a built-in `hash()` derivation would silently depend on `PYTHONHASHSEED` | AGENTS.md §4 is the single reading owner; the iter-0v audit sub-clauses live as blueprint/spec sub-clauses (list in git worklog iter-0v) |

| D-005/D-038/D-039/D-040/D-041 | 2026-08-25 | Director & autonomous mechanics (iter-4/4a): the director is a consequence planner, never an improviser — complications are seeded at event time in a per-run buffer, never invented later; narrative entropy = observable state only (hook weights + global suspicion + visible threats); explicit triggers (time/place/threshold) fire causally; releases ride the intent door as `director_<N>` (the director never writes canon); director-off keeps seeding, suppresses releases (T8 baseline); crossings fire in TICK order. Autonomous: P2b goal ticker — per-NPC urgency specs roll d100 on the substantive stream; a precondition failure stays SILENT (the world's noise floor absorbs autonomous attempts); arrest resolution cause-chained to the attempt, `caught` irreversible (T4). Audit laws (iter-4a): the playscript feed is player-driven only (an autonomous ending NEVER advances the script); the crime-status flip guards the pack's ordered `status_values` progression; `reset_on_rotation` resets BOTH participants; dead pack keys deleted | causality by construction (RimWorld storyteller = the named anti-pattern); D-021 — the world must act without the PC; small-formula dynamics, not a planner; capture/escape is a binary outcome with no chase sub-system (phase-3+); KI#17–20 found by the owner-requested audit (124 probe runs clean) | director-off A/B mandatory (T8); beat order decay → urgencies → director; autonomous intents enqueue at `entry.tick`, never `beat_tick`, decay commits at `beat_tick`; full detail: `docs/DIRECTOR_SPEC.md` (the runtime contract owner) + `docs/INTENT_SCHEMA.md` + git worklog iter-4/4a |

| D-006/D-007/D-008 | 2026-08-25 | v0 epistemics shape: no group reputation (guard-to-guard transfer models it); rumor = knowledge transfer with fidelity decay; lies = crafted knowledge records | "reputation with the watch" = transfer events at watch change; distortion comes from source incompleteness — no extra system; foundation for phase-4 `believes/lies` | one mechanic covers gossip and hearsay distortion; no truth field on records in v0 |

| D-011/D-012/D-013/D-014/D-046/D-183 | 2026-08-28 | **Repo meta & the executable surface**: no CHANGELOG.md (git + STATUS own history); the repo name a placeholder; the concept docs live outside the repo; stdlib-only runtime (D-012, AGENTS §5/§8 — any dependency addition stops for owner confirmation); D-046 `scripts/` is operator tooling INSIDE the executable invariants (PACKAGE_DIRS += scripts + the coverage-closure test; the print exemption CLI-class only; the INV-4 import check runs against every package dir); D-183 the AGPLv3 relicense (iter-150 part 1, the owner's call): `LICENSE` at the root (the canonical AGPL-3.0 text, the verbatim SPDX copy) + the README's License section re-pointed (SPDX `AGPL-3.0-only`); the donor stance (D-015/ROADMAP §4) untouched by design — what the repo TAKES vs what it PUBLISHES UNDER are separate questions; `pyproject.toml` carries no license field before or after (the owner's call if wanted) | a top-level code dir outside `PACKAGE_DIRS` silently escapes INV-4's check; dev tooling stays capped at pytest + ruff; the publishing license the owner's explicit choice, never a default | the stoplist scope (INV-3) stays engine-side (core/ + sim/ + brief/) with the periphery interpretation test-owned |

| D-015/D-016/D-017/D-024/D-026/D-027 | 2026-08-25 | Reference architecture (four places, four jobs): `docs/REFERENCES.md` = the catalog (owner survey rev v2 merged; licenses verified + tagged with check date, unverified quarantined in §11, quarterly review); `docs/ref/<source>.md` = one deep-dive file per source, `docs/REFERENCES_DEEP.md` = index + format template (single-file dives projected 2500–3500 lines, 4–6× the cap); `docs/BLUEPRINT.md` + `docs/blueprint/` = the distillation — the 12-row resolution ledger + cross-cutting laws + build index; the blueprint owns cross-reference resolutions and combinations (mechanics stay in refs, contracts in the specs; single-owner rule preserved — link, never restate, the D-024 anti-drift discipline) | rich but unverified survey (incl. unverifiable, misattributed repos); after 19 reference iterations nothing combined the verdicts — cross-ref tensions (MTTH vs determinism, mutable ECS vs INV-1, GA retrieval vs known_by) each needed one resolved mechanism, recorded once | admission rule: verify via GitHub exact-name search (web SEO misses small repos); donors are inspiration/patterns, never 1:1 copies (`REFERENCES.md` §0.7); a deep dive updates only its ledger rows; a resolution-changing ref needs a worklog note + a superseding D-entry; each per-ref file naturally ≤300 lines (the split-when-bloated law) **+D-015 the license filter (usefulness & phase is the filter — donors gated by phase, ROADMAP §4; proprietary universes pattern rows, never committed content; merged here at iter-140).** |

| D-018/D-022/D-029/D-186 | 2026-08-27..09-20 | Concept absorption & digestion complete: manifestos and owner texts land **surgically into existing docs** (the D-018 pattern — never a new canonical file); one research pass granted then closed (Q4 = no); digestion declared complete at iter-0w — skeleton (phases 0–6, three layers, INV-1..5) confirmed, blueprint = the single build guide; **D-186 (iter-153-worldbuild1, the owner's archive-intake call): the worldbuild-surface EXCEPTION — the authored-setting corpus lands as its own directory `docs/worldbuild/` (10 files, `README.md` the index + the terminology fence), never surgically into existing docs: world canon has no existing owner (VISION frozen, ROADMAP owns phases, specs own contracts) and ~2000 lines would cap-blow any surgical target; the surface owns the authored-setting model ONLY — never runtime contracts, pack schemas, or build order (the wiring: NAV §1/§2/§3, the repo README map, TASKS' standing-rows preamble the separate-track pointer); the terminology fence: worldbuild "canon"/"Echo" ≠ INV-1's log canon ≠ `core/echo.py`'s read model; the legacy source archive stays outside the repo (D-011's line stands); the world track's plan (`WORLD_WORKPLAN.md`) a separate track from the engineering backlog, never a second queue** | a fifth canonical layer would rot, duplicate owners, and violate the caps; foundation hardening before code; 33 references replaced vague intentions with named donor-backed mechanisms without moving a phase gate; **D-186's why: the surgical law targets concept manifestos feeding engineering docs — a first-class setting model with no existing owner is a different content class, and the owner explicitly called the directory** | BRIEF/VALIDATION sketches gained sensory-emitter + beat-boundary delta + structural injection-neutralization clauses; P3e `psychological_echo`; sequencing is iteration-counted (calendar dropped); the doc-loop exception law: no doc pass without a fresh owner request; **future owner-text intakes route by content class: engineering concepts → surgical (D-018), an owned domain model → its own surface under NAV §3's ownership law (D-186 the precedent)** |

| D-019/D-020/D-021/D-033 | 2026-08-28 | CORE_DESIGN_RESEARCH proposals accepted and built: M3 (causal chain length) / M4 (novelty) / M5 (non-PC share) into the metric baseline (P1b–P1d); sparse NPC↔NPC pair relations (P2a); minimal goal ticker (P2b); salience-based talk topics (P2c — knowledge *used*, not just stored) | depth-equation factors must be measurable, not asserted; teller↔listener trust needed a data home; the world must act without the PC for T8 to mean anything; the last owner-pending proposal flipped to accepted per its lifecycle rule | no event-schema break (relations are state); full LLM planning — never (Generative Agents anti-precedent); M5 non-trivially non-zero by construction; metric thresholds set from the measured baseline, same protocol as M1/M2 |

| D-025/D-034 | 2026-08-26 | Cap laws: `docs/*.md` ≤600 lines with the substance-over-cruft filter (AGENTS.md §6.1) replacing the rigid 400-line wall; DECISIONS.md ≤30 entries, enforced by collapsing superseded/absorbed entries into consolidated compound-ID rows at phase gates (`ROADMAP.md` §5) — never by mid-iteration cuts | iter-0i trimmed substance (enum lists, field names, pseudo-code) to fit a hard cap — the owner flagged: "hard cap = crutches, not quality"; the gate-collapse keeps the cap's pressure and the append-only invariant intact with a natural archiving rhythm | filler/restatements/linker-chains = cut always; named systems, field lists, enum values, per-source verdicts = never cut to fit; a cap breach triggers a cruft pass first, over-cap substance stays with a worklog rationale; the file may sit over cap between gates; within a phase: supersede, never delete The post-ladder rhythm: the phase gates retired with the completed ladder — collapses fire on the owner's explicit housekeeping call (D-185, the iter-140/iter-151 precedent). |

| D-030/D-031 | 2026-08-27 | **The owner-directed principle rounds absorbed surgically** (the D-018 pattern — never a new canonical file): D-030 darkness is architecture — tone = data asymmetry inside the existing 8 systems (asymmetric deltas, an observable price marker on every socially meaningful behavior, irreversibility, rumor lethality per D-006); the core stays axis-blind (a future pack adds axes with zero code change); the grim line admissible only as the post-gate `pack-1` candidate under the PACK_SPEC trigger; D-031 the quality round — L13 the abstraction cost gate (Rule-of-Three, the 4-branch registry threshold) + L14 the elegance checklist (BLUEPRINT §2), type discipline + fail-fast + the registry + negative tests (phase0 §1/§2/§6), the architecture fitness test, the stack frozen stdlib-only through phase 2, mypy parked as `qa-1` | the owner fears rot into piled-up complexity; content-first proposals exposed the scope-creep failure mode (a spec before its trigger, ad-hoc mechanics) | AGENTS §4 canon-write-privilege line + §9 the quality DoD bullet; dev tooling capped at pytest + ruff |

| D-035/D-036/D-050 | 2026-08-29 | **The write-path discipline** (engine internals): D-035/D-036 the commit gate + the spread-pass discipline — every event enters through `Simulator._commit` (state deltas validated against the projection BEFORE the write, INV-1's privilege line D-031), the per-layer spread singleton (one live pass per layer, shared causes, KI#16); D-050 the derived indexes live beside their single mutation funnel (iter-8h): `KnowledgeView`'s `who → token → source-ids` (add is the only writer; `before_source` semantics preserved) and the Simulator's `(entity, prop) → latest-committer-tick` (`_commit` the only writer; the decay baseline reads the index — KI#19); plus the same-funnel scan eliminations (the beat-window break, the top-1 salience, the forward-once OCC fold, the once-per-call entropy) | each index has exactly one writer — the invariant is checkable by reading one function; the golden fixtures byte-identical through the pass | the perf numbers: profiled at iter-30 (TECH_NOTES §8 — event-linear, schema validation the hot spot); tests: the fold/replay suites |

| D-037 | 2026-08-28 | iter-3 epistemics architecture: (1) knowledge state = a derived per-knower index (`core/knowledge.py` `KnowledgeView`) rebuilt by folding `knowledge` records — never projection props; (2) system reactions are event-driven, dispatched from the commit door (`_commit` → `_react`: crime first, telling second) — no call site can forget them, the cascade terminates; (3) watch rotations fire on clock crossing, never pre-seeded — a run still ends when its script's queue drains; (4) the systems live in `core/`, **not `sim/systems/`** — anything the canon door must dispatch is kernel mechanics (the import-boundary law) | (1) keeps INV-1 pure (memory = fold of the log, T2 applies); (2) removes the "forgot to react" bug class at zero scheduling cost; (3) pre-seeding would extend every run to tick 1080; (4) core never imports sim — law wins over the stale plan note | suspicion reacts to **novel tokens only**; the arrest threshold has a single owner (`crime_watch.arrest.requires_suspicion`); expectation violations chain to the axis-specific mover; `crime_status` lives on the player entity (namespace-collision avoidance); the telling shares the teller's most salient **novel** fact with one-step fidelity decay + an acceptance roll; official briefings transfer unrolled |

| D-042/D-043/D-044/D-047 | 2026-08-28 | The read side is pure functions of the log — render AND brief: every render entry point builds its `Engine` on a fresh `RngBank` from the log HEADER seed (same log → same bytes in any process/`PYTHONHASHSEED`; a growing log keeps its rendered prefix; tracery ~350 stdlib lines, cosmetic stream only, ShufflePool no-immediate-repeat; the importance gate is pack data `tale_gate.min_importance`); M1–M5 + the emergent-chain count are pure functions of `(events, projection)` in `core/metrics.py` (Mesa `DataCollector` inverted — the simulator never knows a metric exists); the assembler is **zero RNG** (dry structured tokens, L2 — `docs/BRIEF_SPEC.md` owns the skeleton: two-level budgets, whole-block eviction in the fixed order, directives never dropped, `[truncated:N]` markers, ranking caps ≠ budget drops, all budgets at `rules.json::brief`); `scripts/balance_harness.py` runs the 1000-sim distribution (contract `docs/TEST_PLAN.md` §6) | render determinism must be env-independent and prefix-stable; a runtime-coupled metric creates feedback risk + hot-path cost; the same purity law extended to the phase-1 mediator input (D-047's clause: deterministic token proxy keeps byte-identity stdlib-only); separating ranking caps from budget drops keeps the anti-silent-drop law about assembly budgets, not retrieval windows | the 1000-seed baseline is the M-threshold reference (M5 p50=0.77, chains p50=20, M3_mean p50=13.81, M1 p50=0.24, M4 p50=0.18; full table reproducible, output gitignored); T1 covers the chronicle via the log fixture; the session = one opened Simulator, byte-identical to the batch run; brief bytes pin-tested (golden fixture); the real tokenizer must not change committed formats without a spec bump (BRIEF_SPEC §8) |

| D-045/D-058/D-064/D-083/D-094/D-136/D-146/D-149/D-151/D-170/D-172 | 2026-09-06 | **The phase-0..6 gate verdicts — PASS, the ladder complete** (iter-6/26/35/54/65/102/116; the phase table owns closed/open — these rows are the verdict records): D-045 phase 0 — all six MVP_SCOPE §16 exit criteria met, no kill-criteria, day1_full/seed 125 the canonical fixture, the metrics baseline recorded; D-058 phase 1 — 0 canon violations over 109 live beats (the exit criterion), the corpus 105 replayed through the real mediator cycle, DECISIONS 41→30; D-064 phase 2 — 35/35 boundary validity over 51 combined utterances, the §5 re-run IDENTICAL to iter-26's numbers (the parser never touched a canon path), DECISIONS 35→30; D-083 phase 3 — the §5 re-run green (ON seed 125 M1=0.509/M2=0.333, OFF T8 26 chains ≥ 3, stretch max 1), DECISIONS 48→30; D-094 phase 4 — 0 leaks on the blind-NPC suite (the exit criterion), the §5 re-run identical to iter-54's numbers, the mode-F chronicler acceptance green; D-136 phase 5 — the exit criterion 'an emergent chain of 3+ events without the player' MET in its worldgen-fed form (T8 OFF 24 chains — the iter-65 record's −2 the DESIGNED day1_full LOD price, paid on record; the 60-seed band 15–24, M5 p50 0.79), DECISIONS 71→30, TASKS collapsed; D-146 the phase-6 OPENING — the gut-check NO CUTS (the 20-seed ablation + the named-consumer trace), the second-setting shape TRAVEL; D-149 the reskin day — the gate instrument MET BY MEASUREMENT (`content/road_pack/` the TRAVEL-loop reskin, 14m24s, ZERO core edits git-verified); D-151 the phase-6 verdict — PASS (the §5 re-run: the seed-125 pair ON M1=0.518/M2=0.200 reproducing iter-102's numbers exactly / OFF T8 24 chains; T7 both chronicles read as stories; ROADMAP §2 CLOSED — the ladder complete, the open build rows stay owner-gated backlog rows; DECISIONS 44→30); D-170 the L2 depth audit — the verdict material (the three lenses, the A–H ledger, PARTIALLY CONFIRMED as material; phases.md §6's depth-audit block the owner); D-172 the L2 verdict — DELIVERED at the measured band, the two-level gate COMPLETE (world-2 closed, never reopening for slices) | the roadmap's ladder owner-gated at every step; the verdict rows record each gate's evidence checkpoints ONCE — the full protocol ROADMAP §5, the per-gate runs worklog + git | the standing work: the owner-gated backlog rows + the SoW horizon (ROADMAP §6) |

| D-185 | 2026-09-20 | **The memory-GC law + the semantic documentation compaction pass (iter-151-docscomp1, the owner's ownership-matrix session + the «вперёд, приступай к работе» call; the iter-140-memgc precedent — its D-173 row was clobbered when a stale-base archive apply landed iter-140-intake27 wholesale over the GC, and is restored here under a collision-free ID): the post-ladder collapse trigger = the owner's explicit housekeeping call (the D-034 gate rhythm retired with the completed ladder); the pass: phases.md §6's closed research archive kept as finding → consequence → consumer compact blocks (the consult cards, the slice blocks, the depth audit, the L2 verdict intact; the one-pass journals in git at the D-rows' commits); TASKS re-collapsed per the iter-102/iter-140 precedent (the phase-6-era ledger iter-116..150) + the thirteen standing rows extracted to the live queue head; DECISIONS re-collapsed 63→30 (the family pattern); SPECS_BACKLOG's fired-and-absorbed triggers re-pointed (GROUP_SPEC → depth-7, CORE_ONTOLOGY → PACK_SPEC — restored from the clobbered GC); AGENT_NAVIGATION restored to a state map (one line per path, no narrative); REFERENCES_DEEP's verdict column compressed to one-line purposes (D-026 — the mechanics live in the ref files); TEST_PLAN retitled the Verification Stack (the historical per-landing acceptance records → the D-rows + tests + git; the instrument definitions kept); the CORE_DESIGN_RESEARCH inbound collapsed (the file's deletion itself the owner's next call after a zero-inbound check; executed iter-152 — the file deleted, the evidence-class citations swept to their live owners) | the caps' enforcement rhythm died with the completed ladder — the over-cap files had no future collapse point; the matrix session measured the corpus at 13,894 lines / ~1.06 MB with the same facts owned 2–3 times (phases↔TASKS↔DECISIONS the worst); the stale-apply clobber proved an archive built against a pre-GC BASE can silently erase a landed GC | the reading gradient unchanged (STATUS → TASKS → the owning doc → a §6 card only when the row names it); future collapses fire on the owner's explicit call, recorded in the collapse ledger; nothing in the build queue moved — the ORDER owner's rows untouched; the delivery lesson: never apply a delta archive without checking its BASE_COMMIT against HEAD |

| D-048/D-049/D-053/D-056/D-057 | 2026-08-31 | **The scene/brief read-side stack** (the phase-1 structural landings, owner-directed): D-048/D-049/D-053 the scene ledger — canon vs texture (the design iter-8a, hardening iter-8b, the LLM-free half iter-10: the scene lifecycle, the pin/retire vocabulary, the laundering refusals, the D-049 determinism quarantine — the ledger is session render state, never replayable, T1/T2 never touch it); D-056 st-1 presence & entity cards (iter-15: the arrival snapshot, the present set, the card surface — BRIEF_SPEC §3.4); D-057 the pack-declared `layout` (iter-20: canon-from-birth on the scene line, no initial_projection seeding — KI#48's correction: canon_slot reads BOTH prop sources) | the read side is pure functions of (log, ledger) with zero assembler RNG (L2); the projections are folds, the cards are the narrator's surface | contracts: BRIEF_SPEC §3.3/§3.4 + blueprint §1; tests: the ledger/presence/brief suites (iter-10/15/20) |

| D-051/D-063 | 2026-09-01 | **The track-B DF pipeline closed** (bg-1 + bg-2, the owner-supplied world exports): D-051 the SQLite sink — HANDLED records load as typed cores + EAV + `event_participant` + generic JSON, truncation flagged PARTIAL by default (`--strict` aborts), the DB a rebuildable content-deterministic index of the export bytes (INV-1's shape on foreign canon; no wall-clock in `meta`); D-063 the taxonomy + the sink v2 plus pass — `docs/TAXONOMY.md` (120 entries over the 16 target types, measured findings incl. the hfid1/hfid2 participant-index blind spot), the companion's `historical_events` in `event_plus_fields` (same ids, fields only) | the epistemic closure IS the participant index (blind spot inherited — DF history is canon-dense, epistemology-empty: the legends structure donates, knowledge propagation stays ours to design, phases.md §4) | recipes + numbers: TECH_NOTES §3.1-§3.3; tools: scripts/df_{survey,import,taxonomy,briefer}.py; tests: the bg suites (bg-1/bg-2) |

| D-052/D-054/D-055/D-062 | 2026-08-29 | **The two external dev-time doors + the validator** (iter-9/11/12/31): (a) D-054 the texture promotion door — the LLM-free half (`core/intent.py` + `resolvers.py`'s texture branch; contract INTENT_SCHEMA §2/§3/§7): a texture-capable action's pack `texture` block REPLACES the canon preconditions for intents carrying the mediator-resolved `{entry, scope, slot, value}` reference; a take-success IS the promotion — one StateChange, the object's canon birth; a failure promotes nothing; core NEVER sees the ledger (INV-3/D-031); ghost interactivity structurally impossible; `unique_slots` the laundering-family refusal. (b) D-055 the narrator door — agent-in-the-loop, EXTERNAL at dev-time: the FILE contract `output/mediator/call_<N>.md` ↔ `reply_<N>.json`, ONE closed document shape with deep shape gates AT the boundary; a refused document never feeds intents (the beat regens whole; L12 from day one: narrator → template → dry). (c) D-062 the mode-C parser boundary — the same file-contract pattern on the player's free text (iter-31): the grammar snapshot as DATA (pack verbs with pack-derived field constraints ∪ addressable nouns — canon entities + live ledger entries), `parse_call` byte-deterministic, the closed {intent | question | no_intent} reply gated at the boundary (off-grammar = a loud ParseError, never a feed), the pin law (`SceneLedger.pin` — the reference IS the pin; a PROMOTED entry terminal), promotions wired exactly like the narrator path. (d) D-052 the validator contract (iter-9, VALIDATION_SPEC the field-level owner, `brief/validator.py` the LLM-free half): the mediator's gate a pure function of (proposal, log, pack) — one CLOSED verdict vocabulary, ExpectedVersion OCC, the fact transaction, honest verdicts against CURRENT canon under closed-world semantics | the LLM boundary (INV-4) holds by keeping every external side behind a file contract with shape gates at the boundary — no post-hoc text sanitization; prose is never a fact proposal (injection neutralized structurally) | contracts: INTENT_SCHEMA + PARSER_SPEC + VALIDATION_SPEC + BRIEF_SPEC §7.1 (the single owners); the phase-1 corpus (105 cases) replays the refusal vocabulary + both door outcomes through the real cycle; local inference stays the owner-gated engine-1 candidate; per-clause landing detail: the specs + git |

| D-059/D-060/D-061 | 2026-08-31 | **The phase-1 polish menu — three landings on the owner's finish-phase-1 directive**: (a) tune-1 (iter-27): the story-critical importance hook — pack-listed event types (`rules.json::importance.story_critical_events`, lint-closed) score +2 `story_critical_event` (`pack_importance` takes the event type, all emit sites); the tale gate follows low→medium — the RULE owns the signal/noise split per D-045(b), the gate is not the primary knob; + the `recuperate` resolver + `status_effects` pack block (`rest`, the 13th action, 60 ticks, fatigue −30 — the KI#4 counter-play, pack data end to end); day1_full OFF renders 47 events → 14 tale lines, zero decay/wariness noise (pinned); the 1000-sim baseline re-run IDENTICAL. (b) tune-2 (iter-28): `brief.present_entities.card_markers` — the prop-path marker table (threshold rows + value rows, closed marker surface `status.<axis>`/`relations.<axis>`/`crime_status`, lint-checked; the segment is `markers=`); the pack ships `wary` at `relations.suspicion` ≥ 25 (aligned with the `status_suspect_at` flip) + `suspect`/`caught` on the player's card — the narrator's read surface for standing state (the scene_delta half is lawful blindness, blind-NPC D-037: the CARD is the narrator's surface, the delta window the player's). (c) pack-2 (iter-29): the `spot_available` door check — the closed precondition set's 15th test (the target location holds an unburning spot of the declared layer, the exact condition the ignite resolver keys on; the `layer` param lint-checked) — arson on a destroyed or fully-burning location is an `intent_rejected` no-op with `failed_test target.spot_available`, the door-outcome vocabulary's fourth axis | the T7 phase-1 gate re-read flagged the noise floor (27 of 47 day1_full events were decay/wariness repetition — a bare gate raise would have cut story events the raw rule scores low); iter-17's live finding: the suspicion axis and the `crime_status` flip were claimable-but-invisible through the brief; the iter-2a audit note + iter-24's live probe: arson on ashes logged a no-ignition success that pretended the world changed | T1 fixture untouched by (b)/(c); tests 610→629 across the three; the seed-41 corpus probe flipped with (c) (renamed, the old pin lives in git); field-level detail: INTENT_SCHEMA §3 (the requires row), BRIEF_SPEC §3.4/§6 (the marker table law) |

| D-065/D-066/D-067/D-068/D-069/D-070/D-071/D-072/D-073/D-074/D-075/D-077/D-078/D-080/D-082 | 2026-09-01 | **The phase-3 (Director) build column landed — the four columns, condensed at the phase-5→6 gate (iter-36..53; the per-column compound rows live in git + TASKS' phase-3 ledger, the mechanism detail in DIRECTOR_SPEC §3–§5 + TEST_PLAN §6 — the single owners): the pacing stack (D-065 the RAMP/PEAK/REST/STAGNATION clock over narrative entropy, D-066 the eventless-stretch instrument + the DIR-2 A/B harness, D-067 the climax layer + PEAK_CLIMAX, D-068 the multi-channel policies + the per-hook quiet gate), the Paradox event grammar (D-069 predicate/weight + the burn law, D-070 the option layer, D-071 the on_action dispatch), the content column (D-072 document_check, D-075 the echo engine, D-077 the panic ripple, D-080 the echo driver, D-082 the ambient driver), the social stack (D-073 secrets/leverage, D-074 the coerce door + the spend, D-078 the coerce driver). The exit criterion's measurement (max stretch 1) and the gate verdict live in the gate-verdict row (D-083).** | phase 3's declared build column, ROADMAP §2; the owner's per-iteration "continue" calls | the pacing/grammar/content/social four-column shape is the director law's standing form; per-mechanism owners: DIRECTOR_SPEC + TEST_PLAN §6 |

| D-076/D-081/D-138 | 2026-09-03 | **The release-chain layer + its driver + the climax-flag verdict (arc-1 + content-6, iter-47/52 + iter-105)**: D-076 the engine — `director.arcs` pack chains: the ORDER law (a member tag is a candidate only while it is its arc's CURRENT member, all release paths), the GAP law (`min_gap_beats` spacing on the quiet/climax paths only), the entropy mirror (passed members' instances stop counting — the burn law's twin), the one-sided membership lint; D-081 the driver — the aftermath chain `[possible_document_check_relief, barkeep_wary_sweep]` gap 2 LOAD-BEARING (the unchained sweep would land its event BEFORE the check's own — a causality lie; the march holds the order), the successor trigger-less + climax-flagged + weight 0 (the entropy footprint exactly zero), measured 9/10 byte-identical + seed 125's one appended event, 105/105 pin-green ZERO re-distill (the seeding event's hooks field the birth record); the NPC-move successor arm refused (the tune-3/st-6 owner-gated fork); D-138 the climax flag = the peak's END marker (PEAK_CLIMAX then REST unconditionally; a non-boss beat that must fire regardless of the clock carries an explicit trigger instead — D-005's causality law) | the DF event_collections / Paradox event-chain precedent: release-side causality must be pack-declared, never pacing-invented | contracts: DIRECTOR_SPEC §3d/§5/§11; tests: test_director + test_arc_driver; the D-065 nopacing record superseded in part (the closing beat rides the climax path, which the clock-off pack removes) |

| D-084/D-085/D-086/D-087/D-088/D-089/D-090/D-091/D-092/D-093/D-095..D-104 | 2026-09-05 | **Phase 4 (Knowledge & scene) build column + the v0.2 refinement lance (iter-55..72, condensed at the gates)**: the trait crystallization fold (D-084 — `core/traits.py`: threshold DISTINCT tokens of a pack-declared family crystallize the belief, the contributing records' source ids riding the fold); the brief's derived-trait read (D-085 — belief lines lead recalled_facts); reflection-on-recurrence through the canon door (D-086 — LEGEND_SPEC's compression half, written just-in-time at the row); the tavern reflection arming (D-087); the retrieval ladder (D-088 — FTS5 + the vec probe/scan/floor chain + the re-ranker; `knower` IS known_by); the mode-B scene stack (D-089/D-090 — the chorus queue + the knower parameter, the session wiring); the texture identity tier + quotas (D-091 — `identity_slots` + `per_entity_max_items`); the blind-NPC leak suite's phase-4 extension (D-092); the mode-F offline chronicler (D-093 — `scripts/chronicle.py`, the [chronicler] extra the approved D-012 exception, the attach→stdlib ladder, autoinstall off, the manifest content-derived); the v0.2 refinements D-095..D-104 (prosefloor, beliefwire ×2, rumordrift, suspectaxis ×2, testproto, prosefloor-2 — VALIDATION_SPEC §2.1 + TEST_PLAN §8 the owners) | every landing owner-called per the phase-4 wave plan; the exit criterion '0 leaks on the blind-NPC suite' the column's spine | the phase-4 surfaces complete (traits feed BRIEF_SPEC §3.5, reflection mints canon events, retrieval rides known_by, mode B live); gate PASS iter-65 (D-094); per-row detail: TASKS' phase-4 backlog + the owning specs + git |

| D-098/D-109/D-110/D-111/D-112/D-113/D-118/D-129 | 2026-09-07 | **The protocol-law & testproto family (iter-68..94, condensed at the phase-5→6 gate; per-row detail in git + TEST_PLAN §8 + the owning specs): D-098 the intermediate-build LLM-integration protocol — the three-layer synthesis (the contract simulacrum / the live probes / the gate heartbeat) over one pinned corpus + one metric vocabulary, the decomposition not an election; D-109 bg-8 its live half — the deviation corpus F1–F6 through the REAL mode-C door, the world-answer law's first live numbers (coverage 34/34, the ≤2-regen ladder); D-110/D-111 the predicate contract restored — missing answers False under EVERY comparator, and a missing key is a ValueError naming the field (the loud-backstop twin); D-112 the iter-11b stress-test resolutions RATIFIED (the D-056-amended arrival snapshot, the containers law); D-113 the doc-law verdicts (Rule 9's in-repo home AGENTS §7; ORDER → STATUS Next, composition → TASKS; verify-seed + archive-protocol chat-side); D-118 the shadow-replay introspection law (mech-1: agent-facing CLIs replay through the PUBLIC pipeline only, never runtime instrumentation); D-129 the effect-line habit — protocol-law D-rows state their expected observable effect, checked within 1–2 iterations, reported at each gate's §5 staleness pass.** | the protocol laws grew monotonically between gates — the family holds the cross-phase guardrails | the pruning pair is D-128's gate staleness pass (ROADMAP §5 step 6) + D-129's effect lines; owners: TEST_PLAN §8, AGENTS §7, core/predicates.py, scripts/mechanics.py |

| D-105/D-106/D-107/D-108/D-114/D-115/D-116/D-117/D-120/D-121/D-122/D-123/D-124/D-125/D-126/D-127/D-131/D-132/D-133 | 2026-09-08 | **The phase-5 (Depth) build column landed (iter-73..98, condensed at the phase-5→6 gate; per-row detail in git + TASKS' phase-5 ledger + phases.md §5 — the architecture owner): the acquisition gate + arming (D-105/D-106), the lazy-detail family + arming (D-107/D-108), the fold checkpoints (D-114), the ordered worldgen passes + the committed arming (D-115/D-117; D-116 the generator-concept verdict set — the 13 questions resolved, the W0..W4 wave order, the spatial model pinned as phase law), the four W1 bridges (D-120 the scene-line projection pipe, D-121 the DF-legends history bridge + the cause tree, D-122 the placement discipline, D-123 the geometry rework — the grid-hash walk, 10k sites in 1.19 s), the macro clock + its consumers (D-124 the cadence law + the aggregate emission surface, D-125 the scene LOD + the one-gate law, D-126 factions with goals — the KeeperRL small formula, D-127 the write-side group LOD — member_of + the population tier + condensation), the W2 tail (D-131 name-1 the lazy name generator + the condensation consumer, D-132 st-6a travel with the edge price law, D-133 weather-1 the ambient chain + canon erosion + the committed macro arming — the primitive's first consumer). Every landing measured its corpus price both arms before paying it; the unarmed/68a pattern + the one-gate law kept the committed pack's price surface designed, never accidental.** | phase 5's declared build column, ROADMAP §2; the wave order D-116 | the exit verdict: the gate-verdict row (D-136); the phase law: phases.md §5 |

| D-119/D-128/D-129/D-130/D-134/D-135/D-147/D-148/D-150/D-155..D-167/D-171/D-173/D-174/D-175/D-184/D-187/D-189/D-190/D-191 | 2026-09-10..21 | **The research-intake verdict sets (intakes 6..33 + the queue's own revalidation, iter-85..166, doc-only — every external text verified against HEAD before routing, the convenience-copy law; the per-intake compact records: phases.md §6; the full one-pass tables: git at the D-row's commit)**: D-119 intake-6 the residue catalog (Dune's critical-resource binding → res-1's declaration shape; WH40k ideology-as-constraint; Outer Wilds refuted — knowledge never gates, L6); D-128/D-129 intake-7 the Anthropic-2026 corpus (the ROADMAP §5.6 staleness pass; MVP_SCOPE §18 actionable errors; mech-2 the attention budget; the effect-line habit); D-130 intake-8 the three-tier setting posture (the public 2nd setting ORIGINAL — the province sketch riding world-2's L2; the level-1 reskin the instrument); D-134 intake-9 the cost/failure/corruption pack laws + the parked patterns; D-135 intake-10 the encounter catalog — the substrate CONFIRMED complete (since-1 the one genuine gap); D-147 intake-11 the pressure-city donor (the DISPLACEMENT law the one word-for-word keeper; the Cooling Debt → weather-1's chain+erosion shape, never a second res-1; the gauge = res-1's read surface; legal exclusion the D-134 application; biomes authored pack data, never a worldgen biome); D-148 intake-12 the presentation-contract consolidation (the readiness frame REFUSED — a contract without a consumer has unknown requirements; the spec at the engine-1 trigger ABSORBING st-4, the D-055 file-contract pattern's fourth instance, a thin mapping table over the 8 blocks, never a second re-labeling vocabulary; [truncated:N] STAYS — the anti-silent-drop invariant; the behavioral layer answered by PACK_SPEC §6); D-150 intake-13 the open-ended-actions review (the doctrine confirmed, four proposals routed to existing owner-gated rows, UNDER_MODELLED refused in favor of the pack-CI closure law); D-155..D-167 intakes 14..26 (the causal bootstrap, the narrative dossier, procedural generation, interface composition, player-decision mechanics, persistent groups, the pack-strategy framework, the observatory/worldbuilder/gateway, ComfyUI modularity, world-execution/spatial/social, the principle-transplantation synthesis, the re-verification pack, the cumulative reconciliation, the verification lenses → TEST_PLAN §9) — every verdict OWNED-by-standing-law or routed to a named row/card, zero build-grade items, the residues parked as the §6 consult cards; D-173 intake-27 the level-design consolidation — the doctrine the strongest cross-domain confirmation family, FIVE compact instruments adopted (the reader law, the priced-option question, the top-LOD readability question, the realization table, the sacrifice protocol), the topology-aware hook distribution the parked proposal; D-174 intake-28 the Stålberg conspectus — ONE instrument adopted (the COMBINATION FENCE: a combined design carries NO presumption of advantage; the mechanism-of-advantage decomposition + the combination-price question + the Frankenstein test); D-175 intake-29 the external audit CONFIRMED + the intake ADMISSION RULE (a new external intake convenes only with a named open row it feeds + a potential falsifier stated up front — new knowledge now comes from building and measuring) + six contract sharpenings into the rows; D-184 the standing-backlog revalidation (iter-150: all thirteen rows audited with the four questions — pack-3 REWRITTEN, tune-3 RE-AIMED, st-2/scav-1 SHARPENED, companion-1/mech-2/st-5/parse-2 SYNCED, the rest audited-current; no rows deleted, none added, no order changed — D-113); D-187 intake-30 the agent-dense v3 hybrid pack (iter-154, the owner's archive-intake call over the external re-consolidation package): the corpus map — nine of the eleven content units already routed through the intake family (the pack a sibling compression of the same sources, stale pre-iter-145; the two fresh units in the block); the pack's self-declared canonical `agent/` layer REFUSED as a parallel authority (D-024 + the D-018/D-186 content-class law — engineering concepts land surgically; no `docs/agent/`); the visual-system research the ONE un-routed domain → the visual consult card (the development-order law: the semantic read surface first; the fidelity target; the rejection table) parked behind the SoW fence with presentation-1 the wired consumer (D-174/D-148); the UNIFIED GDC knowledge base NOT admitted (D-175's rule + F2 saturation); D-189 intake-31 the Kurvitz consolidated research (iter-158, the owner's routing call over the prior-session research residue on the Kurvitz worldbuilding corpus): PARTIALLY CONFIRMED — mined as a GENERATOR LIBRARY, never a worldbuilding layer (the doctrine already owned, the value operational): the W4 operator set + the test operationalizations + the bounded crossing-household experiment adopted into the worldbuild owners (WORLD_AUTHORING §5/§7/§8/§19, WORLD_TESTS §3, WORLD_WORKPLAN §6), the source mechanics recorded at `docs/ref/kurvitz.md` (ref-21); a Kurvitz document inside `docs/worldbuild/` REFUSED (D-024 — every domain has an owner) + a generic residue subsystem SHOULD-NOT-ADD until the bounded experiment proves a substrate gap + the canon import list binding (races, catalogues, humor tables, thresholds, one-to-one institutions); D-190 intake-32 the Vantiel research handoff (iter-159, the owner's routing call over the uploaded prior-session handoff on the Vantiel RPG rules corpus): PARTIALLY CONFIRMED — a SEPARATIONS donor, never a subsystem donor (all ten distinctions already owned by the substrate, the handoff's map re-verified claim-by-claim at HEAD): the source record `docs/ref/vantiel.md` (ref-22) + the presentation-1 consult material (the re-expansion law + the staged-interpretation sketch, parked behind the owner gate, the intake-30 visual-card precedent); the reject list binding (affinity meters, flag catalogs, cognition multipliers, difficulty percentages, mutable threads, free-form Architect edits, journal-as-state); the deadband law the superior own-form of the inertia proposal; zero build-grade items; D-191 intake-33 the game-design practitioner talks corpus (iter-166, the owner's routing call over the consolidated five-source knowledge base — Sawyer/Meier/Battle Mode/Johnson/Wolverson): PARTIALLY CONFIRMED — a perception + economy-craft donor, never a systems donor, differentiated from the refused UNIFIED GDC base (intake-30's F2) by the one domain no prior family owns — outcome-perception psychology; adopted: the OUTCOME-PERCEPTION LAWS CARD (the layered legibility ladder — truth never bends / failure carries cause + avoidance path / odds as expectation bands / transparency opt-in by flag / outcomes legible through residue / stakes irreversible) → presentation-1's consult material (the third parked card, the visual/Vantiel precedent) + the ANTI-ARBITRAGE SPREAD (exchange lossy by construction, the mechanism never the 2× number — the Resource open question's donor, the water's function-loss arbiter its falsifier) + the TURNOVER QUESTION (the ECS cure = the function-loss probe aimed at holder mortality, the anti-freeze law → WORLD_AUTHORING §5; consumers: the camp's meso half, W5's biography arc) + two parked notes (the Hot-Path placement pair beside the intake-27 topology proposal — the road-traffic rider's material; the weak-coupling authoring law — pack-3's event families); the largest confirmation batch since intake-27 (seed-in-save = INV-2/T2; no-cheats AI = the one-id door; infinite tooltips = mechanics.py; automation-red-flag = mech-2's nothing-dropped-silently; direct-and-verify = the worldgen's MST-by-construction own form); refused binding (all dice-bending — perceived fairness is bought only at the render layer; the 3:1/2× constants — threshold leakage; the tech-deck/order/no-counterattack mechanics — no substrate, no consumer; the K-table as repo taxonomy) — the source record `docs/ref/game_design_talks.md` (ref-23), zero build-grade items | the owner's research calls over uploaded external texts; the repo's own law the arbiter (the transplantation test, D-024) | the intake rhythm now admission-gated (D-175); the revalidation table's essence: TASKS' standing rows + git |

| D-137/D-139/D-140/D-141/D-142/D-143/D-144/D-145 | 2026-09-12..13 | **The iter-104..108 research-verdict + fixation family (the owner's direct calls on the iter-105 chat's priority list, condensed at the phase-6 gate)**: D-137 the Alien unknown-axis resolution — the OBSERVABLE-answers law (the `unknown` axis, whenever it lands, paces against the world's OBSERVABLE answers to ignorance — logged refusals, failed checks, no-knowledge events — never the private actual-vs-perceived gap; the L6/EPIST-1 observable-only fence stands); D-139 the resume door — the INVISIBLE-TO-THE-LOG law (`--resume` opens the session OVER an existing log; a clean-boundary resume is byte-identical to the uninterrupted run — only steps are inputs; the run cursor derived-of-the-run, never canon, never log-derivable); D-140 the payoff-latency + tension-profile riders (the `provenance.cause_hook` pairing, the schema 0.2 additive bump); D-141 the {3–8B, GBNF} gap rows (engine-1's decision inputs); D-142 the CORE-ENDS/PACK-BEGINS admission law (a change lands in `core/` iff it alters WHAT mechanics exist; in pack JSON iff it tunes HOW declared mechanics behave — the test: 'must a second pack carry this for the mechanic to exist at all?'; enforcement the three-layer fence: INV-3's stoplist + the pack lint + test_drift's wiring contract); D-143 the intent_rejected health-rate (≥90% delivery in live traffic, both pure log reads); D-144 the per-component latency budget; D-145 the phase-6 opening-day questions + the derived one-pager `scripts/digest.py` | the research session's accepted list fixed where it lives (the owner's «зафиксируй где нужно» call); the risk-synthesis riders the iter-105 priority list | DIRECTOR_SPEC §3d/§11 + TEST_PLAN §6 the riders' owners; the admission law the phase-6 packs' own gate (pack-1/pack-4 built inside it, zero core change held); per-clause detail: git |

| D-152/D-153/D-154/D-168/D-169 | 2026-09-13..19 | **The phase-6 world-2 build column landed (the two-level gate's material; the mechanism law's owners: AGENT_NAVIGATION §1's `core/` rows + phases.md §6's slice blocks + the owning specs; the landing evidence: TASKS' phase-6-era ledger + worklog + git — the per-slice D-rows' full text in git, collapsed at the owner's memory-GC call iter-140)**: D-152 the admission-lint rungs LIVE (the teleology gate + the live-char crosswalk + the price-marker lint in `core/pack.py` — PACK_SPEC §5/§6 the single reading owner, the 68a twins' completion); D-153 the province skeleton (the Sarrow Vale, 324 sites on the travel lattice, the spine records — the AP crosswalk's first committed consumer, original nouns per D-130, the T1 twin); D-154 the cultures half (the two name-1 phonotactic profiles — name-1's first committed arming; the condensation travelers; the cultures block + prohibition sets); D-168 the triangle (the factions armed 0→1 — the census's only zero-consumer family; the fear ladder + the grievance deadband; the deep feud chronicle re-cut; the composed outcome-divergence experiment; KI#85; the golden regen 39→44); D-169 the calendar (the sub-year cadence family — market days 14400 / fairs 43200 / seasons 129600 + the four-phase cycle, fired coarsest-first; the weather's seasonal ride on the D-030 asymmetric weights; the composed YEAR experiment; KI#86; +16 tests, PROVINCE_STOPLIST +2, the budget's second re-declare) | the phase-6 gate's two-level instrument: L1 the reskin day (D-149), L2 the deep second world — the wave plan D-153's («each slice a row, the ORDER owner decides»); every slice owner-called per STATUS Next step's readiness pick | the two-level gate COMPLETE (L1 PASS iter-116/D-151; L2 DELIVERED iter-139/D-172); world-2 closed, never reopening for slices — the residue routed to the content-side rows (F4's promotion events: pack-1/pack-4) |

| D-176/D-177/D-178/D-179/D-180/D-181/D-182/D-188 | 2026-09-19..20 | **The iter-143..150 + iter-157 standing-row landings (the owner's delegation calls, every build riding its pre-implementation contract — CONTRACTS.md the boundary pattern)**: D-176 ci-1 the GitHub Actions runner LIVE (`.github/workflows/ci.yml`: pytest + ruff on push/PR to `main`, PYTHONHASHSEED=0 job-level, Python 3.12.14 pinned EXACT — the golden byte-compare law, a bump rides a fixture regen never alone); D-177 the three contract writes (roads-1/res-1/since-1: pinned decisions, invariant sets, §9 claim packets, minimal test sets — a contract is the pre-implementation boundary, never the row's spec); D-178 roads-1 the generated-exits pass (the MST backbone + the pack-declared MUTUAL k-nearest overlay over the CLAIMED locations — per-node overlay degree ≤ k; the ONE shared read `core/roads.py::exits` — authored non-empty wins, empty-authored the generated-world marker; the loud WorldgenError conflict rule; the committed packs armed k=0, zero corpus price; the pack.py split rider → `core/packlint/`); D-179 res-1 the economy substrate (the `account.<kind>` primitive, the three verbs `account_sourced`/`account_transferred`/`account_consumed` through the canon door, integer-only, the flows on the maclock cadence, the derived prices `base + per_unit × level`, the underflow floor's soft-door/loud-gate two arms; UNARMED on the committed packs — zero corpus price); D-180 since-1 the re-encounter delta (the per-(reader, entity) encounter-epoch fold — presence per present_entities + the carried-item closure; the cards' `since=` segments atomic with the card; read-side only, zero streams, unarmed); D-181 pack-1 the grim tavern (the FOURTH pack — the dark line as pure pack data: the attraction/intimacy/loyalty + shame/anger axes, the flirt→proposition ladder with the world's reply (never a roll), the consent split structural (the canonical event vs the crafted belief record + the fact/belief vocabulary-split lint), the coerced branch the ONE spend door, the pawn-ticket hinge; res-1's + since-1's first consumer armings); D-182 pack-4 the pressure city (the FIFTH pack, the T1 slice — the displacement law as pure pack data, ZERO CORE CHANGE held exactly: the gauge = res-1's one-meter read surface, the Cooling Debt = the thermal_shock layer, the factions = depth-6's objective-function triangle, the closed cycle = res-1's second consumer; the FIRST authored non-scaffold pack; the engine findings — the group-stock lint gap, the co-due same-account flow limit — recorded, never patched in-core); D-188 companion-1 + tune-3 the companion arming, armed together as the row's own law (iter-157, the owner's «довести до ума сам генератор» call): the crossing household's second hand embodied as pure pack data (Dellan — ANCHOR_REGION §6.1's first meso unit, the pack embodiment §9 deferred to the engineering side; the spine, the kin/runner pair axes, the punt pole committed — KI#87 CLOSED), the FIRST committed NPC movement source (the road-leg urgency beat, kind=move, target the artery, gated adjacent — the HEAD census flip), the traveling-knower probe CONFIRMED per the row's own deciding shape (the crafted twin: alarm_adjacent heard+vague on the MOVER one leg out, the panic echo +10 reaching him, the telling carrying the shout to the player — the movement-driven adjacency distinguished from the worldgen-static arm), and the escort halves over the EXISTING doors (the mode B actor-step voice, the paired move legs — the no-teleport law's letter; the scene-LOD law shapes the split: the beat fires co-located, the warm ring waits for the reader); zero core change — the mid-travel beat-carry duplicates die at the door/OCC as bounded intent_rejected no-ops (attempts are facts), the golden regen's delta exactly the companion's own events (the D-079 add-safety held in the wild) | the queue's own revalidation (D-184) had named the order; every row rode its contract's falsifiers (the §9 claim packets CONFIRMED at the measured bands); the zero-core law (D-142's admission law) held through both packs | the substrate primitives arm by pack data alone (the unarmed landings proven); the post-T1 residues (the legal exclusion D-134, the cultures name-1, the road traffic depth-7, the lore hooks D-140) the future slices' material; per-landing detail: TASKS' ledger one-liners + CONTRACTS + the packs' own notes + git |

