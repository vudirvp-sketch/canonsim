# DECISIONS.md — ADR-lite

> Append-only. Each entry: decision → why → consequence. Supersede with a new
> entry, never delete. Cap: 30 entries.

| ID | Date | Decision | Why | Consequence |
|---|---|---|---|---|
| D-001 | 2026-08-25 | Repo docs & code in English; owner chat in Russian | token economy, owner request | all agents write English; UI localization is pack data, zero code |
| D-002 | 2026-08-25 | JSON / JSONL + JSON Schema; no XML tooling | Python-stdlib native; the log is JSONL; XML appears only as DF Legends *input* | `schemas/event.schema.json` is the machine contract |
| D-003 | 2026-08-25 | JSONL log = truth; SQLite = derived index | event-sourcing invariant INV-1 | replay = fold(log); the DB can be dropped and rebuilt at any time |
| D-004 | 2026-08-25 | No wall-clock anywhere, including the log header | byte-identical determinism | header carries seed, python, schema_version, commit, pack — no timestamps |
| D-005 | 2026-08-25 | Director = consequence planner, never improviser | causality by construction; RimWorld storyteller = named anti-pattern | complications are seeded at event time; director-off A/B is mandatory (T8) |
| D-006 | 2026-08-25 | No group reputation in v0 | knowledge spread between guards models it | "reputation with the watch" = transfer events at watch change |
| D-007 | 2026-08-25 | Rumor = knowledge transfer with fidelity decay | distortion comes from source incompleteness; no extra system | one mechanic covers gossip and hearsay distortion |
| D-008 | 2026-08-25 | Lies = crafted knowledge records | foundation for `believes/lies` (phase 4) | no truth field on records in v0 |
| D-009 | 2026-08-25 | Prototype pack display names in English | consistency with logs/tests, token economy | final-product localization is pack data only |
| D-010 | 2026-08-25 | 2-place schema sync (EVENT_SCHEMA.md ↔ event.schema.json), test-enforced | anti-drift | the doc example is a test fixture |
| D-011 | 2026-08-25 | No CHANGELOG.md | worklog + STATUS + schema_version cover it | one less file to bloat |
| D-012 | 2026-08-25 | stdlib-only runtime dependencies | determinism, zero supply-chain surface | pytest / ruff as dev-only deps |
| D-013 | 2026-08-25 | Repo name `canonsim` is a placeholder | no strong naming constraint yet | rename before external visibility = one mechanical commit |
| D-014 | 2026-08-25 | Source concept docs live outside the repo | the English distillation (VISION) is the working truth; originals rot and bloat | agents never request the source docs |
| D-015 | 2026-08-25 | License filter lifted; usefulness & phase is the filter | no monetization planned; open bases adapted freely incl. functional re-invention | donors gated by phase (ROADMAP §4); phase 0 uses zero external code |
| D-016 | 2026-08-25 | External source catalog created (`docs/REFERENCES.md`); ROADMAP §4 stays the active shortlist | owner's source survey was rich but unverified (incl. unverifiable and misattributed repos); phase-0 law forbids early intake | admission rule: verify via GitHub search by exact name first (web SEO search misses small repos), licenses tagged with check date; unverified items logged in §11; quarterly review alongside TECH_NOTES |
| D-017 | 2026-08-25 | Owner survey rev v2 merged: layer/priority map added (`REFERENCES.md` §14); rev-v2 sources admitted with "verify" licenses (sqlite-vec, nomic-embed-text, bge-m3, cross-encoder rerankers); phase law unchanged | priorities are owner intent for the final concept and conflict nowhere with phase gating (Must ≠ phase 0); license check deferred by owner to phase-4 intake (D-016 procedure applies then) | qdrant demoted to "server infra only"; rev v2's 6-phase plan maps onto ROADMAP 0–4 + 6 (no depth phase — ROADMAP stays 7-phase); owner policy on record: donors are inspiration/patterns, never 1:1 copies (`REFERENCES.md` §0.7) |
| D-018 | 2026-08-26 | Architecture manifesto absorbed **surgically into existing docs**, not as a new file | a separate manifesto file would rot, duplicate VISION/MVP_SCOPE, and violate the anti-bloat caps (`AGENTS.md` §6); the manifesto's job was to surface four missing pieces, not to become a fifth canonical layer | (a) BRIEF_SPEC sketch gets sensory-emitter + beat-boundary delta — the brief is a perception delta, not a world dump; (b) VALIDATION_SPEC sketch gets prompt-injection neutralized **structurally** (the prose→proposal boundary + grammar-constrained Intent JSON — no post-hoc text sanitization, that path is a crutch); (c) CORE_DESIGN_RESEARCH §6 gets P3e `psychological_echo` as a phase-3+ behavior modifier derived from existing knowledge records — not new data; (d) STATUS FAQ gets a `git ls-files` pitfall (workspace ≠ tracked) |
| D-019 | 2026-08-26 | M3 (causal chain length), M4 (novelty/repetition), M5 (non-PC event share) accepted into iter-6 metric baseline (P1b–P1d) | owner answer to Q1: depth-equation factors must be measurable, not just asserted — M1/M2 alone leave Causality, Diversity, and "world not player-centered" unmeasured | metric thresholds set from baseline at the iter-6 gate (same protocol as M1/M2); CORE_DESIGN_RESEARCH §6 P1b–P1d flip from proposals to accepted; `MVP_SCOPE.md` §15 gets M3/M4/M5 |
| D-020 | 2026-08-26 | Sparse NPC↔NPC relations approved for iter-3 (P2a) | owner answer to Q2: rumor acceptance already weighs trust (`rules.json`), but teller↔listener trust has no data home today; the gap is silently papered over | no event-schema break (relations are state, not event fields); sparse pair-keyed map; enables guard coordination, non-PC story lines, richer rumor dynamics; `MVP_SCOPE.md` §4.2 updated |
| D-021 | 2026-08-26 | Minimal goal/urge ticker approved for iter-3/4 (P2b) | owner answer to Q3: the world must act without the PC for T8 director-off to mean anything; minimal autonomous actions through the same tick queue, same discipline | full LLM planning — never (Generative Agents anti-precedent, `VISION.md` §6 held); minimal version in iter-3/4; gate can measure aliveness; non-PC event share (M5) becomes non-trivially non-zero by construction |
| D-022 | 2026-08-26 | One more research pass before iter-1 (Q4 = no) | owner: foundation not yet hardened, references not fully mined, concept not finalized; code before concept = rework risk; same logic as D-018 (a separate manifesto file would rot) applied to the entire iter-1 schedule | iter-0g is that pass; doc-loop alarm exception granted (sixth docs iteration in a row); iter-1 still the next functional step; no further docs iterations without a fresh owner request |
| D-023 | 2026-08-26 | Runtime state = incremental projection; `fold(log)` is for T2 replay only — not the startup hot path | KI#5: `EVENT_SCHEMA.md` §1 says "SQLite is a rebuildable index over the log" but does not state that runtime uses an incremental projection while fold is the truth-test only; ambiguity risks O(N²) at startup if `fold(log)` is misused as the runtime state path | runtime projects `state_new = state_old.apply(event)` per event as emitted, with SQLite kept in lockstep; fold stays the truth-test (T2) and the rebuild path; clarify in `EVENT_SCHEMA.md` §1 next time it is touched |
| D-024 | 2026-08-26 | Per-reference deep dives live in a new `docs/REFERENCES_DEEP.md`; the three-place anti-drift policy for external sources is now: catalog (`REFERENCES.md`) ↔ synthesis (`CORE_DESIGN_RESEARCH.md` §2) ↔ deep dives (`REFERENCES_DEEP.md`). Future reference-deepening iterations are tracked as `ref-N` items in `docs/TASKS.md` infra backlog | the catalog was a faceless list of licensed donors with phase tags; the synthesis was one-line per source. The owner requested concrete depth: named mechanics, real data structures, take/adapt/inspire, strengths/weaknesses. Putting this in either existing file would bloat them past the 400-line cap (`AGENTS.md` §6) and duplicate ownership. A third file, with the catalog as the spine and the synthesis as the index, is the cleanest fit | iter-0h ships the framework (format template + iteration plan) + the first batch (Neighborly, Mesa, DF Legends XML export schema); future batches are planned as `ref-1`..`ref-11` (3 solo: DF worldgen, C:DDA schema, Paradox scripting; 8 batched trios). Cap 400 enforced at the time. Review quarterly alongside the `doc-2` license re-verification. Nothing here changes phase law or D-015 |
| D-025 | 2026-08-26 | Doc cap policy rewritten: `docs/*.md` ≤600 lines with a **substance-over-cruft** filter (AGENTS.md §6.1) replacing the rigid 400-line wall inherited from D-024-era enforcement. The MVP_SCOPE special carve-out (≤450) is dropped — all `docs/*.md` share the 600 ceiling | iter-0i trimmed §2 of `docs/REFERENCES_DEEP.md` (~85 lines cut) to fit the 400 cap, but the trim removed substance, not cruft: the full top-level XML elements list (16 entries), the event-type enumeration with real field names, the Mesa pseudo-code tick-loop block, the DataCollector detailed description, and the "no determinism-by-construction" Mesa weakness bullet. The owner flagged this explicitly: "hard cap = crutches, not quality; some things do not fit in such a volume" | AGENTS.md §6 rewritten with §6.1 substance-vs-cruft criteria — filler words, restatements, linker chains, run-ons, decorative prose = cut always; named systems, real field lists, type enumerations, pseudo-code, per-source verdicts = never cut to fit cap. Cap raised to 600 as a generous ceiling. Cruft pass is the mandatory first action on any cap breach; if substance remains and cap is exceeded, keep, document rationale in worklog. iter-0j applies the new policy by restoring the iter-0i over-cut. Future doc iterations judge substance, not line count |
| D-026 | 2026-08-26 | Per-reference deep dives split from one file into a directory: `docs/ref/<source>.md` (one per source). `docs/REFERENCES_DEEP.md` becomes the **index + format template + iteration plan** (~130 lines) and is no longer the deep-dive content host. Supersedes D-024's "single file" wording — the three-place anti-drift policy stands unchanged (catalog ↔ synthesis ↔ deep dives); the deep-dive place is now a directory, not one file | at iter-0j the single-file `REFERENCES_DEEP.md` was 737 lines (over the new 600 cap, justified by substance per D-025). The iteration plan in §1 has 9 more ref-N iterations queued (ref-3..ref-11); projected final size at single-file scale: ~2500–3500 lines (4–6× the cap). The §6.1 substance-vs-cruft filter is a defence against cutting real depth to fit a line count, not a licence for unbounded growth. The same logic that D-024 applied recursively to the catalog/synthesis/deep-dive split applies again: when one place bloats, split the place | (a) Five per-ref files created (`neighborly.md`, `mesa.md`, `df_legends_xml.md`, `df_worldgen.md`, `cdda_data_json.md`) carrying the iter-0h + ref-1 + ref-2 content verbatim with cross-refs updated (`§2 above` → `df_legends_xml.md`, etc.); (b) `REFERENCES_DEEP.md` rewritten as header + §0 format template + §1 iteration plan + §2 NEW index table (one row per ref: id, source, file, license, phase, one-line verdict, status); (c) future ref-N iterations touch 2 files (one new `docs/ref/<source>.md` + this index to flip status) — well within the 3–5 soft limit (AGENTS §2.3); (d) each per-ref file naturally ≤300 lines, so no substance exceptions are needed. The D-025 substance-vs-cruft filter still applies within each per-ref file. Single-owner rule (AGENT_NAVIGATION §3) preserved: each per-ref file is the single owner of one source's deep dive; `REFERENCES_DEEP.md` is the single owner of the iteration plan + format template. AGENT_NAVIGATION §1 row added for `docs/ref/`; the §3 three-place row stays — the deep-dive place is a directory now, but the ownership contract is unchanged |
| D-027 | 2026-08-27 | The reference distillation becomes a fourth place in the anti-drift system: `docs/BLUEPRINT.md` (method + 12-row resolution ledger + 12 cross-cutting laws + build index) + `docs/blueprint/phase0.md` (iter-1..6 combined donor designs) + `docs/blueprint/phases.md` (phases 1–6 architecture + cross-cutting). The blueprint owns **cross-reference resolutions and combinations** (new facts created by synthesis); mechanics stay in `docs/ref/`, contracts in `MVP_SCOPE`/`EVENT_SCHEMA`, spec clauses in `SPECS_BACKLOG` — linked, never restated. Two standing clarifications enacted by the ledger: RNG-1 (a single RngBank authority holding deterministically seed-derived named streams — a clarification of INV-2's *intent* "single point of randomness control", not a weakening; byte-identical T1 replay is preserved and strengthened by the fingerprint) and EPIST-1 (Influence Boundary adopted as the default iter-2/3 design; owner may veto at the iter-3 design gate) | owner-requested synthesis pass (iter-0u): after 19 reference iterations the per-ref verdicts existed but nothing combined them — a builder facing iter-1 had to re-read ~11k lines and re-derive the same resolutions; STATUS "Next step" had grown a drift-prone prose mapping doing this job in the wrong file. Cross-ref tensions (MTTH vs determinism, mutable ECS vs INV-1, player-stress pacing vs player-blind canon, GA retrieval vs known_by law) each needed one resolved mechanism, recorded once | (a) three new files, each ≤600 cap by construction (149/340/224 lines); (b) STATUS "Next step" shrinks to a pointer — the ref→iteration mapping now lives in the blueprint build index, its single owner; (c) maintenance protocol: a future deep dive updates only the ledger rows and part-file sections it changes, at deep-dive time; a resolution-changing ref requires a worklog note and a superseding D-entry; (d) AGENT_NAVIGATION §1/§2/§3 updated (Deep reading gradient now includes BLUEPRINT); REFERENCES_DEEP header gains a one-line pointer. Phase law unchanged: iter-1 remains unconditionally the next iteration; the blueprint is guidance for building it, not a new spec layer |
| D-028 | 2026-08-27 | INV-2 rewritten from "one `random.Random(seed)` instance" to **single point of randomness control**: one master seed; named streams deterministically derived via the RngBank authority, `stable_hash(s) = int.from_bytes(hashlib.sha256(s.encode()).digest()[:8], "big")` — sha256-based, environment-independent; cosmetic draws can never desync canon replay. Owner-approved in chat (standing directive: act without further questions). The 18 remaining audit resolutions (full list: iter-0v worklog + blueprint sub-clauses) land as blueprint/EVENT_SCHEMA/SPECS_BACKLOG sub-clauses in the same iteration; KI#8 opened/closed; KI#7 resolved (worklog trimmed to the 10×3–5 cap, TASKS done-entries collapsed to one line — pre-trim history lives in git) | iter-0u's audit pass (owner-requested) found the pre-D-028 INV-2 wording contradicting RNG-1 in three load-bearing places (AGENTS law, the TASKS iter-1 task text a builder reads first, the TECH_NOTES §4 recipe) — the donor sources themselves (Brogue `ranctx RNGState[2]`, DCSS N streams) are multi-stream, so the old wording was stricter than its own donors and would have forced a state-slicing crutch; a built-in `hash()`-based stream derivation would have silently depended on `PYTHONHASHSEED` being set outside the code | (a) AGENTS §4 INV-2 + TASKS iter-1 + TECH_NOTES §4 + MVP_SCOPE §3/§13 synced in one pass — no stale prescriptive wording remains (pre-D-028 mentions in `docs/ref/*` and `REFERENCES_DEEP.md` are historical evidence, not prescriptions; single reading owner = AGENTS.md); (b) RNG-1 ledger row carries the stable_hash definition + the substantive-by-definition lint rule; (c) sub-clauses: scheduler DAG annotation language (iter-2), intent `based_on_event_seq` OCC + PROPOSED/ACCEPTED/REJECTED lifecycle (iter-2), price precursor tuple → KnowledgeRecord migration (iter-2→3), director rejection policy + per-run scope (iter-4), ShufflePool for ink `shuffle` + prune_window departure (iter-5), T1 fixture-regeneration guard (iter-6), phase-0 minimum pack lint (iter-1), BRIEF-1 eviction contract (phase 1), STORE-1 precedence chain + reflection provenance (phase 4), copy-from cycle contract (phase 6), event-vocabulary-closed-per-pack (EVENT_SCHEMA §11); (d) doc-loop alarm acknowledged: iter-0v is the 21st docs iteration, owner-requested exception per D-022 wording — **iter-1 (code) is unconditionally next; no further doc passes without a fresh owner request** |
| D-029 | 2026-08-27 | Reference digestion declared **complete**; plan realignment is surgical, not structural: the phase skeleton (0–6), the three-layer architecture, and INV-1..5 stand **confirmed** — the blueprint (`docs/BLUEPRINT.md` + `docs/blueprint/`, D-027/D-028) is the post-reference mechanics layer and the single build guide for iter-1..6. The pre-research two-week day-numbered sprint calendar is dropped in favor of iteration-counted sequencing; `CORE_DESIGN_RESEARCH.md` flips to absorbed per its own lifecycle rule (P2c detail callbacks — the only owner-pending proposal left) | owner-requested concept review (iter-0w): the original plan predated the 21-iteration research phase and its calendar drifted, but its structure survived — 33 references replaced vague intentions with named donor-backed mechanisms (RngBank, SCHEDULED/SEEDED/SAMPLED, incremental projection, Influence Boundary, entropy director, block-pipeline brief, pack/storage/LOD ladders) without moving a single phase gate; ref-14/ref-15 (Sims, Prom Week) stay owner-request-only and synthesis-only — already cited in blueprint donor stacks | (a) MVP_SCOPE §17 becomes iteration-counted (day column removed); TASKS iter-1..6 day-tags removed; (b) ROADMAP §2 gains the blueprint pointer (gates owner = ROADMAP; per-phase architecture owner = `docs/blueprint/phases.md` — anti-drift split preserved); (c) CORE_DESIGN_RESEARCH + AGENT_NAVIGATION §1 flip to absorbed; README Status refreshed; (d) KI#9 opened/closed (calendar/lifecycle/README drift); (e) doc-loop: iter-0w is the 22nd docs iteration, owner-requested (D-022); **iter-1 code is next, unconditionally** |
| D-030 | 2026-08-27 | **Darkness is an architectural property, not content scripts** (content-principles pass, owner-requested): phase-0 `tavern_pack` v0.1 stays exactly as scoped — `MVP_SCOPE.md` §4–§7 own the entity/action counts; tone is produced by **data asymmetry inside the existing 8 systems**, never by new systems. A "grim" pack line (romance → intimacy → coercion; jealousy/shame axes; intimate-act knowledge records) is admissible only as a **post-gate pack candidate** under the `PACK_SPEC.md` trigger (phase 6 / a 2nd setting, `SPECS_BACKLOG.md`); its material accumulates in the PACK_SPEC sketch row, never in a pre-trigger spec. Consent vs coercion, when it exists, is crafted knowledge records (the D-008 pattern), never a special mechanism | owner request (chat, 2026-08-27): the world must not skew positive — real worlds are ruder and crueler than game worlds; the owner wants principles laid down *before* any content. An external-LLM content proposal evaluated the same day exposed the fenced-off failure mode: content-first thinking pulls 4 new actions + 6 new axes + 6 hooks + 4 items into a frozen phase-0 slice (AGENTS §2.4 scope creep), invents ad-hoc mechanics (blackmail, debts, weapon damage-over-time = the banned combat line), leaks a group-reputation field (D-006), and writes a full pack spec before its trigger (SPECS_BACKLOG header rule) — while its correct core insight (darkness via consequence chains, irreversibility, world-without-player) is already the repo's own law (D-005, T4, M5, D-021) | (a) named darkness levers for any pack author: asymmetric deltas (trust/relations build slow, break fast); an immediate observable **price marker** on every socially meaningful behavior (knowledge record or perceivable state token — already a PACK_SPEC clause); irreversibility (`irreversible: true` statuses; fire has no counter-event); rumor lethality via fidelity decay (D-007) hitting **per-NPC** relations — never a global number (D-006 held); (b) core corollary for iter-1..3 builders: stay **axis-blind** — relation axes and status keys resolve from pack data (`rules.json` precedent), event vocabulary from the pack (EVENT_SCHEMA §11), so a future pack adds `attraction`/`shame` with zero code change (INV-3 corollary); (c) tone lives in templates — English in phase 0 (D-009), localization is final-product pack data; (d) `pack-1` grim-pack candidate added to the TASKS infra backlog; PACK_SPEC sketch row gains the darkness clauses; (e) doc-loop: iter-0y is the 24th docs iteration, owner-requested (D-022 fresh request); **iter-1 code is next, unconditionally** |
| D-031 | 2026-08-27 | **Quality round: the two owner architecture texts (INVARIANT-CORE v3, Elegant Solutions) absorbed surgically into existing owners** (the D-018 pattern — no new canonical files): new law L13 abstraction cost gate (Rule-of-Three tiers; 4-branch registry threshold) + L14 elegance standard with review checklist (`BLUEPRINT.md` §2); build clauses: type discipline (frozen/slots DTOs, `Literal` for core closed sets only, minimal `Protocol` ports, `Final`), fail-fast watch-out, `tests/test_architecture.py` fitness test (import boundary · RNG monopoly · network ban — stdlib `ast`, zero new dev deps) in `phase0.md` §1, ActionResolver name→resolver dispatch (§2), tests-document-invariants + negative-test principle (§6); `AGENTS.md` §4 gains the INV-1 canon-write-privilege line, §9 the code-quality DoD bullet; **stack frozen**: Python stdlib-only through phase 2 — Go/Rust revisit gated by `perf-1` data, never speculation; the texts are cataloged as principle donors (`REFERENCES.md` §15) | owner request (chat, 2026-08-27): ensure the quality "bible" survives into the code iterations — the owner fears future work (core build, content packs, LLM/frontend adaptation) rotting into garbage code, piled-up "works somehow" complexity. Two owner-provided analyses came with the texts: one repo-aware (patch-only recommendations), one written without repo access (proposing `docs/ARCHITECTURE.md`, `docs/TYPE_DISCIPLINE.md`, `docs/TESTING_PHILOSOPHY.md`, mypy in CI) — contradictions resolved here, each with a reason | (a) no new canonical files — a fifth law layer rots and duplicates owners (D-018 precedent; AGENT_NAVIGATION §3 single-owner rule); the layer map already lives in `VISION.md` §4, conventions in `MVP_SCOPE.md` §18; (b) mypy now — rejected: dev tooling is capped at pytest + ruff (AGENTS §8/§10); parked as owner-gated `qa-1` in the TASKS infra backlog, values adopted as build clauses instead; (c) Upcaster Registry — already covered: `schema_version` + migration note (AGENTS §8, EVENT_SCHEMA §8) + the `_correction` event-family precedent (phase0 §2); (d) "ES as specialized tool, not foundation" — rejected: the canon is an audit-critical simulation domain by definition (INV-1, D-003), the text's own carve-out; (e) Erlang supervision / lifecycle events — rejected for phase 0: fail-fast replaces let-it-crash (phase0 §1 watch-out). DECISIONS now 31 entries (>30 cap): every row is a distinct stable decision and append-only forbids the cut — kept over per AGENTS §6.1 substance-over-cruft, rationale recorded in the worklog. Doc-loop: iter-0z is the 25th docs iteration, owner-requested (D-022 fresh request); **iter-1 code is next, unconditionally** |
