# DECISIONS.md — ADR-lite

> Append-only **within a phase**; each entry: decision → why → consequence.
> Supersede with a new entry, never delete mid-phase. Cap: 30 entries,
> enforced by collapsing at phase gates (D-034): family merges write compound
> IDs (`D-018/D-022/D-029`) so external citations still resolve; compressed rows
> keep the decision and link the single owner of the detail (D-024 anti-drift
> — spec-restatement inside a decision row is duplication, not substance).
> Pre-collapse history lives in git. Collapsed 46→30 at the phase-0→1 gate
> (iter-7 intake); 41→30 at the phase-1 gate (iter-26 — the collapse ran
> inside the gate-review iteration per the owner's quality-first directive);
> 35→30 at the phase-2 gate (iter-35, the same precedent); 48→30 at the
> phase-3→4 gate (iter-54 — the phase-3 landing rows folded into the
> pacing/grammar/social/arc/content families, the verdicts into the
> gate-verdict family; the landing detail lives in TASKS + TEST_PLAN §6 +
> git, the single owners); 30 held at the phase-4→5 gate (iter-65 — no
> collapse owed: every phase-4 landing rode the single D-084..D-093 family
> row as it landed, and the verdict D-094 joins the gate-verdict family);
> **collapsed 71→30 at the phase-5→6 gate (iter-102 — the phase-5 build
> column D-105..D-133 folded into the single build-family row, the intake
> verdict sets D-119/D-128/D-130/D-134/D-135 into the intake family, the
> testproto/protocol rows into the protocol family, the v0.2 refinement
> lance into the phase-4 row, the four phase-3 columns merged to one
> compound; the verdict D-136 joins the gate-verdict family; the per-row
> detail lives in TASKS + worklog + git, the single owners)**. **Collapsed 44→30 at the phase-6 gate (iter-116 — the fixations D-142/D-143/D-144 into the D-145 family row, the risk-synthesis landings D-137/D-139/D-140/D-141 into one row, D-138 into the D-076/D-081 row, D-062 into the narrator-boundary row D-054/D-055, D-079 into the determinism row D-004/D-028; the intake verdict sets D-147/D-148/D-150 joined the intake family, D-146/D-149/D-151 joined the gate-verdict family; the per-row detail lives in TASKS + worklog + git, the single owners).**
| ID | Date | Decision | Why | Consequence |
|---|---|---|---|---|

| D-001/D-009 | 2026-08-25 | Repo language law: docs, code, identifiers, commits in English; owner chat in Russian; prototype pack display names in English | token economy, owner request; consistency with logs/tests | all agents write English; final-product localization is pack data, zero code |
| D-002/D-010/D-032 | 2026-08-25 | Data contracts: JSON / JSONL + JSON Schema, no XML tooling (XML only as DF Legends *input*); EVENT_SCHEMA.md ↔ `schemas/event.schema.json` 2-place sync, test-enforced (the doc example is a test fixture); schema validation = the stdlib mini-validator `core/schema.py` for the used subset (`type`/`properties`/`required`/`enum`/`pattern`/`additionalProperties`/`items`/`minimum`/`$defs`/`$ref`) — any other keyword fails loudly | Python-stdlib native — the log is JSONL; `jsonschema` is a runtime dependency (D-012 forbids); hand-rolled structural checks duplicate the contract (the exact drift D-010 prevents) | `schemas/event.schema.json` is the machine contract; the log writer is the enforcement point — no invalid line can enter a log; T0 re-validates the doc examples; pack-file JSON-Schemas stay a phase-6 rung |
| D-003/D-023 | 2026-08-25 | Event-sourcing shape: JSONL log = truth, SQLite = derived index (INV-1); runtime state = the incremental projection `state_new = state_old.apply(event)`; `fold(log)` is for T2 replay and rebuild only — never the startup hot path | the ambiguity risks O(N²) at startup if fold is misused as the runtime state path (KI#5) | replay = fold(log); the DB can be dropped and rebuilt at any time; fold stays the truth-test (T2) and the rebuild path |
| D-004/D-028/D-079 | 2026-08-25 | Determinism law: no wall-clock anywhere, including the log header (header carries seed, python, schema_version, commit, pack — no timestamps); INV-2 = single point of randomness control — one master seed, named streams deterministically derived via the RngBank authority (`stable_hash(s) = int.from_bytes(hashlib.sha256(s.encode()).digest()[:8], "big")`, environment-independent); cosmetic draws can never desync canon replay; D-079 (iter-50, engine-2): every pack urgency entry rolls d100 on its OWN content-addressed stream `urgency:<npc>:<kind>` (`core/rng.py::urgency_stream_name` the naming grammar's single owner; the (npc, kind) pair pack-linted unique, the name injective; lazily registered on first use — the closed-set tripwire survives verbatim) — an added or removed entry shifts NEITHER a later check draw NOR another entry's rolls (INV-2's add-safety clause; the single shared `urgency` stream was landed as a candidate and REFUSED by its own A/B — add-safety 4/10 day1_full byte-identical); the nesting law reworked: an urgency-family stream may shadow the assured `substantive` run scope (the ONE legal nesting), cosmetic-inside-substantive stays the loud RNG-1 desync tripwire, substantive-inside-urgency raises (a bug, not a feature) | byte-identical determinism; the old "one Random instance" wording contradicted RNG-1 in three load-bearing places and was stricter than its own multi-stream donors; a built-in `hash()` derivation would silently depend on `PYTHONHASHSEED` | AGENTS.md §4 is the single reading owner; the iter-0v audit sub-clauses live as blueprint/spec sub-clauses (list in git worklog iter-0v) |
| D-005/D-038/D-039/D-040/D-041 | 2026-08-25 | Director & autonomous mechanics (iter-4/4a): the director is a consequence planner, never an improviser — complications are seeded at event time in a per-run buffer, never invented later; narrative entropy = observable state only (hook weights + global suspicion + visible threats); explicit triggers (time/place/threshold) fire causally; releases ride the intent door as `director_<N>` (the director never writes canon); director-off keeps seeding, suppresses releases (T8 baseline); crossings fire in TICK order. Autonomous: P2b goal ticker — per-NPC urgency specs roll d100 on the substantive stream; a precondition failure stays SILENT (the world's noise floor absorbs autonomous attempts); arrest resolution cause-chained to the attempt, `caught` irreversible (T4). Audit laws (iter-4a): the playscript feed is player-driven only (an autonomous ending NEVER advances the script); the crime-status flip guards the pack's ordered `status_values` progression; `reset_on_rotation` resets BOTH participants; dead pack keys deleted | causality by construction (RimWorld storyteller = the named anti-pattern); D-021 — the world must act without the PC; small-formula dynamics, not a planner; capture/escape is a binary outcome with no chase sub-system (phase-3+); KI#17–20 found by the owner-requested audit (124 probe runs clean) | director-off A/B mandatory (T8); beat order decay → urgencies → director; autonomous intents enqueue at `entry.tick`, never `beat_tick`, decay commits at `beat_tick`; full detail: `docs/DIRECTOR_SPEC.md` (the runtime contract owner) + `docs/INTENT_SCHEMA.md` + git worklog iter-4/4a |
| D-006/D-007/D-008 | 2026-08-25 | v0 epistemics shape: no group reputation (guard-to-guard transfer models it); rumor = knowledge transfer with fidelity decay; lies = crafted knowledge records | "reputation with the watch" = transfer events at watch change; distortion comes from source incompleteness — no extra system; foundation for phase-4 `believes/lies` | one mechanic covers gossip and hearsay distortion; no truth field on records in v0 |
| D-011/D-012/D-013/D-014/D-046 | 2026-08-28 | **Repo meta & the executable surface**: no CHANGELOG.md (git + STATUS own history); the repo name a placeholder; the concept docs live outside the repo; stdlib-only runtime (D-012, AGENTS §5/§8 — any dependency addition stops for owner confirmation); D-046 `scripts/` is operator tooling INSIDE the executable invariants (PACKAGE_DIRS += scripts + the coverage-closure test — a new top-level code dir joins in the same iteration it lands; the print exemption is CLI-class tools only; the INV-4 import check runs against every package dir since iter-6a) | a top-level code dir outside `PACKAGE_DIRS` silently escapes INV-4's check; dev tooling stays capped at pytest + ruff | the stoplist scope (INV-3) stays engine-side (core/ + sim/ + brief/) with the periphery interpretation test-owned |
| D-015 | 2026-08-25 | License filter lifted; usefulness & phase is the filter | no monetization planned; open bases adapted freely incl. functional re-invention | donors gated by phase (ROADMAP §4); phase 0 uses zero external code |
| D-016/D-017/D-024/D-026/D-027 | 2026-08-25 | Reference architecture (four places, four jobs): `docs/REFERENCES.md` = the catalog (owner survey rev v2 merged; licenses verified + tagged with check date, unverified quarantined in §11, quarterly review); `docs/ref/<source>.md` = one deep-dive file per source, `docs/REFERENCES_DEEP.md` = index + format template (single-file dives projected 2500–3500 lines, 4–6× the cap); `docs/BLUEPRINT.md` + `docs/blueprint/` = the distillation — the 12-row resolution ledger + cross-cutting laws + build index; the blueprint owns cross-reference resolutions and combinations (mechanics stay in refs, contracts in the specs; single-owner rule preserved — link, never restate, the D-024 anti-drift discipline) | rich but unverified survey (incl. unverifiable, misattributed repos); after 19 reference iterations nothing combined the verdicts — cross-ref tensions (MTTH vs determinism, mutable ECS vs INV-1, GA retrieval vs known_by) each needed one resolved mechanism, recorded once | admission rule: verify via GitHub exact-name search (web SEO misses small repos); donors are inspiration/patterns, never 1:1 copies (`REFERENCES.md` §0.7); a deep dive updates only its ledger rows; a resolution-changing ref needs a worklog note + a superseding D-entry; each per-ref file naturally ≤300 lines (the split-when-bloated law) |
| D-018/D-022/D-029 | 2026-08-27 | Concept absorption & digestion complete: manifestos and owner texts land **surgically into existing docs** (the D-018 pattern — never a new canonical file); one research pass granted then closed (Q4 = no); digestion declared complete at iter-0w — skeleton (phases 0–6, three layers, INV-1..5) confirmed, blueprint = the single build guide | a fifth canonical layer would rot, duplicate owners, and violate the caps; foundation hardening before code; 33 references replaced vague intentions with named donor-backed mechanisms without moving a phase gate | BRIEF/VALIDATION sketches gained sensory-emitter + beat-boundary delta + structural injection-neutralization clauses; P3e `psychological_echo`; sequencing is iteration-counted (calendar dropped); the doc-loop exception law: no doc pass without a fresh owner request |
| D-019/D-020/D-021/D-033 | 2026-08-28 | CORE_DESIGN_RESEARCH proposals accepted and built: M3 (causal chain length) / M4 (novelty) / M5 (non-PC share) into the metric baseline (P1b–P1d); sparse NPC↔NPC pair relations (P2a); minimal goal ticker (P2b); salience-based talk topics (P2c — knowledge *used*, not just stored) | depth-equation factors must be measurable, not asserted; teller↔listener trust needed a data home; the world must act without the PC for T8 to mean anything; the last owner-pending proposal flipped to accepted per its lifecycle rule | no event-schema break (relations are state); full LLM planning — never (Generative Agents anti-precedent); M5 non-trivially non-zero by construction; metric thresholds set from the measured baseline, same protocol as M1/M2 |
| D-025/D-034 | 2026-08-26 | Cap laws: `docs/*.md` ≤600 lines with the substance-over-cruft filter (AGENTS.md §6.1) replacing the rigid 400-line wall; DECISIONS.md ≤30 entries, enforced by collapsing superseded/absorbed entries into consolidated compound-ID rows at phase gates (`ROADMAP.md` §5) — never by mid-iteration cuts | iter-0i trimmed substance (enum lists, field names, pseudo-code) to fit a hard cap — the owner flagged: "hard cap = crutches, not quality"; the gate-collapse keeps the cap's pressure and the append-only invariant intact with a natural archiving rhythm | filler/restatements/linker-chains = cut always; named systems, field lists, enum values, per-source verdicts = never cut to fit; a cap breach triggers a cruft pass first, over-cap substance stays with a worklog rationale; the file may sit over cap between gates; within a phase: supersede, never delete |
| D-030/D-031 | 2026-08-27 | **The owner-directed principle rounds absorbed surgically** (the D-018 pattern — never a new canonical file): D-030 darkness is architecture — tone = data asymmetry inside the existing 8 systems (asymmetric deltas, an observable price marker on every socially meaningful behavior, irreversibility, rumor lethality per D-006); the core stays axis-blind (a future pack adds axes with zero code change); the grim line admissible only as the post-gate `pack-1` candidate under the PACK_SPEC trigger; D-031 the quality round — L13 the abstraction cost gate (Rule-of-Three, the 4-branch registry threshold) + L14 the elegance checklist (BLUEPRINT §2), type discipline + fail-fast + the registry + negative tests (phase0 §1/§2/§6), the architecture fitness test, the stack frozen stdlib-only through phase 2, mypy parked as `qa-1` | the owner fears rot into piled-up complexity; content-first proposals exposed the scope-creep failure mode (a spec before its trigger, ad-hoc mechanics) | AGENTS §4 canon-write-privilege line + §9 the quality DoD bullet; dev tooling capped at pytest + ruff |
| D-035/D-036/D-050 | 2026-08-29 | **The write-path discipline** (engine internals): D-035/D-036 the commit gate + the spread-pass discipline — every event enters through `Simulator._commit` (state deltas validated against the projection BEFORE the write, INV-1's privilege line D-031), the per-layer spread singleton (one live pass per layer, shared causes, KI#16); D-050 the derived indexes live beside their single mutation funnel (iter-8h): `KnowledgeView`'s `who → token → source-ids` (add is the only writer; `before_source` semantics preserved) and the Simulator's `(entity, prop) → latest-committer-tick` (`_commit` the only writer; the decay baseline reads the index — KI#19); plus the same-funnel scan eliminations (the beat-window break, the top-1 salience, the forward-once OCC fold, the once-per-call entropy) | each index has exactly one writer — the invariant is checkable by reading one function; the golden fixtures byte-identical through the pass | the perf numbers: profiled at iter-30 (TECH_NOTES §8 — event-linear, schema validation the hot spot); tests: the fold/replay suites |
| D-037 | 2026-08-28 | iter-3 epistemics architecture: (1) knowledge state = a derived per-knower index (`core/knowledge.py` `KnowledgeView`) rebuilt by folding `knowledge` records — never projection props; (2) system reactions are event-driven, dispatched from the commit door (`_commit` → `_react`: crime first, telling second) — no call site can forget them, the cascade terminates; (3) watch rotations fire on clock crossing, never pre-seeded — a run still ends when its script's queue drains; (4) the systems live in `core/`, **not `sim/systems/`** — anything the canon door must dispatch is kernel mechanics (the import-boundary law) | (1) keeps INV-1 pure (memory = fold of the log, T2 applies); (2) removes the "forgot to react" bug class at zero scheduling cost; (3) pre-seeding would extend every run to tick 1080; (4) core never imports sim — law wins over the stale plan note | suspicion reacts to **novel tokens only**; the arrest threshold has a single owner (`crime_watch.arrest.requires_suspicion`); expectation violations chain to the axis-specific mover; `crime_status` lives on the player entity (namespace-collision avoidance); the telling shares the teller's most salient **novel** fact with one-step fidelity decay + an acceptance roll; official briefings transfer unrolled |
| D-042/D-043/D-044/D-047 | 2026-08-28 | The read side is pure functions of the log — render AND brief: every render entry point builds its `Engine` on a fresh `RngBank` from the log HEADER seed (same log → same bytes in any process/`PYTHONHASHSEED`; a growing log keeps its rendered prefix; tracery ~350 stdlib lines, cosmetic stream only, ShufflePool no-immediate-repeat; the importance gate is pack data `tale_gate.min_importance`); M1–M5 + the emergent-chain count are pure functions of `(events, projection)` in `core/metrics.py` (Mesa `DataCollector` inverted — the simulator never knows a metric exists); the assembler is **zero RNG** (dry structured tokens, L2 — `docs/BRIEF_SPEC.md` owns the skeleton: two-level budgets, whole-block eviction in the fixed order, directives never dropped, `[truncated:N]` markers, ranking caps ≠ budget drops, all budgets at `rules.json::brief`); `scripts/balance_harness.py` runs the 1000-sim distribution (contract `docs/TEST_PLAN.md` §6) | render determinism must be env-independent and prefix-stable; a runtime-coupled metric creates feedback risk + hot-path cost; the same purity law extended to the phase-1 mediator input (D-047's clause: deterministic token proxy keeps byte-identity stdlib-only); separating ranking caps from budget drops keeps the anti-silent-drop law about assembly budgets, not retrieval windows | the 1000-seed baseline is the M-threshold reference (M5 p50=0.77, chains p50=20, M3_mean p50=13.81, M1 p50=0.24, M4 p50=0.18; full table reproducible, output gitignored); T1 covers the chronicle via the log fixture; the session = one opened Simulator, byte-identical to the batch run; brief bytes pin-tested (golden fixture); the real tokenizer must not change committed formats without a spec bump (BRIEF_SPEC §8) |
| D-045/D-058/D-064/D-083/D-094/D-136/D-146/D-149/D-151 | 2026-09-06 | **The phase-0..6 gate verdicts — PASS, the ladder complete** (iter-6/26/35/54/65/102/116, the evidence checkpoints per ROADMAP §5; the phase table owns closed/open — these rows are the verdict records): D-045 phase 0 — all six MVP_SCOPE §16 exit criteria met, no kill-criteria hit, T6/T1/T8 (26 chains OFF)/T7/T3/T5 green, the ontology proved combinatorial depth, day1_full/seed 125 became the canonical fixture, phase-1+ metrics compare against the recorded baseline; D-058 phase 1 — 0 canon violations over 109 live beats (the exit criterion), the corpus at 105 distilled cases replayed through the REAL mediator cycle, the full §5 re-run (M1=0.417/M2=0.500 ON seed 125, T8 OFF 26 chains, T7 reads as a story), DECISIONS collapsed 41→30; D-064 phase 2 — 35/35 boundary validity over 51 combined utterances (0 honest misfires, 10/10 off-grammar probes caught), the §5 re-run IDENTICAL to iter-26's numbers (the parser never touched a canon path), the corpus 105 narrator + 10 parse cases green, DECISIONS collapsed 35→30; D-083 phase 3 — the §5 re-run green (ON M1=0.509/M2=0.333, OFF T8 26 chains, stretch max 1 both pacing arms + the quiet-walk stage, T7 a story), DECISIONS collapsed 48→30 (the detail: TASKS iter-54 + TEST_PLAN §6 + worklog iter-54, the single owners); D-094 phase 4 — 0 leaks on the blind-NPC suite (the exit criterion, the 26-test instrument: the every-prefix × every-knower 10-seed + golden sweep, the adversarial omniscient query + None probe, the live drain's every emitted call, the teeth family), the §5 re-run IDENTICAL to iter-54's numbers (ON seed 125 M1=0.509/M2=0.333, OFF T8 26 chains ≥ 3, stretch max 1 both arms, T1 double-run byte-identical, T7 a story — the phase-4 landings kept the corpus-price-zero promise on the committed scenario), the mode-F chronicler acceptance green (TEST_PLAN §7, 53/53 through the count gate), 1178 tests collected (1168+1 skip pure-dev) + ruff clean, the TASKS phase-4 ledger collapsed + the README resynced (KI#73); D-136 phase 5 — PASS (iter-102, the owner's exit-review call — the queue empty since weather-1, rev5 the pre-gate evidence base): the exit criterion "an emergent chain of 3+ events without the player" MET in its worldgen-fed form — the ARMED committed pack (depth-5b genesis + the year-scale macro arming + the weather chain): T8 OFF seed 125 reads 24 chains (the iter-65 record 26 — the delta is iter-98's DESIGNED day1_full price, the LOD's one-gate engagement, 61→56 events, paid on record); the 60-seed band 15–24, M5 p50 0.79, stretch max 1 (rev5's records hold); ON seed 125 M1=0.52/M2=0.2; T7 the tale reads as a story — the worldgen genesis + world_history lines now OPEN the chronicle; the longrun pins green (T1/T2 on the year-scale log, the day-2 living floor, the idle world's beats); 1589+1 tests + ruff clean (Python 3.12.14, the env pin); no kill-criteria hit (the faction model landed depth-6/7). The §5 step-6 staleness pass verdicts: KEEP the rest, REWORD three discharged-phase-0 clauses (AGENTS §1's phase frame; INV-4's "before the phase-0 gate passes" → the standing boundary; §8's LLM-guard condition → the engine-1 owner gate), DROP none; D-129's effect-line habit UNEXERCISED — zero protocol-law rows since D-128, reported out loud per its own consequence line; D-128's own effect lines: ROADMAP §5 step 6 executed here, MVP_SCOPE §18 landed, mech-2 still the candidate row (rides the next mechanics touch), the SPECS_BACKLOG sketch landed. DECISIONS collapsed 71→30, TASKS collapsed (the phase-5 ledger + the intake sections); **D-146 the phase-6 OPENING** (iter-109, the owner's «продолжай работу по планам… можем его начинать», the iter-55/73 opener precedent: PACK_SPEC written, the ref-18/ref-20 economy dives landed, the gut-check verdict NO CUTS — the 20-seed ablation + the named-consumer trace, the second-setting shape pinned TRAVEL); **D-149 the reskin day — the gate instrument** (iter-112, the owner's «да приступай к работе уже»: `content/road_pack/` the TRAVEL-loop reskin on the open generic stack — SRD 5.1 nouns, CREDITS sidecar, ids stay + surfaces renamed, move → ticks 'edge', `rules.json::travel` the FIRST committed travel-price arming (step 150 / climb 60 / river 45 — the derived prices 150/210/300/360), the T1 twin + ROAD_STOPLIST; the clock 14m24s first-scaffold → green T-suite, ZERO core edits git-verified — the exit criterion "a new T1 reskin without core edits, ≤1 day" MET BY MEASUREMENT); **D-151 the phase-6 verdict — PASS** (iter-116, the owner's gate call «давай уже свой гейт-колл 6 фазы или что там, приступай к работе и незакрытым задачам»: the §5 re-run — the seed-125 pair ON M1=0.518/M2=0.200 (56 events, iter-102's numbers reproduced exactly) / OFF T8 24 chains ≥ 3, M5 0.849; plumbing_smoke + road_smoke byte-identical to the committed fixtures — the road side's evidence the T1 twin itself (the header's commit field the only delta, '0000000' pinned in fixtures per the T1 law); T7 both chronicles read as stories (the road: the genesis + the four history lines + the mid-route watch change + the boatman's ramble; the tavern: the theft → the rumor spread → the watch change → the papers → the arson day); no kill-criteria hit — the module contract (the core/pack split, D-142's admission law) proven right by the reskin day's zero core edits; ROADMAP §2 flipped CLOSED — the open build rows (roads-1, res-1, pack-ci, world-2 L2, since-1) stay owner-gated backlog rows, the ORDER owner decides; the doc debts paid in the same iteration: DECISIONS collapsed 44→30, the STATUS tombstone trim + the FAQ resync, the TASKS/README collapses; the §5 step-6 staleness pass: REWORD AGENTS §1's phase frame + the FAQ cap-laws entry, KEEP the rest, DROP none) | the gate is the evidence checkpoint, not a feature deadline; each verdict's evidence detail: TEST_PLAN §4/§6 + the worklog gate entries | phase 4 opened at iter-55 on the owner's call (the iter-36 precedent); each verdict row stands for its phase, folding into this family at the next gate — D-083 executed the fold it predicted; phase 5 unlocked at iter-65, opened iter-73 (D-105) and CLOSED iter-102; phase 6 unlocked at iter-102, opened iter-109 (D-146) and CLOSED iter-116 (D-151) — the ladder complete; the standing work: the owner-gated backlog rows + the SoW horizon (ROADMAP §6); phases.md §6 the architecture owner |
| D-048/D-049/D-053/D-056/D-057 | 2026-08-31 | **The scene/brief read-side stack** (the phase-1 structural landings, owner-directed): D-048/D-049/D-053 the scene ledger — canon vs texture (the design iter-8a, hardening iter-8b, the LLM-free half iter-10: the scene lifecycle, the pin/retire vocabulary, the laundering refusals, the D-049 determinism quarantine — the ledger is session render state, never replayable, T1/T2 never touch it); D-056 st-1 presence & entity cards (iter-15: the arrival snapshot, the present set, the card surface — BRIEF_SPEC §3.4); D-057 the pack-declared `layout` (iter-20: canon-from-birth on the scene line, no initial_projection seeding — KI#48's correction: canon_slot reads BOTH prop sources) | the read side is pure functions of (log, ledger) with zero assembler RNG (L2); the projections are folds, the cards are the narrator's surface | contracts: BRIEF_SPEC §3.3/§3.4 + blueprint §1; tests: the ledger/presence/brief suites (iter-10/15/20) |
| D-051/D-063 | 2026-09-01 | **The track-B DF pipeline closed** (bg-1 + bg-2, the owner-supplied world exports): D-051 the SQLite sink — HANDLED records load as typed cores + EAV + `event_participant` + generic JSON, truncation flagged PARTIAL by default (`--strict` aborts), the DB a rebuildable content-deterministic index of the export bytes (INV-1's shape on foreign canon; no wall-clock in `meta`); D-063 the taxonomy + the sink v2 plus pass — `docs/TAXONOMY.md` (120 entries over the 16 target types, measured findings incl. the hfid1/hfid2 participant-index blind spot), the companion's `historical_events` in `event_plus_fields` (same ids, fields only) | the epistemic closure IS the participant index (blind spot inherited — DF history is canon-dense, epistemology-empty: the legends structure donates, knowledge propagation stays ours to design, phases.md §4) | recipes + numbers: TECH_NOTES §3.1-§3.3; tools: scripts/df_{survey,import,taxonomy,briefer}.py; tests: the bg suites (bg-1/bg-2) |
| D-052 | 2026-08-29 | **The validator contract** (iter-9, `docs/VALIDATION_SPEC.md` — the trigger-fired spec owns the field-level detail; `brief/validator.py` is the LLM-free half): the mediator's gate is a pure function of (proposal, log, pack) — one CLOSED structured proposal document (claims + intents + `expected_event_seq`; no prose field exists, so injection is neutralized structurally, D-018 — loud `ProposalError` on any shape drift, emitter bugs crash); honest verdicts `supported/contradicted/insufficient_data` evaluated against CURRENT canon under closed-world semantics (invented entity/event = contradicted; unmodeled prop = insufficient, never fabricated — UAP); ExpectedVersion OCC = the intent-door semantics reused (INTENT_SCHEMA §4: stale+broken → refused with first-break attribution; stale+intact → rebased; intents pass through with anchors untouched — the door stays the single commit path, D-037); the ≤2-regens protocol owns the call-budget reconciliation (2-call steady state = narrator + extraction OR regen; 3-call worst case = narrator + 2 regens — bounded, counted, exhaustion → dry mode L12) | every law reuses existing machinery (intent OCC, validate_shape, KnowledgeView, fold — D-018/D-035/D-037 discipline); the C-parser (phase 2) and the narrator boundary wire the same shapes; the scene-ledger gateway (iter-10) reuses the refusal shape + regen budget | the golden set (`tests/fixtures/validation_golden.json`) pins verdict semantics as committed data — computed, never LLM-judged; `invented`/`unverifiable`/`regen_count` are the bg-3 harness metrics; DECISIONS transiently 37/30 (collapse at the phase-1→2 gate, D-034) |
| D-054/D-055/D-062 | 2026-08-29 | **The two external dev-time doors — the narrator + parser boundaries** (iter-11/12/31): (a) the texture promotion door, the LLM-free half (`core/intent.py` + `resolvers.py::_pickup` texture branch; contract `docs/INTENT_SCHEMA.md` §2/§3/§7; mechanism blueprint §1): a texture-capable action's pack `texture` block (`requires` + `knowledge`, lint-checked, `{texture_slot}` not `{target}`) REPLACES the canon preconditions for intents carrying the mediator-resolved reference `{entry, scope, slot, value}` (`requires_for` — one split at proposal, completion, and OCC attribution; the reference is DATA on the Intent, shape-gated loud; core NEVER sees the ledger, INV-3/D-031); a take-success on the reference IS the promotion — one `StateChange(scope_target, slot, None → value)`, the object's canon birth (the gateway's canon-slot check outranks any texture on the slot from that moment); a failure promotes nothing (live+pinned); ghost interactivity structurally impossible; `unique_slots: ["hearth"]` (cross-scope re-establishment = the laundering-family refusal). (b) agent-in-the-loop, external at dev-time (owner verdict in chat): the call is a FILE contract — the mediator emits `output/mediator/call_<N>.md` (the brief + `narrator_protocol`, BRIEF_SPEC §7.1) and the operator writes `reply_<N>.json`, ONE closed `{prose, texture_delta?, proposal?}` (VALIDATION_SPEC §7.1) with deep shape gates AT the boundary; a refused document never feeds intents — the beat regens whole; the mediator's noun resolution (`feedable_intents`) drops non-player actors / non-live entries / per-document duplicates as WITHDRAWN notes, never events; beat order D-049's; L12 from day one: narrator → template → dry. (c) D-062 the phase-2 parser boundary — mode C is EXTERNAL at dev-time over a file contract, mirroring D-055 on the player's free text (iter-31, the owner's start-phase-2 call): the repo-side half is `brief/parser.py` (pure — the grammar snapshot: the pack's verbs with pack-DERIVED field constraints (ticks positive-int when drawn-`N`, method = `rules.checks.methods` keys, near = the action's ignition-layer spots on the actor's folded position, texture = a live-entry reference) ∪ the addressable nouns (canon entities + live ledger entries — ghost interactivity structurally impossible); `parse_call` (utterance + grammar + protocol, byte-deterministic); `parse_reply_from_mapping` — the closed {intent \| question \| no_intent} document gated AT the boundary: off-grammar output is a loud ParseError, never a feed — the world never moves on a malformed parse) + `cli/parser.py` `ParserDoor` (the session door: emit (retire window + scene sync + document) → apply (gate against the CURRENT snapshot → pin → feed ONE step through the front door → promotions wired exactly like the narrator path); the gate does NOT duplicate door-owned checks — one owner per law) + the `say`/`say apply` session commands sharing the session's ONE ledger (D-049); the pin law lands wired: `SceneLedger.pin` — an Intent referencing a texture entry pins it, the reference IS the pin (a door-rejected attempt still pins, a failed attempt keeps it live+pinned, un-pinning does not exist; a PROMOTED entry is TERMINAL — a reply referencing it is off-grammar, the parser takes the disambiguation path); the trigger-fired `docs/PARSER_SPEC.md` owns the contracts + the ≥90% measurement (parse validity ≠ world legality — attempts are facts; door outcomes never count as parse failures) | promotion is the ONLY texture→canon path (zombie texture cannot arise legally; inter-scope movement = take = committed event), reusing the door's whole spine (shape gate, preconditions, checks, OCC, knowledge) instead of a parallel write path (D-037); zero LLM/network/runtime-dependency code — INV-4 unchanged as written, nothing entered the codebase (no §8 stop needed); integration (llama.cpp inside SoW, the dumb-terminal wiring) stayed behind the phase-1 gate exactly as ROADMAP §6 demands; the SoW audit parked as `bg-6` | `brief/mediator.py` (pure document layer) + `cli/mediator.py` (session orchestration, periphery D-046) + the `SceneLedger` wiring; the phase-1 corpus (105 cases) replays the refusal vocabulary, both door outcomes, and the rebased/stale anchor paths through the real cycle; local inference (TECH_NOTES §1) remains the owner-gated runtime-engine candidate (`engine-1`) |
| D-059/D-060/D-061 | 2026-08-31 | **The phase-1 polish menu — three landings on the owner's finish-phase-1 directive**: (a) tune-1 (iter-27): the story-critical importance hook — pack-listed event types (`rules.json::importance.story_critical_events`, lint-closed) score +2 `story_critical_event` (`pack_importance` takes the event type, all emit sites); the tale gate follows low→medium — the RULE owns the signal/noise split per D-045(b), the gate is not the primary knob; + the `recuperate` resolver + `status_effects` pack block (`rest`, the 13th action, 60 ticks, fatigue −30 — the KI#4 counter-play, pack data end to end); day1_full OFF renders 47 events → 14 tale lines, zero decay/wariness noise (pinned); the 1000-sim baseline re-run IDENTICAL. (b) tune-2 (iter-28): `brief.present_entities.card_markers` — the prop-path marker table (threshold rows + value rows, closed marker surface `status.<axis>`/`relations.<axis>`/`crime_status`, lint-checked; the segment is `markers=`); the pack ships `wary` at `relations.suspicion` ≥ 25 (aligned with the `status_suspect_at` flip) + `suspect`/`caught` on the player's card — the narrator's read surface for standing state (the scene_delta half is lawful blindness, blind-NPC D-037: the CARD is the narrator's surface, the delta window the player's). (c) pack-2 (iter-29): the `spot_available` door check — the closed precondition set's 15th test (the target location holds an unburning spot of the declared layer, the exact condition the ignite resolver keys on; the `layer` param lint-checked) — arson on a destroyed or fully-burning location is an `intent_rejected` no-op with `failed_test target.spot_available`, the door-outcome vocabulary's fourth axis | the T7 phase-1 gate re-read flagged the noise floor (27 of 47 day1_full events were decay/wariness repetition — a bare gate raise would have cut story events the raw rule scores low); iter-17's live finding: the suspicion axis and the `crime_status` flip were claimable-but-invisible through the brief; the iter-2a audit note + iter-24's live probe: arson on ashes logged a no-ignition success that pretended the world changed | T1 fixture untouched by (b)/(c); tests 610→629 across the three; the seed-41 corpus probe flipped with (c) (renamed, the old pin lives in git); field-level detail: INTENT_SCHEMA §3 (the requires row), BRIEF_SPEC §3.4/§6 (the marker table law) |
| D-065/D-066/D-067/D-068/D-069/D-070/D-071/D-072/D-073/D-074/D-075/D-077/D-078/D-080/D-082 | 2026-09-01 | **The phase-3 (Director) build column landed — the four columns, condensed at the phase-5→6 gate (iter-36..53; the per-column compound rows live in git + TASKS' phase-3 ledger, the mechanism detail in DIRECTOR_SPEC §3–§5 + TEST_PLAN §6 — the single owners): the pacing stack (D-065 the RAMP/PEAK/REST/STAGNATION clock over narrative entropy, D-066 the eventless-stretch instrument + the DIR-2 A/B harness, D-067 the climax layer + PEAK_CLIMAX, D-068 the multi-channel policies + the per-hook quiet gate), the Paradox event grammar (D-069 predicate/weight + the burn law, D-070 the option layer, D-071 the on_action dispatch), the content column (D-072 document_check, D-075 the echo engine, D-077 the panic ripple, D-080 the echo driver, D-082 the ambient driver), the social stack (D-073 secrets/leverage, D-074 the coerce door + the spend, D-078 the coerce driver). The exit criterion's measurement (max stretch 1) and the gate verdict live in the gate-verdict row (D-083).** | phase 3's declared build column, ROADMAP §2; the owner's per-iteration "continue" calls | the pacing/grammar/content/social four-column shape is the director law's standing form; per-mechanism owners: DIRECTOR_SPEC + TEST_PLAN §6 |

| D-076/D-081/D-138 | 2026-09-03 | **The release-chain layer landed + its driver (arc-1 + content-6, iter-47/52 — the DF event_collections / Paradox event-chain precedent, the release-side twin of the grammar)**: D-076 the engine — `director.arcs` pack chains: the ORDER law (a member tag is a candidate only while it is its arc's CURRENT member, all release paths, explicit triggers included — pack-declared causality, not pacing), the GAP law (`min_gap_beats` spacing on the quiet/climax paths only, never the explicit one), the entropy mirror (passed members' instances stop counting — the burn law's twin), the one-sided membership lint; D-081 the driver — the aftermath chain `[possible_document_check_relief, barkeep_wary_sweep]` gap 2: the GAP law LOAD-BEARING (the unchained sweep would land its event BEFORE the check's own — the same-tick intent ordering, a causality lie; the march holds it to the day's last event), the successor trigger-less + climax-flagged + weight 0 (the entropy footprint exactly zero) + first_time_only, seeded on the steal failure appended last; measured: day1 9/10 byte-identical + seed 125's one appended event, the corpus 105/105 pin-green ZERO re-distill (the first content landing with none — the seeding event's hooks field is the birth record, the FAQ law 9); the NPC-move successor arm refused (the tune-3/st-6 owner-gated fork); D-138 the climax-flag verdict (the owner's, on iter-104's combined proposal at the iter-105 call): the flag is the peak's END marker — a climax-flagged hook releases only through the climax path and its beat ENDS the PEAK (PEAK_CLIMAX then REST unconditionally; the boss beat the first instance, D-081's weight-0 trigger-less closing sweep the live one); a non-boss aftermath beat that must fire regardless of the clock carries an explicit trigger instead (D-005's causality law); DIRECTOR_SPEC §3d/§5's law text stands as written | the D-065 nopacing record superseded in part at both landings (the closing beat rides the climax path, which the clock-off pack removes) | contracts: DIRECTOR_SPEC §3d + §11, TASKS arc-1/content-6, TEST_PLAN §6; tests: tests/test_director.py (iter-47) + test_arc_driver.py |

| D-084/D-085/D-086/D-087/D-088/D-089/D-090/D-091/D-092/D-093/D-095/D-096/D-097/D-099/D-100/D-101/D-102/D-103/D-104 | 2026-09-05 | **Phase 4 (Knowledge & scene) opened + leg-1 trait crystallization (D-084, iter-55 — the owner's "start phase 4" call, the iter-36 opener precedent) + leg-2 the brief's derived-trait read (D-085, iter-56 — leg-1's first consumer) + leg-3 reflection & memory compaction (D-086, iter-57 — the LEGEND_SPEC compression half, written just-in-time at the row, the D-022 law) + leg-3b the tavern reflection set (D-087, iter-58 — the arming, the owner's session call: "the ideal solution" analysis first, the content-column precedent iter-43) + retr-1 the retrieval ladder (D-088, iter-59 — the owner's "continue per plans" call, the §2-design verdict standing from iter-58) + scene-1 the scene manager + mode B (D-089, iter-60 — the owner's "continue per plans" call, TASKS' top row)**: (1) the opener — ROADMAP §2's phase-4 row flipped to open, the backlog drafted from phases.md §4 (leg-1..leg-4, retr-1, scene-1, blind-1), the exit criterion reads T3's zero-leak law extended to the phase-4 surfaces; (2) the fold — `core/traits.py`: a knower holding `threshold` DISTINCT tokens of a pack-declared family (`rules.json::traits`, the closed key set, lint `core/pack.py::_traits`) crystallizes the belief, the contributing records' source event ids riding as PROVENANCE — the expansion law (the source is always queryable, the belief a derived view, never a replacement; INV-1 preserved); the echo's sibling read model — writes nothing, feeds no entropy (the L6 fence), no decay term, `at_tick` gates contribution, breadth not repetition; the lint refuses dead vocabulary (threshold >= 2, family >= threshold, unmintable tokens, one-sided cross-family membership, the belief-token collision); (3) the read (D-085) — `brief/assembler.py::_recalled_fact_lines` reads the fold as DATA at the assembly tick: the PC's crystallized beliefs LEAD the recalled_facts block as belief lines `- belief <token> (t <cross>, sources: <ids>)` (cross = the threshold crossing, the latest source event's tick; the ids the expansion law's demand handle), the family records render nothing raw (the derived view replaces them; a below-threshold family stays raw — no belief, no replacement), belief lines count against `max_items` (the top-k law); `core/traits.py::expand_trait` is the demand side — every family record the knower holds, acquisition order (4) the compaction (D-086) — `core/reflection.py`: reflection-on-recurrence — a knower whose held records of a pack-declared family (`rules.json::reflection`, closed key set, lint `core/pack.py::_reflection`) reach the block's threshold (REPEATS: recurrence, where traits count breadth — the echo makes repetition felt, the reflection makes it KNOWN) mints ONE reflection EVENT per (knower, insight) per run through the canon door (compaction emits higher-level entries that are themselves log entries; originals never dropped, INV-1/INV-5 — letta's `summarize_messages_in_place` the named anti-pattern), `outcome.provenance` the `list[event_id]` demand handle (the source always queryable, the reflection a derived view, never a replacement; `expand_reflection` the demand side), the never-re-reflect law (`view.holds` on the insight — records are never dropped, one mint is final), `stale_reflections` the read-side stale flag (empty on any whole log by INV-1; the derived-store scavenge screening retr-1 must consult), no RNG in the mint (an inference, not a stochastic act), no hooks/state changes (L6 — knowledge-side canon, the director untouched by construction), the knower gate kind npc, `docs/LEGEND_SPEC.md` the contract owner; (5) the arming (D-087) — the committed pack's LIVE block: the measured recurrence (day1_full seeds 123/128, the retried theft) mints the insight pair `sneak_at_work_here` over `figure_reaching_for_purse` (the watcher's conclusion — the targeted guard holds the sighting twice) and `trouble_by_the_bar` over `noise_by_the_bar` (the room's — the barkeep, the drunkard, the serving maid each hold the noise twice), threshold 2, one insight per axis of the theft's evidence; the event `conclusion_drawn` renders "{actor} had noticed it before, and named it: {knows}." and rides `importance.story_critical_events` (a named conclusion is a tale beat — the knowledge-flow precedent: knowledge_transfer/rumor_told story-critical; the director untouched — importance feeds the tale gate and the metrics, never entropy); (6) the ladder (D-088) — `core/retrieval.py`: the read-side retrieval service over (pack, events), an in-memory REBUILDABLE SQLite index (INV-1's derived-store shape — drop, replay, re-index; never a canon write), the corpus = static lore rows (pack declaration order) + every knowledge record (event order — the known_by fold), FTS5 BM25 the zero-dep rung (the unicode61 word view — dry underscored tokens are word-retrievable, token equality, no stemming; OR-joined quoted terms — recall first, the ranker sorts), the sqlite-vec extension PROBED (the pip loader then the plain name; absence = normal operation, D-012 — optional environmental capability, never a dependency) with the vec0 kNN table when vectors ship, the pure-Python cosine scan as the rung's SEMANTIC DEFINITION (rung equivalence: the same top-k, ties by rowid; a probe or DDL failure degrades to the scan, L12 — never a break), the keyword containment floor when the SQLite build lacks FTS5 (no bm25 signal — the honest degradation); the re-ranker score = alpha*recency + beta*authority + gamma*bm25 + delta*cosine over the union (FTS5 matches OR cosine top-k — never an empty result from a rung failure; with vectors the nearest always ride, the RAG shape; without them a no-match query returns the honest empty), coefficients pack data (`rules.json::retrieval`, closed key set, lint `_retrieval`: non-negative with at least one positive — an all-zero block is dead data; knn_k >= 1; vectors keyed by existing lore ids, one dimension, finite values only), per-kind signal sources (recency fact-side `1/(1+tick-at)`, lore timeless 0; authority = authored lore 1.0 / the fact's fidelity chain rank; bm25 `goodness/(1+goodness)`; cosine raw, top-k only — rung equivalence), ties by construction order (rowid); the `knower` query parameter IS the known_by boundary — `knower=None` retrieves lore only, facts reachable solely through a knower's own memory, and vectors live on lore rows only (the hard boundary structural, not conventional); the two LEGEND_SPEC contract points LIVE: the stale law (the build consults `stale_reflections` — a stale reflection's minted record excluded at insert) and the source-outranks law (a retrieved reflection orders below ALL its retrieved provenance sources — the deterministic formalization of the contradiction case: a ranker cannot judge semantic contradiction, it enforces the precedence that makes it harmless); DORMANT in the runtime (the mediator's keyword query is the consumer — BRIEF_SPEC §9's deferral; the committed block declarative-only, the leg-1 precedent: the block ships with the engine row, zero corpus price by construction); (7) the scene manager + mode B (D-089) — `brief/scene.py::speaking_queue`: the chorus is a queue, not a convention — the present, `brief.actors`-declared NPCs at the current scene's location, pack declaration order (INV-2), capped by `brief.chorus.max_actor_calls` (the per-beat actor-call budget; the beyond-cap NPCs the L12 template rung — the chronicle already renders their beats, never a blocked beat), the player never queued (mode A owns its call) and ambients never (the knower gate: a group holds records but does not speak); the knower parameter `assemble_brief(knower=...)` — mode A (None, the player) the committed corpus bytes byte-identical by construction, mode B the SAME pipeline with three parameterized halves (scene_delta reads the knower's own perception — the blind-NPC law parameterized; recalled_facts its own records and beliefs — the per-knower traits fold, the leak surface closed by construction: a held-by-another token can never render; directives + voice_exemplars the `brief.actors` entry — the actor's role and voice, never the narrator's) and four SHARED blocks (scene_texture — one ledger per scene, the chorus reads the same texture block; present_entities — observables, L6; scheduled_lore; active_options — the door's grammar, not the knower's); the knower gate: an id that is neither the player nor a declared pack NPC is a loud ValueError; `narrator_call(knower=...)` the actor call document — the protocol's `actor: <id>` first line (mode A's bytes unchanged, the committed corpus shape); (8) the session wiring (D-090, iter-61 — scene-2, TASKS' top row, the owner's "continue per plans" call) — `cli/mediator.py` owns the drain: a beat is the player's exchange, then the chorus — the player's ACCEPT snapshots `speaking_queue` over the post-action log (the beat's own cast, FIXED AT CURTAIN: mid-beat arrivals join the NEXT beat's chorus, the queue is never re-taken mid-beat) and drains it head-first, one actor call per queued NPC (`narrator_call(knower, query, retrieval)`); each actor exchange is the SAME cycle (shape gate → proposal verdicts → texture gateway → the caller's intents through the door → promotions) with its OWN regen budget (one budget per narrator exchange — mode A's per-beat law preserved exactly: a beat = one exchange there, identical behavior) and its own caller gate; the drain's guards, all L12: the live presence re-verification (`brief/scene.py::present_at_scene` — each emission re-reads presence from the projection, an NPC who left mid-drain is skipped, the template rung; a call never goes to an NPC standing elsewhere), `narrate dry` on an actor call skips and advances (on the player's call it closes the whole beat — declining the head declines the beat), a bare `narrate` DROPS a pending drain (the operator moved on — the actors fall to the template rung, never a blocked beat), an exhausted actor budget drops that actor with the refusal notes on the result; the actor reply door: the step grammar's `actor` key (INTENT_SCHEMA §9 — a step may name the proposer NPC; anything but a pack NPC id is a loud author error; the band NPC_REACTION, the same band as the autonomous intents) and KI#17's generalization — the runner feeds the next step when the CURRENT step's own intent ends (reject or complete, identified by intent id, not by actor; autonomous intents carry other ids and never advance the script; the old actor==player proxy was the phase-0 special case of the exact law, behavior-preserving for every committed flow); `feedable_intents`' caller law (the mode-B half of the mode-A actor gate: a reply proposes its OWN CALLER's actions — mode A's caller is the player, mode B's the actor whose call the reply answers; the mediator's `_step` stamps the actor key, the door commits the NPC's events); the keyword query (BRIEF_SPEC §3.5's relevance signal, the mediator's own): `brief/scene.py::recall_query` — the knows tokens the beat window minted for the knower, first-seen, leak-free by construction (T3's twin: the tokens ARE the knower's fresh records), the empty window the honest empty query; `assemble_brief(query=...)` the assembler's third signal — `relevance_weight * |query_words ∩ knows_words| / |query_words|` over the word view (`core/retrieval.word_tokens`, the ladder's floor semantics made public — the single owner of the word view, rung-independent by construction: the brief's bytes never hinge on a SQLite build's FTS5 presence), `relevance_weight` pack data (linted with the other two), mode A never queries — the term inert, the committed corpus bytes byte-identical; the retrieval ladder's first runtime QUERY consumer (`cli/mediator.py::_emit_actor`: one index build + query per actor call — the mode-B path pays it, mode A never does) surfacing as the actor call's `query:`/`retrieval:` protocol lines (BRIEF_SPEC §7.1 — the relevance signal made visible to the operator + the ladder's top rows as dry demand handles: the fidelity and the minting event id inline, the ORDER carrying the source-outranks precedence; capped at RETRIEVAL_LINES=3, document geometry in code like the eviction order); the notes' subject-scoping: the withdrawal notes ride the NEXT PLAYER call — the actor calls never consume them, an actor's own regen refusals ride its own re-emit (the committed corpus pins the player-side law: `withdrawals_are_notes_never_events`' needle lands on the player's next call with the chorus in between); (9) the identity tier + per-scope quotas (D-091, iter-62 — tex-1, TASKS' top row, the owner's “continue per plans” call) — `brief/assembler.py::_scene_texture_items`, both laws READ-PATH (the ledger, the lifecycle, D-049 untouched): the ranking key widened to the identity-or-pinned tier — an entry whose slot is pack-declared `identity_slots` ranks WITH pinned (key: identity-or-pinned → pinned → newest → construction; the slot a CLASS, never a scope condition — a scene-scoped entry in a declared identity slot ranks in the tier too; pinned above identity WITHIN the tier — pinning the explicit mark, identity the class law; an EMPTY set reduces the key to the pinned-only law exactly, the D-048 bytes) and the quota walk AFTER the ranking — at most `per_entity_max_items` lines per entity scope (the walk follows the ranking, so an entity's identity slots fill its quota first: “identity slot first” WITHOUT a second mechanism, and identity itself is bounded — it cannot become the flooding channel; scene scopes carry no quota: the room's texture is the block's core content, `max_items` bounds it; tombstones carry none: their own cap bounds them and a refuted identity line is gone; beyond-quota lines render nothing, never drop, never mark — the D-047 law; a value >= `max_items` the documented inert state); the pack contract: both keys join the REQUIRED scene_texture set (lint closed vocabulary — `identity_slots` unique non-empty strings, empty = no tier beyond pinned; `per_entity_max_items` an integer >= 1), the committed pack armed DECLARATIVE-ONLY (speech_pattern/look/mannerism, K=2 — the chorus cap's cost posture), the corpus price zero by construction: the committed corpora carry scene-scoped texture only (candles/hearth/exits/shadows/kindling — no tier member, no entity scope past any K) (10) the leak suite's phase-4 extension (D-092, iter-63 — blind-1, the STATUS Next-step fork resolved by law: leg-4 names DuckDB — the §8 dependency fence keeps it the owner's explicit call — while blind-1 is the exit criterion's own instrument, dependency-free, the dir-2 precedent) — tests/test_blind.py, the instrument FIRST: pure TEST-SIDE folds, never the engine's own code paths (a checker that shares the checked code cannot catch its bugs) — every scene_delta line backed by a perceived event count-for-count (the multiset law, the line's own (t, type, actor, target) bytes, never a coarse (t, type) key), every recalled_facts line mapped to the knower's own fold (the raw line's exact record quadruple; the belief line's exact (token, sources, cross) triple — the provenance IS the leak signal, not just the token), every retrieval fact row mapped to the named knower's record; four layers, all measured zero-leak: the offline sweep (the 10-seed day1 family + the committed golden fixture, EVERY log prefix — any prefix is a legal assembly state, the log append-only — every declared actor + the player; the test_scene core generalized from selected cuts to the exhaustive sweep), the retrieval sweep (the known_by boundary under MAXIMAL adversarial input: the real fresh-window query AND the omniscient query — every token on the whole log — for every knower the fold holds, plus the knower=None probe and the assembler twin: the query is a ranking signal, never a content source), the live session wiring (a multi-beat drain over the cap-raised pack — 11 emitted call documents, each verified against its OWN anchor-addressed prefix: anchor = the emission-time event count, append-only makes events[:anchor] of the final log the emission-time log — the document carries its own log address; composition byte-exact + the leak law independently), and the teeth law (planted leaks flagged — crafted records + the pinned seed-125 cross-knower divergence; a checker that cannot fail is decoration) **+ the v0.2 refinement lance (D-095/D-096/D-097/D-099/D-100/D-101/D-102/D-103/D-104, iter-66..72, folded here at the phase-5→6 gate — the owner's post-gate quality pass over the research review's confirmed holes; every landing additive, every corpus price paid in-landing): the invented-entity prose floor (D-095), intake-2 the depth-architecture routing (D-096), the beliefwire trait-gate family + its arming (D-097/D-103), the rumordrift drift families + the arming (D-099/D-100), the per-target suspicion axis + its arming (D-101/D-102), the prose detection surface (D-104). The per-row detail: TASKS' v0.2 ledger + git.** | the alternatives: a belief EVENT refused — it would double-count the knowledge channel and break INV-1's single-writer shape; counting records instead of distinct tokens refused — repetition is the echo's renewal, the breadth reading keeps small families honest; ranking belief lines by the three-signal score refused — a belief has no single recency (the fold carries event ids, not a record tick), declaration order is the deterministic answer, and beliefs lead the block by construction; the provenance ids ride the line INLINE — without them nobody could ever demand the expansion the reflection-mint alternatives: a knowledge-record-only shape refused — `provenance: list[event_id]` does not fit the record's single `source`, and patching the §3 contract is a schema bump for what the outcome object already carries (EVENT_SCHEMA §11, the DF `victim_hfid` shape); an armed v0.1 block refused at THIS row — a live block appends events, the corpus price rides the arming row (leg-3b) instead of the engine row (the arc-1/iter-47 precedent); the arming's own refused: ONE combined insight over both families — a mixed-evidence conclusion muddles the axis split (the figure is the watcher's business, the noise the room's); a non-story-critical landing — a named conclusion is the tale's highest-level beat, cutting it would hide the phase's product behind the gate; arming guard_02's told-dupe — not the pack's call: the never-re-reflect gate over the BRIEFED insight blocks it by law, the honest hearsay knower; a recurrence threshold of 1 refused at lint — a first occurrence is an event, not a pattern; the retr-1 refusals: AND-joined match terms — recall-first wins (an AND reading of a two-keyword query returns nothing on this corpus; bm25 sums what each row matched, the ranker owns precision); FACTS in the vec rung — the hard boundary refuses it twice (vectors map lore ids only, and facts enter solely through the knower's fold); a per-entry lore authority weight — no consumer reads it yet (L13, scope discipline: authored lore is uniformly 1.0, the pack may differentiate when the query side exists); an index persistence/caching layer — rebuild-per-build is the INV-1-purest shape at v0.1 scale (the persistence arrives with the scale that needs it); an LLM-judged contradiction detector for the source-outranks law — the deterministic precedence formalization instead (the ranker enforces the safe ordering, semantics stay the narrator's); dedup by `knows` token inside the ladder — the consumer's law (the brief dedups; the ladder serves evidence, duplicate rows are separate addresses); the scene-1 refusals: a per-knower texture window refused — one ledger per scene, the chorus reads the SAME texture block (D-049's own law; parameterizing it would fork render state); parameterized present_entities refused — the cards are observables (L6), the room's structural answer is the same for every present party, the leak surface lives in knowledge and perception, never in the cards; a present-but-undeclared NPC in the queue refused — the pack's declaration is the eligibility gate (the DORMANT family: silence is pack data, not an error); an `actor:` line in every call document refused — mode A's bytes are the committed corpus shape, the asymmetry documented (the player is the narrator's subject by construction); the session wiring + the actor reply door + the mediator's keyword query refused at THIS row — scene-2's (the underdeliver law: one task, one iteration); the scene-2 refusals: a LIVE re-taken queue refused — the cast fixed at curtain (a re-taken queue could grow mid-drain and never terminate; the snapshot + the live presence re-verification gives the presence honesty WITHOUT the liveness hazard); a per-beat shared regen budget refused — one budget per NARRATOR EXCHANGE (the player's exhaustion dying with the beat, an actor's dropping only that actor; a shared budget would let one chatty exhaustion starve the whole chorus); the ladder's bm25 as the brief's relevance term refused — the overlap over the shared word view instead (FTS5-present vs floor would rank differently, the brief's §7 exact-bytes contract never hinges on a SQLite build; the ladder still gets queried — its rows ride the protocol, the operator the consumer); the retrieval rows as a new BRIEF BLOCK refused — the protocol section is the document's extension point (a ninth block would fork the §3 pipeline and the §6 lint for a per-call annex); the query derived from the narrator's PROSE refused — prose is the emitter's output, the brief a pure fold's (D-049's quarantine: the query derives from the log alone, leak-free by construction); an `emit_call` that materializes the pending drain refused — the corpus's beat loop demands emit_call = the player's next beat (the drop law instead: the operator's bare narrate moves on, the chorus falls to the template rung — the auto-emit on accept is the ONLY mechanism that surfaces the actor calls, the operator never requests them explicitly); the tex-1 refusals: a SCOPE CONDITION on the tier refused — the slot is a class (one rule instead of N exceptions, L14; a scene-scoped identity slot ranking in the tier is the pack author's own declared vocabulary, harmless by the window law's own bounds); identity ranked ABOVE pinned within the tier refused — pinning is the explicit mark (a delta ref or an intent's own reference), identity the class law, and the blueprint's own key order reads identity-or-pinned → pinned; the tier WITHOUT the quota refused — identity would survive, but one chatty entity could still eat the whole window (the resolution's own two halves: the tier fixes eviction, the quota fixes flooding, neither substitutes the other); the quota WITHOUT the tier refused — flooding bounded but identity still loses to fresh texture on recency, the trader problem unsolved; a quota on the SCENE scope refused — the room's texture is the block's core content and one scene scope cannot multiply (the flooding hazard is entity scopes multiplying against the ONE global cap); a quota on TOMBSTONES refused — their own cap bounds them, a refuted identity line is gone (prevention, not persistence); a budget-drop `[truncated]` marker for beyond-quota lines refused — the D-047 ranking-cap law (the marker counts budget drops only, and the caps are ranking caps); counting quota-skipped entries toward max_items refused — a skipped line renders nothing, it cannot consume the global cap; OPTIONAL keys with defaults refused — the closed REQUIRED set keeps the reader branch-free (`config[key]` direct, the scene-2 relevance_weight precedent, and the off states are declared values: the empty list, the inert integer) | measured live: the canonical seed-125 run — the guard pair crystallizes `paranoid_about_thieves` (the eyewitness 2 source events, the hearsay knower 1), the room's other witnesses hold two tokens and honestly stay uncrystallized; the PC holds no family token on ANY committed corpus — leg-2's lens changes ZERO brief bytes: the 10-seed day1_full A/B (seeds 120..129) 10/10 byte-identical (brief + log), zero corpus regen, the golden fixture's no-belief line pin; the testimony-crystallization fidelity floor stays an owner-gated recorded question (v0.1 counts any held token however learned) leg-3 measured: the recurrence EXISTS on the committed corpus — day1_full seeds 123/128 (the PC retries the theft; the targeted guard holds the sighting twice, the room the noise twice); leg-3b measured: the corpus price ZERO — the narrator corpus (105 cases) and the parse corpus (10 cases) replay through the real mediator/parser cycles with ZERO reflection mints, the T1 golden byte-identical (plumbing mints no knowledge), 8/10 day1 seeds byte-identical, exactly 123/128 mint (4 events each: the guard + the room trio at the second pickpocket, cause-chained, provenance [ev_0002, ev_0015]); the told-conclusion law LIVE — the watch-change briefing tells the relief guard the minted insight (told/partial, one step down) and view.holds blocks his own re-derivation (he holds the sighting twice from the one transfer, mints nothing): the craft-pack live-fire mints exactly one event (provenance [ev_0002, ev_0015], cause-chained, the cascade terminating), the stale fold empty on whole logs and firing after scavenge; the 10-seed day1_full A/B (120..129) 10/10 byte-identical through the landing — zero corpus regen (DORMANT, the pack's own declaration the gate); retr-1 measured: the corpus price ZERO — the 10-seed day1_full A/B (committed pack vs a block-less copy, seeds 120..129) 10/10 byte-identical (the block is declarative-only, nothing in the runtime queries it); the live-fire over the armed pack's own seed-123 log — the guard's reflection `sneak_at_work_here` (inferred/exact, score 1.77) retrieved beside its two provenance sightings (saw/partial, score 1.15) by the query "purse sneak": the score alone would put the conclusion FIRST, the law orders it LAST — the law over the score, the live pin; the stale scavenged arm (the event universe minus ev_0002) serves no sneak record and keeps the survivor; the probe reality pinned (vec_loaded False on the reference environment, the scan answering; a lying probe degrades to the scan, never breaks); the keyword floor and the OR recall pinned; determinism double-build identical, PYTHONHASHSEED-independent; scene-1 measured: the queue live on day1 seed 123 — (guard_01, barkeep) mid-tavern, (guard_02, barkeep) after the watch change (PRESENCE swaps the guard, never a convention about who talked), empty at the backyard (no declared actor remains); the leak surface ZERO — every declared actor's brief leak-free at every beat window (6 windows over 5 actors: every scene_delta line maps to a perceived event, every recalled_facts line to a held record or belief), the guard-only sighting token absent from the maid's brief; the mode-B belief line live on seed 125 (the guard's `paranoid_about_thieves` leads HIS brief with his own provenance, the relief's hearsay mint his own, the maid's no-belief fold honest); the corpus price zero — mode A over the committed pack (now carrying chorus+actors) byte-identical to the block-less copy, 1055→1085 green with ZERO fixture regen; scene-2 measured: the drain live on a seed-7 session — the player's accept hands the guard's call, the guard's accept the barkeep's, the barkeep's closes the beat (the CLI prints the chorus handoffs; the actor call carries actor/anchor/regen/query/retrieval — the guard's query `pc_01_arrived`, the ladder's top row the same token's demand handle); the caller law end-to-end — the guard's reply feeds the guard's look through the door, the committed event's actor IS the NPC; the mid-drain departure skipped (the PC's move to the backyard between the guard's call and its dry: the barkeep not present, the beat closes — the corpus's own `between` pattern staging the fold); the actor's refusal ladder (regen 1/2 → the dry with the notes on the result, the drain advancing — a FRESH budget per exchange); the query derivation pinned on day1 seed 123 (the guard's five fresh tokens; the late-day window honestly empty); the relevance rerank live (the synthetic old-record flip: `ale_spilled` above `song_by_the_hearth` under the fresh `ale_order_shouted` query — recency alone ranks them the other way); the ladder's first runtime query pinned (the day1 window's top rows — the source-outranks order visible in the protocol lines); the actor-call determinism double-run identical; the corpus price ZERO — the 10-seed day1_full A/B (committed pack vs the chorus/actors/retrieval/relevance-less copy) 10/10 byte-identical, the narrator corpus 105 + the parse corpus 10 + the T1 golden replay green unchanged (mode A never queries, never builds the index); tex-1 measured: the tier live — the OLDER identity slot above every newer plain entry, and identity SURVIVES max_items=1 pressure (the trader problem closed: a crowded scene never silently evicts exactly what long sessions must keep); pinned above identity within the tier; the quota live — the chatty guard (three plain entries + the identity one) capped at exactly K=2 with the identity first, the barkeep's and the scene's lines untouched (the quota frees the window for the room); three identity slots render exactly two (identity bounded); the tombstone half unquota'd (K=1, both refuted lines render); the empty-set reduction pinned (the pre-tex-1 pinned-only bytes, the D-048 law preserved); the corpus price ZERO — the 10-seed day1_full A/B (committed pack vs the empty-tier/inert-quota copy, seeds 120..129) 10/10 byte-identical, the golden-prefix brief byte-identical over the texture fixture, the narrator corpus 105 + the parse corpus 10 + the T1 golden replay green unchanged over the ARMED pack | contracts: phases.md §4 (P3f), BRIEF_SPEC §3.5 (the derived-trait clause, the same-commit law §8) + TASKS leg-1/leg-2 + STATUS (the read-side-folds FAQ); tests: tests/test_traits.py (18+4 — the read-model laws, the mechanics, the expansion family, the live canonical pins, the lint family) + tests/test_brief.py (+10 — the replacement law, the line shape, the cross tick, the hearsay dedup, the cap law, the declaration order, the no-block gate, the zero-regen witness), 991→1004→1026→1030 green, ruff clean; + LEGEND_SPEC.md (the leg-3 row's just-in-time write, SPECS_BACKLOG flipped; §7 the arming record at iter-58) + TASKS leg-3/leg-3b (done, iter-58); tests: + tests/test_reflection.py (22 + 4 committed-arming pins at iter-58 — the mint laws, the provenance law, the never-re-reflect gate, the within-event dedup, the knower gate, determinism, L6, the stale family, the expansion family, the lint family + the four conclusions, the told-conclusion block, the tale line, the 8/10 byte-identity witness); + the retrieval contracts: phases.md §4 (the retrieval paragraph — the ladder's architecture owner) + LEGEND_SPEC §4/§5 (the two contract points, marked live) + BRIEF_SPEC §9 (the deferred row flipped) + TASKS retr-1 done + tests: + tests/test_retrieval.py (25 — the DORMANT gate + the zero-price A/B, the rung family: FTS5 lore matching + the OR recall + the knower boundary + the honest empty + the keyword floor + the cosine scan RAG shape + the probe degradation + the vector validation, the re-ranker signals + the pack-coefficient flip + the tie-break, the stale whole-log/scavenged pair, the source-precedence live-fire + the alone-ranked-by-score arm, the lint family, the close guard), 1030→1055 green, ruff clean; KI#71 (the sqlite-vec license drift) opened+closed; + the scene-1 contracts: BRIEF_SPEC §3.9 + §6 (the chorus + actors pack contract, the same-commit law §8) + §7.1 (the actor line) + §9 (two rows flipped) + TASKS scene-1 done + the scene-2 row (the wiring, the actor reply door, the keyword query) + tests: + tests/test_scene.py (30 — the queue law family: presence/pack/kind gates, the cap, the pure fold; the mode-B laws: the byte-identical default, the actor's own role/voice, the shared blocks, the knower gate, determinism, the corpus-price A/B; the leak suite core over every declared actor at every beat window; the actor call document pair; the lint family), 1055→1085 green, ruff clean; + the scene-2 contracts: BRIEF_SPEC §3.5 (the three-signal shape, the same-commit law §8) + §3.9 (the drain wiring) + §6 (relevance_weight) + §7.1 (the query/retrieval lines + the notes' subject-scoping) + §9 (three rows flipped, the identity tier re-pointed tex-1) + INTENT_SCHEMA §9 (the actor step key) + TASKS scene-2 done + the tex-1 row + tests: + tests/test_scene.py (+33 — the step family: the actor door, the loud shape gate, the KI#17 chain; the drain family: the handoffs, the player dry, the actor dry, the drop law + the notes law, the mid-drain departure, the actor's refusal ladder, the caller end-to-end; the query family: the derivation, the honest empty, the presence fold, the relevance rerank, the weight's mode-A inertness; the ladder family: the document lines, the live query, the block-less pack; the 10-seed corpus-price A/B; the actor-call determinism) + tests/test_mediator.py (+1 the caller gate unit; the beat-close law adapted: the beat stays open through the drain, the two flipped withdrawals' note text), 1085→1118 green, ruff clean; + the tex-1 contracts: BRIEF_SPEC §3.3 (the tier + quota laws, the same-commit law §8) + §6 (the two keys + the lint clauses) + §9 (the deferred row flipped; the promotion-door half stays st-2, owner-gated) + TASKS tex-1 done + st-2 re-pointed (the read-path half landed, only the door remains) + tests: + tests/test_brief.py (+23 — the tier family: the trader problem, the slot-class law, pinned-within-the-tier, the max_items pressure survival; the quota family: the chatty cap, identity bounded, the inert state, the unquota'd tombstones, the empty-set reduction; the lint family ×4; the 10-seed corpus-price A/B + the golden-prefix brief witness), 1118→1141 green, ruff clean; KI#71 deleted per §5 at iter-62; + the blind-1 contracts: TEST_PLAN §1.3 (T3's phase-4 extension — the instrument's law, the T3 row's pointer) + TASKS blind-1 done + tests: tests/test_blind.py (26 — the leak predicates + the four layers: the 10-seed + golden every-prefix sweep, the omniscient-query retrieval sweep + the None probe, the anchor-addressed live drain, the teeth pair), 1141→1167 green, ruff clean; + the leg-4 landing (D-093, iter-64, 2026-09-06 — the owner's explicit duckdb approval crossing the §8 dependency fence, the leg-4/blind-1 fork's recorded owner-gate resolved): `scripts/chronicle.py` the mode-F offline chronicler (the read_ndjson_auto no-ETL intake behind a count gate — a silent drop is an integrity failure, never a partial archive; events.parquet the canonical 12-column cold archive; state_diffs.parquet the LAG/LEAD window-diff law with the continuous fold-consistency read; chronicle.sqlite the stdlib-readable summary through the attach→stdlib probe+fallback ladder with autoinstall off — the chronicler never phones home; manifest.json the content-derived audit record — no wall-clock, no abs paths, byte-deterministic per environment) + pyproject the [chronicler] optional extra (duckdb>=1.0; runtime deps stay []) + D-012 now executable (tests/test_architecture.py::test_runtime_imports_stdlib_only — the runtime graph imports stdlib + local + ImportError-guarded probes only) + tests/test_chronicle.py (10 — the stdlib-fold cross-checks, determinism, the ladder equivalence, both gates; importorskip: 1168 passed + 1 module skip in the pure-dev env) + TEST_PLAN §7 + TASKS leg-4 done + scav-1 (scavenge with tombstones) deferred to the backlog per the scope law; the corpus price zero by construction (no runtime file touched), 1167→1178 green, ruff clean |
| D-098/D-109/D-110/D-111/D-112/D-113/D-118/D-129 | 2026-09-07 | **The protocol-law & testproto family (iter-68..94, condensed at the phase-5→6 gate; per-row detail in git + TEST_PLAN §8 + the owning specs): D-098 the intermediate-build LLM-integration protocol — the three-layer synthesis (the contract simulacrum / the live probes / the gate heartbeat) over one pinned corpus + one metric vocabulary, the decomposition not an election; D-109 bg-8 its live half — the deviation corpus F1–F6 through the REAL mode-C door, the world-answer law's first live numbers (coverage 34/34, the ≤2-regen ladder); D-110/D-111 the predicate contract restored — missing answers False under EVERY comparator, and a missing key is a ValueError naming the field (the loud-backstop twin); D-112 the iter-11b stress-test resolutions RATIFIED (the D-056-amended arrival snapshot, the containers law); D-113 the doc-law verdicts (Rule 9's in-repo home AGENTS §7; ORDER → STATUS Next, composition → TASKS; verify-seed + archive-protocol chat-side); D-118 the shadow-replay introspection law (mech-1: agent-facing CLIs replay through the PUBLIC pipeline only, never runtime instrumentation); D-129 the effect-line habit — protocol-law D-rows state their expected observable effect, checked within 1–2 iterations, reported at each gate's §5 staleness pass.** | the protocol laws grew monotonically between gates — the family holds the cross-phase guardrails | the pruning pair is D-128's gate staleness pass (ROADMAP §5 step 6) + D-129's effect lines; owners: TEST_PLAN §8, AGENTS §7, core/predicates.py, scripts/mechanics.py |
| D-105/D-106/D-107/D-108/D-114/D-115/D-116/D-117/D-120/D-121/D-122/D-123/D-124/D-125/D-126/D-127/D-131/D-132/D-133 | 2026-09-08 | **The phase-5 (Depth) build column landed (iter-73..98, condensed at the phase-5→6 gate; per-row detail in git + TASKS' phase-5 ledger + phases.md §5 — the architecture owner): the acquisition gate + arming (D-105/D-106), the lazy-detail family + arming (D-107/D-108), the fold checkpoints (D-114), the ordered worldgen passes + the committed arming (D-115/D-117; D-116 the generator-concept verdict set — the 13 questions resolved, the W0..W4 wave order, the spatial model pinned as phase law), the four W1 bridges (D-120 the scene-line projection pipe, D-121 the DF-legends history bridge + the cause tree, D-122 the placement discipline, D-123 the geometry rework — the grid-hash walk, 10k sites in 1.19 s), the macro clock + its consumers (D-124 the cadence law + the aggregate emission surface, D-125 the scene LOD + the one-gate law, D-126 factions with goals — the KeeperRL small formula, D-127 the write-side group LOD — member_of + the population tier + condensation), the W2 tail (D-131 name-1 the lazy name generator + the condensation consumer, D-132 st-6a travel with the edge price law, D-133 weather-1 the ambient chain + canon erosion + the committed macro arming — the primitive's first consumer). Every landing measured its corpus price both arms before paying it; the unarmed/68a pattern + the one-gate law kept the committed pack's price surface designed, never accidental.** | phase 5's declared build column, ROADMAP §2; the wave order D-116 | the exit verdict: the gate-verdict row (D-136); the phase law: phases.md §5 |
| D-119/D-128/D-130/D-134/D-135/D-147/D-148/D-150 | 2026-09-10 | **The research-intake verdict sets (intakes 6..13, iter-85..115, doc-only — every external text verified against HEAD before routing, the convenience-copy law; condensed at the phase-5→6 gate): D-119 intake-6 the residue catalog (Dune's critical-resource binding → res-1's declaration shape; WH40k ideology-as-constraint; Outer Wilds refuted — knowledge never gates, L6); D-128 intake-7 the Anthropic-2026 adoption (ROADMAP §5 step 6 — the gate staleness pass; MVP_SCOPE §18 actionable error text; mech-2 the attention budget) + D-129 the effect-line habit; D-130 intake-8 the three-tier setting posture (the repo's public 2nd setting is ORIGINAL — the province sketch phases.md §6 riding world-2's level-2 half; the level-1 reskin instrument is the open generic stack; the minus ledger; REFERENCES §10 +6); D-134 intake-9 the cost/failure/corruption pack laws + the parked loop-pack/low-magic/soul-scar/belief-as-rules patterns (phases.md §6); D-135 intake-10 the encounter & event-generation block — the substrate CONFIRMED complete (21 of 23 patterns pack-level or read-side over landed primitives; since-1 the one genuine gap, owner-gated; the four declared contradictions dissolved against standing law); D-147 intake-11 the pressure-city donor blueprint ADOPTED as parked material (the DISPLACEMENT law the one word-for-word keeper — stabilization is displacement: reducing pressure at one node raises it at another, part of the cost never repaid in space, deferred into time; the Cooling Debt resolved to weather-1's chain+erosion shape, never a second res-1; the gauge = res-1's read surface; legal exclusion = the D-134 application; objective-function factions = depth-6's shape; lore hooks ride templates + cause_hook; biomes are authored pack data, never a worldgen biome — the lost-city fold world-2 L2's cheap adoption, the full concept pack-4's third slot; refused at the door: pre-written endings, the protagonist arc, the thermal physics, the corruption meter; two micro-drifts corrected: the AP crosswalk rung specified-not-landed (rides pack-ci), no literal meta.json — tone/prohibition data rides the four files' blocks); D-148 intake-12 the presentation-contract consolidation — the eight-row verdict set (the "60–70% ready" frame REFUSED — a contract without a consumer has unknown requirements, the {3–8B, GBNF} run the metric's only honest form, the gap sequenced-by-design per D-022; the narrator_protocol noise re-owned — the block is the operator's reply-contract signal at dev-time, the anchor is what the reply carries, the measured leakage zero at 27B; the concern fires at engine-1 where GBNF owns the output side and the input split is a one-block serializer decision; [truncated:N] STAYS — the anti-silent-drop invariant, the golden-set A/B rides the weak arm; condensation = the fold pattern — the belief lines ARE in-brief condensation, never tracery, never the assembler; LiM REFUSED as a requirement driver — the local instrument is the heartbeat trend line on the engine-1 arm; the presentation spec written at the engine-1 trigger FROM weak-arm results, ABSORBING st-4, a thin mapping table over the 8 blocks — the D-055 file-contract pattern's fourth instance, never a 7-layer re-labeling vocabulary; the behavioral layer ANSWERED by PACK_SPEC §6 — AP-9 spine → AP-8 rule through the intent door's gate family → the committed event → the existing surfaces; the priority dichotomy DISSOLVED — the weak-arm run IS the requirements measurement; the spec's row: TASKS presentation-1, engine-1's decision-input child); D-150 intake-13 the open-ended actions & honest-simulation review — the doctrine half CONFIRMED as standing law (VISION §1/§5, INV-1..5, EPIST-1, near-zero drift; one stale pin corrected: phase 6's litmus test MET BY MEASUREMENT since D-149), the proposal half routed, zero build-grade items (the compositional causal substrate → D-096 + parse-2 — the loop already executes intent chains causally, zero simulator change owed — + engine-1; a generic primitives layer REFUSED, L13; PARTIAL deferred to the first action with separable sub-effects — the failure_total branch precedent; UNDER_MODELLED/NO_MODELED_EFFECT REFUSED as a standing runtime status — the PACK-CI CLOSURE LAW the preferred cure: reaction tables close at authoring time, an unresolvable pair is an author error, never a runtime "I don't know"; inventions-as-emergence-from-repetition parked on res-1 + world-2 L2). The external texts stay outside the repo.** | the owner's chat research calls, routed between queue rows (the D-022 exception, the ORDER owner's prerogative) | the pattern catalog: REFERENCES §10; the pack laws: phases.md §6; the queue itself never touched by an intake |
| D-137/D-139/D-140/D-141 | 2026-09-12 | **The iter-104..107 research-verdict & risk-synthesis landings (the owner's direct calls on the iter-105 chat's priority list, condensed at the phase-6 gate): D-137 the Alien unknown-axis L6 conflict resolved — the OBSERVABLE-answers law (the owner's verdict on iter-104's combined proposal, delivered at the iter-105 call): the `unknown` axis, whenever it lands, paces against the world's OBSERVABLE answers to ignorance — logged refusals, failed checks, no-knowledge events (observable state, entropy-eligible) — NEVER the private actual-vs-perceived gap; the L6/EPIST-1 observable-only fence stands untouched (the iter-46 echo precedent held: no knowledge-derived score ever feeds entropy); D-139 the resume door — the INVISIBLE-TO-THE-LOG law (iter-106): `--resume <log>` opens the session OVER an existing log; a run interrupted at a clean drain boundary and resumed with the same remaining steps is BYTE-IDENTICAL to the uninterrupted run — interruption is not an input, only steps are (T1 across process boundaries); the RUN CURSOR (`core/cursor.py`) is derived-of-the-RUN, never canon, never log-derivable (the entropy state's silent draws own that — urgency misses, weather self-rolls, faction failures consume stream positions without events; bank positions via RngBank export/restore, worldgen excluded — generate_world re-derives it; the director buffer rebuilt exactly by `seed()` over the log; bound to its log by event_count + the prefix digest, a stale pin refuses loud — guessing entropy would be save-scumming, not determinism) + the fold-checkpoint fast-path + the append-mode writer (the same canon door, INV-1/INV-5 untouched; the cursor pinned only at clean drains — a mid-drain state is not resume-able; the scene ledger stays session-scoped: live texture dies with its session, promoted texture rode events); D-140 the CAUSE-HOOK law (iter-107): every director-released event — accepted AND rejected, a released attempt is a fact — carries `provenance.cause_hook`, the discharged hook's tag (additive; the schema's 0.1→0.2 minor bump, the T1 golden fixture regenerated in the same iteration — the header's version line the whole corpus price); `core.metrics.payoff_latencies` folds the seeded→released tick distribution PURELY from the log, FIFO per tag — exact because within a tag the pick order IS seeding order (the oldest-first law); corrupt pairings (unseeded, over-released, release-before-seed) refuse loudly, never a guessed pairing; the tag is the story-level fact "which deferred consequence discharged here" — `scripts/mechanics.py`'s trace family and the SoW read side read the same key; D-141 the iter-107 rider family (the owner's «доделывай остальное», the list's items 2–6): (a) the BLAST-RADIUS DRIFT CONTRACT — the systems table and the metrics prefix map are two independent owners of the system⇔state-vocabulary wiring, the drift test pins them in four directions (phantom systems / claimed prefixes with the pinned aliases pair+crime_status / declared-write coverage / the observed corpus vocabulary with the item-side registry carrier+condition — M1's silent undercounting becomes a named contract a new mechanic MUST grow with); (b) the authoring loop's first rungs — `scripts/pack_scaffold.py` (a lint-clean tavern copy, identity renamed) + `scripts/pack_doctor.py` (the lint's fix-hint surface: diagnosis + trace + the health inventory); (c) the ablation arm is BLOCK-SCOPED — `--systems-minus` removes a mechanic's ARMING block, never a systems-table row (the rows interlocked by preconditions, resolvers and cross-lints); the removable set MEASURED: urgencies, weather, on_action, reflection, secrets, factions; (d) `beat_tension_profile` — per beat window the sum of ALL events' importance weights (low 1 / medium 2 / high 3), the same window axis the stretches walk, the trailing partial window dropped: the rhythm stat, a pure function of the log; (e) the re-ask menu ranks, never widens — every off-grammar refusal family appends up to 3 nearest tokens (Levenshtein, case-folded, ties by declaration order; kinds ranked over token + label), the runtime re-ask ladder stays PARSER_SPEC §7's deferral; (f) the heartbeat ledger's per-component p50/p95 columns (tick/fold/brief/parse/generate) are first-class (TEST_PLAN §8.5) — the component cut owns the latency budget, numbers in TECH_NOTES, gap row until bg-9+/engine-1** | DIRECTOR_SPEC §11 recorded the conflict as the owner's to resolve — the phases.md §3 sketch said the director MAY pace against the gap, the entropy law forbids it | the axis stays recorded-not-built (the first-consumer law, a real Alien-axis pack consumer); DIRECTOR_SPEC §11 re-pointed to this row; any future pacing input derives from the log alone; every rider's cheapest honest form chosen over the speculative one; contracts: EVENT_SCHEMA §7, PARSER_SPEC §4/§7, TEST_PLAN §6/§8.5; tests: test_resume.py, test_drift.py, test_pack_tools.py, test_metrics.py |
| D-142/D-143/D-144/D-145 | 2026-09-13 | **The iter-108 fixation family (the owner's «то, что принято и готово к исполнению, зафиксируй где нужно в документации» call on the research session's accepted list, the light half built in the same call; D-142/D-143/D-144 folded in at the phase-6 gate): (a) D-142 the CORE-ENDS/PACK-BEGINS admission law — a change lands in `core/` iff it alters WHAT mechanics exist (a new resolver kind, a predicate leaf/compound/comparator, an entropy input, an event-grammar field, a systems-table row); it lands in pack JSON iff it tunes HOW declared mechanics behave (weights, thresholds, hooks, templates, entities, actions, profiles, optional blocks); the test question: "must a second pack carry this for the mechanic to exist at all?" — yes is core, no is pack; enforcement is the existing three-layer fence, now the law's named shape: INV-3's stoplist (the vocabulary half), `core/pack.py`'s lint (the shape half, `pack_doctor.py` its fix-hint reader), `tests/test_drift.py` (the wiring half — the systems⇔metrics prefix contract); the phase-6 DSL/macro layer compiles INTO pack JSON, never across the line. (b) D-143 the INTENT_REJECTED health-rate — in live LLM traffic the door-side rejection share (`intent_rejected` over submitted intents, both pure log reads) holds ≥90% delivery: target ≤5% rejected, sustained-breach line 10% (rolling 100-intent window, twice in a row); a breach opens a KI and a stop-and-ask BEFORE any parser or grammar tuning — the mistuned half is diagnosed first (dead doors, stale preconditions, or a brief that misleads its reader); the instrument: TEST_PLAN §8.5's heartbeat census; the number provisional-unmeasured until the first live row. (c) D-144 the LATENCY-BUDGET law — the budget is PER-COMPONENT on §8.5's cut (tick/fold/brief/parse/generate), p50/p95, never a single end-to-end number that hides the eater: sim-side components p95 ≤ 10 ms each (tick/fold/brief carry two orders of measured margin — perf-1's 9.8k events/s; parse an unmeasured envelope until the first live row), generate p95 ≤ 6.3 s, hard ceiling 20.4 s (bg-8's measured envelope — the only component with real pressure), the turn-based end-to-end envelope ≤ 8 s p95; a real-time companion profile is a DIFFERENT budget (owner engine-1); envelopes revisable at every heartbeat row — a breach names its component, never a blind tuning pass. (d) the DIGEST — `scripts/digest.py`, the derived one-pager over the three state-owning docs (STATUS/TASKS/DECISIONS → iteration, phase, test line, the next step in its own words, the KI census, the todo census, the landings, the decisions tail): read-only, zero engine imports, no wall-clock (STATUS's own Date line — INV-2's hygiene extended to derived artifacts, the scaffold's precedent), unparsable shapes degrade to (unparsed) never a crash (the doctor's law); the DERIVED-ONLY law is the row's own: quoted+truncated source text and counts of it, never a computed metric or restated state — the digest is a viewport, the docs stay the single truth, and the drift pin (`tests/test_digest.py` on the real repo) fails the day a doc reshape breaks the parser, in the same iteration; the executable-architecture alternative and the future authoring/tuning UI stay unscheduled — a single-owner repo whose reader is the agent+LLM loop is served by the dense docs, the UI is its own design iteration on the owner's call; (e) the OPENING-DAY QUESTIONS pinned on TASKS' parked section — the "delete half the mechanics" gut-check (the ablation arm's measured removable set its instrument) and the second-setting shape (a different main loop — travel/caravan/station class — not another social hub; the universal-core claim under test; the choice itself stays the owner's, the review's open question)** | the research session's remaining-gap verdict (the review was written at iter-106; iter-107 landed ~80% of its priority list): the digest was the one confirmed real gap — no TASKS row, no tool; the two questions lived only in chat context, at risk of evaporating with the session that carried them | regression `tests/test_digest.py` (the crafted-mini-repo value pins + the real-repo shape pins — the digest of iter-109 must not fail iter-108's test); AGENT_NAVIGATION §1 + the README repo map carry the tool; the three law rows D-142/D-143/D-144 ride the same call |

| D-152 | 2026-09-13 | **The pack-ci landing — the admission-lint rungs LIVE (iter-117, the owner's fresh call «начинай работу по планам… что осталось незакрытого и логичнее всего — то и делай» — STATUS Next step's own readiness pick: pack-ci the natural cheap candidate, two packs now existing to run against). The PACK_SPEC §5/§6 rows are enforcement in `core/pack.py`: `_teleology` (dead action types — the declared-effect witnesses + the on_action/system_of_type/attribution reads + `core/resolvers.py::STATE_MUTATING`, the registry's own owner; orphan entities — the reference walk over data keys/values with `notes`/`_` prose excluded, the exits-edge position reads, the positional-audience reach for knowers, the target-grammar matcher for items incl. the actor-side flag reads; empty matrix rows — every non-player NPC in ≥1 authored surface: urgency/watch/expectation/hook-target/group/pair-relation/carrier; declared-but-unused templates — the emission-witness collector over every declaring field + the core constants (intent_rejected always, status_decayed iff drift axes) + `importance.story_critical_events` as the authored-intent dormancy witness) and `_live_char` (AP-9 the optional all-or-nothing `spine` block on npc records — want/need/flaw/cause, world-2 L2 the first consumer per D-148; AP-8 the urgency entry's optional `flaw` key, both directions closed; AP-11 clone pairs over identical (intent, requires) with the probability-0 stream-placeholder exemption; AP-15 predicate atomicity — compounds hold leaves only, the law inside `_predicate_error` so every site inherits it, the evaluator keeps its recursive grammar; AP-13 ungated opposite-`add` reactions on one prop under one event key; AP-1 the optional `rules.json::budget` block {npcs, items, hooks, templates} each {min, max}; the price-marker lint — non-ambient deferred hooks demand an immediate knowledge/state token on the branch, the ambient channel exempt as the noise floor). `tests/test_packci.py` +28 (the crafted-twin refusals + the green pins: both committed packs, the scaffold output, the COMPLETE v0.1 twin). The 68a twin-compliance pass: a stripped block strips its emission vocabulary — the weather family's template lines go with the blocks (11 crafted-twin helpers across the beat-family suites + `scripts/balance_harness.py::_DROP_DEAD_LINES`, the ablation map, periphery pack knowledge per D-046), the factions muster verb rides a `system_of_type` attribution row (metrics-side metadata, zero canon bytes). 1696 passed + 1 skipped, ruff clean, zero corpus price — the lint is load-time only, no runtime surface touched, both committed packs and the reskin green end to end | PACK_SPEC §5/§6 the single reading owner (the deterministic readings live there, not here); the crafted-twin compliance family: test_packci's own pins | the standing owner-gated rows `qa-1`/`ci-1` (the runner automation) untouched — `load_pack` is the CI today |
| D-153 | 2026-09-13 | **The world-2 L2 opening — the province pack's skeleton slice (iter-118, the owner's fresh call «начинай работу по планам… что осталось незакрытого и логичнее всего было бы начать сейчас то и делай» — the iter-117 precedent phrase, STATUS Next step's own readiness pick: world-2 L2 the natural next pick). `content/province_pack/` — the Sarrow Vale, ORIGINAL nouns (the posture tier, D-130: the repo's public 2nd setting is original; the L1 reskin instrument was the open generic stack, the L2 pack is not; no CREDITS sidecar). The skeleton: the worldgen at the province band (extent 144/spacing 8 = 324 sites — the 200–600 row, the geo-1-measured cost, a 9× growth over the road's 36; the six settlements claimed at seed-realization sites, `place.max_edge_span` 4 — the artery's legs 2–4 cells; the feud/quarrel chronicle over 150 years arming Maren's sweep + Wilmot's murmur), the travel lattice over the derived price family (150/60/45, the legs 345–705 — hours-to-a-day where the road walked in minutes, the L2 scale claim made mechanical), the cast WITH spine records (AP-9's first committed consumer — five NPCs, every flaw consumed by its urgency entry, AP-8; the budget block AP-1's first arming), the T1 twin (`tests/test_t1_province.py` +10: byte-identity + the golden fixture + the regen guard + the price pins + the mid-travel transfer pin — Osgar briefing Ferra at t=3240 inside the Malby→Thornmill leg, the road T1's measured instance re-produced at province scale) + the PROVINCE_STOPLIST self-check extension + the packci committed-pin (the third pack joins the green pins). 1707 passed + 1 skipped, ruff clean, zero corpus price (the third pack is a new directory — the tavern/road corpora byte-untouched by construction) | the wave plan rides TASKS world-2 (the cultures half — the two name-1 profiles + the condensation travelers + the prohibition sets — the factions triangle, the deep feud history, the calendar; each slice a row, the ORDER owner decides); phases.md §6 the architecture owner; PACK_SPEC the contract | the row's own honest scope: the skeleton proves the surface + the crosswalk, the pillars prove the depth — VISION §7's honest twin (a deep unique world takes weeks; the slices are the weeks made explicit) |
| D-154 | 2026-09-13 | **The world-2 L2 cultures slice (iter-119, the owner's fresh call «начинай работу по планам, что осталось незакрытого и логичнее всего было бы начать сейчас то и делай» — the iter-117/118 precedent phrase, STATUS Next step's own readiness pick: slice 2 the natural next pick, D-153's wave plan). The estrangement-as-data family complete in the committed pack: (1) the two name-1 PHONOTACTIC PROFILES (`rules.json::names` — name-1's FIRST committed arming, D-131/D-116(12): `lowland_soft` the river tongue, sonorant codas and no clusters, 2–3 syllables; `hill_hard` the croft tongue, stop codas and clustered onsets, 1–2 — the soft/hard POSTURE made checkable, the T1 posture pin); (2) the CONDENSATION TRAVELERS (`entities.json::groups` — depth-7's first committed arming: `grp_road_traffic` anchored ON the runner's load road, `condense_event` + `macro_event`, three `generated_name` members — two lowland tongues and one hill; the load-state origin law fires at the FIRST beat: t=360, ONE event carrying each member's member_of birth PAIRED with the name birth (one member, one block, the D-054/D-131 shape) + the tombstone, the drawn names Sathranthaem/Wathru/Drist riding the golden bytes, the tale line rendering them — the unborn stay counts until the reader's zone warms, canon after, first-commit-wins); (3) the `cultures` BLOCK — the estrangement METADATA, the budget-block precedent (`core/pack.py::_cultures`, load-time lint, ZERO runtime surface; "prohibitions as pack metadata, never bonuses", REFERENCES §10, enforced as log asserts at gate review — the WH40k grammar): one record per culture (the CK3 culture keying) — `name_profile` (a declared profile, the culture↔name keying made executable: a member's generated_name must name the culture's own), `custom_vocabulary` (the culture's words, each occurring in the pack data OUTSIDE the block, the anti-rot walk — the stoplist self-check's twin at load), `prohibitions` (the laws: `law` + the optional `flaw` — the culture's limit over a MEMBER's flaw), `members` (declared npcs, one culture each). The AP-8 PROHIBITION SURFACE — the crosswalk's THIRD consuming family, landed on its own trigger (PACK_SPEC §6's "prohibitions join on their own triggers" — this is the trigger): the flaw joins the consumed set in `_live_char` (a flaw consumed by the law alone is anchored, never GHOST); the committed arming is the DOUBLY-consumed shape — the guild's shelter law over Maren's "the scales tilt for old coin" (however the beam tilts, a paying stranger is never turned from the crossing's shelter), the hills' wergeld memory over Garrick's "the iron hides when prices fall" (the bloom may hide from prices, never from a blood price — the carrier's goal walks the word). The budget RE-DECLARE (npcs 6–9 → 7–12, AP-1's second arm exercised: the cast grows to 10, the deliberate re-declare not a silent outgrowth). `tests/test_cultures.py` +15 (the refusals: shape, anti-rot, foreign-flaw, one-culture, keying; the green twin proving the consuming UNION by load — a crafted spine consumed by the prohibition ALONE) + the T1 twin +4 (the first-beat condensation pin, the born-names tale pin, the tongue-posture pin, the committed-instance pin) + PROVINCE_STOPLIST +3 (drover, peddler, wergeld — "carrier" deliberately absent, the acquisition family's engine word). The golden fixture regenerated: the ADDITIVE delta (+1 condensation, +6 traveler decays at the two riverroad-active beats; 32→39 events), fingerprint 0 — the name draws ride the family streams. 1726 passed + 1 skipped, ruff clean, zero corpus price outside the province (the tavern/road corpora byte-untouched; the lint is load-time only) | the wave plan rides TASKS world-2 (slice 3 the triangle — the factions + the deep feud history; slice 4 the calendar + the verdict; the ORDER owner decides); PACK_SPEC §6 the prohibition surface's contract; phases.md §5 the name-1/depth-7 law owners | the honest scope: the cultures half proves the estrangement DATA (naming, customs, prohibitions — the Morrowind lesson) over the phase-5 primitives with ONE new metadata block; the wergeld's deep history (the cause tree) deliberately rides slice 3, not invented here |
| D-155 | 2026-09-18 | **The causal-architecture research bootstrap routed (intake-14, iter-120, doc-only — the owner's «начни работу с документом» research call over the uploaded bootstrap; the D-018/D-022 surgical-absorption pattern, the D-150 precedent): the doctrine half re-derives intake-13's confirmed standing law (zero factual drift found beyond its own record); five architecture questions routed, zero build-grade items — (1) causal compression CONFIRMED as standing practice, never as one law sentence: the bootstrap's "explicit fact OR deterministic consequence of more fundamental causes" is L3 plus the authored-facts-win boundary, its strongest landed instance the travel price law (the derived integer function of the WorldModel, armed twice — road 150/210/300/360, province 345–705), beside the faction small formula, the macro-year, the weather fold, the read-side folds, and intake-10's world-state law; both explosion modes fenced (outcome tables by L9/L13 + PACK_SPEC §8's trigger-gated affordance clause; universal models by authored-facts-win — `travel.edges`, the tone dial, the spine records, the names profiles); damage-as-derived rides the future combat-bearing pack (the soul-scar parked family), no pre-work owed. (2) simulation levels CONFIRMED — one world, resolution strategies, never second engines (the LOD one-gate law, maclock-1 under L4, the D-112 cardinality surface, condensation, the id-never-changes law; the separate-engine alternative the named anti-pattern family — D-116 (7), D-116, D-147; domains are pack vocabulary over one door, D-142). (3) large-scale aggregation UNRESOLVED — the substrate landed (macro aggregates, O(groups × macrobeats), the group as intent-door actor, condensation, the cold census; worldgen's 10k sites/1.19 s the only 10k-scale measurement), the 1k–10k battle claim unmeasured and correctly un-promised (the bootstrap's own discipline = the repo's measured-numbers law); parked with the intake-9 family, the first combat-bearing pack decides. (4) stealth PARTIAL — the epistemic half landed (the acquisition conditions stepping birth fidelity, channels, `known_by`, panic contagion, the beliefwire gates), the physical half authored per-action pack data (`drop_break` noise, `alarm_adjacent` through-walls; movement sightings visual-only, same-location never through exits); generalization a first-consumer question per D-142. (5) materialization CONFIRMED at the law level — the D-054 slot shape the PARENT LAW with four landed instances (texture promotion, scene detail, names, member_of — each "never a second mechanism"), entity-birth promotion = st-5's first consumer (the same door with pack grammar, blueprint §7's containers law), the integration half landed (the erosion fold scan, the id-survival law); the bootstrap's §4 framing corrected — texture promotion is not a special case awaiting generalization, it IS the general law already extended, and L13 holds the no-generic-engine line deliberately. The bootstrap's §12 outcome vocabulary re-routes to D-150's standing verdicts unchanged | the bootstrap is research input, never a second source (the FAQ's convenience-copy law); the repo's answers to the bootstrap's own §16 question list were already standing or routed — the intake records the five new verdicts where a future row finds them | phases.md §6's intake-14 block (the architecture owner); the build queue untouched — nothing unpins, nothing reorders; the three parked patterns (the scale benchmark, the stealth emission substrate, damage-as-derived) named in the block; DECISIONS 34 rows — over the 30 cap on the §6.1 substance precedent until the next gate collapse, the D-152/153/154 family's own record |
| D-156 | 2026-09-18 | **The narrative-design research dossier routed (intake-15, iter-121, doc-only — the owner's «начни работу с документом» research call over the uploaded seven-source GDC synthesis (+ the research-workflow methodology document, v2 supplemented); the D-150/D-155 precedent, the consult posture of the intakes 6..14): the doctrine half CONFIRMED — the dossier's 18 proposed design laws re-derive the standing law families (L1–L14, EPIST-1, D-005, the brief contract, the knowledge model) with zero factual drift; the instrument half routes onto landed surfaces; zero build-grade items.** The 18 laws map one-to-one onto standing owners (04→L8 + the price-marker lint; 05→D-005's seeding law; 08→the AP-9 spine; 11→D-005 + T8; 17→BRIEF_SPEC §5's eviction contract verbatim-compatible; 18→the eight blocks). The 7 gaps: five PROVEN as landed instruments (A→the M-family + `systems_touched` — the composite formula refused, D-024/L13; B→`payoff_latencies` D-140, "half-life" a re-labeling refused; C→M4; D→mechanics trace/why/blast D-118 — the dossier's IT-5 proposal predates the landing, a stale pin; F→the spine crosswalk; G→PROVEN by construction, the director releases only seeded hooks through the intent door), one PARTIAL (E breadcrumb density — the substrate landed, the recovery half is since-1's own standing row). The NQ-01..10 vocabulary REFUSED as a second metric label set (the T/M families own the namespace; the translation recorded in the block). The NLAW-* ID family REFUSED (the law namespace is owned — a parallel numbering would be duplicate truth, D-024). The AP-01..AP-10 anti-pattern numbering COLLIDES with the standing live-char crosswalk AP family (PACK_SPEC §6) — the genuine new finding: the anti-patterns (all standing law — D-005, the noise floor, O(relevance), L1, L7, the epistemic laws) route as the block's verdicts, never as a second AP numbering. The 12-iteration program decomposes onto standing instruments and owner-gated rows (IT-1 is the intake verdict set itself; the standalone audit docs refused under D-024; IT-11 is TEST_PLAN §8's testproto). The 8 fixtures: the committed corpus carries the family (F1 the crime ladder, F2 the steal-FAILURE path, F4 beliefwire/suspectaxis, F7 the document_check boss beat); F8 is since-1's row. The five uncovered sources enter REFERENCES §10 (the intake-8/9/10 precedent): The Sims, Slime Rancher, Shrouded Isle, Heaven's Vault, Procedural Narrative Generation. The methodology document CONFIRMED as a re-derivation of the standing intake discipline (its two-axis classification vocabulary adopted as working vocabulary, never repo law; its §6.1's two named research files stay outside — not uploaded this session, recorded in the block) | the dossier is research input, never a second source (the convenience-copy law); the repo's standing instruments already answer the dossier's gap half — the intake records the verdict set + the source rows + the AP collision where a future row finds them | phases.md §6's intake-15 block (the architecture owner); REFERENCES §10's five new rows; the build queue untouched — nothing unpins, nothing reorders; since-1 owns the one genuine residue; DECISIONS 35 rows — over the 30 cap on the §6.1 substance precedent until the next gate collapse, the D-156 row joining the family's own record |
| D-157 | 2026-09-18 | **The procedural-generation research routed (intake-16, iter-123, doc-only — the owner's «отработай по второму файлу» research call over the uploaded research record, the methodology document's own §10 example in its grounded later form — the §13 spike rides the province nouns of D-153; the D-150/D-155/D-156 intake precedent): the four domain verdicts re-verified at HEAD, the three measurement claims RE-DERIVED by a read-only probe through the public API (200 seeds → 200/200 distinct topology probe signatures at the pack's six claimed anchor sites, the height dimension alone 200/200; 60 seeds → 60/60 distinct history signatures; the 3-member faction collapse 8 raw membership states → 3 probability outcomes — the small formula's per-cent floor measured, not asserted). Topology CONFIRMED as standing practice (the compact authored block extent 144/spacing 8 → 324 sites; TECH_NOTES §12 owns the cost ladder — the research's own 0.03/0.23/1.0–1.2 the intake-15 micro-drift (c) family; st-6a the landed consumer, roads-1 the parked one); history CONFIRMED for causal anchors (the province chronicle's 150y/events_max-5 shape IS the sparse-anchors cap; anti-volume = D-140's payoff-latency family); social PARTIALLY CONFIRMED (the machinery all landed, the combinatorial collapse measured, structure/texture = the standing LLM boundary; the outcome-divergence experiment an evaluation discipline whose natural first consumer is world-2 L2 slice 3 — the ORDER owner decides; downstream story divergence stays UNKNOWN); resources PROVISIONALLY HIGH-VALUE, still inference at HEAD (res-1 owner-gated unchanged; the donor list matches the landed dives; the §13 grounded spike — 729 authored configs → 55 event-path signatures / 21 player trajectories / 73 transitions, offline — recorded as external evidence MISSING-as-runtime; the one-knob finding = rank authored knobs by causal-output density, not count, res-1's own row the consumer). The boundary law re-derives INV-4/D-095/D-150/D-052; the five failure modes fenced by standing law (L9/L13, the measured collapse, INV-2 streams, the UNKNOWN discipline, the one-gate LOD); the allocation table a working map, never a new information owner. Zero build-grade items; the build queue untouched** | the research record is external input, never a second source (the convenience-copy law); the strongest verification form — the load-bearing measurements re-derived at HEAD, never trusted from the text | phases.md §6's intake-16 block (the architecture owner); TECH_NOTES §12 stays the numbers' single owner; res-1/world-2 rows untouched — nothing unpins, nothing reorders; DECISIONS 36 rows — over the 30 cap on the §6.1 substance precedent until the next gate collapse, the D-157 row joining the intake family's own record |
| D-158 | 2026-09-18 | **The interface-oriented procedural composition research routed (intake-17, iter-124, doc-only — the owner's «отработай по второму файлу» research call over the uploaded research task, the methodology document's §6.1 FIRST-named file, the owner call that intake-16's tail note awaited; the D-150/D-155/D-156/D-157 intake precedent): the five candidate principles (interface state, secondary topology, constraint propagation, causal motif fitting, multi-node motifs) verified against HEAD, two fresh measurements RE-DERIVED by a read-only probe over the committed province pack through the public API (60 seeds → 60/60 distinct motif-binding sets — the authored feud/quarrel collection vocabulary fixed, the participant pairs/places/years per-seed; the derived edge prices 54/60 distinct vectors, per-edge 6–7 values — interface-local friction is per-seed relation-local). The verdict set: interface state PARTIAL (static PROVEN — st-6a's derived prices, the `pair.<figure>.<axis>` homes; dynamic route state MISSING-as-runtime, its owners the space-pack return (D-116) + res-1's flow-as-macro-events; a generic `InterfaceState` SHOULD-NOT-ADD, L13); secondary topology REJECTED-as-subsystem (one canonical graph + L3 derived projections + pack-data overlays IS the standing anti-coupling answer; a second runtime topology is a second authoritative representation, INV-1); constraint propagation NO ACTION (every selection surface a deterministic small mechanism — drama-2's RNG-free options, the arcs ORDER/GAP laws, the weighted weather chain; WFC stays out); motif fitting PARTIAL (PROVEN at genesis — the chronicle collections are authored motifs whose bindings move per seed, the hooks seeding the director's buffer; the runtime TRADE_BLOCK form IS res-1's declared shape as pack data + existing doors; cross-pack motif reuse is pack-side authoring under D-142, never a MotifEngine); multi-node motifs PROVEN-as-primitives (the collections' parent-map cause chains, the director arcs, the commit-door cascades — each domain owns its composition mechanism). The Townscaper transfer is real but already owned by the standing architecture; the research's own §22 scope guard holds verbatim. Zero build-grade items; res-1/world-2 L2 slice 3/4 stay the priority test cases on their standing rows — nothing unpins, nothing reorders** | the research proposed fitting reusable causal motifs to topology for materially different situations; the reconciliation found no recurring problem a new generic abstraction would solve — the pattern is the standing shape, and the probe re-derived it with measured support | the external document stays outside the repo (the convenience-copy law); phases.md §6's intake-17 block owns the detail incl. the §24 evidence table; the §6.1 cross-synthesis with `RESEARCH_PLAYER_DECISION_MECHANICS` stays OPEN (that file outside, owner-call-gated); DECISIONS 37 rows — over the 30 cap on the §6.1 substance precedent until the next gate collapse, the D-158 row joining the intake family's own record |

| D-159 | 2026-09-18 | **The player-decision-mechanics research routed (intake-18, iter-125, doc-only — the owner's «отработай по второму файлу» research call over the uploaded `RESEARCH_PLAYER_DECISION_MECHANICS.md`, the methodology document's §6.1 SECOND-named file, the owner call that intake-17's tail note awaited — closing the §6.1 pair; the D-150/D-155/D-156/D-157/D-158 intake precedent): the research asked whether the existing simulation produces a compelling recurring player decision loop or only rich state; three fresh measurements RE-DERIVED by a read-only probe over the committed packs + playscripts through the public API (day1_full seed 125: 56 events / 8 player steps / 13 PC knowledge records / 15 suspicion deltas / 5 leverage mints — the epistemic loop end to end: the failed steal leaves 4 witnesses, the violated expectation mints the INFERRED purse_missing, the failed papers check teaches the PC, the drunk's coerce rejected at the same door for leverage — one gated action space for every actor; the removal test executed literally: minus steal 56→31 events, ALL suspicion/leverage/risk deltas vanish while the arson chain survives — the loop decomposes into independent causal chains; minus the final wait the ending loses the t=1080 watch handover — waiting never empty; province_smoke seed 42: the derived legs 705/660/405/345, 14/14 PC records born on the five waypoint arrivals, the t=3240 transfer mid-leg — travel already a time/exposure/information trade). The verdict set over the brief's own A–G domains: time/timing PARTIAL (the pressure half PROVEN, the informed half rides world-2 L2 slice 4's calendar); information/knowledge PROVEN as substrate, the "most distinctive layer" hypothesis INFERENCE + PARTIAL — compellingness not decidable in track A (INV-4; presentation-1 the SoW-horizon owner); social/suspicion PROVEN (the crime chain the v0 core; the faction half awaits slice 3); travel/geography PROVEN (the L1 instrument + the province surface; dynamic route state the space-pack return + res-1 per intake-17); resources DEFERRED (res-1 owner-gated, unchanged); irreversibility PROVEN as law + live stakes; experimentation PARTIAL (the dev instrument PROVEN, the player-facing feedback half presentation-1's). The brief's core model re-derives the standing architecture layer by layer (the intent door / the pack preconditions + OCC / the canon event / the per-knower records / the reaction cascade / the brief's scene_delta / the state-gated action surface); its four-layer separation and Do-not list re-derive L2/L6, D-095, L13/INV-3 verbatim. The §6.1 cross-synthesis with intake-17 CLOSED: the two researches share ONE primitive (the door + the log + the brief), the states change actions/risks/costs/information at the mechanism level (measured), and no new owner is warranted — zero build-grade items, nothing unpins, nothing reorders; the one genuine residue is the brief's next-decision test as an evaluation discipline for world-2 L2 slice 3's outcome-divergence experiment (count divergent NEXT-DECISION surfaces, not only divergent event paths; the ORDER owner decides)** | the research proposed developing a player-facing gameplay layer; the reconciliation found the loop already executable in the committed corpus and owned layer by layer — the missing half is composition, content, presentation and playtesting, exactly the brief's own valid alternative, never new machinery | the external document stays outside the repo (the convenience-copy law); phases.md §6's intake-18 block owns the detail incl. the A–G evidence table; DECISIONS 38 rows — over the 30 cap on the §6.1 substance precedent until the next gate collapse, the D-159 row joining the intake family's own record |
| D-160 | 2026-09-18 | **The persistent-groups / settlement-development research routed (intake-19, iter-126, doc-only — the owner's «отработай по второму файлу» research call over the uploaded `Research task — Persistent Groups, Camps, Settlements and Emergent Territorial Development.md`; the D-150/D-155/D-156/D-157/D-158/D-159 intake precedent): the research asked whether persistent player-created groups and territorial/settlement development are a natural extension of the causal architecture, not special subsystems; ONE fresh probe RE-DERIVED the surface through the public API (a tavern_pack copy carrying the settlement vocabulary as PACK DATA — a second transitions layer `build`, a tier-armed group, a faction goal, an on_action witness reaction, an erosion rule, a buildable location, a `raise_timber` action over the existing ignite resolver — zero core edits, the full pack-ci lint green, seed 7 twice byte-identical): the construction chain measured end to end incl. the MID-BUILD DEPARTURE (started → spread ×2 → usable → completed all firing after the PC left at t=59 — construction runs on world rules, never player presence); the group tiers (the cold aggregate while two steps away, the condensation's member_of births + the write-once tombstone, zero aggregates after, the census on the year turns); the group as actor (tier events + faction-goal waits through the one door, cause_intent faction_0000); visibility (the lone witness, the blind drunk two rooms away at ZERO records, the on_action one-hop reaction, the telling at fidelity vague — partial decayed); decay (the rain's erosion counter-event reverting `usable` while the irreversible `camp_finished` survived — EVENT_SCHEMA §4 live); the resume door byte-identical at a clean drain boundary (plus the honest edge finding: a split inside an autonomous silent tail shifts the session's next feed tick — a live-session semantic, never a determinism breach); and the scale twin (50 cold groups / 100 unborn members / 5 crossings = 250 aggregate lines, 3 per-member events — the D-112 cardinality law at settlement count). The verdict set over the brief's §1–§18: the group/party scaffold PROVEN (one id all tiers, the member_of door join/leave/transfer fold-validated, group-level and member-level intents through the SAME door; a `PartyEngine` SHOULD-NOT-ADD), construction lifecycle PROVEN (the fire chain IS the construction state machine — planned=`spot_available`, usable/completed=follow-ups, damaged/abandoned=counter-events/erosion; `castle` a derived classification, never an entity type), visibility PROVEN as substrate (a `SettlementDiscoverySystem` SHOULD-NOT-ADD — the per-knower index is universal), content-pack independence PROVEN by construction (the named Settlement/Party/Castle/Companion/BanditCamp engines all SHOULD-NOT-ADD), determinism/scale PROVEN; group dimensions mostly over-modeling (per-entity axes + pair relations + derived reads, the KeeperRL anti-psychology-engine precedent), group disagreements REJECTED as first-class mechanics (ordinary consequences of goals/resources/relations, the brief's own minimal-model preference); the genuine gaps ALL own standing rows — st-5 entity birth (the player-FOUNDED entity: `apply_event` rejects unknown ids, the mint is st-5's door), res-1 (the settlement's material life), world-2 L2 slice 3 (the claim/legitimacy first consumer — UNKNOWN/DEFERRED, no canonical claim state demonstrated needed) + slice 4 (the calendar), and the one mechanic-scale verb gap (the membership writer — a resolver family, companion-1's natural shape). The final question answered YES on the standing substrate: group + place + state-door + relations + knowledge + intents + events + macro cadence all landed, `structure` IS the transitions layer armed as data; the brief's §20 vertical slice is the probe's chain extended by those rows, its validation-target status holds — zero build-grade items, nothing unpins, nothing reorders** | the research proposed settlement/party mechanics; the reconciliation found the causal substrate already expresses them as pack data — the missing halves are entity birth, the resource layer, the claim consumer and the membership verb, each already owning its queue row | the external text stays outside the repo (the convenience-copy law); phases.md §6's intake-19 block owns the detail incl. the verdict table; DECISIONS 39 rows — over the 30 cap on the §6.1 substance precedent until the next gate collapse, the D-160 row joining the intake family's own record |
| D-161 | 2026-09-18 | **The content-archetype / pack-strategy framework research routed (intake-20, iter-127, doc-only — the owner's «используя research_method_v5.md => разберись что перенять можно, дополнить или улучшить… долгосрок важен и качество» call over the uploaded `canonsim — Content Archetype, Pack Strategy & Capability Audit.md`, a FRAMEWORK document unlike the mechanism researches of intakes 16..19; the D-150..D-160 intake precedent, the method file itself external per the convenience-copy law): the framework's 26-item "what should become permanent" list reconciled item-by-item per the method's four questions (principle / form / quality / transfer), the capability truth table verified at HEAD (1726+1 green, ruff clean, all three committed packs load-green through the admission lint; resources grep-verified absent from core — res-1's own row). The verdict set: the core thesis (compositional breadth of a domain-blind causal core; surface vs causal archetype; proof value; novelty types) OWNED — D-146's loop-change law, L9 and VISION §7 are the same principles in measured form; the capability discipline (proven/expressible/pending; implemented ≠ demonstrated generic; the evidence ladder) OWNED — the intake verdict discipline of 16..19, with ONE genuine sharpening adopted: "LANDED BUT UNDER-TESTED" names the province-only single-consumer surfaces (the cultures block, spines, condensation travelers, price overrides), and the cross-context promotion condition (a mechanism's architectural proof completes when a SECOND materially different pack arms it — the AP-9 precedent's own ladder) joins the card; the topology framework (7 dimensions + decision friction + canon/knowledge) OWNED-as-substance (every dimension's standing system cited), the consult read adopted as a composition checklist, never a subsystem proposal (the framework's own test-first guards re-derive the standing law); the anchor ontology PARTIAL — the mobility × persistence vocabulary adopted (tombstone/historical-only/erosion are the repo's own forms); the loop/signature/counterfactual family OWNED (TEST-1, T8, --systems-minus, intake-18's literally-executed removal test), with the transfer point recorded: engine-1's validation inherits "prose is never proof"; the portfolio family (orthogonality; behavioral yield per authored complexity) ADOPT-AS-PARKED — the genuine new residue, the floor already enforced (AP-1, the teleology gate, the reskin clock), the SELECTION question previously owning no row; the admission test OWNED (the framework's own §31 orders using the repo's real rules — verified sufficient, no replacement caps); the heavyweight artifacts (the 22-column standing matrix, a standing truth-table document, the 17-candidate catalogue) REJECTED — parallel truth (D-024), premature per the framework's OWN §34 empirical-first law, the speculative-worldbuilding ban. The combined design: ONE parked pack-candidate consult card (9 lines, phases.md §6's intake-20 block — proof-value proposition / causal-archetype litmus / topology read / anchor read / novelty type / portfolio orthogonality / behavioral yield / evidence ladder / verdict vocabulary), consumed at the next pack-slot opening or pack intake, recalibrated by the first independent multi-pack authoring experiment; the §37 follow-up iteration NOT warranted — this intake is the routing, and the card's validation is bound to world-2 L2 slice 3's outcome-divergence experiment + the first pack slot. Zero build-grade items; nothing unpins, nothing reorders** | the framework proposed a durable content-selection method; the reconciliation found ~four fifths of it already standing law in stronger measured form — the durable new share is the selection discipline for the pack portfolio, which parks as a consult card rather than a standing artifact, exactly the framework's own empirical-first guard | the external framework and method files stay outside the repo (the convenience-copy law); phases.md §6's intake-20 block owns the detail incl. both tables + the card; PACK_SPEC unchanged (the contract owner — the card cites, never restates); DECISIONS 40 rows — over the 30 cap on the §6.1 substance precedent until the next gate collapse, the D-161 row joining the intake family's own record |
| D-162 | 2026-09-18 | **The unified-observatory / worldbuilder / agent-gateway research routed (intake-21, iter-128, doc-only — the owner's «используя research_method_v5.md => разберись что перенять можно, дополнить или улучшить… долгосрок важен и качество» call over the uploaded architecture-research task; the D-150..D-161 intake precedent, the intake-14 complement — substrate questions asked there, observer-side questions here): the doctrine half (§1..§4: one world / one canon / deterministic ordering / LOD-as-resolution / knowledge-not-canon) re-derives standing law + intake-14's confirmed record with zero drift; the observer-side half decomposes — the §9 four-question frame (STATE/CAUSALITY/KNOWLEDGE/COUNTERFACTUAL) ADOPTED as the parked observability consult card (each question cites its proven instruments: the fold/matrix/chronicler, the provenance family + trace/why + M3 + chron-2, the records + the blind suite + L6/EPIST-1, blast + the harness arms + checkpoints; the God Tool and standing truth-table documents REFUSED per D-024 + the document's own §12.C); the counterfactual half PARTIAL (blast's two-arm same-seed fingerprint divergence the proven primitive; the first-divergence read routes to world-2 L2 slice 3's outcome-divergence experiment — already carrying intake-18's next-decision census; branch-from-checkpoint rides the owner-gated resume door); the single-event postmortem (trace_event's shape — the log already carries all four quadrants) rides mech-2's next mechanics.py touch; the §6 three-class balance taxonomy OWNED one-to-one by the standing instruments (T-suite + admission lint / balance-1 harness / T8 + corpus + heartbeat), the bug-vs-imbalance-vs-emergence routing joins the card; the §8 worldbuilder claim OWNED (the Phase-6 litmus met by measurement, D-149/D-152; mode G through pack CI; the UI correctly deferred to bg-6/presentation-1); the §10 agent-gateway principle CONFIRMED as standing law (the doors + the D-055 file-contract family + testproto + AGENTS.md's own iteration protocol the dev-time gateway; the SoW formal surface rides engine-1/presentation-1, nothing to formalize now); the §11 detector family PARKED (first-consumer law: slice 3/4's verdict calls + the phase-gate heartbeat); concept canonization: 4 already named, 3 correctly deferred (label-matching refused); the §13/§16 dedicated formalization iteration REJECTED (every build-grade item owns a carrier or standing row). Zero build-grade items, the queue untouched. Detail: `docs/blueprint/phases.md` §6's intake-21 block (the capability map + the verdict table + the card); DECISIONS 41 rows — over the 30 cap on the §6.1 substance precedent until the next gate collapse, the D-162 row joining the intake family's own record |
| D-163 | 2026-09-18 | **The ComfyUI/modularity consolidation research routed (intake-22, iter-129, doc-only — the owner's «используя research_method_v5.md => разберись что перенять можно, дополнить или улучшить… долгосрок важен и качество» call over the uploaded multi-session consolidated report; the D-155..D-162 intake precedent, the consult posture — the external text and the method file stay outside the repo): the repo-facts half CONFIRMED zero-error at a REAL clone (BASE_COMMIT 309aa17, 180 commits, 1726+1 green, ruff clean — resolving the report's own disputed BASE_COMMIT/access record: most consolidated sessions DNS-failed at clone); the node-graph-as-runtime rejection re-derived from standing law (INV-1/INV-2 + VISION §7 + the no-second-engine family); the replaceability formula (canonical engine → contracts → projections → interchangeable clients) re-derives the D-055 file-contract family + the dumb-terminal law — OWNED, zero drift; the three-senses frame: code/authoring modularity OWNED (L13 answers the plugin-layer ask), mechanics modularity PARTIAL — the SHARPENED asymmetry: rules.json's systems reads/writes DAG covers the nine original passes while the newer families (weather/travel/echo/traits/cultures/names/budget) declare heterogeneous top-level blocks + per-family lint; the minimal system contract + the agent-speedup claim stay HYPOTHESIS — the 3..5 system decomposition the named next verification, parked on the modularity consult card (engine-1/presentation-1 the consumers, no new row — the first-consumer law); the DAG viz rides mech-2's next touch; the ComfyUI lesson CONFIRMED at the principle level (explicit attachment points + contracts-before-perimeter = the doors + the admission lint), no donor registration (the owner's catalog policy); the v5 method file itself fully internalized (intakes 14..21 the evidence — nothing left to adopt); the report's six disputed items resolved. Zero build-grade items; the queue untouched. Detail: `docs/blueprint/phases.md` §6's intake-22 block (the verification map + the verdict table + the card); DECISIONS 42 rows — over the 30 cap on the §6.1 substance precedent until the next gate collapse, the D-163 row joining the intake family's own record |
| D-164 | 2026-09-18 | **The world-execution / spatial-topology / social-information architecture research routed (intake-23, iter-130, doc-only — the owner's «используя research_method_v5.md => разберись что перенять можно, дополнить или улучшить… долгосрок важен и качество» call over the uploaded architecture-research task; the D-155..D-163 intake precedent, the consult posture — the external text and the method file stay outside the repo; intake-21's own agenda EXPANDED with the world-structure side — where intake-14 asked the substrate questions and intake-21 the observer-side questions, this document asks the execution-model, spatial-topology and social-information questions; its observer-side sections re-derive intake-21's routed record zero-drift, not restated): ONE fresh read-only probe through the public API answered the document's §3.1 six engine questions mechanically over the committed province pack (exits = the authored graph; between = claimed lattice sites + the derived integer prices 660/300/510/600 ticks; region = the typed claim reads biome/height/region/river; adjacent = the exits list; reachable = the graph walk 6/6; outside = the lattice IS the pack-declared extent, 324 sites, mechanically closed; regeneration from the same seed byte-equal — L11) — CONFIRMING the document's own key invariant (mechanically-relevant space between anchors needs an authoritative or reconstructable representation) as already met by the standing form: the lattice + place-1's claim↔exits law + st-6a's derived prices. The §2 intent-lifecycle question set CLOSED mechanically (OCC `based_on_event_seq`/`projection_moved` + the breaking-event cause; the windowed family's unconditional re-run; player rejections = `intent_rejected` no-op events, autonomous failures = the noise floor; re-planning = the beat re-roll; conflict = the queue key + first-commit-wins + `spot_available`) — the FORMAL conflict taxonomy REJECTED as new law (label-matching, D-024 — the document's own "do not invent unless the repo demonstrates the need" clause); the answer set joins the card. The §8 fact→knowledge→belief→goal→intent→action ladder verified transition-by-transition (channels/fidelity, traits+counters, reflection, the door gates; omniscience fenced by the blind suite + L6/EPIST-1; faction knowledge never group state, D-006). Scenarios A–G routed: A/C/E per intake-21's proven records, B's economy half res-1's row, D = blast + slice 3's first-divergence; F (urban topology) + G (political cascade) = composition questions whose stress-test QUESTIONS join slice 3's consult material (the factions triangle + feud history the natural carrier — depth-6 DORMANT in every committed pack, armed by none). The streets/districts/traversal-regions vocabulary, the six-sense extent vocabulary, the dependency-graph abstraction, the standing capability-truth-table, the dedicated formalization iteration: all REJECTED (no consumer — the document's own §24 law; D-024; the ladder complete, the L2 wave plan D-153 the owner-gated carrier). The residue crystallized as the WORLD-STRUCTURE consult card (6 lines, phases.md §6's intake-23 block) — the six engine questions, the three-way existence distinction (exists/simulated-at-resolution/observable), the §2 answer set, the closed social ladder, F/G as slice-3 verification prompts, the between-anchors law. Zero build-grade items; the queue untouched. Detail: `docs/blueprint/phases.md` §6's intake-23 block (the probe table + the verification map + the verdict table + the card); DECISIONS 43 rows — over the 30 cap on the §6.1 substance precedent until the next gate collapse, the D-164 row joining the intake family's own record |
| D-165 | 2026-09-18 | **The cross-domain principle-transplantation synthesis routed (intake-24, iter-132, doc-only — the owner's «проанализируй глубоко документ и оцени… что можно перенять и почему, что можно взять но с переработкой и адаптацией, какой потенциал есть? какие плюсы, какие минусы (подводные камни)? есть ли ультимативное решение…» call over the uploaded synthesis document; the D-155..D-164 intake precedent, the consult posture — the external text stays outside the repo; a CROSS-INTAKE SYNTHESIS, not a fresh agenda: it compresses the routed records (intake-11/D-147, intake-16/D-157, intake-17/D-158, intake-19/D-160, D-134/REFERENCES §10) into six named principles P1–P6 + a unified causal-leverage metric + four verification slices; no fresh probe run — the document claims no measurements, its claims are mappings and the mappings were verified against HEAD 37bcf6b): the six principles ARE the standing architecture, each owned in operational form — P1 dual projection = L3/L11/L6 + the brief's never-invent law (the named projections and all four extension candidates are landed or standing-row shapes); P2 displacement+debt = D-147's law with res-1/weather-1/the thermometer minus as the owners; P3 causal motif = D-158's verdict set verbatim; P4 group-as-entity = D-160's PROVEN scaffold; P5 gauge law = the DD/FP read-surface law; P6 controlled irregularity = worldgen's `PASS_ORDER` incl. the relax pass (the synthesis's "relaxation" names the repo's own primitive) + place-1's consistency law + intake-16's no-volume-without-connectivity verdict; §4's metric = the correct composition of the standing discipline (MVP_SCOPE §15 + L1 + the gut-check family + TEST-1 + the slice-3 outcome-divergence experiment); §5's constraints = standing law verbatim; slices 1/3/4 route to res-1 / world-2 L2 slice 3 / pack-4's T1 slice unchanged. The genuine deltas all die at the door or park: P2's universal MUST form REJECTED as engine law (over-generalizes the pressure-city pack's design constraint into a law over all operations — the one real quality flaw: compression converted "verified in one domain" into "applies uniformly to five"); the four extra P2 applications (knowledge closure, conflict resolution, group policy adoption, director tension release) UNVERIFIED — no substrate, no demonstrated problem; slice 2 (knowledge-closure-as-displacement) REJECTED as law — the knowledge system is a positive-record lattice, no "uncertainty" object exists to close, the authoring pattern expressible today as ordinary events; the P1–P6 NAMES as repo law = label-matching (D-024, intake-23's §2-taxonomy precedent). The residue crystallized as the PRINCIPLE INDEX CARD (6 lines, phases.md §6's intake-24 block — each line names the owner + the over-application fence; the future intakes' pattern-matching step and the res-1/pack-4/slice-3 evaluation disciplines the consumers). Three micro-drifts recorded (the "brief cache" vs the assemble-fresh law; P4's capability-vs-expressible overstatement; §3's "medium" vs the PROVEN scaffold). Zero build-grade items; the queue untouched. Detail: `docs/blueprint/phases.md` §6's intake-24 block (the verification map + the verdict + the card); DECISIONS 44 rows — over the 30 cap on the §6.1 substance precedent until the next gate collapse, the D-165 row joining the intake family's own record |
| D-166 | 2026-09-18 | **The re-verification consolidation pack routed (intake-25, iter-133, doc-only — the owner's «проанализируй глубоко документ и оцени, присмотрись, убедись и так далее => что можно перенять и почему, что можно взять но с переработкой и адаптацией, какой потенциал есть? какие плюсы и какие минусы (подводные камни)? есть ли ультимативное решение, которое нивелирует минусы и приумножит плюсы?» call over the uploaded three-document consolidation of the prior intakes 13..19's own sources + the research_method_v5 method file; the D-150..D-165 intake precedent, the consult posture — the external pack stays outside the repo; where intake-24 compressed the routed records into principles, this pack re-asks them as quality-open rows, its explicit anti-conservatism being «form-matching is not enough»): content-accurate at every load-bearing claim (zero factual drift; the pack's BASE_COMMIT 37bcf6b and its no-files-modified claim both check out), and the anti-conservatism challenge ANSWERED WITH MEASUREMENT, not assertion — two fresh read-only probes through the public API executed the pack's own two smallest-verification steps. Probe A (the mid-chain failure semantics, doc 1 §15.1): the dependent step rejects cause-chained at the door (drop_break → intent_rejected, failed_test target.carried_by, cause ev_0024; the second steal → carries_flagged, cause ev_0006 — step N+1 reads step N's real state), zero fake-success events, the twin run event/outcome-equal — the quality row MEASURED CLOSED, the utterance-level decomposition staying parse-2's deferred row with zero simulator change owed. Probe B (the status census, doc 1 §15.2): five of the six conceptual statuses first-class at their proper layer (parse-boundary question/no_intent/ParseError+menu; door intent_rejected with the named failed_test + cause id; resolver success/failure branch events with on_failure bands); PARTIAL honestly absent (0/48 action declarations across the three packs carry separable sub-effects — the deferral's own law); NO_MODELED_EFFECT structurally unreachable (the D-152 teleology rung closes the class at load) — D-150 v3's verdict now measured. The ten quality-open rows of the pack's three re-check tables all hold their standing dispositions with their re-open conditions intact (compression CONFIRMED as practice, materialization parity CONFIRMED, the scale benchmark UNRESOLVED-corrected, MotifEngine/interface-state/narrative-satisfaction/decision-stress/settlement-loop PARTIAL riding res-1, world-2 slice 3/4, presentation-1/SoW, st-5/companion-1). Refused at the door: the pack as a repo reading surface (convenience-copy law, D-024 — the routed blocks are the repo form, the pack the owner's external index); re-opening UNDER_MODELLED/NO_MODELED_EFFECT as runtime statuses (the class is load-closed; an authoring failure must never become a playable outcome); PARTIAL as a standing status (no consumer); any new card (the residue decomposes onto existing owners — the census mapping is the intake-25 block's one-pass form). Zero build-grade items; the queue untouched | the pack's real question — whether the prior routing was premature form-matching — resolves NO: every SHOULD-NOT-ADD verdict in the family cites its re-open condition, and the two rows the pack marks quality-suspicious are now measured closed; the anti-conservatism discipline it institutionalizes is research_method_v5's four questions, already internalized (D-163) — the pack's value is the owner-side consolidated index, never a second record | detail: `docs/blueprint/phases.md` §6's intake-25 block (the two probes + the ten-row verdict table); DECISIONS 45 rows — over the 30 cap on the §6.1 substance precedent until the next gate collapse, the D-166 row joining the intake family's own record |
| D-167 | 2026-09-19 | **The cumulative research reconciliation routed (iter-134, doc-only — the owner's «согласно CUMULATIVE_RESEARCH_v2 (1).md работай» call over the uploaded second-stage cumulative method; the closure form the method's own trigger awaited: the intake family 4..25 closed by its last two members, the ladder 0..6 complete, nothing pinned; the D-155..D-166 family the corpus, the method file external per the convenience-copy law): ONE fresh read-only measurement — the ARMING CENSUS over the three committed packs (factions depth-6: 0 consumers, DORMANT — intake-23's claim now measured; names/cultures/spines/groups+condensation: 1 consumer, the province-only family — intake-20's LANDED-BUT-UNDER-TESTED list confirmed whole; travel: 2; weather + the nine core families: 3, promoted) — and five cumulative findings: F1 the frontier is composition-limited, never implementation-limited (the residue is arming/promotion/content, never machinery — D-160's zero-core-edit probe the strongest single proof); F2 the corpus is saturated (intakes 11..25, 15 documents + 2 re-entries, the last 14 iterations all owner-called doc-only routing, every one zero build-grade; the marginal research value now lives in the open rows' own evaluation disciplines — re-open condition: a fresh question aimed at an open row's evaluation form); F3 the convergence — world-2 L2 slice 3 is the highest-leverage proof (six intakes' residues consume the one row: the composed outcome-divergence experiment — one two-arm run, four read surfaces, the blast shape — + the claim/legitimacy first consumer + the card-recalibration input + the census's only zero-consumer family armed; also a live probe of intake-22's minimal-system-contract hypothesis: zero core edits expected); F4 the promotion ladder is the content-side frontier (slice 3 arms factions 0→1 same-pack; the promotion events for the four province-only families are the future pack slots, never more province arming — the consult card the selection discipline); F5 the SoW fence holds (compellingness INV-4-fenced, engine-1/presentation-1 + the {3–8B, GBNF} hardware arm the owners). Cross-pollination: intake-21's four-question card = slice-3's experiment design review (STATE/CAUSALITY/KNOWLEDGE/COUNTERFACTUAL all already the composed experiment's read surfaces); intake-24's principle index card = slice-3's authoring checklist (P4/P2/P5/P6). Should-not-add: no fresh research capacity pre-slice-3; no factions spike separate from the slice; no capability-truth-table document (the census one-pass); no res-1 pre-work; no queue reorder (the standing readiness order IS the cumulative work order — D-153's wave plan confirmed). Verdict CONFIRMED; disposition NO ACTION on the queue; zero build-grade items** | the corpus taken as one body of evidence answers its own central question: the capability frontier is measured (the census), the highest-leverage path is the standing readiness pick (slice 3, now with the composed experiment + the zero-consumer arming as its measured motivation), and the research loop itself is closed — further capability comes from the build queue, the pack slots and the SoW gates, never from more intake routing before the open rows' evaluations | detail: `docs/blueprint/phases.md` §6's cumulative block (the census table + the five findings); DECISIONS 46 rows — over the 30 cap on the §6.1 substance precedent until the next gate collapse, the D-167 row joining the intake family's own record |
| D-168 | 2026-09-19 | **The world-2 L2 triangle slice (iter-135, the owner's «мы провели кучу исследовательских итераций, теперь настала пора приступать к работе по планам» call — the iter-118/119 precedent phrase, STATUS Next step's own readiness pick: slice 3 the natural next pick, iter-134's F3 convergence; D-153's wave plan, D-154's wergeld backlog). THE FACTIONS ARMED 0→1 (the census's only zero-consumer family — depth-6's first committed arming, pack data over the settlement cast, the budget block's own prediction): (1) the THREE GROUPS + ENTRIES — the river guild (Maren + Ketta, anchored Malby, fear/30/0/40 → `council`), the old families (Wilmot + Garrick, anchored Thornmill, `grievance`/40/50/60 → `hold_vigil`), the half-pay garrison (Osgar + Ferra, anchored the keep, fear/40/0/30 → `patrol`) — the ESCALATION LADDER (the same fear axis at two thresholds: the merchants flinch at 30, the trained watch holds to 40) + the DEEP-HISTORY ARM (the new `grievance` axis, decay 0 — the wergeld law, "a man-price once named stays named until paid" — at the deliberate deadband: threshold 50 with two members means BOTH elders must grieve); the goal verbs are bare story beats (resolver wait, the D-160 muster pattern) with the tale lines + the story-critical listing + the attribution rows; (2) the GRIEF-WAKE REACTION (`on_action.alarm_raised` += witnesses → grievance +20 — "the mill burned once before": the numeric-home law is the filter, only the seeded elders carry a home) — ONE verb, MANY ROADS made mechanical: burn the market → the guild councils + Wilmot wakes (Malby heard at Thornmill); burn the keep → the garrison patrols + Garrick wakes (the keep heard at the crofts); both fires → the deadband opens and the FAMILIES HOLD THE VIGIL (intake-19's claim/legitimacy FIRST CONSUMER — the contested wergeld as ratio dynamics over live per-entity axes, never a canonical claim state, exactly the L3-favours-derived-control verdict); (3) the DEEP FEUD HISTORY — the chronicle 5→9 events over the same 150 years, the collection vocabulary RE-CUT (feud roots = war_fought + lineage_ended — the blood endings open feuds; quarrel = the pacts under them; exodus = the foundings at the leaves) and the wergeld claim joins the story-critical hooks (`garrick_wergeld_count` — the feud's hill half, the third buffer-seeding claim: the sweep, the murmur, the count); the cause TREE lands in the log (the members chain to their nearest lower tier, the roots to the previous top-level — the sagas chain) and renders through the tier clauses + the year chain; (4) the COMPOSED OUTCOME-DIVERGENCE EXPERIMENT (F3's four read surfaces, the sandbox driver per Rule 9, seed 53, both arms from t=0): STATE +16 events zero drops (the fire chain, the grief, the council, suspicion ×3), CAUSALITY the first divergence at the take + the amplifying chain (the council at t=3443 riding `faction_0000`), KNOWLEDGE the PC +4/−1 records + the watch's suspicion 0→30 + the runner SUSPECT (the next-decision surfaces diverged), COUNTERFACTUAL the same-seed fork — intake-23's F prompt answered (Wilmot's grief wakes at Thornmill while the runner stands at Malby — the world moves where the player is not) and the G cascade named (arson → alarm → fear/grief → the ratio + the crime chain — three systems answering one verb); (5) KI#85 — the ONE forced core edit (the alarm fear spike now applies the ripple's numeric-home law: an ambient knowledge-holder hears the shout but takes no fear write; the province's market crowd at a burning location first exercised the latent D-035 mismatch — the modularity probe's sharpening finding, exactly F3's predicted shape: pack-data-only held everywhere else). `tests/test_triangle.py` +11 + the KI#85 pin + the T1 re-pin + PROVINCE_STOPLIST +6; the golden fixture 39→44 (+4 deep history, +1 ramble — the count), fingerprint 0. 1738+1 green, ruff clean, zero corpus price outside the province | the wave plan rides TASKS world-2 (slice 4 the calendar + the verdict call — the ORDER owner decides); phases.md §6's slice-3 block the architecture record; the census recalibration: factions 0→1 ARMED (intake-20's card input) | the honest scope: the triangle proves the ratio dynamics at province scale over the ordinary verbs (the fire family) — the player tips ratios, never script gates; the vigil's deadband is the FNV "both houses" shape, the patrol's rarity the crown's slow answer; the compellingness half stays INV-4-fenced (F5) | 
| D-169 | 2026-09-19 | **The world-2 L2 calendar slice (iter-136, the owner's «продолжай работу по планам» call — the iter-135 precedent phrase's continuation; STATUS Next step's own readiness pick: slice 4 the wave plan's remainder): maclock-1's middle granularities LANDED — L4 grown to three tiers (micro-time, the SUB-YEAR calendar cadences, macro-time, one authority). `core/calendar.py` + `rules.json::time.calendar`: named entries `{every_ticks, event_type}` or `{every_ticks, cycle}` (the cycle = phase pairs `{phase, event_type}` — each season's turn renders its OWN tale line, never a shared template keyed on a raw id; the phases rotate by pure arithmetic `cycle[k % len]`, the run OPENS at cycle[0] — the weather `initial`'s own law); crossings the positive multiples fired coarsest-first at co-occurring ticks (the year → the season → the fair → the market day → the rotation → the beat), the deterministic order `every_ticks` DESC then id; each turn ONE canon event (actor world, outcome `{calendar, day, phase?}`, no knowledge/state_changes/hooks). The lints: the pairing law (a calendar without time.macro is dead data), the sub-year law (every_ticks < cadence_ticks — the year clock owns the year turns, a second year-scale clock a duplicate declaration), the identity laws (unique types ≠ the macro's/weather's), the both-or-neither cycle refusal, the emission witnesses (the teleology gate's §5 row 4 reads the phase types). The weather's SEASONAL layer (`weather.seasonal`: `ride` names a declared calendar entry — the chain's rolls MOVE to that cadence's crossings, ONE roll cadence per family, the reference never a declaration, "one clock, one cadence"'s substance held by reference; `weights` the per-phase per-current-state overrides — the D-030 asymmetric data: the rise breaks the sky toward storm fast from ANY state, the long light carries no override — the calm rebuilds slow; the phase at the roll pure tick arithmetic, L3, the fold never scanned). The committed ARMING (the province): market_days 14400 + fairs 43200 + seasons 129600 [thaw, high_water, long_light, first_frost] — the crossings beyond every day-scale corpus script's horizon BY CONSTRUCTION (the weather-1 arming's own law — the golden bytes untouched, the corpus price zero, the T1 pin green). ONE forced core edit — KI#86: the erosion's seed-time gate (`erosion_due` — a rule whose `from` no entity holds seeds NO queue entry; the seasonal ride at test scale exposed the latent unbounded feed: dead entries per changing roll chasing the crossings when the ride cadence sits below the rule's after_ticks; the fire-time read unchanged). The composed YEAR experiment (province_calendar.json, seed 42, the F3 four-read-surfaces form): 36 markets, 12 fairs, the four seasons in cycle order, the year's turn (151 — the chronicle binding), four weather rolls (the rise draws STORM — the D-030 read measured), the co-occurrence order pinned (the rise → its weather roll → the fair → the market), the twin run byte-identical, the tale rendering the calendar's lines (the tune-1 split) while the weather stays canon-without-a-line; the T7 playtest read lives in worklog iter-136 (the year-scale noise finding — the watch/talk texture dominates, the calendar the spine — honestly recorded for the verdict call). tests/test_calendar.py +16; PROVINCE_STOPLIST +2 (thaw, frost); the budget's second honest re-declare (templates 52→58, ceiling 55→60 — the phase lines the slice's own readability surface); the brief's lore the INFORMED half (the market cycle + the river's seasons). 1754 passed + 1 skipped, ruff clean | the ride-vs-default choice: the weather's rolls COULD have stayed at the macro year (the standing letter) — refused: the seasonal bias would be structurally constant (the phase at every year crossing identical when the seasons divide the year evenly), the wave plan names weather-1's chain as the carrier; the per-phase event types over one shared type: the tale's readability surface (the cycle pairs) won over the minimal shape at the honest budget re-declare | the world-2 L2 WAVE PLAN COMPLETE (slices 1-4 done: the skeleton/cultures/triangle/calendar) — the row's own verdict call stands open for the owner (D-153's "each slice a row, the ORDER owner decides"; the wave's verdict question: the deep second world's honest scope vs the weeks made explicit — VISION §7's twin); phases.md §6 the architecture owner; KI#86 the erosion gate's record |
| D-170 | 2026-09-19 | **The world-2 L2 depth audit routed (iter-137, doc-only — the owner's research call over the uploaded research_method_v5 depth-audit task; the three lens documents — the Cadwell roguelike principles / the Ingold Narrative-Sorcery discipline / the Haggis scene-change material — external per the convenience-copy law, repository evidence authoritative; verdict MATERIAL for the standing owner verdict call, never the call itself): the three lenses CONVERGED on one falsifiable unit — depth = the changed NEXT-DECISION landscape per ordinary verb (Haggis's complexification unit + Cadwell's strategic-commitment stake + Ingold's encounter-robustness validity condition), operationalized as the F3 four-read-surfaces form EXTENDED with three read-only arms (the Rule-9 runners outside the repo, logs outside the repo, zero corpus price): (1) the DOOR-SURFACE CENSUS (province_feud, seed 53, BURN vs CALM — the arson alone removed): the burn = +15 events / 13 types, 13 projection properties over six entities (the market destroyed, three watchers' suspicion 0→30, the steward's grief 35→55, the runner SUSPECT), knowledge +4/+9 — and EXACTLY ONE of six probed door answers changes (the re-ignition closed by `target.spot_available`; the other five identical, the two rejections arm-independent state facts — the sergeant's pay tin, the unflagged mistress); (2) the REORDER ARM (seed 53, Thornmill-first): the grief-wake IDENTICAL (route-independent), the vigil deadband holds, the council fires, the twin byte-identical, the tale coherent, the one lawful difference an NPC's own talk refused at `target.same_location` (the door's uniform law), the longer route growing MORE autonomous texture (76 vs 64 events); (3) the CALENDAR ABLATION (the year run, seed 42, `time.calendar` + `weather.seasonal` stripped, the macro clock untouched): the stripped layers' own 57 events + 52 tale lines, the mechanical leverage beyond them EXACTLY ONE second-order path (the rise's storm roll → the storm-murmur hook [D-133] → the ambient quiet gate's release → Maren's ramble at the emptied stall row — the one ablated projection property → three hearers' knowledge → one rumor told back) and the 48 market/fair turns ZERO consumers measured. The A–H ledger: A/C/D/H CONFIRMED, E CONFIRMED at the measured band (the entry-order corpus since-1/qa-1's), B/F/G PARTIAL (the option surface 1-of-6; the calendar spine + one path; the composition concentrated at the triangle's vertex). The principles ledger: twelve rows, each the five questions answered — the standing forms OWNED as substrate (the event grammar = Haggis's beat shape; the door's uniform law = Ingold's defensive validation; the consumer discipline = Haggis's motivation law), the residues ROUTED (the corpus to since-1/qa-1, the ceremony to presentation-1, the authoring tension to the pack slots), nothing new owed. The strong counterexamples searched and each answered (the 48 turns the one found-and-named; the reverse — simple mechanisms, many futures — CONFIRMED). The critical separation: NONE of res-1 / roads-1 / since-1 / pack-1 / pack-4 / engine-1 / presentation-1 a prerequisite for the depth claim — no future completeness smuggled into the definition. Verdict PARTIALLY CONFIRMED — the anti-collection / anti-reskin halves measured, the depth's DISTRIBUTION concentrated; the L2 verdict call the OWNER's, never self-called. Disposition NO ACTION — the depth battery (the door battery + the reorder arm + the ablation) joins the F3 form as the standing pack-gate evaluation discipline (F4's consult card carries it); zero build-grade items** | the audit answers the method's nine final questions in one pass: the unit converged, the claim's halves measured, the sources' residues all owned or routed, and the evidence complete in the measured band — the smallest remaining verification NONE owed for the verdict call itself; the synthesis's own yield (the converged unit + the battery protocol) is the standing experiment's shape extended, never a new instrument | detail: `docs/blueprint/phases.md` §6's depth-audit block (the A–H ledger + the principles table); DECISIONS 49 rows — over the 30 cap on the §6.1 substance precedent (the standing post-ladder record, D-167's own note), the D-170 row joining the intake/gate-verdict family |

| D-171 | 2026-09-19 | **The verification-lenses research routed (intake-26, iter-138, doc-only — the owner's «перед этим вероятно стоит рассмотреть дополнительно идеи с файла VERIFICATION_LENSES_RESEARCH.md» call, the world-2-verdict session's first half; the research file external per the convenience-copy law, repository evidence authoritative; the D-155..D-170 intake precedent): the document's own verdict PARTIALLY CONFIRMED verified at HEAD — the verification MECHANISMS are all owned (the FACT rows content-matched: TEST_PLAN's gate/metrics/A-B/corpus/offline separation, `scripts/mechanics.py`'s four read-side questions, `core/metrics.py`'s M1–M5 + emergent chains + eventless stretches + payoff latency + beat tension, the checkpoint/blind/deviation/drift/longrun suites, intake-21's parked card, intake-24's six principles, intake-16's one-knob spike record); the missing piece the compact SELECTION GRAMMAR — which existing instrument falsifies which claim (iter-137's audit re-derived its verification form from scratch; the intake family's verdict tables each re-derived the instrument mapping — the diagnosis confirmed by the repo's own history). The adoption set: (a) the CLAIM PACKET (Claim/Problem/Lens(es)/Prism/Scenario+corpus/Oracle/Falsifier/Counterexamples/Expected/Observed/Epistemic class/Disposition — the oracle an observable condition on the canonical substrate, the falsifier explicit; the disposition vocabulary the repo family, never a numeric score) + the claim-shape → instrument SELECTION TABLE + the independent-re-derivation oracle law + the order-probe contract law land in `docs/TEST_PLAN.md` §9 — the verification owner's surface (the document's own §15 routing), marked research-derived: a routing aid, never a gate; (b) the full ten-lens / nine-prism catalog (every one mapped to standing carriers) held in the intake-26 block (`docs/blueprint/phases.md` §6 — the one-pass record, never a second standing document, D-024); (c) the stale-at-HEAD corrections recorded (the door-surface battery = the next-decision census landed iter-137; the first-divergence read consumed iter-135; the A–H ledger the packet's standing instance, retroactively recognized). Refused at the door: the lens/prism names as repo law (label-matching, the intake-24 precedent), any executable lens helper / registry document / numeric score / Verifier-runtime form (the document's own §10 reject list = standing law: INV-1/INV-4/D-024/the no-second-engine family), REFERENCES.md as the surface (the external-source catalog; internal synthesis lives in the intake blocks). The measurement obligation now standing: the next owner-gated build row (res-1 / pack-1 the natural candidates) writes its verification plan as ONE claim packet (≤3 lenses, ≤2 prisms) — that run decides whether any executable helper is ever warranted** | the long-term quality call the owner delegated («что будет качественнее на долгосрок»): pure parking leaves the grammar unfindable exactly when a new claim arrives (the document's own diagnosis); full adoption would formalize an unmeasured vocabulary into law (label-matching, D-024). The compact §9 form neutralizes both disadvantages: the packet + the table are an INDEX over standing owners (never a second truth), self-describing names only (no L-numbers/P-numbers as law), the catalog held one-pass in the block, the measurement bound to the next real consumer — adopt-by-measurement, never by decree | detail: `docs/blueprint/phases.md` §6's intake-26 block + `docs/TEST_PLAN.md` §9 (the claim packet + the selection table); DECISIONS 50 rows — over the 30 cap on the §6.1 substance precedent (the standing post-ladder record, D-167's own note), the row joining the intake family |

| D-172 | 2026-09-19 | **The world-2 L2 verdict — the two-level gate's level-2 question ANSWERED on the owner's convening call (iter-139, doc-only — the owner's «я решил что следует world 2 закончить, вердикты вынести» directive, the session's second half; the verdict material iter-137's depth audit [D-170] + the four slices' committed records [D-153/D-154/D-168/D-169] + the census [iter-134] + the composed experiments + the T7 reads — the material COMPLETE in the measured band, iter-137's own law): the deep second world's HONEST SCOPE DELIVERED at the measured band — the anti-collection half MEASURED (one ordinary verb composes through three systems into irreversible, divergent, route-stable state: the burn closes the re-ignition door, arms SUSPECT, wakes the feud, tips the guild's ratio — the A/C/D/H ledger CONFIRMED), the anti-reskin half MEASURED (L1's 14m24s reskin day vs the weeks-authored province: the spine + the vertex composition + the year-run almanac — the T7 reads), the depth's DISTRIBUTION the recorded residue (B/F/G partial: the option surface 1-of-6 at the probed band; the composition concentrated at the triangle's vertex; the cultures/calendar pillars the spine + texture + one second-order path; the 48 market/fair turns zero consumers — the one found-and-named counterexample). The verdict's routing: the future depth consumers are the CONTENT-side rows — the pack slots' promotion events (F4: pack-1/pack-4), the cadences' decision half a BUILD question (arming one consumer on a market/fair cadence — co-presence, travel exposure, or a price read) — world-2 NEVER reopens for slices; the robustness corpus sharpens via since-1/qa-1 as standing rows, never as verdict inputs (the critical separation holds: no open row was a prerequisite). The two-level gate COMPLETE: L1 the reskin day (PASS iter-116, D-151), L2 the deep second world (DELIVERED iter-139 — this row); the world-2 TASKS row flips done** | the honest-scope reading of VISION §7's twin («deep unique worlds take weeks; the slices are the weeks made explicit» — the weeks spent, the audit measured what they bought): the gate asks the honest scope, never uniform distribution — the audit's PARTIALLY CONFIRMED is the depth claim's honest split, which the verdict records as residue with named future consumers rather than as a deficiency; deferring until B/F/G close would be the completeness-smuggling the audit explicitly refused (the claim decidable on current evidence) | detail: `docs/blueprint/phases.md` §6's verdict block + the TASKS world-2 row; DECISIONS 51 rows — over the 30 cap on the §6.1 substance precedent, the row joining the gate-verdict family (D-136/D-151/D-170's own) |
| D-173 | 2026-09-19 | **The level-design & worldbuilding consolidation routed (intake-27, iter-140, doc-only — the owner's «проанализируй документ и определи что из него можно полезного перенять в проект и почему... где прокатит междоменная трансплантация принципов» research call over the uploaded `level-design-consolidated.md`, a conspectus of twelve GDC/Konsoll/LDL talks; the D-155..D-171 intake precedent, the file external per the convenience-copy law, the intake-24 transplantation posture — the mechanism source-free, never the form; the sources spatial/rendering talks, the target a deterministic text simulation with no camera): verdict PARTIALLY CONFIRMED — the doctrine overwhelmingly OWNED as the strongest cross-domain confirmation family since intake-16 (figure–ground = the brief's budgets/eviction + the chronicle's pruning + AGENTS §6.1's substance filter; vistas/non-game space = the cold tiers + lazy detail + the chronicle horizon; space reuse = fold(log) itself; PCG-is-design × 4 talks = INV-3/PACK-1/D-082/D-134; Control's representation switching = L12/LOD-1/INV-3 — a destruction pipeline and a narrative sim sharing one law; production data-quality = the pack-lint family, the philosophy verbatim; Qud's abstraction mountain = the pass ladder + the chronicle horizon, L7 STRICTER than the talk's outcome-first trick at runtime). The residue — FIVE compact instruments + ONE proposal, all routing to standing rows: (1) the READER LAW (Deck's discoverability × the census law: an emitted event with zero read surfaces — no projection consumer, no knowledge channel, no door check, no tale line, no hook — is functionally dead; name the reader of every new event type; consumers F4's promotion events + the cadences' decision half, the law's own BUILD form); (2) the PRICED-OPTION question (Serra: count differently-priced options — risk/exposure/time/information deltas — never labeled options; static prices the fictitious-choice symptom the A–H B row measured; the future gate's B-row form, res-1 the dynamic half's owner); (3) the TOP-LOD READABILITY question (Brown's squint test: the critical path — who/where/what changed/why — survives the coarsest-LOD blur; the depth battery's read-surface arm, no instrument owed); (4) the REALIZATION TABLE (Edwards: why exists / which layer / linked to what / what the reader understands + the removal test — the admission lint's authoring half; PARKED for pack-1/pack-3/pack-4, the motivation scope question riding it, INV-4-fenced); (5) the SACRIFICE PROTOCOL (Jaffe: promise pair → the both-promises test → Barrier/Gate/Carrot/S'more with the price named — the repo's resolved cases its precedents [Barrier: canon/voice; Carrot: mechanics-cured sycophancy; partial: the improvisation straitjacket, VISION §6]; PARKED for the SoW debates — bg-6/engine-1/presentation-1/companion-1 — a debate form, never a gate, never a spec); plus the PROPOSAL (topology-aware hook distribution: the site graph's junction/through/dead-end degree classes × the hook families — Eldritch's playtest placement laws transplanted as the reasons-to-go question; the cadences' decision half + roads-1 + the F4 slots the consumers, never law until a pack arms it). Carriers: the pack-candidate consult card's item 10 (the battery's three sharpenings + the two authoring companions) + REFERENCES §10's nine source rows + the intake-27 block (the one-pass verdict table). REFUSED at the door: spatial-composition machinery (renderer-side, presentation-1's, no camera), motivation-model law (INV-4), any named-principles document (D-024), cursed-problem runtime/spec/check, topology arming now, Kirill's eight methods + Totten's documentation culture (fully-owned confirmations, zero residue). Zero build-grade items; the queue untouched** | the transplantation test applied per principle (form exists ≠ adopted — quality/transfer/combined demanded): the document's spatial surface never transfers, but its LAWS are the repo's laws arriving from an unrelated domain — independent confirmation raising confidence in standing law, never new machinery; the five instruments are the smallest forms surviving counterexamples and each names its consumer; the owner's standing preference honored both ways — no "minimal change" conservatism, but also no restructuring on elegance alone (the one-click temptations refused: a cursed-problem checker, a topology-hook engine, a motivation matrix) | detail: `docs/blueprint/phases.md` §6's intake-27 block (the verdict table + the five instruments + the proposal) + the consult card's item 10 + REFERENCES §10's nine rows; DECISIONS 52 rows — over the 30 cap on the §6.1 substance precedent (the standing post-ladder record), the row joining the intake family |
| D-174 | 2026-09-19 | **The Stålberg-conspectus / transplantation-method output routed (intake-28, iter-141, doc-only — the owner's «разбери 1231.md => что стоит перенять и почему, зачем, как использовать и где это улучшит проект» research call over the uploaded `1231.md`, itself a PRIOR SESSION'S OUTPUT (a research conclusion over an external Stålberg/Townscaper conspectus + the owner's competing-solutions preference; that session verified against the same `main` 8f4fbd4e but DNS-failed its clone — the D-163 precedent for consolidated outputs of clone-less sessions, re-verified this round at a REAL clone: 1754+1 green, ruff clean): verdict PARTIALLY CONFIRMED — the content overwhelmingly OWNED, zero factual errors in the document's load-bearing repo-facing claims. The Stålberg half joins the cross-domain confirmation family (the intake-16/17/24/27 line; the Townscaper reconciliation itself intake-17's): small-alphabet + procedural composition = L9/PACK-1; constraints-enable-quality = INV-3/schema/doors; variation-inside-invariants = the road-pack scaffold law (ids stay, surfaces renamed) + the cosmetic streams + the texture promotion door; representation switching = L12/LOD-1; dual grid / quadrilateral mesh / WFC / jitter as FORMS = intake-17's §22 scope guard + L13, REJECTED in agreement. The methodology half = the standing practice itself (D-163's v5 checks; D-173's quality/transfer/combined demanded; BLUEPRINT §0's tension→resolution→sequencing; L13/L14). The residue — ONE compact instrument ADOPTED: the COMBINATION FENCE, the fifth check's operational sharpening: a combined design carries NO presumption of advantage — it is itself a separate hypothesis entering the same PROVEN/PARTIAL/REJECTED ladder as any transplant; its three questions: the mechanism-of-advantage decomposition (which mechanism earns each option's plus), the combination-price question (what cost exists ONLY because of the combination, in neither option alone), and the Frankenstein test (more mechanisms but not more quality → reject). Consumers: the standing design forks — the engine-1/presentation-1 spec debates and res-1's evaluation discipline (the same parking family as intake-27's sacrifice protocol; a research-move fence, never a gate, never a spec). ROUTED, never a row: the "local canonical pattern → higher-order consequence" HYPOTHESIS (the document's own gate holds — whether the standing event grammar already covers it; res-1's aggregate macro-events + the cadences' decision half the standing consumers, intake-17's multi-node motifs PROVEN in three owners the substrate record). Two recognition handles recorded block-only, each naming its standing owner (the intake-24 card precedent): "change the representation, shrink the case space" → PACK-1's closed enums + the parser grammar snapshot + INV-3's zero-engine-change claim; "vary the representation, preserve the invariant substrate" → the scaffold law + the cosmetic streams + the promotion door. REFUSED at the door: a grand-synthesis law/DECISION (the document itself refuses — D-024 label-matching agrees), named-principles documents (D-024), methodology codification into AGENTS/BLUEPRINT (D-163: the method file CONFIRMED-OWNED, external, nothing left to adopt), Stålberg-form features (no renderer; intake-17's fence), local-pattern machinery now (HYPOTHESIS), a Canonical-Visual-Memory row (NOT a repo record — the claim's referent is session-external; presentation-1 owns the player-facing surface question, INV-4-fenced). Zero build-grade items; the queue untouched** | the transplantation test applied to the document itself: its repo-facing claims hold at a real clone (D-173's test wording, BLUEPRINT §0, L13/L14, the owner-preference record D-165/D-173); the Stålberg figures stay second-hand claims (the conspectus external, the KI#51 family — never repo facts), which costs nothing because the adopted residue is repo-internal; the one keeper is the fence because it is the smallest form naming a real gap — the fifth check asked "is there a combined design?" but never carried the no-presumption clause, the combination-price question, or the Frankenstein test, and the failure mode is real (D-165's P2 catch: compression converted "verified in one domain" into "applies uniformly to five" — a combination accepted on presumption) | detail: `docs/blueprint/phases.md` §6's intake-28 block (the verification map + the fence + the two handles + the provenance caveats); the fence joins the consult material for the design-fork debates (STATUS Next step's pointer); DECISIONS 53 rows — over the 30 cap on the §6.1 substance precedent (the standing post-ladder record), the row joining the intake family |
| D-175 | 2026-09-19 | **The external-audit / roadmap-review output routed (intake-29, iter-142, doc-only — the owner's «проанализируй текст далее и его предложения, если согласен => нужно будет заложить в планы работу и соответственно пересмотреть оный или типа того. по пунктам разбери что надо что не надо и почему» research call over the uploaded audit text, itself a prior DNS-less session's static roadmap review of `main` 8f4fbd4e (its own caveat: no pytest run); the D-163 precedent, re-verified at a REAL clone one commit ahead at aa1097f AND with the live suite: 1754+1 green, ruff clean): verdict CONFIRMED on the facts — every load-bearing repo-facing claim verified (the sizes, the `_Lint` structure, the missing CI, the unprotected branch, the row wordings, the sites-vs-locations split, the undefined co-presence baseline), two framings corrected, none fatal; the foundation SUPPORTED (no reopening of INV-1..5 / the closed phases / world-2). The residue — SIX contract sharpenings ADOPTED into the TASKS rows (the queue's composition changed, its order never — D-113): (1) res-1's MECHANISM SPLIT (a minimal domain-blind account/transfer/consume primitive = a new WHAT in core, named, integer-only, through the canon door, INV-3-clean; the economy stays pack data; a pack-only reading would grow the hidden mechanism the row itself forbids) + the IRREVERSIBILITY SPLIT (event immutability ≠ stock immutability — a stock regrows via new source events, never a 'revert'; the `irreversible` state_change flag the wrong instrument for stock props) + §9's claim packet the verification form (the conservation oracle, the no-negative floor, the decision-effect arm, the one-knob ablation); (2) roads-1's TOPOLOGY CONTRACT owed before implementation (the NODE question pinned — all sites vs claimed/settlement nodes; the bare 'MST/k-nearest' RETIRED; the invariant set: connected/no-self-loop/symmetric/bounded-degree/deterministic-tie-breaking+edge-order/pathological-edge-bound/cycle-policy; the behavioral falsifier: same world, different topology → the expected LOD/autonomy differences measured — the warm ring the row's own named consumer); (3) since-1's ENCOUNTER BASELINE owed before implementation ('the last co-presence tick' has no standing formal definition; the per-entity encounter epoch the leading candidate — read-side, zero canon writes; the scene_delta surface separation explicit); (4) companion-1's NO-TELEPORT clause (INV-1's consequence made explicit: ordinary movement/travel intents only, lag legal, a follow cadence a measured mechanic — never implicit sync); (5) pack-1's CONSENT SPLIT (the canonical event outcome vs the crafted knowledge records — the D-008 belief half; the mutually-exclusive consistency lint); (6) ci-1's PRIORITY NOTE (the session's recommendation as the FIRST owner pick: main unprotected with checks off at HEAD, verified live; the runner the cheapest risk reduction on the board; the pick stays the owner's — AGENTS §8). Two RIDER RULES adopted: the pack.py split rides the FIRST pack.py-growing row (never a standalone refactor — 'split the implementation of the ownership, never the ownership'; `load_pack` stays the single admission gate; no DSL/base-class framework) and the INTAKE ADMISSION rule (a new external intake convenes only with a named open build row/standing debate it feeds + a potential falsifier stated up front; the owner's explicit fresh question always overrides, recorded as such). REFUSED with cause: the 'internal contradiction' framing for res-1 (the row is SILENT on the mechanism's location, never contradictory — pack-1/3/4 say 'zero core change by design', res-1 does not; the action identical either way), the front-loaded presentation-1 stabilization (already owned piece by piece: truncation markers, the canon/control split, the call budget, the failure semantics, the parser contract — and the 'versioned brief envelope' contradicts BRIEF_SPEC §8's derived-read-side discipline + §9's JIT law), any M3/T8 law change (owned since iter-138 — §9 the standing depth form), the standalone pack.py refactor now (L13/AGENTS §2.4). Zero build-grade items; the ORDER recommendation recorded never enforced (ci-1 → the three contract writes → the build rows — the audit's own closing proposal)** | the audit's value is exactly its two-step verification shape: the FACT layer (repo state, verified stronger than the audit could — live suite + live API) separated from the PROPOSAL layer (each tested against the standing laws before adoption: PACK_SPEC's WHAT/HOW test for res-1, iter-104's own consumer list for roads-1's behavioral falsifier, BRIEF_SPEC §8/§9 contra the front-loading, D-113's ORDER=owner contra the reordering). The two corrections matter for the record's honesty (silence ≠ contradiction; already-owned ≠ new demand) while changing no action — the sharpenings the rows now carry are the pre-implementation contracts the audit itself names as its most valuable next step | detail: `docs/blueprint/phases.md` §6's intake-29 block (the verification map + the ten point verdicts + the order recommendation); the six TASKS row sharpenings; DECISIONS 54 rows — over the 30 cap on the §6.1 substance precedent (the standing post-ladder record), the row joining the intake family |
| D-176 | 2026-09-19 | **The ci-1 GitHub Actions runner LANDED (iter-143 — the owner's «продолжай работы по планам, решай сам что сейчас логичнее начать/открыть и прочее» delegation call; the pick taken from the plan's own recorded first recommendation, intake-29/D-175's ci-1-FIRST; AGENTS §8's CI-files gate satisfied by the owner's fresh delegation, recorded as such — the precedent shape of D-154's precedent-phrase picks): `.github/workflows/ci.yml` — the local green discipline automated for `main`. The contract: pytest + ruff on push to `main` (+ the `pull_request` trigger riding along — the row's own "protect main after" clause needs check runs on PR heads once required status checks exist; direct-push flow unaffected: the owner's apply-archive-and-push rhythm keeps firing the push trigger); `PYTHONHASHSEED=0` at the job level (INV-2); Python `3.12.14` pinned EXACT — the env-pin law, not convenience: the golden T1 fixture byte-compares a fresh run against the committed bytes (TEST_PLAN §1.1) and the log header records the interpreter (`core/log.py` — byte-identical replay is same-environment only), so a version bump rides with a fixture regen in the same iteration, never alone; the runner python is not PEP-668-fenced — the plain `pip install -e ".[dev]"`, no `--break-system-packages`; `permissions: contents: read` (the public-repo least-privilege floor); `timeout-minutes: 15` (~10x the measured run). Verification: the suite green at BASE_COMMIT 15e568d BEFORE working (1754 passed + 1 skipped, ruff clean, Python 3.12.14) AND the exact runner path simulated in a clean venv (fresh install → `python -m ruff check .` → `PYTHONHASHSEED=0 python -m pytest -q`: the same 1754+1, ruff clean; the skip is the chronicler module's duckdb import — the `[chronicler]` extra by design outside `[dev]`, pyproject's own comment); the actions' majors (checkout@v7, setup-python@v7) and the 3.12.14 availability in actions/python-versions live-checked 2026-09-19 (ls-remote — no guessed versions in the file). Zero corpus price, zero runtime-code touch, INV-4 untouched (the runner is dev-side only — the same class as pytest itself). Branch protection ("protect main after") stays the owner's: a GitHub-settings action no repo file can carry — the recipe handed over in the iter-143 stop-point report** | the deferred-verification surface named honestly: the runner's first LIVE execution happens on the owner's push (a sandbox cannot run GitHub's runners) — everything local to that push is verified (the install path, both commands, the exact pinned interpreter, the YAML structure), which is everything the repo side owns; the design keeps the repo's own workflow rhythm as the primary path (push to main) rather than importing a PR-first convention the owner does not use — the PR trigger is additive, never load-bearing | detail: `.github/workflows/ci.yml` + the TASKS ci-1 row (flipped done) + worklog iter-143; DECISIONS 55 rows — over the 30 cap on the §6.1 substance precedent (the standing post-ladder record), the row joining the build-landing family |
