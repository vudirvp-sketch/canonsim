"""The Workbench family (CONTRACTS.md §5, D-200): CanonSim's read side
projected toward a visual frontend. Periphery per D-046 — imports core
read-side APIs only, never writes canon, never touches the network.
"""
